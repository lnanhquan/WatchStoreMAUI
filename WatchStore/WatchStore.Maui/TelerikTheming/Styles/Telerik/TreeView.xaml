<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikAutoComplete="clr-namespace:Telerik.Maui.Controls.AutoComplete;assembly=Telerik.Maui.Controls"
                    xmlns:telerikImageEditor="clr-namespace:Telerik.Maui.Controls.ImageEditor;assembly=Telerik.Maui.Controls"
                    xmlns:telerikCollectionView="clr-namespace:Telerik.Maui.Controls.CollectionView;assembly=Telerik.Maui.Controls"
                    xmlns:telerikAIPrompt="clr-namespace:Telerik.Maui.Controls.AIPrompt;assembly=Telerik.Maui.Controls"
                    xmlns:telerikTreeView="clr-namespace:Telerik.Maui.Controls.TreeView;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Telerik.Styles.TreeView">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <!-- Dependencies: Checkbox.xaml -->

    <Style x:Key="TreeViewItemExpandButtonStyle" TargetType="telerikTreeView:TreeViewItemExpandButton">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconEmpty}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="BorderWidth" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="WidthRequest" Value="16" />
        <Setter Property="HeightRequest" Value="16" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="ExpandStates">
                    <VisualState x:Name="Leaf" />
                    <VisualState x:Name="Expanded">
                        <VisualState.Setters>
                            <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconDownDir}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Collapsed">
                        <VisualState.Setters>
                            <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconRightDir}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="TreeViewItemCheckBoxStyle" TargetType="telerikTreeView:TreeViewItemCheckBox" BasedOn="{StaticResource RadCheckBoxStyle}">
        <Setter Property="IsAnimated" Value="False" />
        <Setter Property="WidthRequest" Value="NaN" />
        <Setter Property="HeightRequest" Value="NaN" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <ControlTemplate x:Key="TreeViewItemViewControlTemplate">
        <telerikTreeView:TreeViewItemLayout Indentation="{TemplateBinding Indentation}"
                                            Spacing="{TemplateBinding Spacing}">
            <telerikTreeView:TreeViewItemBusyIndicator Style="{TemplateBinding BusyIndicatorStyle}"
                                                       IsBusy="{TemplateBinding IsBusy}"
                                                       IsVisible="{TemplateBinding IsExpandButtonVisible}">
                <telerikTreeView:TreeViewItemExpandButton Style="{TemplateBinding ExpandButtonStyle}"
                                                  Command="{TemplateBinding ToggleExpandedCommand}"
                                                  IsExpanded="{TemplateBinding IsExpanded}"
                                                  IsLeaf="{TemplateBinding IsLeaf}"
                                                  IsEnabled="{TemplateBinding IsEnabled}" />
            </telerikTreeView:TreeViewItemBusyIndicator>
            <telerikTreeView:TreeViewItemCheckBox Style="{TemplateBinding CheckBoxStyle}"
                                                  IsChecked="{TemplateBinding IsChecked}"
                                                  IsEnabled="{TemplateBinding IsEnabled}"
                                                  IsVisible="{TemplateBinding IsCheckBoxVisible}" />
            <telerikTreeView:TreeViewItemImage Source="{TemplateBinding ImageSource}"
                                               Style="{TemplateBinding ImageStyle}"
                                               IsEnabled="{TemplateBinding IsEnabled}"
                                               IsVisible="{TemplateBinding IsImageVisible}" />
            <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                           BorderColor="{TemplateBinding BorderColor}"
                                           BorderBrush="{TemplateBinding BorderBrush}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="{TemplateBinding CornerRadius}"
                                           Padding="{TemplateBinding ContentPadding}">
                <ContentPresenter />
            </telerikMauiControls:RadBorder>
        </telerikTreeView:TreeViewItemLayout>
    </ControlTemplate>

    <Style x:Key="TreeViewItemViewStyle" TargetType="telerikTreeView:TreeViewItemView">
        <Setter Property="MinimumHeightRequest" Value="24" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="2" />
        <Setter Property="Spacing" Value="4" />
        <Setter Property="ContentPadding" Value="8, 0" />
        <Setter Property="ExpandButtonStyle" Value="{StaticResource TreeViewItemExpandButtonStyle}" />
        <Setter Property="CheckBoxStyle" Value="{StaticResource TreeViewItemCheckBoxStyle}" />
        <Setter Property="ControlTemplate" Value="{StaticResource TreeViewItemViewControlTemplate}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="Transparent" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RadTreeViewStyle" TargetType="telerikMauiControls:RadTreeView">
        <Setter Property="ItemStyle" Value="{StaticResource TreeViewItemViewStyle}" />
        <Setter Property="LevelIndentation" Value="32" />
    </Style>
    <Style TargetType="telerikMauiControls:RadTreeView" BasedOn="{StaticResource RadTreeViewStyle}" />

</ResourceDictionary>