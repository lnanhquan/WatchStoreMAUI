<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMauiSlideView="clr-namespace:Telerik.Maui.Controls.SlideView;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Telerik.Styles.SlideView">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <ControlTemplate x:Key="SlideViewControlTemplate">
        <Grid BackgroundColor="Transparent"
              IsClippedToBounds="True">
            <Grid.Triggers>
                <DataTrigger TargetType="Grid" Binding="{Binding Orientation, Source={RelativeSource Mode=TemplatedParent}}" Value="Horizontal">
                    <Setter Property="RowDefinitions" Value="Auto, *, Auto" />
                </DataTrigger>
                <DataTrigger TargetType="Grid" Binding="{Binding Orientation, Source={RelativeSource Mode=TemplatedParent}}" Value="Vertical">
                    <Setter Property="ColumnDefinitions" Value="Auto, *, Auto" />
                </DataTrigger>
            </Grid.Triggers>
            <telerikMauiSlideView:SlideViewContent x:Name="PART_SlideViewContent"
                                                   Items="{Binding Items, Source={RelativeSource Mode=TemplatedParent}}"
                                                   Spacing="{Binding Spacing, Source={RelativeSource Mode=TemplatedParent}}"
                                                   HasLooping="{Binding HasLooping, Source={RelativeSource Mode=TemplatedParent}}"
                                                   Orientation="{Binding Orientation, Source={RelativeSource Mode=TemplatedParent}}">
                <telerikMauiSlideView:SlideViewContent.Triggers>
                    <DataTrigger TargetType="telerikMauiSlideView:SlideViewContent" Binding="{Binding Orientation, Source={RelativeSource Mode=TemplatedParent}}" Value="Horizontal">
                        <Setter Property="Grid.Row" Value="1" />
                    </DataTrigger>
                    <DataTrigger TargetType="telerikMauiSlideView:SlideViewContent" Binding="{Binding Orientation, Source={RelativeSource Mode=TemplatedParent}}" Value="Vertical">
                        <Setter Property="Grid.Column" Value="1" />
                    </DataTrigger>
                </telerikMauiSlideView:SlideViewContent.Triggers>
            </telerikMauiSlideView:SlideViewContent>
            <StackLayout BackgroundColor="Transparent">
                <StackLayout.Triggers>
                    <DataTrigger TargetType="StackLayout" Binding="{Binding Orientation, Source={RelativeSource Mode=TemplatedParent}}" Value="Horizontal">
                        <Setter Property="HorizontalOptions" Value="Start" />
                        <Setter Property="VerticalOptions" Value="Center" />
                        <Setter Property="Grid.Row" Value="1" />
                    </DataTrigger>
                    <DataTrigger TargetType="StackLayout" Binding="{Binding Orientation, Source={RelativeSource Mode=TemplatedParent}}" Value="Vertical">
                        <Setter Property="HorizontalOptions" Value="Center" />
                        <Setter Property="VerticalOptions" Value="Start" />
                        <Setter Property="Grid.Column" Value="1" />
                    </DataTrigger>
                </StackLayout.Triggers>
                <View.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NavigateToPreviousItemCommand, Source={RelativeSource Mode=TemplatedParent}}" />
                </View.GestureRecognizers>
                <telerikMauiControls:RadButton Command="{Binding NavigateToPreviousItemCommand, Source={RelativeSource Mode=TemplatedParent}}"
                                               Style="{Binding ActualNavigateToPreviousItemButtonStyle, Source={RelativeSource Mode=TemplatedParent}}" 
                                               AutomationId="RadSlideViewNavigateToPreviousItemButton" />
            </StackLayout>
            <StackLayout BackgroundColor="Transparent">
                <StackLayout.Triggers>
                    <DataTrigger TargetType="StackLayout" Binding="{Binding Orientation, Source={RelativeSource Mode=TemplatedParent}}" Value="Horizontal">
                        <Setter Property="HorizontalOptions" Value="End" />
                        <Setter Property="VerticalOptions" Value="Center" />
                        <Setter Property="Grid.Row" Value="1" />
                    </DataTrigger>
                    <DataTrigger TargetType="StackLayout" Binding="{Binding Orientation, Source={RelativeSource Mode=TemplatedParent}}" Value="Vertical">
                        <Setter Property="HorizontalOptions" Value="Center" />
                        <Setter Property="VerticalOptions" Value="End" />
                        <Setter Property="Grid.Column" Value="1" />
                    </DataTrigger>
                </StackLayout.Triggers>
                <View.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NavigateToNextItemCommand, Source={RelativeSource Mode=TemplatedParent}}" />
                </View.GestureRecognizers>
                <telerikMauiControls:RadButton Command="{Binding NavigateToNextItemCommand, Source={RelativeSource Mode=TemplatedParent}}"
                                               Style="{Binding ActualNavigateToNextItemButtonStyle, Source={RelativeSource Mode=TemplatedParent}}" 
                                               AutomationId="RadSlideViewNavigateToNextItemButton" />
            </StackLayout>
            <telerikMauiSlideView:SlideViewIndicator ItemsSource="{Binding IndicatorItemsSource, Source={RelativeSource Mode=TemplatedParent}}"
                                                     CurrentIndex="{Binding CurrentIndex, Source={RelativeSource Mode=TemplatedParent}}"
                                                     HasLooping="{Binding HasLooping, Source={RelativeSource Mode=TemplatedParent}}"
                                                     Style="{Binding ActualIndicatorStyle, Source={RelativeSource Mode=TemplatedParent}}"
                                                     IsEnabled="{Binding IsEnabled, Source={RelativeSource Mode=TemplatedParent}}"
                                                     AutomationId="SlideViewIndicator" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="SlideViewNavigationButtonStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="40" />
        <Setter Property="MinimumWidthRequest" Value="64" />
        <Setter Property="MinimumHeightRequest" Value="64" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnDarkColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource SlideViewNavigationButtonBackgroundColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="telerikMauiControls:ButtonUtils.Visibility" Value="{Binding NavigationButtonsVisibility, Source={RelativeSource Mode=TemplatedParent}}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver" />
                    <VisualState x:Name="Pressed" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SlideViewNavigateToPreviousItemButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SlideViewNavigationButtonStyle}">
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconLeftArrow}" />
        <Setter Property="AutomationId" Value="RadSlideViewIndicatorNavigateToPreviousItemButton" />
    </Style>

    <Style x:Key="SlideViewNavigateToNextItemButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SlideViewNavigationButtonStyle}">
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconRightArrow}" />
        <Setter Property="AutomationId" Value="RadSlideViewIndicatorNavigateToNextItemButton" />
    </Style>

    <Style x:Key="SlideViewVerticalNavigateToPreviousItemButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SlideViewNavigationButtonStyle}">
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconUpArrow}" />
        <Setter Property="AutomationId" Value="RadSlideViewIndicatorNavigateToPreviousItemButton" />
    </Style>

    <Style x:Key="SlideViewVerticalNavigateToNextItemButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SlideViewNavigationButtonStyle}">
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconDownArrow}" />
        <Setter Property="AutomationId" Value="RadSlideViewIndicatorNavigateToNextItemButton" />
    </Style>

    <ControlTemplate x:Key="SlideViewIndicatorItemControlTemplate">
        <Grid WidthRequest="20" HeightRequest="20">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter TargetName="ellipse" Property="Ellipse.Fill" Value="{DynamicResource RadBaseBrush}" />
                            <Setter TargetName="ellipse" Property="Ellipse.Stroke" Value="{DynamicResource RadBorderAltBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="MouseOver">
                        <VisualState.Setters>
                            <Setter TargetName="ellipse" Property="Ellipse.WidthRequest" Value="12" />
                            <Setter TargetName="ellipse" Property="Ellipse.HeightRequest" Value="12" />
                            <Setter TargetName="ellipse" Property="Ellipse.Fill" Value="{DynamicResource RadBaseBrush}" />
                            <Setter TargetName="ellipse" Property="Ellipse.Stroke" Value="{DynamicResource RadOnLightColorAlpha13}" />
                            <Setter TargetName="ellipse" Property="Ellipse.StrokeThickness" Value="2" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <VisualState.Setters>
                            <Setter TargetName="ellipse" Property="Ellipse.Fill" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter TargetName="ellipse" Property="Ellipse.Stroke" Value="{DynamicResource RadBorderAltBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="SelectedMouseOver">
                        <VisualState.Setters>
                            <Setter TargetName="ellipse" Property="Ellipse.Fill" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter TargetName="ellipse" Property="Ellipse.WidthRequest" Value="12" />
                            <Setter TargetName="ellipse" Property="Ellipse.HeightRequest" Value="12" />
                            <Setter TargetName="ellipse" Property="Ellipse.Stroke" Value="{DynamicResource RadOnLightColorAlpha13}" />
                            <Setter TargetName="ellipse" Property="Ellipse.StrokeThickness" Value="2" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter TargetName="ellipse" Property="Opacity" Value="0.6" />
                            <Setter TargetName="ellipse" Property="Ellipse.Fill" Value="{DynamicResource RadBaseBrush}" />
                            <Setter TargetName="ellipse" Property="Ellipse.Stroke" Value="{DynamicResource RadBorderAltBrush}" />
                            <Setter TargetName="ellipse" Property="Ellipse.StrokeThickness" Value="0" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <Ellipse x:Name="ellipse"
                     WidthRequest="10"
                     HeightRequest="10"
                     HorizontalOptions="Center"
                     VerticalOptions="Center"
                     Fill="{DynamicResource RadBaseBrush}"
                     Stroke="{DynamicResource RadBorderAltBrush}"
                     StrokeThickness="0" />
        </Grid>
    </ControlTemplate>

    <Style TargetType="telerikMauiSlideView:SlideViewIndicatorItem">
        <Setter Property="ControlTemplate" Value="{StaticResource SlideViewIndicatorItemControlTemplate}" />
    </Style>

    <Style x:Key="RadSlideViewStyle" TargetType="telerikMauiControls:RadSlideView">
        <Setter Property="ControlTemplate" Value="{StaticResource SlideViewControlTemplate}" />
        <Setter Property="NavigateToPreviousItemButtonStyle" Value="{StaticResource SlideViewNavigateToPreviousItemButtonStyle}" />
        <Setter Property="NavigateToNextItemButtonStyle" Value="{StaticResource SlideViewNavigateToNextItemButtonStyle}" />
        <Style.Triggers>
            <Trigger TargetType="telerikMauiControls:RadSlideView" Property="Orientation" Value="Vertical">
                <Setter Property="NavigateToPreviousItemButtonStyle" Value="{DynamicResource SlideViewVerticalNavigateToPreviousItemButtonStyle}" />
                <Setter Property="NavigateToNextItemButtonStyle" Value="{DynamicResource SlideViewVerticalNavigateToNextItemButtonStyle}" />
            </Trigger>
        </Style.Triggers>
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver" />
                    <VisualState x:Name="Pressed" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:RadSlideView" BasedOn="{StaticResource RadSlideViewStyle}" />

</ResourceDictionary>
