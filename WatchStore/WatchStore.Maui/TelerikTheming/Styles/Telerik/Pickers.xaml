<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Telerik.Styles.Pickers">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <!-- Dependencies: Buttons.xaml -->

    <Shadow x:Key="PickerContentViewShadow" Offset="0, 4" Radius="5" Opacity="0.04" />

    <Style x:Key="PickerDisplayLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="PickerPlaceholderLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="PickerClearButtonStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Text" Value="&#xe84e;" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0, 0, 4, 0" />
        <Setter Property="WidthRequest" Value="20" />
        <Setter Property="HeightRequest" Value="20" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="PickerSpinnerHeaderStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
    </Style>

    <Style x:Key="PickerSpinnerSeparatorStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style x:Key="PickerSpinnerHeaderSeparatorStyle" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource PickerSpinnerSeparatorStyle}" />

    <Style x:Key="PickerSpinnerItemStyle" TargetType="telerikMauiControls:SpinnerItemView">
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
    </Style>

    <Style x:Key="PickerSpinnerSelectedItemStyle" TargetType="telerikMauiControls:SpinnerItemView">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="PickerSpinnerSelectionHighlightStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0, 1" />
    </Style>

    <Style x:Key="PickerViewStyle" TargetType="telerikMauiControls:PickerContentView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Shadow" Value="{StaticResource PickerContentViewShadow}" />
    </Style>

    <Style x:Key="PickerViewCancelButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}">
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="{OnPlatform Default='9, 5', iOS='9, 6.5', MacCatalyst='9, 6.5'}" />
        <Setter Property="Margin" Value="0" />
    </Style>

    <Style x:Key="PickerViewAcceptButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadPrimaryButtonStyle}">
        <Setter Property="Padding" Value="{OnPlatform Default='9, 5', iOS='9, 6.5', MacCatalyst='9, 6.5'}" />
        <Setter Property="Margin" Value="0" />
    </Style>

    <Style x:Key="PickerViewHeaderLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="Margin" Value="8" />
    </Style>

    <Style x:Key="PickerViewHeaderStyle" TargetType="telerikMauiControls:PickerContentView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
    </Style>

    <ControlTemplate x:Key="PickerViewFooterTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, *"
                  ColumnSpacing="4">
                <telerikMauiControls:RadButton Grid.Column="{OnPlatform Default=0, WinUI=1}"
                                               Text="{TemplateBinding CancelButtonText}"
                                               Style="{TemplateBinding CancelButtonStyle}"
                                               Command="{TemplateBinding CancelCommand}"
                                               AutomationId="PickerPopupCancelButton" />
                <telerikMauiControls:RadButton Grid.Column="{OnPlatform Default=1, WinUI=0}"
                                               Text="{TemplateBinding AcceptButtonText}"
                                               Style="{TemplateBinding AcceptButtonStyle}"
                                               Command="{TemplateBinding AcceptCommand}"
                                               AutomationId="PickerPopupOkButton" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="PickerViewFooterStyle" TargetType="telerikMauiControls:PickerContentView">
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Margin" Value="8" />
    </Style>

    <Style x:Key="PickerDropDownButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}">
        <Setter Property="FontFamily" Value="{Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Text" Value="{Static telerikMaui:TelerikFont.IconDownDir}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=32, iOS=32, MacCatalyst=28, WinUI=28}" />
        <Setter Property="WidthRequest" Value="NaN" />
        <Setter Property="HeightRequest" Value="NaN" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>

    <Style x:Key="OutlinePickerDropDownButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadOutlineButtonStyle}">
        <Setter Property="FontFamily" Value="{Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Text" Value="{Static telerikMaui:TelerikFont.IconDownDir}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=32, iOS=32, MacCatalyst=28, WinUI=28}" />
        <Setter Property="WidthRequest" Value="NaN" />
        <Setter Property="HeightRequest" Value="NaN" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>

    <Style x:Key="FlatPickerDropDownButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadFlatButtonStyle}">
        <Setter Property="FontFamily" Value="{Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Text" Value="{Static telerikMaui:TelerikFont.IconDownDir}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=32, iOS=32, MacCatalyst=28, WinUI=28}" />
        <Setter Property="WidthRequest" Value="NaN" />
        <Setter Property="HeightRequest" Value="NaN" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>

    <Style x:Key="PickerBaseStyle" TargetType="telerikMauiControls:RadPickerBase">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="DisplayLabelStyle" Value="{StaticResource PickerDisplayLabelStyle}" />
        <Setter Property="PlaceholderLabelStyle" Value="{StaticResource PickerPlaceholderLabelStyle}" />
        <Setter Property="ClearButtonStyle" Value="{StaticResource PickerClearButtonStyle}" />
        <Setter Property="ToggleButtonStyle" Value="{StaticResource PickerDropDownButtonStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderAltColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="OutlinePickerBaseStyle" TargetType="telerikMauiControls:RadPickerBase" BasedOn="{StaticResource PickerBaseStyle}">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
        <Setter Property="ToggleButtonStyle" Value="{StaticResource OutlinePickerDropDownButtonStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha80}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="FlatPickerBaseStyle" TargetType="telerikMauiControls:RadPickerBase" BasedOn="{StaticResource PickerBaseStyle}">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="CornerRadius" Value="4, 4, 0, 0" />
        <Setter Property="ToggleButtonStyle" Value="{StaticResource FlatPickerDropDownButtonStyle}" />
    </Style>

    <Style x:Key="PickerDropDownSettingsStyle" TargetType="telerikMauiControls:PickerDropDownSettings">
        <Setter Property="DropDownViewStyle" Value="{StaticResource PickerViewStyle}" />
        <Setter Property="FooterTemplate" Value="{StaticResource PickerViewFooterTemplate}" />
        <Setter Property="FooterStyle" Value="{StaticResource PickerViewFooterStyle}" />
        <Setter Property="CancelButtonStyle" Value="{StaticResource PickerViewCancelButtonStyle}" />
        <Setter Property="AcceptButtonStyle" Value="{StaticResource PickerViewAcceptButtonStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:PickerDropDownSettings" BasedOn="{StaticResource PickerDropDownSettingsStyle}" />

    <Style x:Key="PickerPopupSettingsStyle" TargetType="telerikMauiControls:PickerPopupSettings">
        <Setter Property="PopupViewStyle" Value="{StaticResource PickerViewStyle}" />
        <Setter Property="HeaderLabelStyle" Value="{StaticResource PickerViewHeaderLabelStyle}" />
        <Setter Property="HeaderStyle" Value="{StaticResource PickerViewHeaderStyle}" />
        <Setter Property="FooterTemplate" Value="{StaticResource PickerViewFooterTemplate}" />
        <Setter Property="FooterStyle" Value="{StaticResource PickerViewFooterStyle}" />
        <Setter Property="CancelButtonStyle" Value="{StaticResource PickerViewCancelButtonStyle}" />
        <Setter Property="AcceptButtonStyle" Value="{StaticResource PickerViewAcceptButtonStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:PickerPopupSettings" BasedOn="{StaticResource PickerPopupSettingsStyle}" />

    <Style x:Key="RadTemplatedPickerStyle" TargetType="telerikMauiControls:RadTemplatedPicker" BasedOn="{StaticResource PickerBaseStyle}" />
    <Style TargetType="telerikMauiControls:RadTemplatedPicker" BasedOn="{StaticResource RadTemplatedPickerStyle}" />

    <Style x:Key="RadOutlineTemplatedPickerStyle" TargetType="telerikMauiControls:RadTemplatedPicker" BasedOn="{StaticResource OutlinePickerBaseStyle}" />
    <Style x:Key="RadFlatTemplatedPickerStyle" TargetType="telerikMauiControls:RadTemplatedPicker" BasedOn="{StaticResource FlatPickerBaseStyle}" />

    <Style x:Key="RadListPickerStyle" TargetType="telerikMauiControls:RadListPicker" BasedOn="{StaticResource PickerBaseStyle}">
        <Setter Property="ItemStyle" Value="{StaticResource PickerSpinnerItemStyle}" />
        <Setter Property="SelectedItemStyle" Value="{StaticResource PickerSpinnerSelectedItemStyle}" />
        <Setter Property="SelectionHighlightStyle" Value="{StaticResource PickerSpinnerSelectionHighlightStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadListPicker" BasedOn="{StaticResource RadListPickerStyle}" />

    <Style x:Key="RadOutlineListPickerStyle" TargetType="telerikMauiControls:RadListPicker" BasedOn="{StaticResource OutlinePickerBaseStyle}" />
    <Style x:Key="RadFlatListPickerStyle" TargetType="telerikMauiControls:RadListPicker" BasedOn="{StaticResource FlatPickerBaseStyle}" />

    <Style x:Key="MultiSpinnerPickerSpinnerStyle" TargetType="telerikMauiControls:RadSpinner">
        <Setter Property="ItemStyle" Value="{StaticResource PickerSpinnerItemStyle}" />
        <Setter Property="SelectedItemStyle" Value="{StaticResource PickerSpinnerSelectedItemStyle}" />
    </Style>

    <Style x:Key="MultiSpinnerPickerBaseStyle" TargetType="telerikMauiControls:RadMultiSpinnerPickerBase" BasedOn="{StaticResource PickerBaseStyle}">
        <Setter Property="SpinnerStyle" Value="{StaticResource MultiSpinnerPickerSpinnerStyle}" />
        <Setter Property="SpinnerHeaderStyle" Value="{StaticResource PickerSpinnerHeaderStyle}" />
        <Setter Property="SpinnerHeaderSeparatorStyle" Value="{StaticResource PickerSpinnerHeaderSeparatorStyle}" />
        <Setter Property="SpinnerSeparatorStyle" Value="{StaticResource PickerSpinnerSeparatorStyle}" />
        <Setter Property="SelectionHighlightStyle" Value="{StaticResource PickerSpinnerSelectionHighlightStyle}" />
    </Style>

    <Style x:Key="OutlineMultiSpinnerPickerBaseStyle" TargetType="telerikMauiControls:RadMultiSpinnerPickerBase" BasedOn="{StaticResource MultiSpinnerPickerBaseStyle}">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
        <Setter Property="ToggleButtonStyle" Value="{StaticResource OutlinePickerDropDownButtonStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha80}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="FlatMultiSpinnerPickerBaseStyle" TargetType="telerikMauiControls:RadMultiSpinnerPickerBase" BasedOn="{StaticResource MultiSpinnerPickerBaseStyle}">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="CornerRadius" Value="4, 4, 0, 0" />
        <Setter Property="ToggleButtonStyle" Value="{StaticResource FlatPickerDropDownButtonStyle}" />
    </Style>

    <Style x:Key="MultiSpinnerPickerTabStripStyle" TargetType="telerikMauiControls:TabViewHeader">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="8" />
    </Style>

    <Style x:Key="MultiSpinnerPickerTabStripItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="MinimumHeightRequest" Value="30" />
        <Setter Property="ContentPadding" Value="8, 4" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RadDatePickerStyle" TargetType="telerikMauiControls:RadDatePicker" BasedOn="{StaticResource MultiSpinnerPickerBaseStyle}" />
    <Style TargetType="telerikMauiControls:RadDatePicker" BasedOn="{StaticResource RadDatePickerStyle}" />

    <Style x:Key="RadOutlineDatePickerStyle" TargetType="telerikMauiControls:RadDatePicker" BasedOn="{StaticResource OutlineMultiSpinnerPickerBaseStyle}" />
    <Style x:Key="RadFlatDatePickerStyle" TargetType="telerikMauiControls:RadDatePicker" BasedOn="{StaticResource FlatMultiSpinnerPickerBaseStyle}" />

    <Style x:Key="RadTimePickerStyle" TargetType="telerikMauiControls:RadTimePicker" BasedOn="{StaticResource MultiSpinnerPickerBaseStyle}" />
    <Style TargetType="telerikMauiControls:RadTimePicker" BasedOn="{StaticResource RadTimePickerStyle}" />

    <Style x:Key="RadOutlineTimePickerStyle" TargetType="telerikMauiControls:RadTimePicker" BasedOn="{StaticResource OutlineMultiSpinnerPickerBaseStyle}" />
    <Style x:Key="RadFlatTimePickerStyle" TargetType="telerikMauiControls:RadTimePicker" BasedOn="{StaticResource FlatMultiSpinnerPickerBaseStyle}" />

    <Style x:Key="RadTimeSpanPickerStyle" TargetType="telerikMauiControls:RadTimeSpanPicker" BasedOn="{StaticResource MultiSpinnerPickerBaseStyle}" />
    <Style TargetType="telerikMauiControls:RadTimeSpanPicker" BasedOn="{StaticResource RadTimeSpanPickerStyle}" />

    <Style x:Key="RadOutlineTimeSpanPickerStyle" TargetType="telerikMauiControls:RadTimeSpanPicker" BasedOn="{StaticResource OutlineMultiSpinnerPickerBaseStyle}" />
    <Style x:Key="RadFlatTimeSpanPickerStyle" TargetType="telerikMauiControls:RadTimeSpanPicker" BasedOn="{StaticResource FlatMultiSpinnerPickerBaseStyle}" />

    <Style x:Key="RadDateTimePickerStyle" TargetType="telerikMauiControls:RadDateTimePicker" BasedOn="{StaticResource MultiSpinnerPickerBaseStyle}">
        <Setter Property="TabStripStyle" Value="{StaticResource MultiSpinnerPickerTabStripStyle}" />
        <Setter Property="TabStripItemStyle" Value="{StaticResource MultiSpinnerPickerTabStripItemStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadDateTimePicker" BasedOn="{StaticResource RadDateTimePickerStyle}" />

    <Style x:Key="RadOutlineDateTimePickerStyle" TargetType="telerikMauiControls:RadDateTimePicker" BasedOn="{StaticResource OutlineMultiSpinnerPickerBaseStyle}">
        <Setter Property="TabStripStyle" Value="{StaticResource MultiSpinnerPickerTabStripStyle}" />
        <Setter Property="TabStripItemStyle" Value="{StaticResource MultiSpinnerPickerTabStripItemStyle}" />
    </Style>
    <Style x:Key="RadFlatDateTimePickerStyle" TargetType="telerikMauiControls:RadDateTimePicker" BasedOn="{StaticResource FlatMultiSpinnerPickerBaseStyle}">
        <Setter Property="TabStripStyle" Value="{StaticResource MultiSpinnerPickerTabStripStyle}" />
        <Setter Property="TabStripItemStyle" Value="{StaticResource MultiSpinnerPickerTabStripItemStyle}" />
    </Style>

</ResourceDictionary>
