<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Telerik.Styles.NumericInput">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <!-- Dependencies: Buttons.xaml -->

    <telerikMauiControls:CornerRadiusToThicknessConverter x:Key="CornerRadiusToThicknessConverter" />

    <Style x:Key="NumericInputButtonBaseStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource RadTemplatedButtonStyle}">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="BorderBrush" Value="{Binding BorderBrush, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadNumericInput}}}" />
        <Setter Property="BorderThickness" Value="1, 0, 0, 0" />
        <Setter Property="MinimumWidthRequest" Value="0" />
        <Setter Property="WidthRequest" Value="{OnPlatform Android=32, iOS=32, MacCatalyst=28, WinUI=28}" />
        <Setter Property="HeightRequest" Value="NaN" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0" />
    </Style>
    <Style x:Key="NumericInputIncreaseButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource NumericInputButtonBaseStyle}">
        <Setter Property="VerticalTextAlignment" Value="End" />
        <Setter Property="CornerRadius" Value="0, 4, 0, 0" />
    </Style>
    <Style x:Key="NumericInputDecreaseButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource NumericInputButtonBaseStyle}">
        <Setter Property="VerticalTextAlignment" Value="Start" />
        <Setter Property="CornerRadius" Value="0, 0, 0, 4" />
    </Style>
    <Style x:Key="NumericInputTextInputStyle" TargetType="telerikMauiControls:NumericInputTextInput">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=0, iOS=19}" />
        <Setter Property="Padding" Value="{OnPlatform Android='9, 12', iOS='9, 11.5', MacCatalyst='9, 5.75', WinUI='9, 4.5'}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="ReadOnly" />
                    <VisualState x:Name="Disabled" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <ControlTemplate x:Key="RadNumericInputTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       Background="{TemplateBinding Background}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}">
            <Grid ColumnDefinitions="*, Auto">
                <telerikMauiControls:NumericInputTextInput x:Name="PART_TextInput"
                                                           Style="{TemplateBinding ActualTextInputStyle}" />
                <Grid Grid.Column="1"
                      RowDefinitions="*, *">
                    <telerikMauiControls:RadTemplatedButton Style="{TemplateBinding ActualIncreaseButtonStyle}" />
                    <telerikMauiControls:RadTemplatedButton Grid.Row="1"
                                                Style="{TemplateBinding ActualDecreaseButtonStyle}" />
                </Grid>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="RadNumericInputStyle" TargetType="telerikMauiControls:RadNumericInput">
        <Setter Property="ControlTemplate" Value="{StaticResource RadNumericInputTemplate}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="TextInputStyle" Value="{StaticResource NumericInputTextInputStyle}" />
        <Setter Property="IncreaseButtonText" Value="{x:Static telerikMaui:TelerikFont.IconUpDir}" />
        <Setter Property="IncreaseButtonStyle" Value="{StaticResource NumericInputIncreaseButtonStyle}" />
        <Setter Property="DecreaseButtonText" Value="{x:Static telerikMaui:TelerikFont.IconDownDir}" />
        <Setter Property="DecreaseButtonStyle" Value="{StaticResource NumericInputDecreaseButtonStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="MouseOver" />
                    <VisualState Name="Focused" />
                    <VisualState Name="ReadOnly" />
                    <VisualState Name="ReadOnlyMouseOver" />
                    <VisualState Name="ReadOnlyFocused" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:RadNumericInput" BasedOn="{StaticResource RadNumericInputStyle}" />

    <Style x:Key="OutlineNumericInputButtonBaseStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource NumericInputButtonBaseStyle}">
        <Setter Property="Background" Value="Transparent" />
    </Style>
    <Style x:Key="OutlineNumericInputIncreaseButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource OutlineNumericInputButtonBaseStyle}">
        <Setter Property="VerticalTextAlignment" Value="End" />
        <Setter Property="CornerRadius" Value="0, 4, 0, 0" />
    </Style>
    <Style x:Key="OutlineNumericInputDecreaseButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource OutlineNumericInputButtonBaseStyle}">
        <Setter Property="VerticalTextAlignment" Value="Start" />
        <Setter Property="CornerRadius" Value="0, 0, 0, 4" />
    </Style>
    <Style x:Key="OutlineNumericInputTextInputStyle" TargetType="telerikMauiControls:NumericInputTextInput" BasedOn="{StaticResource NumericInputTextInputStyle}" />
    <Style x:Key="RadOutlineNumericInputStyle" TargetType="telerikMauiControls:RadNumericInput" BasedOn="{StaticResource RadNumericInputStyle}">
        <Setter Property="IncreaseButtonStyle" Value="{StaticResource OutlineNumericInputIncreaseButtonStyle}" />
        <Setter Property="DecreaseButtonStyle" Value="{StaticResource OutlineNumericInputDecreaseButtonStyle}" />
        <Setter Property="TextInputStyle" Value="{StaticResource OutlineNumericInputTextInputStyle}" />
    </Style>

    <Style x:Key="FlatNumericInputButtonBaseStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource NumericInputButtonBaseStyle}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
    </Style>
    <Style x:Key="FlatNumericInputIncreaseButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource FlatNumericInputButtonBaseStyle}">
        <Setter Property="VerticalTextAlignment" Value="End" />
        <Setter Property="CornerRadius" Value="0, 4, 0, 0" />
    </Style>
    <Style x:Key="FlatNumericInputDecreaseButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource FlatNumericInputButtonBaseStyle}">
        <Setter Property="VerticalTextAlignment" Value="Start" />
        <Setter Property="CornerRadius" Value="0" />
    </Style>
    <Style x:Key="FlatNumericInputTextInputStyle" TargetType="telerikMauiControls:NumericInputTextInput" BasedOn="{StaticResource NumericInputTextInputStyle}" />
    <Style x:Key="RadFlatNumericInputStyle" TargetType="telerikMauiControls:RadNumericInput" BasedOn="{StaticResource RadNumericInputStyle}">
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="CornerRadius" Value="4, 4, 0, 0" />
        <Setter Property="IncreaseButtonStyle" Value="{StaticResource FlatNumericInputIncreaseButtonStyle}" />
        <Setter Property="DecreaseButtonStyle" Value="{StaticResource FlatNumericInputDecreaseButtonStyle}" />
        <Setter Property="TextInputStyle" Value="{StaticResource FlatNumericInputTextInputStyle}" />
    </Style>

</ResourceDictionary>
