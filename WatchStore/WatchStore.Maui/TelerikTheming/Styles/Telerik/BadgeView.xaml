<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Telerik.Styles.BadgeView">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <Style x:Key="RadPrimaryBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView">
        <Setter Property="BadgeTextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="BadgeBackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BadgeBorderColor" Value="{DynamicResource RadPrimaryColor}" />
    </Style>

    <Style x:Key="RadInfoBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView">
        <Setter Property="BadgeTextColor" Value="{DynamicResource RadOnInfoColor}" />
        <Setter Property="BadgeBackgroundColor" Value="{DynamicResource RadInfoColor}" />
        <Setter Property="BadgeBorderColor" Value="{DynamicResource RadInfoColor}" />
    </Style>

    <Style x:Key="RadSuccessBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView">
        <Setter Property="BadgeTextColor" Value="{DynamicResource RadOnSuccessColor}" />
        <Setter Property="BadgeBackgroundColor" Value="{DynamicResource RadSuccessColor}" />
        <Setter Property="BadgeBorderColor" Value="{DynamicResource RadSuccessColor}" />
    </Style>

    <Style x:Key="RadWarningBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView">
        <Setter Property="BadgeTextColor" Value="{DynamicResource RadOnWarningColor}" />
        <Setter Property="BadgeBackgroundColor" Value="{DynamicResource RadWarningColor}" />
        <Setter Property="BadgeBorderColor" Value="{DynamicResource RadWarningColor}" />
    </Style>

    <Style x:Key="RadErrorBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView">
        <Setter Property="BadgeTextColor" Value="{DynamicResource RadOnErrorColor}" />
        <Setter Property="BadgeBackgroundColor" Value="{DynamicResource RadErrorColor}" />
        <Setter Property="BadgeBorderColor" Value="{DynamicResource RadErrorColor}" />
    </Style>

    <Style x:Key="RadPrimaryOutlineBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView">
        <Setter Property="BadgeTextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BadgeBackgroundColor" Value="Transparent" />
        <Setter Property="BadgeBorderColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BadgeBorderThickness" Value="1" />
    </Style>

    <Style x:Key="RadInfoOutlineBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView">
        <Setter Property="BadgeTextColor" Value="{DynamicResource RadInfoColor}" />
        <Setter Property="BadgeBackgroundColor" Value="Transparent" />
        <Setter Property="BadgeBorderColor" Value="{DynamicResource RadInfoColor}" />
        <Setter Property="BadgeBorderThickness" Value="1" />
    </Style>

    <Style x:Key="RadSuccessOutlineBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView">
        <Setter Property="BadgeTextColor" Value="{DynamicResource RadSuccessColor}" />
        <Setter Property="BadgeBackgroundColor" Value="Transparent" />
        <Setter Property="BadgeBorderColor" Value="{DynamicResource RadSuccessColor}" />
        <Setter Property="BadgeBorderThickness" Value="1" />
    </Style>

    <Style x:Key="RadWarningOutlineBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView">
        <Setter Property="BadgeTextColor" Value="{DynamicResource RadWarningColor}" />
        <Setter Property="BadgeBackgroundColor" Value="Transparent" />
        <Setter Property="BadgeBorderColor" Value="{DynamicResource RadWarningColor}" />
        <Setter Property="BadgeBorderThickness" Value="1" />
    </Style>

    <Style x:Key="RadErrorOutlineBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView">
        <Setter Property="BadgeTextColor" Value="{DynamicResource RadErrorColor}" />
        <Setter Property="BadgeBackgroundColor" Value="Transparent" />
        <Setter Property="BadgeBorderColor" Value="{DynamicResource RadErrorColor}" />
        <Setter Property="BadgeBorderThickness" Value="1" />
    </Style>

    <Style x:Key="RadPrimaryRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadPrimaryBadgeViewStyle}">
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeBorderThickness" Value="0" />
        <Setter Property="BadgeCornerRadius" Value="0" />
    </Style>

    <Style x:Key="RadPrimaryOutlineRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadPrimaryOutlineBadgeViewStyle}">
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeBorderThickness" Value="0" />
        <Setter Property="BadgeCornerRadius" Value="0" />
    </Style>

    <Style x:Key="RadPrimaryRoundedRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadPrimaryBadgeViewStyle}">
        <Setter Property="BadgeCornerRadius" Value="2" />
    </Style>

    <Style x:Key="RadPrimaryOutlineRoundedRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadPrimaryOutlineBadgeViewStyle}">
        <Setter Property="BadgeCornerRadius" Value="2" />
    </Style>

    <Style x:Key="RadPrimaryCircleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadPrimaryBadgeViewStyle}">
        <Setter Property="MinimumWidthRequest" Value="20" />
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeCornerRadius" Value="10" />
    </Style>

    <Style x:Key="RadPrimaryOutlineCircleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadPrimaryOutlineBadgeViewStyle}">
        <Setter Property="MinimumWidthRequest" Value="20" />
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeCornerRadius" Value="10" />
    </Style>

    <Style x:Key="RadPrimaryDotBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadPrimaryBadgeViewStyle}">
        <Setter Property="BadgeMinimumWidth" Value="10" />
        <Setter Property="BadgeMinimumHeight" Value="10" />
        <Setter Property="BadgeCornerRadius" Value="5" />
    </Style>

    <Style x:Key="RadPrimaryOutlineDotBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadPrimaryOutlineBadgeViewStyle}">
        <Setter Property="BadgeMinimumWidth" Value="10" />
        <Setter Property="BadgeMinimumHeight" Value="10" />
        <Setter Property="BadgeCornerRadius" Value="5" />
    </Style>

    <Style x:Key="RadInfoRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadInfoBadgeViewStyle}">
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeBorderThickness" Value="0" />
        <Setter Property="BadgeCornerRadius" Value="0" />
    </Style>

    <Style x:Key="RadInfoOutlineRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadInfoOutlineBadgeViewStyle}">
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeBorderThickness" Value="0" />
        <Setter Property="BadgeCornerRadius" Value="0" />
    </Style>

    <Style x:Key="RadInfoRoundedRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadInfoRectangleBadgeViewStyle}">
        <Setter Property="BadgeCornerRadius" Value="2" />
    </Style>

    <Style x:Key="RadInfoOutlineRoundedRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadInfoOutlineRectangleBadgeViewStyle}">
        <Setter Property="BadgeCornerRadius" Value="2" />
    </Style>

    <Style x:Key="RadInfoCircleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadInfoBadgeViewStyle}">
        <Setter Property="MinimumWidthRequest" Value="20" />
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeCornerRadius" Value="10" />
    </Style>

    <Style x:Key="RadInfoOutlineCircleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadInfoOutlineBadgeViewStyle}">
        <Setter Property="MinimumWidthRequest" Value="20" />
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeCornerRadius" Value="10" />
    </Style>

    <Style x:Key="RadInfoDotBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadInfoBadgeViewStyle}">
        <Setter Property="BadgeMinimumWidth" Value="10" />
        <Setter Property="BadgeMinimumHeight" Value="10" />
        <Setter Property="BadgeCornerRadius" Value="5" />
    </Style>

    <Style x:Key="RadInfoOutlineDotBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadInfoOutlineBadgeViewStyle}">
        <Setter Property="BadgeMinimumWidth" Value="10" />
        <Setter Property="BadgeMinimumHeight" Value="10" />
        <Setter Property="BadgeCornerRadius" Value="5" />
    </Style>

    <Style x:Key="RadSuccessRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadSuccessBadgeViewStyle}">
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeBorderThickness" Value="0" />
        <Setter Property="BadgeCornerRadius" Value="0" />
    </Style>

    <Style x:Key="RadSuccessOutlineRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadSuccessOutlineBadgeViewStyle}">
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeBorderThickness" Value="0" />
        <Setter Property="BadgeCornerRadius" Value="0" />
    </Style>

    <Style x:Key="RadSuccessRoundedRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadSuccessRectangleBadgeViewStyle}">
        <Setter Property="BadgeCornerRadius" Value="2" />
    </Style>

    <Style x:Key="RadSuccessOutlineRoundedRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadSuccessOutlineRectangleBadgeViewStyle}">
        <Setter Property="BadgeCornerRadius" Value="2" />
    </Style>

    <Style x:Key="RadSuccessCircleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadSuccessBadgeViewStyle}">
        <Setter Property="MinimumWidthRequest" Value="20" />
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeCornerRadius" Value="10" />
    </Style>

    <Style x:Key="RadSuccessOutlineCircleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadSuccessOutlineBadgeViewStyle}">
        <Setter Property="MinimumWidthRequest" Value="20" />
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeCornerRadius" Value="10" />
    </Style>

    <Style x:Key="RadSuccessDotBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadSuccessBadgeViewStyle}">
        <Setter Property="BadgeMinimumWidth" Value="10" />
        <Setter Property="BadgeMinimumHeight" Value="10" />
        <Setter Property="BadgeCornerRadius" Value="5" />
    </Style>

    <Style x:Key="RadSuccessOutlineDotBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadSuccessOutlineBadgeViewStyle}">
        <Setter Property="BadgeMinimumWidth" Value="10" />
        <Setter Property="BadgeMinimumHeight" Value="10" />
        <Setter Property="BadgeCornerRadius" Value="5" />
    </Style>

    <Style x:Key="RadWarningRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadWarningBadgeViewStyle}">
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeBorderThickness" Value="0" />
        <Setter Property="BadgeCornerRadius" Value="0" />
    </Style>

    <Style x:Key="RadWarningOutlineRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadWarningOutlineBadgeViewStyle}">
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeBorderThickness" Value="0" />
        <Setter Property="BadgeCornerRadius" Value="0" />
    </Style>

    <Style x:Key="RadWarningRoundedRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadWarningRectangleBadgeViewStyle}">
        <Setter Property="BadgeCornerRadius" Value="2" />
    </Style>

    <Style x:Key="RadWarningOutlineRoundedRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadWarningOutlineRectangleBadgeViewStyle}">
        <Setter Property="BadgeCornerRadius" Value="2" />
    </Style>

    <Style x:Key="RadWarningCircleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadWarningBadgeViewStyle}">
        <Setter Property="MinimumWidthRequest" Value="20" />
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeCornerRadius" Value="10" />
    </Style>

    <Style x:Key="RadWarningOutlineCircleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadWarningOutlineBadgeViewStyle}">
        <Setter Property="MinimumWidthRequest" Value="20" />
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeCornerRadius" Value="10" />
    </Style>

    <Style x:Key="RadWarningDotBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadWarningBadgeViewStyle}">
        <Setter Property="BadgeMinimumWidth" Value="10" />
        <Setter Property="BadgeMinimumHeight" Value="10" />
        <Setter Property="BadgeCornerRadius" Value="5" />
    </Style>

    <Style x:Key="RadWarningOutlineDotBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadWarningOutlineBadgeViewStyle}">
        <Setter Property="BadgeMinimumWidth" Value="10" />
        <Setter Property="BadgeMinimumHeight" Value="10" />
        <Setter Property="BadgeCornerRadius" Value="5" />
    </Style>

    <Style x:Key="RadErrorRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadErrorBadgeViewStyle}">
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeBorderThickness" Value="0" />
        <Setter Property="BadgeCornerRadius" Value="0" />
    </Style>

    <Style x:Key="RadErrorOutlineRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadErrorOutlineBadgeViewStyle}">
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeBorderThickness" Value="0" />
        <Setter Property="BadgeCornerRadius" Value="0" />
    </Style>

    <Style x:Key="RadErrorRoundedRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadErrorRectangleBadgeViewStyle}">
        <Setter Property="BadgeCornerRadius" Value="2" />
    </Style>

    <Style x:Key="RadErrorOutlineRoundedRectangleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadErrorOutlineRectangleBadgeViewStyle}">
        <Setter Property="BadgeCornerRadius" Value="2" />
    </Style>

    <Style x:Key="RadErrorCircleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadErrorBadgeViewStyle}">
        <Setter Property="MinimumWidthRequest" Value="20" />
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeCornerRadius" Value="10" />
    </Style>

    <Style x:Key="RadErrorOutlineCircleBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadErrorOutlineBadgeViewStyle}">
        <Setter Property="MinimumWidthRequest" Value="20" />
        <Setter Property="MinimumHeightRequest" Value="20" />
        <Setter Property="BadgeCornerRadius" Value="10" />
    </Style>

    <Style x:Key="RadErrorDotBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadErrorBadgeViewStyle}">
        <Setter Property="BadgeMinimumWidth" Value="10" />
        <Setter Property="BadgeMinimumHeight" Value="10" />
        <Setter Property="BadgeCornerRadius" Value="5" />
    </Style>

    <Style x:Key="RadErrorOutlineDotBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadErrorOutlineBadgeViewStyle}">
        <Setter Property="BadgeMinimumWidth" Value="10" />
        <Setter Property="BadgeMinimumHeight" Value="10" />
        <Setter Property="BadgeCornerRadius" Value="5" />
    </Style>

</ResourceDictionary>
