<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMauiRichTextEditor="clr-namespace:Telerik.Maui.Controls.RichTextEditor;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Telerik.Styles.RichTextEditor">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <!-- Dependencies: Buttons.xaml, Toolbar.xaml -->

    <Shadow x:Key="RichTextEditorDialogShadow" Offset="0, 4" Radius="5" Opacity="0.04" />

    <Style x:Key="RichTextEditorListPickerToolbarItemViewStyle" TargetType="telerikMauiRichTextEditor:RichTextEditorListPickerToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style TargetType="telerikMauiRichTextEditor:RichTextEditorListPickerToolbarItemView" BasedOn="{StaticResource RichTextEditorListPickerToolbarItemViewStyle}" />
    <Style TargetType="telerikMauiRichTextEditor:RichTextEditorListPickerToolbarItemViewContent" BasedOn="{StaticResource DropDownButtonToolbarItemViewContentStyle}" />

    <DataTemplate x:Key="RichTextEditorNullColorPickerToolbarItemItemTemplate">
        <Grid WidthRequest="{OnPlatform Android=48, iOS=44, MacCatalyst=22, WinUI=40}"
              HeightRequest="{OnPlatform Android=48, iOS=44, MacCatalyst=22, WinUI=40}">
            <telerikMauiControls:RadBorder BorderColor="{DynamicResource RadBorderColor}"
                                           IsVisible="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:ToolbarListItemView}}, Path=IsSelected}"
                                           WidthRequest="{OnPlatform  Default=30, MacCatalyst=16, WinUI=24}"
                                           HeightRequest="{OnPlatform Default=30, MacCatalyst=16, WinUI=24}"
                                           CornerRadius="{OnPlatform Default=15, MacCatalyst=8, WinUI=12}"
                                           BorderThickness="1"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center" />
            <telerikMauiControls:RadBorder WidthRequest="{OnPlatform Default=24, MacCatalyst=12, WinUI=20}"
                                           HeightRequest="{OnPlatform Default=24, MacCatalyst=12, WinUI=20}"
                                           CornerRadius="{OnPlatform Default=12, MacCatalyst=6, WinUI=10}"
                                           BorderThickness="1"
                                           BorderColor="{DynamicResource RadBorderColor}"
                                           BackgroundColor="Transparent"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center">
                <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource RadBorderColor}"
                                               HeightRequest="2"
                                               Rotation="-45" />
            </telerikMauiControls:RadBorder>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="RichTextEditorColorPickerToolbarItemItemTemplate">
        <Grid WidthRequest="{OnPlatform Android=48, iOS=44, MacCatalyst=22, WinUI=40}"
              HeightRequest="{OnPlatform Android=48, iOS=44, MacCatalyst=22, WinUI=40}">
            <telerikMauiControls:RadBorder BorderColor="{Binding}"
                                           IsVisible="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:ToolbarListItemView}}, Path=IsSelected}"                                          
                                           WidthRequest="{OnPlatform  Default=30, MacCatalyst=16, WinUI=24}"
                                           HeightRequest="{OnPlatform Default=30, MacCatalyst=16, WinUI=24}"
                                           CornerRadius="{OnPlatform Default=15, MacCatalyst=8, WinUI=12}"
                                           BorderThickness="1"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center" />
            <telerikMauiControls:RadBorder BackgroundColor="{Binding}"
                                           WidthRequest="{OnPlatform Default=24, MacCatalyst=12, WinUI=20}"
                                           HeightRequest="{OnPlatform Default=24, MacCatalyst=12, WinUI=20}"
                                           CornerRadius="{OnPlatform Default=12, MacCatalyst=6, WinUI=10}"
                                           BorderThickness="0"
                                           BorderColor="Transparent"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center" />
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="RichTextEditorWhiteColorPickerToolbarItemItemTemplate">
        <Grid WidthRequest="{OnPlatform Android=48, iOS=44, MacCatalyst=22, WinUI=40}"
              HeightRequest="{OnPlatform Android=48, iOS=44, MacCatalyst=22, WinUI=40}">
            <telerikMauiControls:RadBorder BorderColor="{DynamicResource RadBorderColor}"
                                           IsVisible="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:ToolbarListItemView}}, Path=IsSelected}"
                                           WidthRequest="{OnPlatform  Default=30, MacCatalyst=16, WinUI=24}"
                                           HeightRequest="{OnPlatform Default=30, MacCatalyst=16, WinUI=24}"
                                           CornerRadius="{OnPlatform Default=15, MacCatalyst=8, WinUI=12}"
                                           BorderThickness="1"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center" />
            <telerikMauiControls:RadBorder BackgroundColor="{Binding}"
                                           WidthRequest="{OnPlatform Default=24, MacCatalyst=12, WinUI=20}"
                                           HeightRequest="{OnPlatform Default=24, MacCatalyst=12, WinUI=20}"
                                           CornerRadius="{OnPlatform Default=12, MacCatalyst=6, WinUI=10}"
                                           BorderThickness="1"
                                           BorderColor="{DynamicResource RadBorderColor}"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center" />
        </Grid>
    </DataTemplate>

    <telerikMauiRichTextEditor:RichTextEditorColorPickerItemTemplateSelector x:Key="RichTextEditorColorPickerItemTemplateSelector"
                                                                             ItemTemplate="{StaticResource RichTextEditorColorPickerToolbarItemItemTemplate}"
                                                                             NullTemplate="{StaticResource RichTextEditorNullColorPickerToolbarItemItemTemplate}"
                                                                             WhiteTemplate="{StaticResource RichTextEditorWhiteColorPickerToolbarItemItemTemplate}" />

    <Style x:Key="RichTextEditorColorPickerToolbarItemViewStyle" TargetType="telerikMauiRichTextEditor:RichTextEditorColorPickerToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}">
        <Setter Property="ItemTemplate" Value="{StaticResource RichTextEditorColorPickerItemTemplateSelector}" />
    </Style>
    <Style TargetType="telerikMauiRichTextEditor:RichTextEditorColorPickerToolbarItemView" BasedOn="{StaticResource RichTextEditorColorPickerToolbarItemViewStyle}" ApplyToDerivedTypes="True" />
    <Style TargetType="telerikMauiRichTextEditor:RichTextEditorColorPickerToolbarItemViewContent" BasedOn="{StaticResource DropDownButtonToolbarItemViewContentStyle}" />

    <Style x:Key="RichTextEditorDialogHeaderStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="Padding" Value="16, 12" />
    </Style>

    <Style x:Key="RichTextEditorDialogFooterStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0, 1, 0, 0" />
        <Setter Property="Padding" Value="8" />
    </Style>

    <Style x:Key="RichTextEditorDialogOkButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadPrimaryButtonStyle}">
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>

    <Style x:Key="RichTextEditorDialogCancelButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}">
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>

    <ControlTemplate x:Key="RichTextEditorHyperlinkDialogTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Shadow="{StaticResource RichTextEditorDialogShadow}">
            <Grid RowDefinitions="Auto, *, Auto">
                <telerikMauiControls:RadBorder Style="{TemplateBinding ActualHeaderStyle}">
                    <Label Text="{TemplateBinding HeaderText}"
                           AutomationId="RichTextEditorHyperlinkDialogToolbarItemViewContent_Header"
                           TextColor="{DynamicResource RadOnPrimaryColor}"
                           FontSize="16"
                           VerticalOptions="Center" />
                </telerikMauiControls:RadBorder>
                <Grid Grid.Row="1"
                      RowDefinitions="*, *"
                      RowSpacing="{OnPlatform Default=4, WinUI=8}"
                      Padding="{OnPlatform Default='20, 12', WinUI='24, 12, 12, 24'}">
                    <Label Text="{TemplateBinding ActualUrlLabelText}"
                           AutomationId="RichTextEditorHyperlinkDialogToolbarItemViewContent_Header"
                           TextColor="{DynamicResource RadOnAppSurfaceColor}"
                           VerticalOptions="End" />
                    <telerikMauiControls:RadEntry Grid.Row="1"
                                                  Placeholder="{TemplateBinding ActualUrlPlaceholderText}"
                                                  Text="{TemplateBinding Url}"
                                                  VerticalOptions="Start" />
                </Grid>
                <telerikMauiControls:RadBorder Grid.Row="2"
                                               Style="{TemplateBinding ActualFooterStyle}">
                    <Grid ColumnDefinitions="*, *"
                          ColumnSpacing="4"
                          VerticalOptions="Center">
                        <telerikMauiControls:RadButton Text="{TemplateBinding CancelButtonText}"
                                                       Style="{TemplateBinding ActualCancelButtonStyle}"
                                                       Command="{TemplateBinding CancelCommand}" />
                        <telerikMauiControls:RadButton Grid.Column="1"
                                                       Text="{TemplateBinding OkButtonText}"
                                                       Style="{TemplateBinding ActualOkButtonStyle}"
                                                       Command="{TemplateBinding OkCommand}" />
                    </Grid>
                </telerikMauiControls:RadBorder>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>
    <Style x:Key="RichTextEditorHyperlinkDialogContentStyle" TargetType="telerikMauiRichTextEditor:RichTextEditorHyperlinkDialogToolbarItemViewContent">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="HeaderStyle" Value="{StaticResource RichTextEditorDialogHeaderStyle}" />
        <Setter Property="FooterStyle" Value="{StaticResource RichTextEditorDialogFooterStyle}" />
        <Setter Property="OkButtonStyle" Value="{StaticResource RichTextEditorDialogOkButtonStyle}" />
        <Setter Property="CancelButtonStyle" Value="{StaticResource RichTextEditorDialogCancelButtonStyle}" />
        <Setter Property="ControlTemplate" Value="{StaticResource RichTextEditorHyperlinkDialogTemplate}" />
    </Style>
    <Style TargetType="telerikMauiRichTextEditor:RichTextEditorHyperlinkDialogToolbarItemViewContent" BasedOn="{StaticResource RichTextEditorHyperlinkDialogContentStyle}" ApplyToDerivedTypes="True" />

    <ControlTemplate x:Key="RichTextEditorImageDialogTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Shadow="{StaticResource RichTextEditorDialogShadow}">
            <Grid RowDefinitions="Auto, *, Auto">
                <telerikMauiControls:RadBorder Style="{TemplateBinding ActualHeaderStyle}">
                    <Label Text="{TemplateBinding HeaderText}"
                           AutomationId="RichTextEditorImageDialogToolbarItemViewContentHeaderTextLabel"
                           TextColor="{DynamicResource RadOnPrimaryColor}"
                           FontSize="16"
                           VerticalOptions="Center" />
                </telerikMauiControls:RadBorder>
                <VerticalStackLayout Grid.Row="1"
                                     Padding="16, 12"
                                     Spacing="12">
                    <Grid HorizontalOptions="Center">
                        <Image WidthRequest="{OnPlatform Default=96, WinUI=112}"
                               HeightRequest="{OnPlatform Default=96, WinUI=112}"
                               Aspect="AspectFit"
                               Source="{TemplateBinding ImageSource}" />
                        <telerikMauiControls:RadButton Text="{TemplateBinding ActualPickButtonText}"
                                                       AutomationId="RichTextEditorImageDialogToolbarItemViewContentPickButton" 
                                                       Style="{TemplateBinding ActualPickButtonStyle}"
                                                       Command="{TemplateBinding PickCommand}" />
                    </Grid>
                    <Label Text="{TemplateBinding ActualResizeLabelText}"
                           AutomationId="RichTextEditorImageDialogToolbarItemViewContentResizeLabel"
                           TextColor="{DynamicResource RadOnAppSurfaceColor}" />
                    <Grid ColumnDefinitions="Auto, *, Auto"
                          ColumnSpacing="4">
                        <Label VerticalOptions="Center"
                               Text="{TemplateBinding ActualMinimumLabelText}"
                               AutomationId="RichTextEditorImageDialogToolbarItemViewContentMinimumLabel"
                               TextColor="{DynamicResource RadOnAppSurfaceColor}" />
                        <telerikMauiControls:RadSlider Grid.Column="1"
                                                       Minimum="{TemplateBinding MinimumScale}"
                                                       Maximum="{TemplateBinding MaximumScale}"
                                                       Value="{TemplateBinding ImageScale}" />
                        <Label Grid.Column="2"
                               VerticalOptions="Center"
                               Text="{TemplateBinding ActualMaximumLabelText}"
                               AutomationId="RichTextEditorImageDialogToolbarItemViewContentMaximumLabel"
                               TextColor="{DynamicResource RadOnAppSurfaceColor}" />
                    </Grid>
                </VerticalStackLayout>
                <telerikMauiControls:RadBorder Grid.Row="2"
                                               Style="{TemplateBinding ActualFooterStyle}">
                    <Grid ColumnDefinitions="*, *"
                          ColumnSpacing="4"
                          VerticalOptions="Center">
                        <telerikMauiControls:RadButton Text="{TemplateBinding CancelButtonText}"
                                                       Style="{TemplateBinding ActualCancelButtonStyle}"
                                                       Command="{TemplateBinding CancelCommand}" />
                        <telerikMauiControls:RadButton Grid.Column="1"
                                                       Text="{TemplateBinding OkButtonText}"
                                                       Style="{TemplateBinding ActualOkButtonStyle}"
                                                       Command="{TemplateBinding OkCommand}" />
                    </Grid>
                </telerikMauiControls:RadBorder>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>
    <Style x:Key="RichTextEditorImageDialogPickButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="WidthRequest" Value="30" />
        <Setter Property="HeightRequest" Value="30" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="4" />
        <Setter Property="HorizontalOptions" Value="End" />
        <Setter Property="VerticalOptions" Value="End" />
    </Style>
    <Style x:Key="RichTextEditorImageDialogContentStyle" TargetType="telerikMauiRichTextEditor:RichTextEditorImageDialogToolbarItemViewContent">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="HeaderStyle" Value="{StaticResource RichTextEditorDialogHeaderStyle}" />
        <Setter Property="FooterStyle" Value="{StaticResource RichTextEditorDialogFooterStyle}" />
        <Setter Property="OkButtonStyle" Value="{StaticResource RichTextEditorDialogOkButtonStyle}" />
        <Setter Property="CancelButtonStyle" Value="{StaticResource RichTextEditorDialogCancelButtonStyle}" />
        <Setter Property="PickButtonStyle" Value="{StaticResource RichTextEditorImageDialogPickButtonStyle}" />
        <Setter Property="ControlTemplate" Value="{StaticResource RichTextEditorImageDialogTemplate}" />
    </Style>
    <Style TargetType="telerikMauiRichTextEditor:RichTextEditorImageDialogToolbarItemViewContent" BasedOn="{StaticResource RichTextEditorImageDialogContentStyle}" ApplyToDerivedTypes="True" />

    <Style x:Key="RadRichTextEditorToolbarStyle" TargetType="telerikMauiRichTextEditor:RadRichTextEditorToolbar" BasedOn="{StaticResource RadToolbarStyle}" />
    <Style TargetType="telerikMauiRichTextEditor:RadRichTextEditorToolbar" BasedOn="{StaticResource RadRichTextEditorToolbarStyle}" />

    <Style x:Key="RadRichTextEditorStyle" TargetType="telerikMauiControls:RadRichTextEditor">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadOnDarkColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>
    <Style TargetType="telerikMauiControls:RadRichTextEditor" BasedOn="{StaticResource RadRichTextEditorStyle}" />

</ResourceDictionary>