<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikAutoComplete="clr-namespace:Telerik.Maui.Controls.AutoComplete;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Telerik.Styles.AutoComplete">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <Style x:Key="AutoCompleteClearButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Content" Value="&#xe84e;" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0, 0, 4, 0" />
        <Setter Property="WidthRequest" Value="20" />
        <Setter Property="HeightRequest" Value="20" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="AutoCompleteTokenClearButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconCancel2}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="WidthRequest" Value="16" />
        <Setter Property="HeightRequest" Value="16" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver" />
                    <VisualState Name="Pressed" />
                    <VisualState Name="Disabled" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <DataTemplate x:Key="AutoCompleteShowMoreTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource RadOnAppSurfaceColorAlpha30}"
                                       BorderColor="{DynamicResource RadOnAppSurfaceColorAlpha8}"
                                       BorderThickness="1"
                                       CornerRadius="4"
                                       Padding="{OnPlatform Default=4, WinUI='4, 1.5'}"
                                       Margin="2"
                                       VerticalOptions="Center">
            <Label Text="{Binding ., StringFormat='+{0}'}"
                   TextColor="{DynamicResource RadOnAppSurfaceColor}"
                   VerticalTextAlignment="Center"
                   Margin="4, 0" />
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="OutlineAutoCompleteShowMoreTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="Transparent"
                                       BorderColor="{DynamicResource RadOnAppSurfaceColor}"
                                       BorderThickness="1"
                                       CornerRadius="4"
                                       Padding="{OnPlatform Default=4, WinUI='4, 1.5'}"
                                       Margin="0, 0, 1, 0"
                                       VerticalOptions="Center">
            <Label Text="{Binding ., StringFormat='+{0}'}"
               TextColor="{DynamicResource RadOnAppSurfaceColor}"
               VerticalTextAlignment="Center"
               Margin="4, 0" />
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="AutoCompleteTokenTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource RadOnAppSurfaceColorAlpha30}"
                                       BorderColor="{DynamicResource RadOnAppSurfaceColorAlpha8}"
                                       BorderThickness="1"
                                       CornerRadius="4"
                                       Padding="{OnPlatform Default=4, WinUI='4, 1.5'}"
                                       Margin="2"
                                       VerticalOptions="Center">
            <telerikMauiControls:RadDockLayout>
                <telerikMauiControls:RadTemplatedButton Command="{Binding RemoveTokenCommand, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                        CommandParameter="{Binding BindingContext, Source={RelativeSource Self}}"
                                                        Style="{StaticResource AutoCompleteTokenClearButtonStyle}"
                                                        VerticalOptions="Center"
                                                        telerikMauiControls:RadDockLayout.Dock="Right"
                                                        AutomationId="TokenCloseButton">
                    <telerikMauiControls:RadTemplatedButton.Triggers>
                        <DataTrigger TargetType="telerikMauiControls:RadTemplatedButton" Binding="{Binding IsFocused, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}" Value="False">
                            <Setter Property="telerikMauiControls:RadTemplatedButton.IsVisible" Value="False" />
                        </DataTrigger>
                    </telerikMauiControls:RadTemplatedButton.Triggers>
                </telerikMauiControls:RadTemplatedButton>
                <telerikAutoComplete:AutoCompleteTokenLabel TextPath="{Binding TextSearchPath, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                            DisplayTextFormatter="{Binding DisplayTextFormatter, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                            TextColor="{DynamicResource RadOnAppSurfaceColor}"
                                                            Margin="4, 0"
                                                            VerticalOptions="Center"
                                                            AutomationId="TokenLabel" />
            </telerikMauiControls:RadDockLayout>
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="OutlineAutoCompleteTokenTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="Transparent"
                                       BorderColor="{DynamicResource RadOnAppSurfaceColor}"
                                       BorderThickness="1"
                                       CornerRadius="4"
                                       Padding="{OnPlatform Default=4, WinUI='4, 1.5'}"
                                       Margin="2"
                                       VerticalOptions="Center">
            <telerikMauiControls:RadDockLayout>
                <telerikMauiControls:RadTemplatedButton Content="{x:Static telerikMaui:TelerikFont.IconCancel2}"
                                                        Command="{Binding RemoveTokenCommand, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                        CommandParameter="{Binding BindingContext, Source={RelativeSource Self}}"
                                                        Style="{StaticResource AutoCompleteTokenClearButtonStyle}"
                                                        VerticalOptions="Center"
                                                        telerikMauiControls:RadDockLayout.Dock="Right"
                                                        AutomationId="TokenCloseButton">
                    <telerikMauiControls:RadTemplatedButton.Triggers>
                        <DataTrigger TargetType="telerikMauiControls:RadTemplatedButton" Binding="{Binding IsFocused, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}" Value="False">
                            <Setter Property="telerikMauiControls:RadTemplatedButton.IsVisible" Value="False" />
                        </DataTrigger>
                    </telerikMauiControls:RadTemplatedButton.Triggers>
                </telerikMauiControls:RadTemplatedButton>
                <telerikAutoComplete:AutoCompleteTokenLabel TextPath="{Binding TextSearchPath, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                            DisplayTextFormatter="{Binding DisplayTextFormatter, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                            TextColor="{DynamicResource RadOnAppSurfaceColor}"
                                                            Margin="4, 0"
                                                            VerticalOptions="Center"
                                                            AutomationId="TokenLabel" />
            </telerikMauiControls:RadDockLayout>
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="AutoCompleteNoResultsTemplate">
        <Label AutomationId="RadAutoCompleteNoResultsLabel"
               Text="{Binding NoResultsMessage, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
               TextColor="{DynamicResource RadOnAppSurfaceColor}"
               MinimumHeightRequest="28"
               Padding="9, 3" />
    </DataTemplate>

    <DataTemplate x:Key="AutoCompleteSuggestionItemTemplate">
        <telerikMauiControls:RadBorder MinimumWidthRequest="{Binding Width, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                       MinimumHeightRequest="28"
                                       Padding="8, 4">
            <telerikMauiControls:RadHighlightLabel telerikMauiControls:LabelUtils.TextPath="{Binding TextSearchPath, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                   HighlightTextColor="{Binding SuggestionItemHighlightTextColor, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                   HighlightText="{Binding SearchText, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                   TextColor="{DynamicResource RadOnAppSurfaceColor}"
                                                   FontSize="14"
                                                   VerticalOptions="Center" />
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <ControlTemplate x:Key="AutoCompleteSuggestionItemControlTemplate">
        <ContentPresenter />
    </ControlTemplate>

    <Style x:Key="AutoCompleteSuggestionItemStyle" TargetType="telerikMauiControls:RadCollectionViewItemView">
        <Setter Property="ControlTemplate" Value="{StaticResource AutoCompleteSuggestionItemControlTemplate}" />
        <Setter Property="MinimumHeightRequest" Value="0" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="Margin" Value="0, 0, 1, 0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="AutomationId" Value="AutoCompleteSuggestionItemStyle" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="MouseOver" />
                    <VisualState Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedMouseOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="AutoCompleteTextInputStyle" TargetType="telerikMauiControls:RadTextInput">
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=0, iOS=19}" />
        <Setter Property="Padding" Value="{OnPlatform Android='9, 12', iOS='9, 11.5', MacCatalyst='9, 5.75', WinUI='9, 4.5'}" />
    </Style>

    <Style x:Key="RadAutoCompleteStyle" TargetType="telerikMauiControls:RadAutoComplete">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="PlaceholderColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="TextInputStyle" Value="{StaticResource AutoCompleteTextInputStyle}" />
        <Setter Property="ClearButtonStyle" Value="{StaticResource AutoCompleteClearButtonStyle}" />
        <Setter Property="SuggestionViewBackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="SuggestionViewBorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="SuggestionViewCornerRadius" Value="0" />
        <Setter Property="SuggestionViewBorderThickness" Value="1" />
        <Setter Property="SuggestionItemHighlightTextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="SuggestionItemTemplate" Value="{StaticResource AutoCompleteSuggestionItemTemplate}" />
        <Setter Property="TokenTemplate" Value="{StaticResource AutoCompleteTokenTemplate}" />
        <Setter Property="NoResultsTemplate" Value="{StaticResource AutoCompleteNoResultsTemplate}" />
        <Setter Property="ShowMoreTemplate" Value="{StaticResource AutoCompleteShowMoreTemplate}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderAltColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:RadAutoComplete" BasedOn="{StaticResource RadAutoCompleteStyle}" />

    <Style x:Key="RadOutlineAutoCompleteStyle" TargetType="telerikMauiControls:RadAutoComplete" BasedOn="{StaticResource RadAutoCompleteStyle}">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
        <Setter Property="TokenTemplate" Value="{StaticResource OutlineAutoCompleteTokenTemplate}" />
        <Setter Property="ShowMoreTemplate" Value="{StaticResource OutlineAutoCompleteShowMoreTemplate}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha80}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RadFlatAutoCompleteStyle" TargetType="telerikMauiControls:RadAutoComplete" BasedOn="{StaticResource RadAutoCompleteStyle}">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="CornerRadius" Value="4, 4, 0, 0" />
    </Style>

</ResourceDictionary>
