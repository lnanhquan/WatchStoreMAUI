<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Telerik.Styles.TabView">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <ControlTemplate x:Key="TabViewHeaderControlTemplate">
        <Grid>
            <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                           BorderColor="{TemplateBinding BorderColor}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="{TemplateBinding CornerRadius}" />
            <ContentPresenter Padding="{TemplateBinding ContentPadding}"
                              Content="{TemplateBinding Content}" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="TabViewHeaderStyle" TargetType="telerikMauiControls:TabViewHeader">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>

    <Style x:Key="TabViewBottomHeaderStyle" TargetType="telerikMauiControls:TabViewHeader" BasedOn="{StaticResource TabViewHeaderStyle}">
        <Setter Property="BorderThickness" Value="0, 1, 0, 0" />
    </Style>

    <Style x:Key="TabViewLeftHeaderStyle" TargetType="telerikMauiControls:TabViewHeader" BasedOn="{StaticResource TabViewHeaderStyle}">
        <Setter Property="BorderThickness" Value="0, 0, 1, 0" />
    </Style>

    <Style x:Key="TabViewRightHeaderStyle" TargetType="telerikMauiControls:TabViewHeader" BasedOn="{StaticResource TabViewHeaderStyle}">
        <Setter Property="BorderThickness" Value="1, 0, 0, 0" />
    </Style>

    <ControlTemplate x:Key="TabViewHeaderItemControlTemplate">
        <Grid>
            <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                           BorderColor="{TemplateBinding BorderColor}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="{TemplateBinding CornerRadius}"
                                           Padding="{TemplateBinding ContentPadding}">
                <ContentPresenter />
            </telerikMauiControls:RadBorder>
            <BoxView IsVisible="{Binding IsSelected, Source={RelativeSource Mode=TemplatedParent}}"
                     BackgroundColor="{TemplateBinding BackgroundColor}"
                     HorizontalOptions="Fill"
                     VerticalOptions="Fill">
                <BoxView.Triggers>
                    <DataTrigger TargetType="BoxView" Binding="{Binding Position, Source={RelativeSource Mode=TemplatedParent}}" Value="Top">
                        <Setter Property="VerticalOptions" Value="End" />
                        <Setter Property="HeightRequest" Value="1" />
                    </DataTrigger>
                    <DataTrigger TargetType="BoxView" Binding="{Binding Position, Source={RelativeSource Mode=TemplatedParent}}" Value="Bottom">
                        <Setter Property="VerticalOptions" Value="Start" />
                        <Setter Property="HeightRequest" Value="1" />
                    </DataTrigger>
                    <DataTrigger TargetType="BoxView" Binding="{Binding Position, Source={RelativeSource Mode=TemplatedParent}}" Value="Left">
                        <Setter Property="HorizontalOptions" Value="End" />
                        <Setter Property="WidthRequest" Value="1" />
                    </DataTrigger>
                    <DataTrigger TargetType="BoxView" Binding="{Binding Position, Source={RelativeSource Mode=TemplatedParent}}" Value="Right">
                        <Setter Property="HorizontalOptions" Value="Start" />
                        <Setter Property="WidthRequest" Value="1" />
                    </DataTrigger>
                </BoxView.Triggers>
            </BoxView>
        </Grid>
    </ControlTemplate>

    <Style x:Key="TabViewHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="2, 2, 0, 0" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="{DynamicResource RadBorderColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderThickness" Value="1, 1, 1, 0" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="Transparent" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="TabViewBottomHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem" BasedOn="{StaticResource TabViewHeaderItemStyle}">
        <Setter Property="CornerRadius" Value="0, 0, 2, 2" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="{DynamicResource RadBorderColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderThickness" Value="1, 0, 1, 1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="Transparent" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="TabViewLeftHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem" BasedOn="{StaticResource TabViewHeaderItemStyle}">
        <Setter Property="CornerRadius" Value="2, 0, 0, 2" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="{DynamicResource RadBorderColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderThickness" Value="1, 1, 0, 1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="Transparent" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="TabViewRightHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem" BasedOn="{StaticResource TabViewHeaderItemStyle}">
        <Setter Property="CornerRadius" Value="0, 2, 2, 0" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="{DynamicResource RadBorderColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderThickness" Value="0, 1, 1, 1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="Transparent" />
                            <Setter Property="telerikMauiControls:TabViewHeaderItem.Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="TabViewContentAtBottomStyle" TargetType="telerikMauiControls:TabViewContent">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1, 0, 1, 1" />
        <Setter Property="CornerRadius" Value="0, 0, 2, 2" />
    </Style>

    <Style x:Key="TabViewContentAtTopStyle" TargetType="telerikMauiControls:TabViewContent">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1, 1, 1, 0" />
        <Setter Property="CornerRadius" Value="0, 2, 0, 0" />
    </Style>

    <Style x:Key="TabViewContentAtRightStyle" TargetType="telerikMauiControls:TabViewContent">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0, 1, 1, 1" />
        <Setter Property="CornerRadius" Value="0, 2, 2, 0" />
    </Style>

    <Style x:Key="TabViewContentAtLeftStyle" TargetType="telerikMauiControls:TabViewContent">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1, 1, 0, 1" />
        <Setter Property="CornerRadius" Value="2, 0, 0, 2" />
    </Style>

    <Style x:Key="RadTabViewStyle" TargetType="telerikMauiControls:RadTabView">
        <Setter Property="HeaderStyle" Value="{StaticResource TabViewHeaderStyle}" />
        <Setter Property="HeaderTemplate" Value="{StaticResource TabViewHeaderControlTemplate}" />
        <Setter Property="HeaderItemStyle" Value="{StaticResource TabViewHeaderItemStyle}" />
        <Setter Property="HeaderItemTemplate" Value="{StaticResource TabViewHeaderItemControlTemplate}" />
        <Setter Property="ContentStyle" Value="{StaticResource TabViewContentAtBottomStyle}" />
        <Setter Property="IsHeaderOverlaid" Value="False" />
        <Style.Triggers>
            <Trigger TargetType="telerikMauiControls:RadTabView" Property="HeaderPosition" Value="Bottom">
                <Setter Property="HeaderStyle" Value="{DynamicResource TabViewBottomHeaderStyle}" />
                <Setter Property="HeaderItemStyle" Value="{DynamicResource TabViewBottomHeaderItemStyle}" />
                <Setter Property="ContentStyle" Value="{DynamicResource TabViewContentAtTopStyle}" />
            </Trigger>
            <Trigger TargetType="telerikMauiControls:RadTabView" Property="HeaderPosition" Value="Left">
                <Setter Property="HeaderStyle" Value="{DynamicResource TabViewLeftHeaderStyle}" />
                <Setter Property="HeaderItemStyle" Value="{DynamicResource TabViewLeftHeaderItemStyle}" />
                <Setter Property="ContentStyle" Value="{DynamicResource TabViewContentAtRightStyle}" />
            </Trigger>
            <Trigger TargetType="telerikMauiControls:RadTabView" Property="HeaderPosition" Value="Right">
                <Setter Property="HeaderStyle" Value="{DynamicResource TabViewRightHeaderStyle}" />
                <Setter Property="HeaderItemStyle" Value="{DynamicResource TabViewRightHeaderItemStyle}" />
                <Setter Property="ContentStyle" Value="{DynamicResource TabViewContentAtLeftStyle}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style TargetType="telerikMauiControls:RadTabView" BasedOn="{StaticResource RadTabViewStyle}" />

</ResourceDictionary>