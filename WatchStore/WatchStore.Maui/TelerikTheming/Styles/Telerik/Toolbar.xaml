<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Telerik.Styles.Toolbar">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <Style x:Key="SeparatorToolbarItemViewStyle" TargetType="telerikMauiControls:SeparatorToolbarItemView">
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
    </Style>
    <Style TargetType="telerikMauiControls:SeparatorToolbarItemView" BasedOn="{StaticResource SeparatorToolbarItemViewStyle}" />

    <Style x:Key="GroupToolbarItemViewStyle" TargetType="telerikMauiControls:GroupToolbarItemView">
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
    </Style>
    <Style TargetType="telerikMauiControls:GroupToolbarItemView" BasedOn="{StaticResource GroupToolbarItemViewStyle}" />

    <Style x:Key="LabelToolbarItemViewStyle" TargetType="telerikMauiControls:LabelToolbarItemView">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="MinimumWidthRequest" Value="30" />
        <Setter Property="MinimumHeightRequest" Value="30" />
        <Setter Property="ImageSpacing" Value="4" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:LabelToolbarItemView" BasedOn="{StaticResource LabelToolbarItemViewStyle}" />

    <Style x:Key="ButtonToolbarItemViewBaseStyle" TargetType="telerikMauiControls:ButtonToolbarItemView">
        <Setter Property="MinimumWidthRequest" Value="30" />
        <Setter Property="MinimumHeightRequest" Value="30" />
        <Setter Property="ImageSpacing" Value="4" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Opened">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:ButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style TargetType="telerikMauiControls:NavigationButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style TargetType="telerikMauiControls:OptionsButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />

    <Style x:Key="SelectableButtonToolbarItemViewStyle" TargetType="telerikMauiControls:SelectableButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}">
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="SelectedDisabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:SelectableButtonToolbarItemView" BasedOn="{StaticResource SelectableButtonToolbarItemViewStyle}" />
    <Style TargetType="telerikMauiControls:RadioButtonToolbarItemView" BasedOn="{StaticResource SelectableButtonToolbarItemViewStyle}" />
    <Style TargetType="telerikMauiControls:ToggleButtonToolbarItemView" BasedOn="{StaticResource SelectableButtonToolbarItemViewStyle}" />

    <Style x:Key="DropDownButtonToolbarItemViewContentStyle" TargetType="telerikMauiControls:DropDownButtonToolbarItemViewContent">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="CornerRadius" Value="0" />
    </Style>
    <Style TargetType="telerikMauiControls:DropDownButtonToolbarItemViewContent" BasedOn="{StaticResource DropDownButtonToolbarItemViewContentStyle}" />

    <Style x:Key="DropDownButtonToolbarItemViewStyle" TargetType="telerikMauiControls:DropDownButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style x:Key="DropDownButtonToolbarItemViewIndicatorStyle" TargetType="telerikMauiControls:DropDownButtonToolbarItemViewIndicator" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style TargetType="telerikMauiControls:DropDownButtonToolbarItemView" BasedOn="{StaticResource DropDownButtonToolbarItemViewStyle}" />
    <Style TargetType="telerikMauiControls:DropDownButtonToolbarItemViewIndicator" BasedOn="{StaticResource DropDownButtonToolbarItemViewIndicatorStyle}">
        <Setter Property="BorderThickness" Value="1, 0, 0, 0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style TargetType="telerikMauiControls:ListPickerButtonToolbarItemViewContent" BasedOn="{StaticResource DropDownButtonToolbarItemViewContentStyle}" />
    <Style TargetType="telerikMauiControls:ListPickerButtonToolbarItemView" BasedOn="{StaticResource DropDownButtonToolbarItemViewStyle}" />

    <Style x:Key="SplitButtonToolbarItemViewStyle" TargetType="telerikMauiControls:SplitButtonToolbarItemView" BasedOn="{StaticResource DropDownButtonToolbarItemViewStyle}">
        <Setter Property="DropDownContentStyle" Value="{StaticResource DropDownButtonToolbarItemViewContentStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:SplitButtonToolbarItemView" BasedOn="{StaticResource SplitButtonToolbarItemViewStyle}" />
    <Style x:Key="SplitButtonToolbarItemViewIndicatorStyle" TargetType="telerikMauiControls:SplitButtonToolbarItemViewIndicator" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}">
        <Setter Property="BorderThickness" Value="1, 0, 0, 0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>
    <Style TargetType="telerikMauiControls:SplitButtonToolbarItemViewIndicator" BasedOn="{StaticResource SplitButtonToolbarItemViewIndicatorStyle}" />

    <Style x:Key="RadToolbarOptionsPanelStyle" TargetType="telerikMauiControls:RadToolbarOptionsPanel">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadToolbarOptionsPanel" BasedOn="{StaticResource RadToolbarOptionsPanelStyle}" />

    <Style x:Key="OverflowMenuButtonToolbarItemViewContentStyle" TargetType="telerikMauiControls:DropDownMenuButtonToolbarItemViewContent" BasedOn="{StaticResource DropDownButtonToolbarItemViewContentStyle}">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
    </Style>
    <Style x:Key="OverflowMenuButtonStyle" TargetType="telerikMauiControls:OverflowMenuButtonToolbarItemView" BasedOn="{StaticResource DropDownButtonToolbarItemViewStyle}">
        <Setter Property="DropDownContentStyle" Value="{StaticResource OverflowMenuButtonToolbarItemViewContentStyle}" />
    </Style>

    <Style x:Key="BackNavigationButtonStyle" TargetType="telerikMauiControls:BackNavigationButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style x:Key="ScrollForwardButtonStyle" TargetType="telerikMauiControls:ScrollForwardButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style x:Key="ScrollBackwardButtonStyle" TargetType="telerikMauiControls:ScrollBackwardButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />

    <Style TargetType="telerikMauiControls:ToolbarListItemView">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="Transparent" />
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadBorderColor}" />
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="BackgroundColor" Value="Transparent" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="EntryToolbarItemViewStyle" TargetType="telerikMauiControls:EntryToolbarItemView">
        <Setter Property="Padding" Value="0" />
        <Setter Property="ContentPadding" Value="0" />
        <Setter Property="MinimumWidthRequest" Value="30" />
        <Setter Property="MinimumHeightRequest" Value="30" />
        <Setter Property="EntryStyle">
            <Style TargetType="telerikMauiControls:RadEntry">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="PlaceholderColor" Value="{DynamicResource RadSubtleColor}" />
                <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
            </Style>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:EntryToolbarItemView" BasedOn="{StaticResource EntryToolbarItemViewStyle}" />

    <Style x:Key="SliderToolbarItemViewStyle" TargetType="telerikMauiControls:SliderToolbarItemView">
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=20}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=20}" />
    </Style>
    <Style TargetType="telerikMauiControls:SliderToolbarItemView" BasedOn="{StaticResource SliderToolbarItemViewStyle}" />

    <Style x:Key="RadToolbarStyle" TargetType="telerikMauiControls:RadToolbar">
        <Setter Property="ContentPadding" Value="8" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="OverflowMenuButtonStyle" Value="{StaticResource OverflowMenuButtonStyle}" />
        <Setter Property="BackNavigationButtonStyle" Value="{StaticResource BackNavigationButtonStyle}" />
        <Setter Property="ScrollForwardButtonStyle" Value="{StaticResource ScrollForwardButtonStyle}" />
        <Setter Property="ScrollBackwardButtonStyle" Value="{StaticResource ScrollBackwardButtonStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadToolbar" BasedOn="{StaticResource RadToolbarStyle}" />

</ResourceDictionary>
