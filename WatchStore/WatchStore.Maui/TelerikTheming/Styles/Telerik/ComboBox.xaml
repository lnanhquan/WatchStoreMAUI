<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikAutoComplete="clr-namespace:Telerik.Maui.Controls.AutoComplete;assembly=Telerik.Maui.Controls"
                    xmlns:telerikImageEditor="clr-namespace:Telerik.Maui.Controls.ImageEditor;assembly=Telerik.Maui.Controls"
                    xmlns:telerikCollectionView="clr-namespace:Telerik.Maui.Controls.CollectionView;assembly=Telerik.Maui.Controls"
                    xmlns:telerikAIPrompt="clr-namespace:Telerik.Maui.Controls.AIPrompt;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Telerik.Styles.ComboBox">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <!-- Dependencies: Buttons.xaml -->

    <Style x:Key="ComboBoxClearButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Content" Value="&#xe84e;" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0, 0, 4, 0" />
        <Setter Property="WidthRequest" Value="20" />
        <Setter Property="HeightRequest" Value="20" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="ComboBoxTokenClearButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconCancel2}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="WidthRequest" Value="16" />
        <Setter Property="HeightRequest" Value="16" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver" />
                    <VisualState Name="Pressed" />
                    <VisualState Name="Disabled" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <DataTemplate x:Key="ComboBoxShowMoreTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource RadOnAppSurfaceColorAlpha30}"
                                       BorderColor="{DynamicResource RadOnAppSurfaceColorAlpha8}"
                                       BorderThickness="1"
                                       CornerRadius="4"
                                       Padding="{OnPlatform Default=4, WinUI='4, 1.5'}"
                                       Margin="2"
                                       VerticalOptions="Center">
            <Label Text="{Binding ., StringFormat='+{0}'}"
                   TextColor="{DynamicResource RadOnAppSurfaceColor}"
                   VerticalTextAlignment="Center"
                   Margin="4, 0" />
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="OutlineComboBoxShowMoreTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="Transparent"
                                       BorderColor="{DynamicResource RadOnAppSurfaceColor}"
                                       BorderThickness="1"
                                       CornerRadius="4"
                                       Padding="{OnPlatform Default=4, WinUI='4, 1.5'}"
                                       Margin="0, 0, 1, 0"
                                       VerticalOptions="Center">
            <Label Text="{Binding ., StringFormat='+{0}'}"
                   TextColor="{DynamicResource RadOnAppSurfaceColor}"
                   VerticalTextAlignment="Center"
                   Margin="4, 0" />
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="ComboBoxTokenTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource RadOnAppSurfaceColorAlpha30}"
                                       BorderColor="{DynamicResource RadOnAppSurfaceColorAlpha8}"
                                       BorderThickness="1"
                                       CornerRadius="4"
                                       Padding="{OnPlatform Default=4, WinUI='4, 1.5'}"
                                       Margin="2"
                                       VerticalOptions="Center"
                                       AutomationId="RadComboBoxToken">
            <HorizontalStackLayout>
                <Label telerikMauiControls:LabelUtils.TextPath="{Binding DisplayMemberPath, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
                       TextColor="{DynamicResource RadOnAppSurfaceColor}"
                       Margin="4, 0"
                       VerticalOptions="Center" />
                <telerikMauiControls:RadTemplatedButton Command="{Binding RemoveTokenCommand, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
                                                        CommandParameter="{Binding BindingContext, Source={RelativeSource Self}}"
                                                        Style="{StaticResource ComboBoxTokenClearButtonStyle}"
                                                        VerticalOptions="Center"
                                                        AutomationId="RadComboBoxTokenClearButton">
                    <telerikMauiControls:RadTemplatedButton.Triggers>
                        <DataTrigger TargetType="telerikMauiControls:RadTemplatedButton" Binding="{Binding IsFocused, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}" Value="False">
                            <Setter Property="IsVisible" Value="False" />
                        </DataTrigger>
                    </telerikMauiControls:RadTemplatedButton.Triggers>
                </telerikMauiControls:RadTemplatedButton>
            </HorizontalStackLayout>
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="OutlineComboBoxTokenTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="Transparent"
                                       BorderColor="{DynamicResource RadOnAppSurfaceColor}"
                                       BorderThickness="1"
                                       CornerRadius="4"
                                       Padding="{OnPlatform Default=4, WinUI='4, 1.5'}"
                                       Margin="2"
                                       VerticalOptions="Center"
                                       AutomationId="RadComboBoxToken">
            <HorizontalStackLayout>
                <Label telerikMauiControls:LabelUtils.TextPath="{Binding DisplayMemberPath, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
                       TextColor="{DynamicResource RadOnAppSurfaceColor}"
                       Margin="4, 0"
                       VerticalOptions="Center" />
                <telerikMauiControls:RadTemplatedButton Command="{Binding RemoveTokenCommand, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
                                                        CommandParameter="{Binding BindingContext, Source={RelativeSource Self}}"
                                                        Style="{StaticResource ComboBoxTokenClearButtonStyle}"
                                                        VerticalOptions="Center"
                                                        AutomationId="RadComboBoxTokenClearButton">
                    <telerikMauiControls:RadTemplatedButton.Triggers>
                        <DataTrigger TargetType="telerikMauiControls:RadTemplatedButton" Binding="{Binding IsFocused, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}" Value="False">
                            <Setter Property="IsVisible" Value="False" />
                        </DataTrigger>
                    </telerikMauiControls:RadTemplatedButton.Triggers>
                </telerikMauiControls:RadTemplatedButton>
            </HorizontalStackLayout>
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="ComboBoxDropDownItemTemplate">
        <telerikMauiControls:RadComboBoxItem MinimumWidthRequest="{Binding Width, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadItemsControl}}}">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroupList>
                    <VisualStateGroup Name="CommonStates">
                        <VisualState Name="Normal" />
                        <VisualState Name="MouseOver">
                            <VisualState.Setters>
                                <Setter Property="telerikMauiControls:RadComboBoxItem.BackgroundColor" Value="{DynamicResource RadBorderColor}" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState Name="Highlighted">
                            <VisualState.Setters>
                                <Setter Property="telerikMauiControls:RadComboBoxItem.BackgroundColor" Value="{DynamicResource RadBorderColor}" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState Name="Disabled" />
                    </VisualStateGroup>
                </VisualStateGroupList>
            </VisualStateManager.VisualStateGroups>
            <telerikMauiControls:RadHighlightLabel telerikMauiControls:LabelUtils.TextPath="{Binding DisplayMemberPath, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
                                                   Provider="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
                                                   MinimumHeightRequest="28"
                                                   HighlightText="{Binding SearchText, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
                                                   HighlightTextColor="{Binding HighlightTextColor, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
                                                   TextColor="{DynamicResource RadOnAppSurfaceColor}"
                                                   Padding="8, 4"
                                                   VerticalTextAlignment="Center" />
        </telerikMauiControls:RadComboBoxItem>
    </DataTemplate>

    <DataTemplate x:Key="ComboBoxSelectedDropDownItemTemplate">
        <telerikMauiControls:RadComboBoxItem MinimumWidthRequest="{Binding Width, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadItemsControl}}}"
                                             BackgroundColor="{DynamicResource RadBaseHoverColor}">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroupList>
                    <VisualStateGroup Name="CommonStates">
                        <VisualState Name="Normal" />
                        <VisualState Name="MouseOver">
                            <VisualState.Setters>
                                <Setter Property="telerikMauiControls:RadComboBoxItem.BackgroundColor" Value="{DynamicResource RadBorderColor}" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState Name="Highlighted">
                            <VisualState.Setters>
                                <Setter Property="telerikMauiControls:RadComboBoxItem.BackgroundColor" Value="{DynamicResource RadBorderColor}" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState Name="Disabled" />
                    </VisualStateGroup>
                </VisualStateGroupList>
            </VisualStateManager.VisualStateGroups>
            <telerikMauiControls:RadHighlightLabel telerikMauiControls:LabelUtils.TextPath="{Binding DisplayMemberPath, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
                                                   Provider="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
                                                   MinimumHeightRequest="28"
                                                   HighlightText="{Binding SearchText, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
                                                   HighlightTextColor="{Binding HighlightTextColor, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
                                                   TextColor="{DynamicResource RadOnBaseColor}"
                                                   Padding="8, 4"
                                                   VerticalTextAlignment="Center" />
        </telerikMauiControls:RadComboBoxItem>
    </DataTemplate>

    <DataTemplate x:Key="ComboBoxNoResultsTemplate">
        <Label Text="{Binding NoResultsMessage, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadComboBox}}}"
               TextColor="{DynamicResource RadOnAppSurfaceColor}"
               MinimumHeightRequest="28"
               Padding="9, 3" />
    </DataTemplate>

    <Style x:Key="ComboBoxTextInputStyle" TargetType="telerikMauiControls:RadTextInput">
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=0, iOS=19}" />
        <Setter Property="Padding" Value="{OnPlatform Android='9, 12', iOS='9, 11.5', MacCatalyst='9, 5.75', WinUI='9, 4.5'}" />
    </Style>

    <Style x:Key="ComboBoxDropDownButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource RadTemplatedButtonStyle}">
        <Setter Property="FontFamily" Value="{Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Content" Value="{Static telerikMaui:TelerikFont.IconDownDir}" />
        <Setter Property="BorderThickness" Value="1, 0, 0, 0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=32, iOS=32, MacCatalyst=28, WinUI=28}" />
        <Setter Property="WidthRequest" Value="NaN" />
        <Setter Property="HeightRequest" Value="NaN" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>

    <Style x:Key="OutlineComboBoxDropDownButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource ComboBoxDropDownButtonStyle}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
    </Style>

    <Style x:Key="FlatComboBoxDropDownButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource ComboBoxDropDownButtonStyle}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style x:Key="RadComboBoxStyle" TargetType="telerikMauiControls:RadComboBox" ApplyToDerivedTypes="True">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="PlaceholderColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="TextInputStyle" Value="{StaticResource ComboBoxTextInputStyle}" />
        <Setter Property="ClearButtonStyle" Value="{StaticResource ComboBoxClearButtonStyle}" />
        <Setter Property="DropDownButtonStyle" Value="{StaticResource ComboBoxDropDownButtonStyle}" />
        <Setter Property="DropDownBackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="DropDownBorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="DropDownCornerRadius" Value="0" />
        <Setter Property="DropDownBorderThickness" Value="1" />
        <Setter Property="HighlightTextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="ItemTemplate" Value="{StaticResource ComboBoxDropDownItemTemplate}" />
        <Setter Property="SelectedItemTemplate" Value="{StaticResource ComboBoxSelectedDropDownItemTemplate}" />
        <Setter Property="TokenTemplate" Value="{StaticResource ComboBoxTokenTemplate}" />
        <Setter Property="ShowMoreTemplate" Value="{StaticResource ComboBoxShowMoreTemplate}" />
        <Setter Property="NoResultsTemplate" Value="{StaticResource ComboBoxNoResultsTemplate}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderAltColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:RadComboBox" BasedOn="{StaticResource RadComboBoxStyle}" />

    <Style x:Key="RadOutlineComboBoxStyle" TargetType="telerikMauiControls:RadComboBox" BasedOn="{StaticResource RadComboBoxStyle}">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
        <Setter Property="DropDownButtonStyle" Value="{StaticResource OutlineComboBoxDropDownButtonStyle}" />
        <Setter Property="TokenTemplate" Value="{StaticResource OutlineComboBoxTokenTemplate}" />
        <Setter Property="ShowMoreTemplate" Value="{StaticResource OutlineComboBoxShowMoreTemplate}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha80}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                            <Setter Property="Opacity" Value="0.6" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RadFlatComboBoxStyle" TargetType="telerikMauiControls:RadComboBox" BasedOn="{StaticResource RadComboBoxStyle}">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="CornerRadius" Value="4, 4, 0, 0" />
        <Setter Property="DropDownButtonStyle" Value="{StaticResource FlatComboBoxDropDownButtonStyle}" />
    </Style>

</ResourceDictionary>
