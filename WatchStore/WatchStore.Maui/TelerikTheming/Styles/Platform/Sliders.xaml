<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikRangeSlider="clr-namespace:Telerik.Maui.Controls.RangeSlider;assembly=Telerik.Maui.Controls"
                    xmlns:telerikSliders="clr-namespace:Telerik.Maui.Controls.Sliders;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.Sliders">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <Shadow x:Key="SliderThumbShadow" Offset="{OnPlatform Default='0, 0', Android='0, 3', iOS='0, 3'}" Radius="{OnPlatform Default=0, Android=3, iOS=3}" Brush="{DynamicResource RadOnAppSurfaceColor}" Opacity="0.15" />
    <Shadow x:Key="SliderTooltipShadow" Offset="{OnPlatform Default='0, 0', WinUI='0, 4'}" Radius="{OnPlatform Default=0, MacCatalyst=3, WinUI=8}" Brush="{OnPlatform Default=Transparent, MacCatalyst={DynamicResource RadOnAppSurfaceColor}, WinUI={DynamicResource RadOnAppSurfaceColor}}" Opacity="{OnPlatform Default=1, MacCatalyst=0.5, WinUI=0.14}" />
    <Shadow x:Key="SliderTooltipArrowShadow" Offset="0, 2" Radius="{OnPlatform Default=0, MacCatalyst=3}" Brush="{OnPlatform Default=Transparent, MacCatalyst={DynamicResource RadOnAppSurfaceColor}}" Opacity="{OnPlatform Default=1, MacCatalyst=0.5}" />

    <telerikMauiControls:DictionaryConverter x:Key="TicksPlacementToCornerRadiusConverter">
        <telerikMauiControls:DictionaryConverter.FallbackValue>
            <OnPlatform x:TypeArguments="Thickness">
                <On Platform="Android" Value="0" />
                <On Platform="iOS" Value="6" />
                <On Platform="MacCatalyst" Value="4" />
                <On Platform="WinUI" Value="10" />
            </OnPlatform>
        </telerikMauiControls:DictionaryConverter.FallbackValue>
        <telerikMauiControls:DictionaryConverterItem Key="{x:Static telerikRangeSlider:SliderTicksPlacement.None}">
            <telerikMauiControls:DictionaryConverterItem.Value>
                <OnPlatform x:TypeArguments="Thickness" Default="10">
                    <On Platform="Android" Value="0" />
                    <On Platform="iOS" Value="14" />
                </OnPlatform>
            </telerikMauiControls:DictionaryConverterItem.Value>
        </telerikMauiControls:DictionaryConverterItem>
    </telerikMauiControls:DictionaryConverter>

    <ControlTemplate x:Key="SliderRangeTrackControlTemplate">
        <telerikMauiControls:RadBorder HeightRequest="{TemplateBinding TrackThickness}"
                                       BackgroundColor="{TemplateBinding Fill}"
                                       BorderColor="{TemplateBinding Stroke}"
                                       BorderThickness="{TemplateBinding StrokeThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}" />
    </ControlTemplate>

    <Style x:Key="SliderRangeTrackStyle" TargetType="telerikRangeSlider:SliderRangeTrack">
        <Setter Property="ControlTemplate" Value="{StaticResource SliderRangeTrackControlTemplate}" />
        <Setter Property="Fill" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=40, MacCatalyst=28, WinUI=32}" />
        <Setter Property="CornerRadius" Value="2" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="1.0" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.9" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.8" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SliderTicksPresenterStyle" TargetType="telerikRangeSlider:SliderTicksPresenter">
        <Setter Property="VerticalOptions" Value="Center" />
        <Style.Triggers>
            <DataTrigger TargetType="VisualElement"
                         Binding="{Binding TicksPlacement, Source={RelativeSource TemplatedParent}}"
                         Value="Start">
                <Setter Property="Grid.Row" Value="1" />
            </DataTrigger>
            <DataTrigger TargetType="VisualElement"
                         Binding="{Binding TicksPlacement, Source={RelativeSource TemplatedParent}}"
                         Value="Center">
                <Setter Property="Grid.Row" Value="2" />
            </DataTrigger>
            <DataTrigger TargetType="VisualElement"
                         Binding="{Binding TicksPlacement, Source={RelativeSource TemplatedParent}}"
                         Value="End">
                <Setter Property="Grid.Row" Value="3" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SliderLabelsPresenterStyle" TargetType="telerikRangeSlider:SliderLabelsPresenter">
        <Style.Triggers>
            <DataTrigger TargetType="VisualElement"
                         Binding="{Binding LabelsPlacement, Source={RelativeSource TemplatedParent}}"
                         Value="Start">
                <Setter Property="Grid.Row" Value="0" />
            </DataTrigger>
            <DataTrigger TargetType="VisualElement"
                         Binding="{Binding LabelsPlacement, Source={RelativeSource TemplatedParent}}"
                         Value="End">
                <Setter Property="Grid.Row" Value="4" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SliderBackTrackStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="CornerRadius" Value="2" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSliderBackTrackColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Pressed" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadBorder.Background" Value="{DynamicResource RadSliderBackTrackDisabledColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SliderTickStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="CornerRadius" Value="{OnPlatform Default=1, iOS=2, WinUI=0}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=2, iOS=4, WinUI=1}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=2, iOS=10, MacCatalyst=6, WinUI=4}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSliderTickBackgroundColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Focused" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadSliderTickDisabledBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SliderMiddleTickStyle" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource SliderTickStyle}">
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=10, Android=2, WinUI=12}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSliderMiddleTickBackgroundColor}" />
    </Style>

    <Style x:Key="SliderOutOfRangeTickStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="CornerRadius" Value="{OnPlatform Default=1, iOS=2, WinUI=0}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=2, iOS=4, WinUI=1}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=2, iOS=10, MacCatalyst=6, WinUI=4}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSliderOutOfRangeTickBackgroundColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Focused" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadSliderOutOfRangeTickDisabledBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SliderOutOfRangeMiddleTickStyle" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource SliderOutOfRangeTickStyle}">
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=10, Android=2, WinUI=12}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSliderOutOfRangeMiddleTickBackgroundColor}" />
    </Style>

    <Style x:Key="SliderLabelStyle" TargetType="Label">
        <Setter Property="FontSize" Value="{OnPlatform Android=12, iOS=13, MacCatalyst=10, WinUI=14}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <ControlTemplate x:Key="SliderTooltipControlTemplate">
        <Grid RowDefinitions="*, Auto">
            <telerikMauiControls:RadBorder IsVisible="{OnPlatform Default=False, MacCatalyst=True, WinUI=True}"
                                           BackgroundColor="{DynamicResource TooltipBackgroundColor}"
                                           CornerRadius="{OnPlatform Default=4, iOS=8, MacCatalyst=1}"
                                           Shadow="{StaticResource SliderTooltipShadow}" />
            <Path x:Name="TooltipArrow"
                  Grid.Row="1"
                  IsVisible="{OnPlatform Default=True, WinUI=False}"
                  Data="{OnPlatform Android='M5.5 4.5L0.3 -9.7e-07L10.6 -7.0e-08L5.5 4.5Z', iOS='M7 10.5L0.9 -1.1e-06L13.0 -8.1e-08L7 10.5Z', MacCatalyst='M3.5 6.5L0.0 0.4L6.9 0.5L3.5 6.5Z'}"
                  Fill="{DynamicResource TooltipBackgroundColor}"
                  StrokeThickness="0"
                  WidthRequest="{OnPlatform Android=12, iOS=14, MacCatalyst=8}"
                  HeightRequest="{OnPlatform Android=6, iOS=14, MacCatalyst=8}"
                  HorizontalOptions="Center"
                  VerticalOptions="Start"
                  Margin="{OnPlatform MacCatalyst='0, -1, 0, 0'}"
                  Shadow="{StaticResource SliderTooltipArrowShadow}" />
            <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource TooltipBackgroundColor}"
                                           BorderThickness="{OnPlatform Default=0, WinUI=1}"
                                           BorderColor="{OnPlatform WinUI={DynamicResource RadBorderColor}}"
                                           CornerRadius="{OnPlatform Default=4, iOS=8, MacCatalyst=1}"
                                           Padding="{OnPlatform Android='12, 4', iOS='16, 8', MacCatalyst='4, 2', WinUI='10, 5'}">
                <ContentPresenter />
            </telerikMauiControls:RadBorder>
        </Grid>
    </ControlTemplate>

    <DataTemplate x:Key="SliderTooltipContentTemplate">
        <Label Text="{Binding FormattedValue}"
               TextColor="{DynamicResource TooltipTextColor}"
               FontSize="{OnPlatform iOS=13, MacCatalyst=10}" />
    </DataTemplate>

    <ControlTemplate x:Key="SliderThumbControlTemplate">
        <Rectangle WidthRequest="{OnPlatform iOS=12, MacCatalyst=8}"
                   HeightRequest="{OnPlatform iOS=28, MacCatalyst=20}"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   Fill="{TemplateBinding Fill}"
                   Stroke="{TemplateBinding Stroke}"
                   StrokeThickness="{TemplateBinding StrokeThickness}"
                   RadiusX="{Binding CornerRadius.Left, Source={RelativeSource Mode=TemplatedParent}}"
                   RadiusY="{Binding CornerRadius.Top, Source={RelativeSource Mode=TemplatedParent}}">
            <Rectangle.Triggers>
                <DataTrigger TargetType="Rectangle"
                             Binding="{Binding TicksPlacement, Source={RelativeSource AncestorType={Type telerikMauiControls:RadSliderBase}}}"
                             Value="None">
                    <Setter Property="WidthRequest" Value="{OnPlatform Default=28, MacCatalyst=20}" />
                </DataTrigger>
            </Rectangle.Triggers>
        </Rectangle>
    </ControlTemplate>

    <ControlTemplate x:Key="SliderThumbControlTemplate_Android">
        <telerikMauiControls:RadLayout>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter TargetName="Ellipse" Property="Ellipse.Fill" Value="{TemplateBinding Fill}" />
                            <Setter TargetName="BackgroundEllipse" Property="Ellipse.Opacity" Value="0.0" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter TargetName="Ellipse" Property="Ellipse.Fill" Value="{TemplateBinding Fill}" />
                            <Setter TargetName="Ellipse" Property="Ellipse.ScaleX" Value="1.67" />
                            <Setter TargetName="Ellipse" Property="Ellipse.ScaleY" Value="1.67" />
                            <Setter TargetName="BackgroundEllipse" Property="Ellipse.Opacity" Value="0.12" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter TargetName="Ellipse" Property="Ellipse.Fill" Value="{DynamicResource RadSliderThumbDisabledColor}" />
                            <Setter TargetName="BackgroundEllipse" Property="Ellipse.Opacity" Value="0.0" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <Ellipse x:Name="Ellipse"
                     WidthRequest="12"
                     HeightRequest="12"
                     HorizontalOptions="Center"
                     VerticalOptions="Center"
                     Stroke="{TemplateBinding Stroke}"
                     StrokeThickness="{TemplateBinding StrokeThickness}" />
            <Ellipse x:Name="BackgroundEllipse"
                     Fill="{TemplateBinding Fill}" />
        </telerikMauiControls:RadLayout>
    </ControlTemplate>

    <ControlTemplate x:Key="SliderThumbControlTemplate_WinUI">
        <telerikMauiControls:RadLayout BackgroundColor="Transparent">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter TargetName="Ellipse" Property="Ellipse.Fill" Value="{TemplateBinding Fill}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter TargetName="Ellipse" Property="Ellipse.Fill" Value="{TemplateBinding Fill}" />
                            <Setter TargetName="Ellipse" Property="Ellipse.Opacity" Value="0.9" />
                            <Setter TargetName="Ellipse" Property="Ellipse.ScaleX" Value="1.16" />
                            <Setter TargetName="Ellipse" Property="Ellipse.ScaleY" Value="1.16" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter TargetName="Ellipse" Property="Ellipse.Fill" Value="{TemplateBinding Fill}" />
                            <Setter TargetName="Ellipse" Property="Ellipse.Opacity" Value="0.8" />
                            <Setter TargetName="Ellipse" Property="Ellipse.ScaleX" Value="0.83" />
                            <Setter TargetName="Ellipse" Property="Ellipse.ScaleY" Value="0.83" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter TargetName="Ellipse" Property="Ellipse.Fill" Value="{DynamicResource RadSliderThumbDisabledColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <telerikMauiControls:RadBorder WidthRequest="22"
                                           HeightRequest="22"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center"
                                           BackgroundColor="{DynamicResource RadSurfaceAltColor}"
                                           BorderColor="{TemplateBinding Stroke}"
                                           BorderThickness="{TemplateBinding StrokeThickness}"
                                           CornerRadius="{TemplateBinding CornerRadius}" />
            <Ellipse x:Name="Ellipse"
                     WidthRequest="12"
                     HeightRequest="12"
                     HorizontalOptions="Center"
                     VerticalOptions="Center" />
        </telerikMauiControls:RadLayout>
    </ControlTemplate>

    <Style x:Key="SliderThumbStyle" TargetType="telerikRangeSlider:SliderThumb">
        <Setter Property="ControlTemplate" Value="{OnPlatform Default={StaticResource SliderThumbControlTemplate}, Android={StaticResource SliderThumbControlTemplate_Android}, WinUI={StaticResource SliderThumbControlTemplate_WinUI}}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=40, MacCatalyst=28, WinUI=32}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=40, MacCatalyst=28, WinUI=32}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Fill" Value="{DynamicResource RadSliderThumbFillColor}" />
        <Setter Property="Stroke" Value="{DynamicResource RadSliderThumbStrokeColor}" />
        <Setter Property="StrokeThickness" Value="{OnPlatform Default=1, Android=0}" />
        <Setter Property="CornerRadius" Value="{Binding TicksPlacement, Source={RelativeSource AncestorType={Type telerikMauiControls:RadSliderBase}}, Converter={StaticResource TicksPlacementToCornerRadiusConverter}}" />
        <Setter Property="Shadow" Value="{StaticResource SliderThumbShadow}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Focused" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Fill" Value="{DynamicResource RadSliderThumbDisabledColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <ControlTemplate x:Key="RadRangeSliderControlTemplate">
        <Grid BackgroundColor="Transparent"
              RowDefinitions="Auto, Auto, Auto, Auto, Auto">
            <telerikRangeSlider:SliderBackTrackPresenter Grid.Row="2">
                <telerikMauiControls:RadBorder Style="{Binding ActualBackTrackStyle, Source={RelativeSource Mode=TemplatedParent}}" />
            </telerikRangeSlider:SliderBackTrackPresenter>
            <telerikRangeSlider:SliderTicksPresenter Style="{StaticResource SliderTicksPresenterStyle}" />
            <telerikRangeSlider:SliderLabelsPresenter Style="{StaticResource SliderLabelsPresenterStyle}" />
            <telerikRangeSlider:SliderRangeThumbPresenter Grid.Row="2">
                <telerikRangeSlider:SliderRangeThumbPresenter.RangeTrack>
                    <telerikRangeSlider:SliderRangeTrack Style="{TemplateBinding ActualRangeTrackStyle}" />
                </telerikRangeSlider:SliderRangeThumbPresenter.RangeTrack>
                <telerikRangeSlider:SliderRangeThumbPresenter.StartThumb>
                    <telerikRangeSlider:SliderThumb Style="{TemplateBinding ActualStartThumbStyle}" />
                </telerikRangeSlider:SliderRangeThumbPresenter.StartThumb>
                <telerikRangeSlider:SliderRangeThumbPresenter.EndThumb>
                    <telerikRangeSlider:SliderThumb Style="{TemplateBinding ActualEndThumbStyle}" />
                </telerikRangeSlider:SliderRangeThumbPresenter.EndThumb>
            </telerikRangeSlider:SliderRangeThumbPresenter>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="RadRangeSliderControlTemplate_Android">
        <Grid BackgroundColor="Transparent"
              RowDefinitions="Auto, Auto, Auto, Auto, Auto">
            <telerikRangeSlider:SliderBackTrackPresenter Grid.Row="2">
                <telerikMauiControls:RadBorder Style="{Binding ActualBackTrackStyle, Source={RelativeSource Mode=TemplatedParent}}" />
            </telerikRangeSlider:SliderBackTrackPresenter>
            <telerikRangeSlider:SliderRangeThumbPresenter Grid.Row="2">
                <telerikRangeSlider:SliderRangeThumbPresenter.RangeTrack>
                    <telerikRangeSlider:SliderRangeTrack Style="{TemplateBinding ActualRangeTrackStyle}" />
                </telerikRangeSlider:SliderRangeThumbPresenter.RangeTrack>
            </telerikRangeSlider:SliderRangeThumbPresenter>
            <telerikRangeSlider:SliderTicksPresenter Style="{StaticResource SliderTicksPresenterStyle}" />
            <telerikRangeSlider:SliderLabelsPresenter Style="{StaticResource SliderLabelsPresenterStyle}" />
            <telerikRangeSlider:SliderRangeThumbPresenter Grid.Row="2">
                <telerikRangeSlider:SliderRangeThumbPresenter.StartThumb>
                    <telerikRangeSlider:SliderThumb Style="{TemplateBinding ActualStartThumbStyle}" />
                </telerikRangeSlider:SliderRangeThumbPresenter.StartThumb>
                <telerikRangeSlider:SliderRangeThumbPresenter.EndThumb>
                    <telerikRangeSlider:SliderThumb Style="{TemplateBinding ActualEndThumbStyle}" />
                </telerikRangeSlider:SliderRangeThumbPresenter.EndThumb>
            </telerikRangeSlider:SliderRangeThumbPresenter>
        </Grid>
    </ControlTemplate>

    <Style x:Key="RadSliderBaseStyle" TargetType="telerikMauiControls:RadSliderBase">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="RangeTrackFill" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="TooltipContentTemplate" Value="{StaticResource SliderTooltipContentTemplate}" />
        <Setter Property="TooltipControlTemplate" Value="{StaticResource SliderTooltipControlTemplate}" />
        <Setter Property="LabelStyle" Value="{StaticResource SliderLabelStyle}" />
        <Setter Property="BackTrackStyle" Value="{StaticResource SliderBackTrackStyle}" />
        <Setter Property="InRangeTickStyle" Value="{StaticResource SliderTickStyle}" />
        <Setter Property="OutOfRangeTickStyle" Value="{StaticResource SliderOutOfRangeTickStyle}" />
        <Style.Triggers>
            <Trigger TargetType="telerikMauiControls:RadSliderBase" Property="TicksPlacement" Value="Center">
                <Setter Property="InRangeTickStyle" Value="{DynamicResource SliderMiddleTickStyle}" />
                <Setter Property="OutOfRangeTickStyle" Value="{DynamicResource SliderOutOfRangeMiddleTickStyle}" />
            </Trigger>
            <Trigger TargetType="telerikMauiControls:RadSliderBase" Property="IsEnabled" Value="False">
                <Setter Property="RangeTrackFill" Value="{DynamicResource RadSliderThumbDisabledColor}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="RadRangeSliderStyle" TargetType="telerikMauiControls:RadRangeSlider" BasedOn="{StaticResource RadSliderBaseStyle}">
        <Setter Property="ControlTemplate" Value="{OnPlatform Default={StaticResource RadRangeSliderControlTemplate}, Android={StaticResource RadRangeSliderControlTemplate_Android}}" />
        <Setter Property="StartThumbStyle" Value="{StaticResource SliderThumbStyle}" />
        <Setter Property="EndThumbStyle" Value="{StaticResource SliderThumbStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadRangeSlider" BasedOn="{StaticResource RadRangeSliderStyle}" />

    <ControlTemplate x:Key="RadSliderControlTemplate">
        <Grid BackgroundColor="Transparent"
              RowDefinitions="Auto, Auto, Auto, Auto, Auto">
            <telerikRangeSlider:SliderBackTrackPresenter Grid.Row="2">
                <telerikMauiControls:RadBorder Style="{Binding ActualBackTrackStyle, Source={RelativeSource Mode=TemplatedParent}}" />
            </telerikRangeSlider:SliderBackTrackPresenter>
            <telerikRangeSlider:SliderTicksPresenter Style="{StaticResource SliderTicksPresenterStyle}" />
            <telerikRangeSlider:SliderLabelsPresenter Style="{StaticResource SliderLabelsPresenterStyle}" />
            <telerikSliders:SliderSingleThumbPresenter Grid.Row="2">
                <telerikSliders:SliderSingleThumbPresenter.RangeTrack>
                    <telerikRangeSlider:SliderRangeTrack Style="{TemplateBinding ActualRangeTrackStyle}" />
                </telerikSliders:SliderSingleThumbPresenter.RangeTrack>
                <telerikSliders:SliderSingleThumbPresenter.Thumb>
                    <telerikRangeSlider:SliderThumb Style="{TemplateBinding ActualThumbStyle}" />
                </telerikSliders:SliderSingleThumbPresenter.Thumb>
            </telerikSliders:SliderSingleThumbPresenter>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="RadSliderControlTemplate_Android">
        <Grid BackgroundColor="Transparent"
              RowDefinitions="Auto, Auto, Auto, Auto, Auto">
            <telerikRangeSlider:SliderBackTrackPresenter Grid.Row="2">
                <telerikMauiControls:RadBorder Style="{Binding ActualBackTrackStyle, Source={RelativeSource Mode=TemplatedParent}}" />
            </telerikRangeSlider:SliderBackTrackPresenter>
            <telerikSliders:SliderSingleThumbPresenter Grid.Row="2">
                <telerikSliders:SliderSingleThumbPresenter.RangeTrack>
                    <telerikRangeSlider:SliderRangeTrack Style="{TemplateBinding ActualRangeTrackStyle}" />
                </telerikSliders:SliderSingleThumbPresenter.RangeTrack>
            </telerikSliders:SliderSingleThumbPresenter>
            <telerikRangeSlider:SliderTicksPresenter Style="{StaticResource SliderTicksPresenterStyle}" />
            <telerikRangeSlider:SliderLabelsPresenter Style="{StaticResource SliderLabelsPresenterStyle}" />
            <telerikSliders:SliderSingleThumbPresenter Grid.Row="2">
                <telerikSliders:SliderSingleThumbPresenter.Thumb>
                    <telerikRangeSlider:SliderThumb Style="{TemplateBinding ActualThumbStyle}" />
                </telerikSliders:SliderSingleThumbPresenter.Thumb>
            </telerikSliders:SliderSingleThumbPresenter>
        </Grid>
    </ControlTemplate>

    <Style x:Key="RadSliderStyle" TargetType="telerikMauiControls:RadSlider" BasedOn="{StaticResource RadSliderBaseStyle}">
        <Setter Property="ControlTemplate" Value="{OnPlatform Default={StaticResource RadSliderControlTemplate}, Android={StaticResource RadSliderControlTemplate_Android}}" />
        <Setter Property="ThumbStyle" Value="{StaticResource SliderThumbStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadSlider" BasedOn="{StaticResource RadSliderStyle}" />
</ResourceDictionary>