<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.Pickers">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <telerikMauiControls:InvertedBooleanConverter x:Key="InvertedBooleanConverter" />

    <Shadow x:Key="PickerContentViewShadow"
            Offset="{OnPlatform Default='0, 0', MacCatalyst='0, 2', WinUI='0, 4'}"
            Radius="{OnPlatform Default=0, MacCatalyst=12, WinUI=8}"
            Brush="{OnPlatform Default=Transparent, MacCatalyst={DynamicResource RadOnAppSurfaceColorAlpha20}, WinUI={DynamicResource RadOnAppSurfaceColorAlpha14}}" />

    <ControlTemplate x:Key="RadPickerBaseControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       IsEnabled="{TemplateBinding IsEnabled}"
                                       AutomationId="PickerBorder">
            <Grid ColumnDefinitions="*, Auto, Auto">
                <Grid>
                    <telerikMauiControls:PickerPlaceholderView ControlTemplate="{TemplateBinding PlaceholderTemplate}"
                                                               PlaceholderLabelStyle="{TemplateBinding ActualPlaceholderLabelStyle}"
                                                               Placeholder="{TemplateBinding Placeholder}"
                                                               ToggleCommand="{TemplateBinding ToggleCommand}"
                                                               IsVisible="{TemplateBinding IsPlaceholderVisible}"
                                                               IsEnabled="{TemplateBinding IsEnabled}"/>
                </Grid>
                <telerikMauiControls:PickerDisplayView ControlTemplate="{TemplateBinding DisplayTemplate}"
                                                       DisplayLabelStyle="{TemplateBinding ActualDisplayLabelStyle}"
                                                       DisplayString="{TemplateBinding DisplayString}"
                                                       ToggleCommand="{TemplateBinding ToggleCommand}"
                                                       IsVisible="{TemplateBinding IsPlaceholderVisible, Converter={StaticResource InvertedBooleanConverter}}"
                                                       IsEnabled="{TemplateBinding IsEnabled}"/>
                <telerikMauiControls:RadButton Grid.Column="1"
                                               IsVisible="{TemplateBinding IsClearButtonVisible}"
                                               IsEnabled="{TemplateBinding IsEnabled}"
                                               Style="{TemplateBinding ActualClearButtonStyle}"
                                               Command="{TemplateBinding ClearCommand}"
                                               AutomationId="ClearButton" />
                <telerikMauiControls:RadButton Grid.Column="2"
                                               IsVisible="{TemplateBinding IsToggleButtonVisible}"
                                               IsEnabled="{TemplateBinding IsEnabled}"
                                               Style="{TemplateBinding ActualToggleButtonStyle}"
                                               Command="{TemplateBinding ToggleCommand}"
                                               AutomationId="ToggleButton" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="RadPickerBaseDisplayViewControlTemplate">
        <Grid>
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{TemplateBinding ToggleCommand}" />
            </Grid.GestureRecognizers>
            <Label Text="{TemplateBinding DisplayString}"
                   IsEnabled="{TemplateBinding IsEnabled}"
                   Style="{TemplateBinding DisplayLabelStyle}"
                   AutomationId="PickerDisplayLabel"/>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="RadPickerBasePlaceholderViewControlTemplate">
        <Grid>
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{TemplateBinding ToggleCommand}" />
            </Grid.GestureRecognizers>
            <Label Text="{TemplateBinding Placeholder}"
                   IsEnabled="{TemplateBinding IsEnabled}"
                   Style="{TemplateBinding PlaceholderLabelStyle}"
                   AutomationId="PickerPlaceholderLabel"/>
        </Grid>
    </ControlTemplate>

    <Style x:Key="PickerDisplayLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadPickerTextColor}" />
        <Setter Property="Margin" Value="{OnPlatform Android='8, 11, 8, 14', iOS='8, 9', MacCatalyst='6, 8, 6, 7', WinUI='10, 5, 10, 6'}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="FontSize" Value="{OnPlatform Default=14, iOS=17, MacCatalyst=13}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadPickerDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="PickerPlaceholderLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="Margin" Value="{OnPlatform Android='8, 11, 8, 14', iOS='8, 9', MacCatalyst='6, 8, 6, 7', WinUI='10, 5, 10, 6'}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="FontSize" Value="{OnPlatform Default=14, iOS=17, MacCatalyst=13}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadPickerDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="PickerClearButtonStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="Text" Value="&#xE858;" />
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="WidthRequest" Value="30" />
        <Setter Property="HeightRequest" Value="30" />
        <Setter Property="BorderWidth" Value="0" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="FontFamily" Value="TelerikFont" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadPickerDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="PickerToggleButtonStyle" TargetType="Button">
        <Setter Property="Text" Value="&#xE812;" />
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="WidthRequest" Value="30" />
        <Setter Property="HeightRequest" Value="30" />
        <Setter Property="BorderWidth" Value="0" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="FontFamily" Value="TelerikFont" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadPickerDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="PickerSpinnerHeaderStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=16, WinUI=14}" />
        <Setter Property="Margin" Value="0, 10" />
        <Setter Property="FontAttributes" Value="{OnPlatform Default=None, MacCatalyst=Bold, WinUI=Bold}" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="PickerSpinnerSeparatorStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BorderColor" Value="{DynamicResource SeparatorColor}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, WinUI='1, 0, 0, 0'}" />
        <Setter Property="Margin" Value="0, 16" />
    </Style>

    <Style x:Key="PickerSpinnerHeaderSeparatorStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BorderColor" Value="{DynamicResource SeparatorColor}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, WinUI='0, 1, 0, 0'}" />
    </Style>

    <Style x:Key="PickerSpinnerItemStyle" TargetType="telerikMauiControls:SpinnerItemView">
        <Setter Property="TextColor" Value="{OnPlatform Default={DynamicResource RadOnAppSurfaceColorAlpha50}, MacCatalyst={DynamicResource RadOnAppSurfaceColor}, WinUI={DynamicResource RadOnAppSurfaceColor}}" />
    </Style>

    <Style x:Key="PickerSpinnerSelectedItemStyle" TargetType="telerikMauiControls:SpinnerItemView">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="PickerSpinnerSelectionHighlightStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, Android='0, 1', iOS='0, 1'}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPickerSpinnerSelectionHighlightBackgroundColor}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=44, MacCatalyst=28, WinUI=40}" />
    </Style>

    <Style x:Key="PickerSpinnerStyle" TargetType="telerikMauiControls:RadSpinner">
        <Setter Property="HeightRequest" Value="220" />
        <Setter Property="ItemLength" Value="{OnPlatform Default=44, MacCatalyst=28, WinUI=40}" />
        <Setter Property="Margin" Value="0, 16" />
        <Setter Property="ItemStyle">
            <Setter.Value>
                <Style TargetType="telerikMauiControls:SpinnerItemView">
                    <Setter Property="TextColor" Value="{OnPlatform Default={DynamicResource RadOnAppSurfaceColorAlpha50}, MacCatalyst={DynamicResource RadOnAppSurfaceColor}, WinUI={DynamicResource RadOnAppSurfaceColor}}" />
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="SelectedItemStyle">
            <Setter.Value>
                <Style TargetType="telerikMauiControls:SpinnerItemView">
                    <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MultiSpinnerPickerTabViewHeaderStyle" TargetType="telerikMauiControls:TabViewHeader">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="CornerRadius" Value="4, 4, 0, 0" />
    </Style>

    <VisualStateGroupList x:Key="MultiSpinnerPickerTabViewHeaderItem_VisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPickerDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="{DynamicResource RadPrimaryColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="MultiSpinnerPickerTabViewHeaderItem_VisualStates_iOS">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPickerDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="MultiSpinnerPickerTabViewHeaderItem_VisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPickerDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="Transparent" />
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderThickness" Value="0, 0, 1, 0" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="MultiSpinnerPickerTabViewHeaderItem_VisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPickerDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="MultiSpinnerPickerTabViewHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPickerHeaderBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Android='0, 0, 0, 2', iOS=0, MacCatalyst=1, WinUI=1}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=16, Android=18, WinUI=14}" />
        <Setter Property="Padding" Value="{OnPlatform Default=0, MacCatalyst=-1, WinUI='-1, -1, -1, 0'}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default=16, WinUI='16, 6'}" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Android={StaticResource MultiSpinnerPickerTabViewHeaderItem_VisualStates_Android},
                                   iOS={StaticResource MultiSpinnerPickerTabViewHeaderItem_VisualStates_iOS},
                                   MacCatalyst={StaticResource MultiSpinnerPickerTabViewHeaderItem_VisualStates_MacCatalyst},
                                   WinUI={StaticResource MultiSpinnerPickerTabViewHeaderItem_VisualStates_WinUI}}" />
    </Style>

    <Style x:Key="MultiSpinnerPickerTabViewContentStyle" TargetType="telerikMauiControls:TabViewContent">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
    </Style>

    <Style x:Key="PickerContentViewStyle" TargetType="telerikMauiControls:PickerContentView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPickerContentViewBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
    </Style>

    <ControlTemplate x:Key="PickerContentViewHeaderTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       HeightRequest="{TemplateBinding HeightRequest}">
            <Label Text="{TemplateBinding HeaderLabelText}"
                   Style="{TemplateBinding HeaderLabelStyle}"
                   AutomationId="PickerPopupHeaderLabel" />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="PickerContentViewHeaderStyle" TargetType="telerikMauiControls:PickerContentView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPickerHeaderBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=64, MacCatalyst=48, WinUI=48}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, WinUI='0, 0, 0, 1'}" />
        <Setter Property="CornerRadius" Value="4, 4, 0, 0" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>

    <Style x:Key="PickerContentViewHeaderLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="HorizontalOptions" Value="{OnPlatform Default=Center, MacCatalyst=Start, WinUI=Start}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="Margin" Value="{OnPlatform Default='10, 0', WinUI='20, 0'}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=22, WinUI=20}" />
    </Style>

    <ControlTemplate x:Key="PickerContentViewFooterTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="{OnPlatform Default=*, WinUI=0}" />
                    <ColumnDefinition Width="{OnPlatform Default=Auto, WinUI=*}" />
                    <ColumnDefinition Width="{OnPlatform Default=Auto, WinUI=*}" />
                </Grid.ColumnDefinitions>
                <telerikMauiControls:RadButton Grid.Column="{OnPlatform Default=1, WinUI=2}"
                                               Text="{TemplateBinding CancelButtonText}"
                                               Style="{TemplateBinding CancelButtonStyle}"
                                               Command="{TemplateBinding CancelCommand}"
                                               AutomationId="PickerPopupCancelButton" />
                <telerikMauiControls:RadButton Grid.Column="{OnPlatform Default=2, WinUI=1}"
                                               Text="{TemplateBinding AcceptButtonText}"
                                               Style="{TemplateBinding AcceptButtonStyle}"
                                               Command="{TemplateBinding AcceptCommand}"
                                               AutomationId="PickerPopupOkButton" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="PickerContentViewFooterStyle" TargetType="telerikMauiControls:PickerContentView">
        <Setter Property="BorderThickness" Value="0, 1, 0, 0" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Android=52, iOS=60, MacCatalyst=36, WinUI=48}" />
        <Setter Property="CornerRadius" Value="0, 0, 4, 4" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>

    <Style x:Key="PickerContentViewFooterButtonBaseStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BorderWidth" Value="0" />
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="Margin" Value="0, 8, 8, 8" />
        <Setter Property="Padding" Value="26, 8" />
    </Style>

    <Style x:Key="PickerContentViewFooterCancelButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource PickerContentViewFooterButtonBaseStyle}" />

    <Style x:Key="PickerContentViewFooterAcceptButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource PickerContentViewFooterButtonBaseStyle}" />

    <ControlTemplate x:Key="PickerPopupViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Shadow="{StaticResource PickerContentViewShadow}">
            <VerticalStackLayout>
                <telerikMauiControls:PickerPopupHeaderView HeaderLabelText="{TemplateBinding HeaderLabelText}"
                                                           HeaderLabelStyle="{TemplateBinding HeaderLabelStyle}"
                                                           Style="{TemplateBinding HeaderStyle}"
                                                           ControlTemplate="{TemplateBinding HeaderTemplate}"
                                                           IsVisible="{TemplateBinding IsHeaderVisible}" />
                <ContentPresenter />
                <telerikMauiControls:PickerPopupFooterView ControlTemplate="{TemplateBinding FooterTemplate}"
                                                           Style="{TemplateBinding FooterStyle}"
                                                           AcceptButtonText="{TemplateBinding AcceptButtonText}"
                                                           AcceptButtonStyle="{TemplateBinding AcceptButtonStyle}"
                                                           AcceptCommand="{TemplateBinding AcceptCommand}"
                                                           CancelButtonText="{TemplateBinding CancelButtonText}"
                                                           CancelButtonStyle="{TemplateBinding CancelButtonStyle}"
                                                           CancelCommand="{TemplateBinding CancelCommand}"
                                                           IsVisible="{TemplateBinding IsFooterVisible}" />
            </VerticalStackLayout>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="PickerPopupViewStyle" TargetType="telerikMauiControls:PickerPopupContentView" BasedOn="{StaticResource PickerContentViewStyle}">
        <Setter Property="ControlTemplate" Value="{StaticResource PickerPopupViewControlTemplate}" />
        <Setter Property="WidthRequest" Value="280" />
    </Style>

    <ControlTemplate x:Key="PickerDropDownViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <VerticalStackLayout>
                <ContentPresenter />
                <telerikMauiControls:PickerDropDownFooterView ControlTemplate="{TemplateBinding FooterTemplate}"
                                                              Style="{TemplateBinding FooterStyle}"
                                                              AcceptButtonText="{TemplateBinding AcceptButtonText}"
                                                              AcceptButtonStyle="{TemplateBinding AcceptButtonStyle}"
                                                              AcceptCommand="{TemplateBinding AcceptCommand}"
                                                              CancelButtonText="{TemplateBinding CancelButtonText}"
                                                              CancelButtonStyle="{TemplateBinding CancelButtonStyle}"
                                                              CancelCommand="{TemplateBinding CancelCommand}"
                                                              IsVisible="{TemplateBinding IsFooterVisible}" />
            </VerticalStackLayout>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="PickerDropDownViewStyle" TargetType="telerikMauiControls:PickerDropDownContentView" BasedOn="{StaticResource PickerContentViewStyle}">
        <Setter Property="ControlTemplate" Value="{StaticResource PickerDropDownViewControlTemplate}" />
        <Setter Property="MinimumWidthRequest" Value="280" />
    </Style>

    <ControlTemplate x:Key="RadPickerControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <ContentPresenter />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <VisualStateGroupList x:Key="RadPickerVisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadPickerBase.BackgroundColor" Value="{DynamicResource RadPickerDisabledBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadPickerBase.BorderBrush" Value="{DynamicResource RadPickerDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadPickerVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadPickerBase.BackgroundColor" Value="{DynamicResource RadPickerPointerOverBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadPickerBase.BackgroundColor" Value="{DynamicResource RadPickerDisabledBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadPickerBase.BorderBrush" Value="{DynamicResource RadPickerDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="PickerBaseStyle" TargetType="telerikMauiControls:RadPickerBase">
        <Setter Property="ControlTemplate" Value="{StaticResource RadPickerControlTemplate}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPickerBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadPickerBorderBrush}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=1, Android='0, 0, 0, 1'}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Android='4, 4, 0, 0', iOS=10, MacCatalyst=6, WinUI=4}" />
        <Setter Property="DisplayLabelStyle" Value="{StaticResource PickerDisplayLabelStyle}" />
        <Setter Property="PlaceholderLabelStyle" Value="{StaticResource PickerPlaceholderLabelStyle}" />
        <Setter Property="ClearButtonStyle" Value="{StaticResource PickerClearButtonStyle}" />
        <Setter Property="ToggleButtonStyle" Value="{StaticResource PickerToggleButtonStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Default={StaticResource RadPickerVisualStates}, WinUI={StaticResource RadPickerVisualStates_WinUI}}" />
    </Style>

    <Style x:Key="PickerDropDownSettingsStyle" TargetType="telerikMauiControls:PickerDropDownSettings">
        <Setter Property="DropDownViewStyle" Value="{StaticResource PickerContentViewStyle}" />
        <Setter Property="FooterTemplate" Value="{StaticResource PickerContentViewFooterTemplate}" />
        <Setter Property="FooterStyle" Value="{StaticResource PickerContentViewFooterStyle}" />
        <Setter Property="CancelButtonStyle" Value="{StaticResource PickerContentViewFooterCancelButtonStyle}" />
        <Setter Property="AcceptButtonStyle" Value="{StaticResource PickerContentViewFooterAcceptButtonStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:PickerDropDownSettings" BasedOn="{StaticResource PickerDropDownSettingsStyle}" />

    <Style x:Key="PickerPopupSettingsStyle" TargetType="telerikMauiControls:PickerPopupSettings">
        <Setter Property="PopupViewStyle" Value="{StaticResource PickerContentViewStyle}" />
        <Setter Property="HeaderTemplate" Value="{StaticResource PickerContentViewHeaderTemplate}" />
        <Setter Property="HeaderLabelStyle" Value="{StaticResource PickerContentViewHeaderLabelStyle}" />
        <Setter Property="HeaderStyle" Value="{StaticResource PickerContentViewHeaderStyle}" />
        <Setter Property="FooterTemplate" Value="{StaticResource PickerContentViewFooterTemplate}" />
        <Setter Property="FooterStyle" Value="{StaticResource PickerContentViewFooterStyle}" />
        <Setter Property="CancelButtonStyle" Value="{StaticResource PickerContentViewFooterCancelButtonStyle}" />
        <Setter Property="AcceptButtonStyle" Value="{StaticResource PickerContentViewFooterAcceptButtonStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:PickerPopupSettings" BasedOn="{StaticResource PickerPopupSettingsStyle}" />

    <Style x:Key="RadTemplatedPickerStyle" TargetType="telerikMauiControls:RadTemplatedPicker" BasedOn="{StaticResource PickerBaseStyle}" />
    <Style TargetType="telerikMauiControls:RadTemplatedPicker" BasedOn="{StaticResource RadTemplatedPickerStyle}" />

    <Style x:Key="ListPickerSpinnerStyle" TargetType="telerikMauiControls:RadSpinner">
        <Setter Property="HeightRequest" Value="220" />
        <Setter Property="ItemLength" Value="44" />
    </Style>

    <Style x:Key="ListPickerSelectionHighlightStyle" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource PickerSpinnerSelectionHighlightStyle}">
        <Setter Property="HeightRequest" Value="44" />
    </Style>

    <Style x:Key="RadListPickerStyle" TargetType="telerikMauiControls:RadListPicker" BasedOn="{StaticResource PickerBaseStyle}">
        <Setter Property="ItemStyle" Value="{StaticResource PickerSpinnerItemStyle}" />
        <Setter Property="SelectedItemStyle" Value="{StaticResource PickerSpinnerSelectedItemStyle}" />
        <Setter Property="SelectionHighlightStyle" Value="{StaticResource ListPickerSelectionHighlightStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadListPicker" BasedOn="{StaticResource RadListPickerStyle}" />

    <Style x:Key="MultiSpinnerPickerSpinnerStyle" TargetType="telerikMauiControls:RadSpinner">
        <Setter Property="ItemStyle" Value="{StaticResource PickerSpinnerItemStyle}" />
        <Setter Property="SelectedItemStyle" Value="{StaticResource PickerSpinnerSelectedItemStyle}" />
    </Style>

    <Style x:Key="MultiSpinnerPickerBaseStyle" TargetType="telerikMauiControls:RadMultiSpinnerPickerBase" BasedOn="{StaticResource PickerBaseStyle}">
        <Setter Property="SpinnerStyle" Value="{StaticResource MultiSpinnerPickerSpinnerStyle}" />
        <Setter Property="SpinnerHeaderStyle" Value="{StaticResource PickerSpinnerHeaderStyle}" />
        <Setter Property="SpinnerHeaderSeparatorStyle" Value="{StaticResource PickerSpinnerHeaderSeparatorStyle}" />
        <Setter Property="SpinnerSeparatorStyle" Value="{StaticResource PickerSpinnerSeparatorStyle}" />
        <Setter Property="SelectionHighlightStyle" Value="{StaticResource PickerSpinnerSelectionHighlightStyle}" />
    </Style>

    <Style x:Key="RadDatePickerStyle" TargetType="telerikMauiControls:RadDatePicker" BasedOn="{StaticResource MultiSpinnerPickerBaseStyle}" />
    <Style TargetType="telerikMauiControls:RadDatePicker" BasedOn="{StaticResource RadDatePickerStyle}" />

    <Style x:Key="RadTimePickerStyle" TargetType="telerikMauiControls:RadTimePicker" BasedOn="{StaticResource MultiSpinnerPickerBaseStyle}" />
    <Style TargetType="telerikMauiControls:RadTimePicker" BasedOn="{StaticResource RadTimePickerStyle}" />

    <Style x:Key="RadTimeSpanPickerStyle" TargetType="telerikMauiControls:RadTimeSpanPicker" BasedOn="{StaticResource MultiSpinnerPickerBaseStyle}" />
    <Style TargetType="telerikMauiControls:RadTimeSpanPicker" BasedOn="{StaticResource RadTimeSpanPickerStyle}" />

    <Style x:Key="RadDateTimePickerStyle" TargetType="telerikMauiControls:RadDateTimePicker" BasedOn="{StaticResource MultiSpinnerPickerBaseStyle}">
        <Setter Property="TabStripStyle" Value="{StaticResource MultiSpinnerPickerTabViewHeaderStyle}" />
        <Setter Property="TabStripItemStyle" Value="{StaticResource MultiSpinnerPickerTabViewHeaderItemStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadDateTimePicker" BasedOn="{StaticResource RadDateTimePickerStyle}" />
</ResourceDictionary>