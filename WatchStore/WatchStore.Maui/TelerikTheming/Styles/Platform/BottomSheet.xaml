<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikBottomSheet="clr-namespace:Telerik.Maui.Controls.BottomSheet;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.BottomSheet">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <Shadow x:Key="BottomSheetShadow"
            Offset="{OnPlatform Default='0, -1', MacCatalyst='0, 8', WinUI='0, -4'}"
            Radius="{OnPlatform Default=4, MacCatalyst=40, WinUI=16}"
            Brush="#FF000000"
            Opacity="{OnPlatform Default=0.15, MacCatalyst=0.24, WinUI=0.14}" />

    <Style x:Key="BottomSheetHandleStyle" TargetType="telerikBottomSheet:BottomSheetHandle">
        <Setter Property="BackgroundColor" Value="{DynamicResource BottomSheetHandleBackgroundColor}" />
        <Setter Property="WidthRequest" Value="{OnPlatform Default=50, Android=32, iOS=36}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=5, Android=4, WinUI=6}" />
        <Setter Property="Margin" Value="{OnPlatform Default='0, 16', MacCatalyst='0, 9.5', iOS='0, 15'}" />
        <Setter Property="CornerRadius" Value="4" />
    </Style>

    <Style TargetType="telerikBottomSheet:BottomSheetHandle" BasedOn="{StaticResource BottomSheetHandleStyle}" />

    <Style x:Key="BottomSheetContentStyle" TargetType="telerikBottomSheet:BottomSheetContentView">
        <Setter Property="BackgroundColor" Value="{DynamicResource BottomSheetContentViewBackgroundColor}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default='12, 12, 0, 0', MacCatalyst='13, 13, 0, 0', WinUI='8, 8, 0, 0'}" />
    </Style>

    <Style x:Key="RadBottomSheetStyle" TargetType="telerikMauiControls:RadBottomSheet">
        <Setter Property="BottomSheetContentStyle" Value="{DynamicResource BottomSheetContentStyle}" />
    </Style>

    <Style TargetType="telerikMauiControls:RadBottomSheet" BasedOn="{StaticResource RadBottomSheetStyle}" />

</ResourceDictionary>