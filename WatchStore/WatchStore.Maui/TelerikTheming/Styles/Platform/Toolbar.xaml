<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMauiToolbar="clr-namespace:Telerik.Maui.Controls.Toolbar;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.Toolbar">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <!-- Dependencies: Entry.xaml -->

    <telerikMauiControls:InvertedNullToBooleanConverter x:Key="InvertedNullToBooleanConverter" />

    <Style x:Key="ToolbarSpacingBoxViewStyle" TargetType="BoxView">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Color" Value="Transparent" />
        <Setter Property="Opacity" Value="0" />
    </Style>

    <ControlTemplate x:Key="ToolbarListItemViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       Content="{TemplateBinding Content}" />
    </ControlTemplate>

    <Style x:Key="ToolbarListItemViewStyle" TargetType="telerikMauiControls:ToolbarListItemView">
        <Setter Property="ContentPadding" Value="{OnPlatform Default='16, 10', MacCatalyst='6, 2'}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, MacCatalyst=4}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=24}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=24}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadToolbarListItemViewPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:ToolbarListItemView" BasedOn="{StaticResource ToolbarListItemViewStyle}" />

    <Style x:Key="GroupToolbarItemViewStyle" TargetType="telerikMauiControls:GroupToolbarItemView">
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
    </Style>

    <Style x:Key="GroupToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:GroupToolbarItemView" BasedOn="{StaticResource GroupToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>

    <Style x:Key="GroupToolbarItemViewStyleVertical" TargetType="telerikMauiControls:GroupToolbarItemView" BasedOn="{StaticResource GroupToolbarItemViewStyle}">
        <Setter Property="VerticalOptions" Value="Start" />
    </Style>

    <Style x:Key="GroupToolbarItemViewStyleDropDown" TargetType="telerikMauiControls:GroupToolbarItemView" BasedOn="{StaticResource GroupToolbarItemViewStyle}">
        <Setter Property="BorderThickness" Value="0, 1" />
        <Setter Property="ItemSpacing" Value="0" />
    </Style>
    <Style TargetType="telerikMauiControls:GroupToolbarItemView" BasedOn="{StaticResource GroupToolbarItemViewStyle}" />

    <ControlTemplate x:Key="ToolbarItemViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       Content="{TemplateBinding Content}" />
    </ControlTemplate>

    <Style x:Key="ToolbarItemViewStyle" TargetType="telerikMauiControls:ToolbarItemView" />

    <Style TargetType="telerikMauiControls:ToolbarItemView" BasedOn="{StaticResource ToolbarItemViewStyle}" />

    <Style x:Key="ToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:ToolbarItemView" BasedOn="{StaticResource ToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>
    <Style x:Key="ToolbarItemViewStyleVertical" TargetType="telerikMauiControls:ToolbarItemView" BasedOn="{StaticResource ToolbarItemViewStyle}">
        <Setter Property="VerticalOptions" Value="Start" />
    </Style>
    <Style x:Key="ToolbarItemViewStyleDropDown" TargetType="telerikMauiControls:ToolbarItemView" BasedOn="{StaticResource ToolbarItemViewStyle}" />

    <Style x:Key="SeparatorToolbarItemViewStyle" TargetType="telerikMauiControls:SeparatorToolbarItemView" BasedOn="{StaticResource ToolbarItemViewStyle}">
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="MinimumWidthRequest" Value="1" />
        <Setter Property="MinimumHeightRequest" Value="1" />
    </Style>
    <Style TargetType="telerikMauiControls:SeparatorToolbarItemView" BasedOn="{StaticResource SeparatorToolbarItemViewStyle}" />

    <Style x:Key="SeparatorToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:SeparatorToolbarItemView"  BasedOn="{StaticResource SeparatorToolbarItemViewStyle}">
        <Setter Property="Padding" Value="{OnPlatform Default=0, iOS='0, 6', Android='0, 8', WinUI='0, 8'}" />
        <Setter Property="BorderThickness" Value="1, 0, 0, 0" />
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>

    <Style x:Key="SeparatorToolbarItemViewStyleVertical" TargetType="telerikMauiControls:SeparatorToolbarItemView" BasedOn="{StaticResource SeparatorToolbarItemViewStyle}">
        <Setter Property="BorderThickness" Value="0, 1, 0, 0" />
        <Setter Property="VerticalOptions" Value="Start" />
    </Style>

    <Style x:Key="SeparatorToolbarItemViewStyleDropDown" TargetType="telerikMauiControls:SeparatorToolbarItemView" BasedOn="{StaticResource SeparatorToolbarItemViewStyle}">
        <Setter Property="BorderThickness" Value="0, 1, 0, 0" />
    </Style>

    <ControlTemplate x:Key="LabelToolbarItemViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <ContentPresenter HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                              VerticalOptions="{TemplateBinding VerticalContentOptions}" />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="LabelToolbarItemViewControlTemplateText">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <Label Text="{TemplateBinding Text}"
                   TextColor="{TemplateBinding TextColor}"
                   FontFamily="{TemplateBinding FontFamily}"
                   FontSize="{TemplateBinding FontSize}"
                   FontAttributes="{TemplateBinding FontAttributes}"
                   TextDecorations="{TemplateBinding TextDecorations}"
                   HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                   VerticalOptions="{TemplateBinding VerticalContentOptions}"
                   HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                   VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="LabelToolbarItemViewControlTemplateImage">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <telerikMauiToolbar:ToolbarImage Source="{TemplateBinding ActualImageSource}"
                                             Aspect="{TemplateBinding ImageAspect}"
                                             WidthRequest="{TemplateBinding ImageWidth}"
                                             HeightRequest="{TemplateBinding ImageHeight}"
                                             HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                                             VerticalOptions="{TemplateBinding VerticalContentOptions}" />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="LabelToolbarItemViewControlTemplateLeft">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <Grid ColumnDefinitions="Auto, *"
                  ColumnSpacing="{TemplateBinding ImageSpacing}"
                  HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                  VerticalOptions="{TemplateBinding VerticalContentOptions}">
                <telerikMauiToolbar:ToolbarImage Grid.Column="0"
                                                 Source="{TemplateBinding ActualImageSource}"
                                                 Aspect="{TemplateBinding ImageAspect}"
                                                 WidthRequest="{TemplateBinding ImageWidth}"
                                                 HeightRequest="{TemplateBinding ImageHeight}" />
                <Label Grid.Column="1"
                       Text="{TemplateBinding Text}"
                       TextColor="{TemplateBinding TextColor}"
                       FontFamily="{TemplateBinding FontFamily}"
                       FontSize="{TemplateBinding FontSize}"
                       FontAttributes="{TemplateBinding FontAttributes}"
                       TextDecorations="{TemplateBinding TextDecorations}"
                       HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                       VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="LabelToolbarItemViewControlTemplateTop">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <Grid RowDefinitions="Auto, *"
                  RowSpacing="{TemplateBinding ImageSpacing}"
                  HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                  VerticalOptions="{TemplateBinding VerticalContentOptions}">
                <telerikMauiToolbar:ToolbarImage Grid.Row="0"
                                                 Source="{TemplateBinding ActualImageSource}"
                                                 Aspect="{TemplateBinding ImageAspect}"
                                                 WidthRequest="{TemplateBinding ImageWidth}"
                                                 HeightRequest="{TemplateBinding ImageHeight}" />
                <Label Grid.Row="1"
                       Text="{TemplateBinding Text}"
                       TextColor="{TemplateBinding TextColor}"
                       FontFamily="{TemplateBinding FontFamily}"
                       FontSize="{TemplateBinding FontSize}"
                       FontAttributes="{TemplateBinding FontAttributes}"
                       TextDecorations="{TemplateBinding TextDecorations}"
                       HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                       VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="LabelToolbarItemViewControlTemplateRight">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding ImageSpacing}"
                  HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                  VerticalOptions="{TemplateBinding VerticalContentOptions}">
                <Label Grid.Column="0"
                       Text="{TemplateBinding Text}"
                       TextColor="{TemplateBinding TextColor}"
                       FontFamily="{TemplateBinding FontFamily}"
                       FontSize="{TemplateBinding FontSize}"
                       FontAttributes="{TemplateBinding FontAttributes}"
                       TextDecorations="{TemplateBinding TextDecorations}"
                       HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                       VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
                <telerikMauiToolbar:ToolbarImage Grid.Column="1"
                                                 Source="{TemplateBinding ActualImageSource}"
                                                 Aspect="{TemplateBinding ImageAspect}"
                                                 WidthRequest="{TemplateBinding ImageWidth}"
                                                 HeightRequest="{TemplateBinding ImageHeight}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="LabelToolbarItemViewControlTemplateBottom">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <Grid RowDefinitions="*, Auto"
                  RowSpacing="{TemplateBinding ImageSpacing}"
                  HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                  VerticalOptions="{TemplateBinding VerticalContentOptions}">
                <Label Grid.Row="0"
                       Text="{TemplateBinding Text}"
                       TextColor="{TemplateBinding TextColor}"
                       FontFamily="{TemplateBinding FontFamily}"
                       FontSize="{TemplateBinding FontSize}"
                       FontAttributes="{TemplateBinding FontAttributes}"
                       TextDecorations="{TemplateBinding TextDecorations}"
                       HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                       VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
                <telerikMauiToolbar:ToolbarImage Grid.Row="1"
                                                 Source="{TemplateBinding ActualImageSource}"
                                                 Aspect="{TemplateBinding ImageAspect}"
                                                 WidthRequest="{TemplateBinding ImageWidth}"
                                                 HeightRequest="{TemplateBinding ImageHeight}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="LabelToolbarItemViewStyle" TargetType="telerikMauiControls:LabelToolbarItemView" BasedOn="{StaticResource ToolbarItemViewStyle}">
        <Setter Property="Padding" Value="{OnPlatform Default=0, WinUI='3, 4', Android='8, 4'}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default=8, WinUI='8, 6', MacCatalyst=2}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=14, MacCatalyst=13, iOS=17}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=20}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=20}" />
        <Setter Property="ToolTipProperties.Text" Value="{OnPlatform Default={x:Null}, 
                                                                     WinUI={Binding Source={RelativeSource Mode=Self}, Path=Text},
                                                                     MacCatalyst={Binding Source={RelativeSource Mode=Self}, Path=Text}}" />
        <Setter Property="ImageSpacing" Value="8" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Selected" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:LabelToolbarItemView" BasedOn="{StaticResource LabelToolbarItemViewStyle}" />

    <Style x:Key="LabelToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:LabelToolbarItemView"  BasedOn="{StaticResource LabelToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>
    <Style x:Key="LabelToolbarItemViewStyleVertical"  TargetType="telerikMauiControls:LabelToolbarItemView" BasedOn="{StaticResource LabelToolbarItemViewStyle}">
        <Setter Property="VerticalOptions" Value="Start" />
    </Style>
    <Style x:Key="LabelToolbarItemViewStyleDropDown" TargetType="telerikMauiControls:LabelToolbarItemView" BasedOn="{StaticResource LabelToolbarItemViewStyle}">
        <Setter Property="Padding" Value="0" />
    </Style>

    <Style x:Key="ButtonToolbarItemViewBaseStyle" TargetType="telerikMauiControls:ButtonToolbarItemView" BasedOn="{StaticResource LabelToolbarItemViewStyle}">
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, WinUI=1}" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadToolbarButtonToolbarItemViewPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Opened">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="ButtonToolbarItemViewStyle" TargetType="telerikMauiControls:ButtonToolbarItemView"  BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />

    <Style x:Key="ButtonToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:ButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>

    <Style x:Key="ButtonToolbarItemViewStyleVertical" TargetType="telerikMauiControls:ButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewStyle}">
        <Setter Property="VerticalOptions" Value="Start" />
    </Style>

    <Style x:Key="ButtonToolbarItemViewStyleDropDown" TargetType="telerikMauiControls:ButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewStyle}">
        <Setter Property="Padding" Value="0" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default='16, 10', Android='16, 13', MacCatalyst='6, 4'}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, MacCatalyst=4}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="DisplayOptions" Value="Image, Text" />
    </Style>

    <Style TargetType="telerikMauiControls:ButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style TargetType="telerikMauiControls:NavigationButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />

    <Style x:Key="OptionsButtonToolbarItemViewStyle" TargetType="telerikMauiControls:OptionsButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}">
        <Setter Property="IsDropDownIndicatorVisible" Value="False" />
    </Style>

    <Style x:Key="OptionsButtonToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:OptionsButtonToolbarItemView" BasedOn="{StaticResource OptionsButtonToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="DropDownPlacement" Value="Bottom" />
        <Setter Property="DropDownHorizontalOffset" Value="{OnPlatform Default=8, iOS=3, MacCatalyst=0}" />
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform iOS=2, Android=3, WinUI=2, MacCatalyst=9}" />
    </Style>

    <Style x:Key="OptionsButtonToolbarItemViewStyleVertical" TargetType="telerikMauiControls:OptionsButtonToolbarItemView" BasedOn="{StaticResource OptionsButtonToolbarItemViewStyle}">
        <Setter Property="VerticalOptions" Value="Start" />
        <Setter Property="DropDownPlacement" Value="Right" />
        <Setter Property="DropDownHorizontalOffset" Value="{OnPlatform iOS=2, Android=3, WinUI=2, MacCatalyst=9}" />
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform Default=0, WinUI=2, iOS=3, Android=8}" />
    </Style>

    <Style x:Key="OptionsButtonToolbarItemViewStyleDropDown" TargetType="telerikMauiControls:OptionsButtonToolbarItemView" BasedOn="{StaticResource OptionsButtonToolbarItemViewStyle}">
        <Setter Property="Padding" Value="0" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="DisplayOptions" Value="Image, Text" />
        <Setter Property="DropDownPlacement" Value="Right" />
        <Setter Property="DropDownHorizontalOffset" Value="{OnPlatform iOS=2, Android=3, WinUI=2, MacCatalyst=9}" />
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform Default=0, WinUI=2, iOS=3, Android=8}" />
    </Style>
    <Style TargetType="telerikMauiControls:OptionsButtonToolbarItemView" BasedOn="{StaticResource OptionsButtonToolbarItemViewStyle}" />

    <Style x:Key="SelectableButtonToolbarItemViewStyle" TargetType="telerikMauiControls:SelectableButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}">
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadToolbarButtonToolbarItemViewPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="SelectedDisabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadToolbarSelectedDisabledBackgroundColor}" />
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SelectableButtonToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:SelectableButtonToolbarItemView" BasedOn="{StaticResource SelectableButtonToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>

    <Style x:Key="SelectableButtonToolbarItemViewStyleVertical" TargetType="telerikMauiControls:SelectableButtonToolbarItemView" BasedOn="{StaticResource SelectableButtonToolbarItemViewStyle}">
        <Setter Property="VerticalOptions" Value="Start" />
    </Style>

    <Style x:Key="SelectableButtonToolbarItemViewStyleDropDown" TargetType="telerikMauiControls:SelectableButtonToolbarItemView" BasedOn="{StaticResource SelectableButtonToolbarItemViewStyle}">
        <Setter Property="Padding" Value="0" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default='16, 10', Android='16, 13', MacCatalyst='6, 4'}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, MacCatalyst=4}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="DisplayOptions" Value="Image, Text" />
    </Style>
    <Style TargetType="telerikMauiControls:SelectableButtonToolbarItemView" BasedOn="{StaticResource SelectableButtonToolbarItemViewStyle}" />
    <Style TargetType="telerikMauiControls:RadioButtonToolbarItemView" BasedOn="{StaticResource SelectableButtonToolbarItemViewStyle}" />
    <Style TargetType="telerikMauiControls:ToggleButtonToolbarItemView" BasedOn="{StaticResource SelectableButtonToolbarItemViewStyle}" />

    <ControlTemplate x:Key="BusyIndicatorToolbarItemViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                                       VerticalOptions="{TemplateBinding VerticalContentOptions}">
            <telerikMauiControls:RadBusyIndicator IsBusy="{TemplateBinding IsBusy}"
                                                  Style="{TemplateBinding ActualBusyIndicatorStyle}" />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="BusyIndicatorToolbarItemViewBusyIndicatorStyle" TargetType="telerikMauiControls:RadBusyIndicator">
        <Setter Property="AnimationContentColor" Value="{DynamicResource RadPrimaryColor}" />
    </Style>

    <Style x:Key="BusyIndicatorToolbarItemViewStyle" TargetType="telerikMauiControls:BusyIndicatorToolbarItemView" BasedOn="{StaticResource ToolbarItemViewStyle}">
        <Setter Property="ContentPadding" Value="{OnPlatform Default=8, WinUI='8, 6', MacCatalyst=2}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=20}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=20}" />
    </Style>
    <Style TargetType="telerikMauiControls:BusyIndicatorToolbarItemView" BasedOn="{StaticResource BusyIndicatorToolbarItemViewStyle}" />

    <Style x:Key="BusyIndicatorToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:BusyIndicatorToolbarItemView" BasedOn="{StaticResource BusyIndicatorToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>

    <Style x:Key="BusyIndicatorToolbarItemViewStyleVertical" TargetType="telerikMauiControls:BusyIndicatorToolbarItemView" BasedOn="{StaticResource BusyIndicatorToolbarItemViewStyle}">
        <Setter Property="VerticalOptions" Value="Start" />
    </Style>

    <Style x:Key="BusyIndicatorToolbarItemViewStyleDropDown" TargetType="telerikMauiControls:BusyIndicatorToolbarItemView" BasedOn="{StaticResource BusyIndicatorToolbarItemViewStyle}">
        <Setter Property="Padding" Value="0" />
    </Style>

    <ControlTemplate x:Key="EntryToolbarItemViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <telerikMauiControls:RadEntry x:Name="PART_Entry"
                                          Text="{TemplateBinding Text}"
                                          HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                                          VerticalOptions="{TemplateBinding VerticalContentOptions}"
                                          IsEnabled="{TemplateBinding IsEnabled}"
                                          Style="{TemplateBinding ActualEntryStyle}" />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="EntryToolbarItemViewEntryStyle" TargetType="telerikMauiControls:RadEntry" BasedOn="{StaticResource RadEntryStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=14, MacCatalyst=13, iOS=17}" />
    </Style>

    <Style x:Key="EntryToolbarItemViewStyle" TargetType="telerikMauiControls:EntryToolbarItemView">
        <Setter Property="Padding" Value="{OnPlatform Default=0, WinUI=2}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default=8, WinUI=2, MacCatalyst=2, Android=0}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=20}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=20}" />
    </Style>
    <Style TargetType="telerikMauiControls:EntryToolbarItemView" BasedOn="{StaticResource EntryToolbarItemViewStyle}" />

    <Style x:Key="EntryToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:EntryToolbarItemView" BasedOn="{StaticResource EntryToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="VerticalContentOptions" Value="Center" />
    </Style>

    <Style x:Key="EntryToolbarItemViewStyleVertical" TargetType="telerikMauiControls:EntryToolbarItemView" BasedOn="{StaticResource EntryToolbarItemViewStyle}">
        <Setter Property="VerticalOptions" Value="Start" />
        <Setter Property="VerticalContentOptions" Value="Center" />
    </Style>

    <Style x:Key="EntryToolbarItemViewStyleDropDown" TargetType="telerikMauiControls:EntryToolbarItemView" BasedOn="{StaticResource EntryToolbarItemViewStyle}">
        <Setter Property="Padding" Value="0" />
    </Style>

    <ControlTemplate x:Key="SliderToolbarItemViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <telerikMauiControls:RadSlider Maximum="{TemplateBinding Maximum, Mode=TwoWay}"
                                           Minimum="{TemplateBinding Minimum, Mode=TwoWay}"
                                           Value="{TemplateBinding Value, Mode=TwoWay}"
                                           Style="{TemplateBinding SliderStyle}"
                                           IsEnabled="{TemplateBinding IsInteractionEnabled}" />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="SliderToolbarItemViewStyle" TargetType="telerikMauiControls:SliderToolbarItemView" BasedOn="{StaticResource ToolbarItemViewStyle}">
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=20}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=20}" />
    </Style>
    <Style TargetType="telerikMauiControls:SliderToolbarItemView" BasedOn="{StaticResource SliderToolbarItemViewStyle}" />

    <Style x:Key="SliderToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:SliderToolbarItemView" BasedOn="{StaticResource SliderToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>
    <Style x:Key="SliderToolbarItemViewStyleVertical" TargetType="telerikMauiControls:SliderToolbarItemView" BasedOn="{StaticResource SliderToolbarItemViewStyle}">
        <Setter Property="VerticalOptions" Value="Start" />
    </Style>
    <Style x:Key="SliderToolbarItemViewStyleDropDown" TargetType="telerikMauiControls:SliderToolbarItemView" BasedOn="{StaticResource SliderToolbarItemViewStyle}" />

    <Style x:Key="ButtonToolbarItemViewIndicatorStyle" TargetType="telerikMauiControls:ButtonToolbarItemViewIndicator" BasedOn="{StaticResource ButtonToolbarItemViewStyle}">
        <Setter Property="Padding" Value="0" />
        <Setter Property="ContentPadding" Value="0" />
        <Setter Property="BorderThickness" Value="1, 0, 0, 0" />
        <Setter Property="CornerRadius" Value="0, 4, 4, 0" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=32, MacCatalyst=20}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=32, MacCatalyst=20}" />
    </Style>

    <ControlTemplate x:Key="DropDownButtonToolbarItemViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <ContentPresenter Grid.Column="0"
                                  Margin="{TemplateBinding ContentPadding}"
                                  HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                                  VerticalOptions="{TemplateBinding VerticalContentOptions}" />
                <telerikMauiControls:DropDownButtonToolbarItemViewIndicator Grid.Column="1"
                                                                            Toolbar="{TemplateBinding Toolbar}"
                                                                            IsOpen="{TemplateBinding IsOpen}"
                                                                            Orientation="{TemplateBinding Orientation}"
                                                                            PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                            Command="{TemplateBinding ToggleCommand}"
                                                                            Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                            ControlTemplate="{TemplateBinding ActualDropDownIndicatorTemplate}"
                                                                            IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="DropDownButtonToolbarItemViewControlTemplateText">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <Label Grid.Column="0"
                       Margin="{TemplateBinding ContentPadding}"
                       Text="{TemplateBinding Text}"
                       TextColor="{TemplateBinding TextColor}"
                       FontFamily="{TemplateBinding FontFamily}"
                       FontSize="{TemplateBinding FontSize}"
                       FontAttributes="{TemplateBinding FontAttributes}"
                       TextDecorations="{TemplateBinding TextDecorations}"
                       HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                       VerticalOptions="{TemplateBinding VerticalContentOptions}"
                       HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                       VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
                <telerikMauiControls:DropDownButtonToolbarItemViewIndicator Grid.Column="1"
                                                                            Toolbar="{TemplateBinding Toolbar}"
                                                                            IsOpen="{TemplateBinding IsOpen}"
                                                                            Orientation="{TemplateBinding Orientation}"
                                                                            PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                            Command="{TemplateBinding ToggleCommand}"
                                                                            Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                            ControlTemplate="{TemplateBinding ActualDropDownIndicatorTemplate}"
                                                                            IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="DropDownButtonToolbarItemViewControlTemplateImage">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <telerikMauiToolbar:ToolbarImage Grid.Column="0"
                                                 Margin="{TemplateBinding ContentPadding}"
                                                 Source="{TemplateBinding ActualImageSource}"
                                                 Aspect="{TemplateBinding ImageAspect}"
                                                 WidthRequest="{TemplateBinding ImageWidth}"
                                                 HeightRequest="{TemplateBinding ImageHeight}"
                                                 HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                                                 VerticalOptions="{TemplateBinding VerticalContentOptions}" />
                <telerikMauiControls:DropDownButtonToolbarItemViewIndicator Grid.Column="1"
                                                                            Toolbar="{TemplateBinding Toolbar}"
                                                                            IsOpen="{TemplateBinding IsOpen}"
                                                                            Orientation="{TemplateBinding Orientation}"
                                                                            PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                            Command="{TemplateBinding ToggleCommand}"
                                                                            Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                            ControlTemplate="{TemplateBinding ActualDropDownIndicatorTemplate}"
                                                                            IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="DropDownButtonToolbarItemViewControlTemplateLeft">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <Grid Grid.Column="0"
                      ColumnDefinitions="Auto, *"
                      ColumnSpacing="{TemplateBinding ImageSpacing}"
                      Margin="{TemplateBinding ContentPadding}"
                      HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                      VerticalOptions="{TemplateBinding VerticalContentOptions}">
                    <telerikMauiToolbar:ToolbarImage Grid.Column="0"
                                                     Source="{TemplateBinding ActualImageSource}"
                                                     Aspect="{TemplateBinding ImageAspect}"
                                                     WidthRequest="{TemplateBinding ImageWidth}"
                                                     HeightRequest="{TemplateBinding ImageHeight}" />
                    <Label Grid.Column="1"
                           Text="{TemplateBinding Text}"
                           TextColor="{TemplateBinding TextColor}"
                           FontFamily="{TemplateBinding FontFamily}"
                           FontSize="{TemplateBinding FontSize}"
                           FontAttributes="{TemplateBinding FontAttributes}"
                           TextDecorations="{TemplateBinding TextDecorations}"
                           HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                           VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
                </Grid>
                <telerikMauiControls:DropDownButtonToolbarItemViewIndicator Grid.Column="1"
                                                                            Toolbar="{TemplateBinding Toolbar}"
                                                                            IsOpen="{TemplateBinding IsOpen}"
                                                                            Orientation="{TemplateBinding Orientation}"
                                                                            PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                            Command="{TemplateBinding ToggleCommand}"
                                                                            Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                            ControlTemplate="{TemplateBinding ActualDropDownIndicatorTemplate}"
                                                                            IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="DropDownButtonToolbarItemViewControlTemplateTop">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <Grid Grid.Column="0"
                      RowDefinitions="Auto, *"
                      RowSpacing="{TemplateBinding ImageSpacing}"
                      Margin="{TemplateBinding ContentPadding}"
                      HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                      VerticalOptions="{TemplateBinding VerticalContentOptions}">
                    <telerikMauiToolbar:ToolbarImage Grid.Row="0"
                                                     Source="{TemplateBinding ActualImageSource}"
                                                     Aspect="{TemplateBinding ImageAspect}"
                                                     WidthRequest="{TemplateBinding ImageWidth}"
                                                     HeightRequest="{TemplateBinding ImageHeight}" />
                    <Label Grid.Row="1"
                           Text="{TemplateBinding Text}"
                           TextColor="{TemplateBinding TextColor}"
                           FontFamily="{TemplateBinding FontFamily}"
                           FontSize="{TemplateBinding FontSize}"
                           FontAttributes="{TemplateBinding FontAttributes}"
                           TextDecorations="{TemplateBinding TextDecorations}"
                           HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                           VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
                </Grid>
                <telerikMauiControls:DropDownButtonToolbarItemViewIndicator Grid.Column="1"
                                                                            Toolbar="{TemplateBinding Toolbar}"
                                                                            IsOpen="{TemplateBinding IsOpen}"
                                                                            Orientation="{TemplateBinding Orientation}"
                                                                            PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                            Command="{TemplateBinding ToggleCommand}"
                                                                            Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                            ControlTemplate="{TemplateBinding ActualDropDownIndicatorTemplate}"
                                                                            IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="DropDownButtonToolbarItemViewControlTemplateRight">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <Grid Grid.Column="0"
                      ColumnDefinitions="*, Auto"
                      ColumnSpacing="{TemplateBinding ImageSpacing}"
                      Margin="{TemplateBinding ContentPadding}"
                      HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                      VerticalOptions="{TemplateBinding VerticalContentOptions}">
                    <Label Grid.Column="0"
                           Text="{TemplateBinding Text}"
                           TextColor="{TemplateBinding TextColor}"
                           FontFamily="{TemplateBinding FontFamily}"
                           FontSize="{TemplateBinding FontSize}"
                           FontAttributes="{TemplateBinding FontAttributes}"
                           TextDecorations="{TemplateBinding TextDecorations}"
                           HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                           VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
                    <telerikMauiToolbar:ToolbarImage Grid.Column="1"
                                                     Source="{TemplateBinding ActualImageSource}"
                                                     Aspect="{TemplateBinding ImageAspect}"
                                                     WidthRequest="{TemplateBinding ImageWidth}"
                                                     HeightRequest="{TemplateBinding ImageHeight}" />
                </Grid>
                <telerikMauiControls:DropDownButtonToolbarItemViewIndicator Grid.Column="1"
                                                                            Toolbar="{TemplateBinding Toolbar}"
                                                                            IsOpen="{TemplateBinding IsOpen}"
                                                                            Orientation="{TemplateBinding Orientation}"
                                                                            PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                            Command="{TemplateBinding ToggleCommand}"
                                                                            Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                            ControlTemplate="{TemplateBinding ActualDropDownIndicatorTemplate}"
                                                                            IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="DropDownButtonToolbarItemViewControlTemplateBottom">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <Grid Grid.Column="0"
                      RowDefinitions="*, Auto"
                      RowSpacing="{TemplateBinding ImageSpacing}"
                      Margin="{TemplateBinding ContentPadding}"
                      HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                      VerticalOptions="{TemplateBinding VerticalContentOptions}">
                    <Label Grid.Row="0"
                           Text="{TemplateBinding Text}"
                           TextColor="{TemplateBinding TextColor}"
                           FontFamily="{TemplateBinding FontFamily}"
                           FontSize="{TemplateBinding FontSize}"
                           FontAttributes="{TemplateBinding FontAttributes}"
                           TextDecorations="{TemplateBinding TextDecorations}"
                           HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                           VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
                    <telerikMauiToolbar:ToolbarImage Grid.Row="1"
                                                     Source="{TemplateBinding ActualImageSource}"
                                                     Aspect="{TemplateBinding ImageAspect}"
                                                     WidthRequest="{TemplateBinding ImageWidth}"
                                                     HeightRequest="{TemplateBinding ImageHeight}" />
                </Grid>
                <telerikMauiControls:DropDownButtonToolbarItemViewIndicator Grid.Column="1"
                                                                            Toolbar="{TemplateBinding Toolbar}"
                                                                            IsOpen="{TemplateBinding IsOpen}"
                                                                            Orientation="{TemplateBinding Orientation}"
                                                                            PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                            Command="{TemplateBinding ToggleCommand}"
                                                                            Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                            ControlTemplate="{TemplateBinding ActualDropDownIndicatorTemplate}"
                                                                            IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="DropDownButtonToolbarItemViewStyle" TargetType="telerikMauiControls:DropDownButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style TargetType="telerikMauiControls:DropDownButtonToolbarItemView" BasedOn="{StaticResource DropDownButtonToolbarItemViewStyle}" />

    <FontImageSource x:Key="DropDownButtonToolbarItemViewIndicatorImageSourceHorizontal"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconDownArrow}"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Size="{OnPlatform Default=12, MacCatalyst=10}" />
    <FontImageSource x:Key="DropDownButtonToolbarItemViewIndicatorImageSourceVertical"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconRightArrow}"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Size="{OnPlatform Default=12, MacCatalyst=10}" />
    <FontImageSource x:Key="DropDownButtonToolbarItemViewIndicatorImageSourceDropDown"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconRightArrow}"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Size="{OnPlatform Default=12, MacCatalyst=10}" />

    <Style x:Key="DropDownButtonToolbarItemViewIndicatorStyle" TargetType="telerikMauiControls:DropDownButtonToolbarItemViewIndicator" BasedOn="{StaticResource ButtonToolbarItemViewIndicatorStyle}" />

    <Style x:Key="DropDownButtonToolbarItemViewIndicatorStyleHorizontal" TargetType="telerikMauiControls:DropDownButtonToolbarItemViewIndicator" BasedOn="{StaticResource DropDownButtonToolbarItemViewIndicatorStyle}">
        <Setter Property="ImageSource" Value="{StaticResource DropDownButtonToolbarItemViewIndicatorImageSourceHorizontal}" />
    </Style>

    <Style x:Key="DropDownButtonToolbarItemViewIndicatorStyleVertical" TargetType="telerikMauiControls:DropDownButtonToolbarItemViewIndicator" BasedOn="{StaticResource DropDownButtonToolbarItemViewIndicatorStyle}">
        <Setter Property="ImageSource" Value="{StaticResource DropDownButtonToolbarItemViewIndicatorImageSourceVertical}" />
    </Style>

    <Style x:Key="DropDownButtonToolbarItemViewIndicatorStyleDropDown" TargetType="telerikMauiControls:DropDownButtonToolbarItemViewIndicator" BasedOn="{StaticResource DropDownButtonToolbarItemViewIndicatorStyle}">
        <Setter Property="ImageSource" Value="{StaticResource DropDownButtonToolbarItemViewIndicatorImageSourceDropDown}" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>
    <Style TargetType="telerikMauiControls:DropDownButtonToolbarItemViewIndicator" BasedOn="{StaticResource DropDownButtonToolbarItemViewIndicatorStyle}" />

    <ControlTemplate x:Key="DropDownButtonToolbarItemViewContentControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <ScrollView Padding="{TemplateBinding ContentPadding}">
                <ContentPresenter />
            </ScrollView>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="DropDownButtonToolbarItemViewContentStyle" TargetType="telerikMauiControls:DropDownButtonToolbarItemViewContent">
        <Setter Property="BackgroundColor" Value="{DynamicResource InputDropDownBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, WinUI=8, MacCatalyst=10, iOS=12}" />
    </Style>
    <Style TargetType="telerikMauiControls:DropDownButtonToolbarItemViewContent" BasedOn="{StaticResource DropDownButtonToolbarItemViewContentStyle}" />

    <ControlTemplate x:Key="DropDownListButtonToolbarItemViewContentControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <ScrollView Padding="{TemplateBinding ContentPadding}">
                <ContentPresenter />
            </ScrollView>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="DropDownListButtonToolbarItemViewContentStyle" TargetType="telerikMauiControls:DropDownListButtonToolbarItemViewContent" BasedOn="{StaticResource DropDownButtonToolbarItemViewContentStyle}">
        <Setter Property="ContentPadding" Value="{OnPlatform Default=0, MacCatalyst='5, 10'}" />
        <Setter Property="ItemSpacing" Value="0" />
        <Setter Property="LineSpacing" Value="0" />
    </Style>
    <Style TargetType="telerikMauiControls:DropDownListButtonToolbarItemViewContent" BasedOn="{StaticResource DropDownListButtonToolbarItemViewContentStyle}" />

    <Style x:Key="ListPickerButtonToolbarItemViewStyle" TargetType="telerikMauiControls:ListPickerButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewStyle}" />

    <Style x:Key="ListPickerButtonToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:ListPickerButtonToolbarItemView" BasedOn="{StaticResource ListPickerButtonToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="DropDownPlacement" Value="Bottom" />
        <Setter Property="DropDownHorizontalOffset" Value="{OnPlatform Default=0, WinUI=2, iOS=3, Android=8}" />
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform iOS=2, Android=3, WinUI=2, MacCatalyst=9}" />
    </Style>

    <Style x:Key="ListPickerButtonToolbarItemViewStyleVertical" TargetType="telerikMauiControls:ListPickerButtonToolbarItemView" BasedOn="{StaticResource ListPickerButtonToolbarItemViewStyle}">
        <Setter Property="VerticalOptions" Value="Start" />
        <Setter Property="DropDownPlacement" Value="Right" />
        <Setter Property="DropDownHorizontalOffset" Value="{OnPlatform iOS=2, Android=3, WinUI=2, MacCatalyst=9}" />
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform Default=0, WinUI=2, iOS=3, Android=8}" />
    </Style>

    <Style x:Key="ListPickerButtonToolbarItemViewStyleDropDown" TargetType="telerikMauiControls:ListPickerButtonToolbarItemView" BasedOn="{StaticResource ListPickerButtonToolbarItemViewStyle}">
        <Setter Property="Padding" Value="0" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default='16, 10', MacCatalyst='6, 4'}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="DisplayOptions" Value="Image, Text" />
        <Setter Property="DropDownPlacement" Value="Right" />
        <Setter Property="DropDownHorizontalOffset" Value="{OnPlatform iOS=2, Android=3, WinUI=2, MacCatalyst=9}" />
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform Default=0, WinUI=2, iOS=3, Android=8}" />
    </Style>

    <Style TargetType="telerikMauiControls:ListPickerButtonToolbarItemView" BasedOn="{StaticResource ListPickerButtonToolbarItemViewStyle}" />
    <Style TargetType="telerikMauiControls:ListPickerButtonToolbarItemViewContent" BasedOn="{StaticResource DropDownButtonToolbarItemViewContentStyle}" />

    <ControlTemplate x:Key="SplitButtonToolbarItemViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <ContentPresenter Grid.Column="0"
                                  Margin="{TemplateBinding ContentPadding}"
                                  HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                                  VerticalOptions="{TemplateBinding VerticalContentOptions}" />
                <telerikMauiControls:SplitButtonToolbarItemViewIndicator Grid.Column="1"
                                                                         Toolbar="{TemplateBinding Toolbar}"
                                                                         IsOpen="{TemplateBinding IsOpen}"
                                                                         Orientation="{TemplateBinding Orientation}"
                                                                         PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                         Command="{TemplateBinding ToggleCommand}"
                                                                         Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                         ControlTemplate="{TemplateBinding DropDownIndicatorTemplate}"
                                                                         IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="SplitButtonToolbarItemViewControlTemplateText">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <Label Grid.Column="0"
                       Margin="{TemplateBinding ContentPadding}"
                       Text="{TemplateBinding Text}"
                       TextColor="{TemplateBinding TextColor}"
                       FontFamily="{TemplateBinding FontFamily}"
                       FontSize="{TemplateBinding FontSize}"
                       FontAttributes="{TemplateBinding FontAttributes}"
                       TextDecorations="{TemplateBinding TextDecorations}"
                       HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                       VerticalOptions="{TemplateBinding VerticalContentOptions}"
                       HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                       VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
                <telerikMauiControls:SplitButtonToolbarItemViewIndicator Grid.Column="1"
                                                                         Toolbar="{TemplateBinding Toolbar}"
                                                                         IsOpen="{TemplateBinding IsOpen}"
                                                                         Orientation="{TemplateBinding Orientation}"
                                                                         PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                         Command="{TemplateBinding ToggleCommand}"
                                                                         Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                         ControlTemplate="{TemplateBinding DropDownIndicatorTemplate}"
                                                                         IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="SplitButtonToolbarItemViewControlTemplateImage">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <telerikMauiToolbar:ToolbarImage Grid.Column="0"
                                                 Margin="{TemplateBinding ContentPadding}"
                                                 Source="{TemplateBinding ActualImageSource}"
                                                 Aspect="{TemplateBinding ImageAspect}"
                                                 WidthRequest="{TemplateBinding ImageWidth}"
                                                 HeightRequest="{TemplateBinding ImageHeight}"
                                                 HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                                                 VerticalOptions="{TemplateBinding VerticalContentOptions}" />
                <telerikMauiControls:SplitButtonToolbarItemViewIndicator Grid.Column="1"
                                                                         Toolbar="{TemplateBinding Toolbar}"
                                                                         IsOpen="{TemplateBinding IsOpen}"
                                                                         Orientation="{TemplateBinding Orientation}"
                                                                         PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                         Command="{TemplateBinding ToggleCommand}"
                                                                         Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                         ControlTemplate="{TemplateBinding DropDownIndicatorTemplate}"
                                                                         IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="SplitButtonToolbarItemViewControlTemplateLeft">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <Grid Grid.Column="0"
                      ColumnDefinitions="Auto, *"
                      ColumnSpacing="{TemplateBinding ImageSpacing}"
                      Margin="{TemplateBinding ContentPadding}"
                      HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                      VerticalOptions="{TemplateBinding VerticalContentOptions}">
                    <telerikMauiToolbar:ToolbarImage Grid.Column="0"
                                                     Source="{TemplateBinding ActualImageSource}"
                                                     Aspect="{TemplateBinding ImageAspect}"
                                                     WidthRequest="{TemplateBinding ImageWidth}"
                                                     HeightRequest="{TemplateBinding ImageHeight}" />
                    <Label Grid.Column="1"
                           Text="{TemplateBinding Text}"
                           TextColor="{TemplateBinding TextColor}"
                           FontFamily="{TemplateBinding FontFamily}"
                           FontSize="{TemplateBinding FontSize}"
                           FontAttributes="{TemplateBinding FontAttributes}"
                           TextDecorations="{TemplateBinding TextDecorations}"
                           HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                           VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
                </Grid>
                <telerikMauiControls:SplitButtonToolbarItemViewIndicator Grid.Column="1"
                                                                         Toolbar="{TemplateBinding Toolbar}"
                                                                         IsOpen="{TemplateBinding IsOpen}"
                                                                         Orientation="{TemplateBinding Orientation}"
                                                                         PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                         Command="{TemplateBinding ToggleCommand}"
                                                                         Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                         ControlTemplate="{TemplateBinding DropDownIndicatorTemplate}"
                                                                         IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="SplitButtonToolbarItemViewControlTemplateTop">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <Grid Grid.Column="0"
                      RowDefinitions="Auto, *"
                      RowSpacing="{TemplateBinding ImageSpacing}"
                      Margin="{TemplateBinding ContentPadding}"
                      HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                      VerticalOptions="{TemplateBinding VerticalContentOptions}">
                    <telerikMauiToolbar:ToolbarImage Grid.Row="0"
                                                     Source="{TemplateBinding ActualImageSource}"
                                                     Aspect="{TemplateBinding ImageAspect}"
                                                     WidthRequest="{TemplateBinding ImageWidth}"
                                                     HeightRequest="{TemplateBinding ImageHeight}" />
                    <Label Grid.Row="1"
                           Text="{TemplateBinding Text}"
                           TextColor="{TemplateBinding TextColor}"
                           FontFamily="{TemplateBinding FontFamily}"
                           FontSize="{TemplateBinding FontSize}"
                           FontAttributes="{TemplateBinding FontAttributes}"
                           TextDecorations="{TemplateBinding TextDecorations}"
                           HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                           VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
                </Grid>
                <telerikMauiControls:SplitButtonToolbarItemViewIndicator Grid.Column="1"
                                                                         Toolbar="{TemplateBinding Toolbar}"
                                                                         IsOpen="{TemplateBinding IsOpen}"
                                                                         Orientation="{TemplateBinding Orientation}"
                                                                         PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                         Command="{TemplateBinding ToggleCommand}"
                                                                         Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                         ControlTemplate="{TemplateBinding DropDownIndicatorTemplate}"
                                                                         IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="SplitButtonToolbarItemViewControlTemplateRight">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <Grid Grid.Column="0"
                      ColumnDefinitions="*, Auto"
                      ColumnSpacing="{TemplateBinding ImageSpacing}"
                      Margin="{TemplateBinding ContentPadding}"
                      HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                      VerticalOptions="{TemplateBinding VerticalContentOptions}">
                    <Label Grid.Column="0"
                           Text="{TemplateBinding Text}"
                           TextColor="{TemplateBinding TextColor}"
                           FontFamily="{TemplateBinding FontFamily}"
                           FontSize="{TemplateBinding FontSize}"
                           FontAttributes="{TemplateBinding FontAttributes}"
                           TextDecorations="{TemplateBinding TextDecorations}"
                           HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                           VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
                    <telerikMauiToolbar:ToolbarImage Grid.Column="1"
                                                     Source="{TemplateBinding ActualImageSource}"
                                                     Aspect="{TemplateBinding ImageAspect}"
                                                     WidthRequest="{TemplateBinding ImageWidth}"
                                                     HeightRequest="{TemplateBinding ImageHeight}" />
                </Grid>
                <telerikMauiControls:SplitButtonToolbarItemViewIndicator Grid.Column="1"
                                                                         Toolbar="{TemplateBinding Toolbar}"
                                                                         IsOpen="{TemplateBinding IsOpen}"
                                                                         Orientation="{TemplateBinding Orientation}"
                                                                         PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                         Command="{TemplateBinding ToggleCommand}"
                                                                         Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                         ControlTemplate="{TemplateBinding DropDownIndicatorTemplate}"
                                                                         IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="SplitButtonToolbarItemViewControlTemplateBottom">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="*, Auto"
                  ColumnSpacing="{TemplateBinding DropDownIndicatorSpacing}">
                <Grid Grid.Column="0"
                      RowDefinitions="*, Auto"
                      RowSpacing="{TemplateBinding ImageSpacing}"
                      Margin="{TemplateBinding ContentPadding}"
                      HorizontalOptions="{TemplateBinding HorizontalContentOptions}"
                      VerticalOptions="{TemplateBinding VerticalContentOptions}">
                    <Label Grid.Row="0"
                           Text="{TemplateBinding Text}"
                           TextColor="{TemplateBinding TextColor}"
                           FontFamily="{TemplateBinding FontFamily}"
                           FontSize="{TemplateBinding FontSize}"
                           FontAttributes="{TemplateBinding FontAttributes}"
                           TextDecorations="{TemplateBinding TextDecorations}"
                           HorizontalTextAlignment="{TemplateBinding HorizontalTextAlignment}"
                           VerticalTextAlignment="{TemplateBinding VerticalTextAlignment}" />
                    <telerikMauiToolbar:ToolbarImage Grid.Row="1"
                                                     Source="{TemplateBinding ActualImageSource}"
                                                     Aspect="{TemplateBinding ImageAspect}"
                                                     WidthRequest="{TemplateBinding ImageWidth}"
                                                     HeightRequest="{TemplateBinding ImageHeight}" />
                </Grid>
                <telerikMauiControls:SplitButtonToolbarItemViewIndicator Grid.Column="1"
                                                                         Toolbar="{TemplateBinding Toolbar}"
                                                                         IsOpen="{TemplateBinding IsOpen}"
                                                                         Orientation="{TemplateBinding Orientation}"
                                                                         PlacementLocation="{TemplateBinding PlacementLocation}"
                                                                         Command="{TemplateBinding ToggleCommand}"
                                                                         Style="{TemplateBinding ActualDropDownIndicatorStyle}"
                                                                         ControlTemplate="{TemplateBinding DropDownIndicatorTemplate}"
                                                                         IsVisible="{TemplateBinding IsDropDownIndicatorVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="SplitButtonToolbarItemViewStyle" TargetType="telerikMauiControls:SplitButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}">
        <Setter Property="BackgroundColor" Value="{OnPlatform Default='Transparent', WinUI={DynamicResource RadSurfaceAltColorDarken2}}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default='8, 0', MacCatalyst='10, 0'}" />
        <Setter Property="DropDownContentStyle" Value="{StaticResource DropDownButtonToolbarItemViewContentStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadToolbarButtonToolbarItemViewPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SplitButtonToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:SplitButtonToolbarItemView" BasedOn="{StaticResource SplitButtonToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="DropDownPlacement" Value="Bottom" />
        <Setter Property="DropDownHorizontalOffset" Value="{OnPlatform Default=0, WinUI=2, iOS=3, Android=8}" />
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform iOS=2, Android=3, WinUI=2, MacCatalyst=9}" />
    </Style>

    <Style x:Key="SplitButtonToolbarItemViewStyleVertical" TargetType="telerikMauiControls:SplitButtonToolbarItemView" BasedOn="{StaticResource SplitButtonToolbarItemViewStyle}">
        <Setter Property="VerticalOptions" Value="Start" />
        <Setter Property="DropDownPlacement" Value="Right" />
        <Setter Property="DropDownHorizontalOffset" Value="{OnPlatform iOS=2, Android=3, WinUI=2, MacCatalyst=9}" />
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform Default=0, WinUI=2, iOS=3, Android=8}" />
    </Style>

    <Style x:Key="SplitButtonToolbarItemViewStyleDropDown" TargetType="telerikMauiControls:SplitButtonToolbarItemView" BasedOn="{StaticResource SplitButtonToolbarItemViewStyle}">
        <Setter Property="DisplayOptions" Value="Image, Text" />
        <Setter Property="DropDownPlacement" Value="Right" />
        <Setter Property="DropDownHorizontalOffset" Value="{OnPlatform iOS=2, Android=3, WinUI=2, MacCatalyst=9}" />
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform Default=0, WinUI=2, iOS=3, Android=8}" />
    </Style>

    <Style TargetType="telerikMauiControls:SplitButtonToolbarItemView" BasedOn="{StaticResource SplitButtonToolbarItemViewStyle}" />

    <FontImageSource x:Key="SplitButtonToolbarItemViewIndicatorImageSourceHorizontal"
                    Glyph="{x:Static telerikMaui:TelerikFont.IconDownArrow}"
                    FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                    Size="{OnPlatform Default=12, MacCatalyst=10}" />
    <FontImageSource x:Key="SplitButtonToolbarItemViewIndicatorImageSourceVertical"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconRightArrow}"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Size="{OnPlatform Default=12, MacCatalyst=10}" />
    <FontImageSource x:Key="SplitButtonToolbarItemViewIndicatorImageSourceSplit"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconRightArrow}"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Size="{OnPlatform Default=12, MacCatalyst=10}" />

    <Style x:Key="SplitButtonToolbarItemViewIndicatorStyle" TargetType="telerikMauiControls:SplitButtonToolbarItemViewIndicator" BasedOn="{StaticResource DropDownButtonToolbarItemViewIndicatorStyle}">
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=32, MacCatalyst=20}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=32, MacCatalyst=20}" />
    </Style>
    <Style TargetType="telerikMauiControls:SplitButtonToolbarItemViewIndicator" BasedOn="{StaticResource SplitButtonToolbarItemViewIndicatorStyle}" />

    <Style x:Key="SplitButtonToolbarItemViewIndicatorStyleHorizontal" TargetType="telerikMauiControls:SplitButtonToolbarItemViewIndicator" BasedOn="{StaticResource SplitButtonToolbarItemViewIndicatorStyle}">
        <Setter Property="ImageSource" Value="{StaticResource SplitButtonToolbarItemViewIndicatorImageSourceHorizontal}" />
    </Style>

    <Style x:Key="SplitButtonToolbarItemViewIndicatorStyleVertical" TargetType="telerikMauiControls:SplitButtonToolbarItemViewIndicator" BasedOn="{StaticResource SplitButtonToolbarItemViewIndicatorStyle}">
        <Setter Property="ImageSource" Value="{StaticResource SplitButtonToolbarItemViewIndicatorImageSourceVertical}" />
    </Style>

    <Style x:Key="SplitButtonToolbarItemViewIndicatorStyleDropDown" TargetType="telerikMauiControls:SplitButtonToolbarItemViewIndicator" BasedOn="{StaticResource SplitButtonToolbarItemViewIndicatorStyle}">
        <Setter Property="ImageSource" Value="{StaticResource SplitButtonToolbarItemViewIndicatorImageSourceSplit}" />
    </Style>

    <ControlTemplate x:Key="RadToolbarOptionsPanelControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       IsVisible="{TemplateBinding Content, Converter={StaticResource InvertedNullToBooleanConverter}}">
            <ScrollView Padding="{TemplateBinding ContentPadding}">
                <ContentPresenter />
            </ScrollView>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="RadToolbarOptionsPanelStyle" TargetType="telerikMauiControls:RadToolbarOptionsPanel">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadToolbarBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>
    <Style TargetType="telerikMauiControls:RadToolbarOptionsPanel" BasedOn="{StaticResource RadToolbarOptionsPanelStyle}" />

    <FontImageSource x:Key="OverflowMenuButtonToolbarItemViewImageSource"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconDotsVert}"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Size="16" />

    <Style x:Key="OverflowMenuButtonToolbarItemViewContentStyle" TargetType="telerikMauiControls:DropDownMenuButtonToolbarItemViewContent" BasedOn="{StaticResource DropDownButtonToolbarItemViewContentStyle}">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
    </Style>

    <Style x:Key="OverflowMenuButtonToolbarItemViewStyle" TargetType="telerikMauiControls:OverflowMenuButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewStyle}">
        <Setter Property="DropDownContentStyle" Value="{StaticResource OverflowMenuButtonToolbarItemViewContentStyle}" />
        <Setter Property="ImageSource" Value="{StaticResource OverflowMenuButtonToolbarItemViewImageSource}" />
        <Setter Property="IsDropDownIndicatorVisible" Value="False" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadToolbarButtonToolbarItemViewPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Opened">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:OverflowMenuButtonToolbarItemView" BasedOn="{StaticResource OverflowMenuButtonToolbarItemViewStyle}" />

    <Style x:Key="OverflowMenuButtonToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:OverflowMenuButtonToolbarItemView" BasedOn="{StaticResource OverflowMenuButtonToolbarItemViewStyle}">
        <Setter Property="DropDownPlacement" Value="Bottom" />
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform iOS=2, Android=3, WinUI=2, MacCatalyst=9}" />
    </Style>

    <Style x:Key="OverflowMenuButtonToolbarItemViewStyleVertical" TargetType="telerikMauiControls:OverflowMenuButtonToolbarItemView" BasedOn="{StaticResource OverflowMenuButtonToolbarItemViewStyle}">
        <Setter Property="DropDownPlacement" Value="Right" />
        <Setter Property="DropDownHorizontalOffset" Value="{OnPlatform iOS=2, Android=3, WinUI=2, MacCatalyst=9}" />
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform Default=0, WinUI=2, iOS=3, Android=8}" />
    </Style>

    <FontImageSource x:Key="ScrollForwardButtonToolbarItemViewImageSourceHorizontal"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconRightArrow}"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Size="16" />
    <FontImageSource x:Key="ScrollForwardButtonToolbarItemViewImageSourceVertical"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconDownArrow}"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Size="16" />
    <FontImageSource x:Key="ScrollBackwardButtonToolbarItemViewImageSourceHorizontal"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconLeftArrow}"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Size="16" />
    <FontImageSource x:Key="ScrollBackwardButtonToolbarItemViewImageSourceVertical"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconUpArrow}"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Size="16" />
    <FontImageSource x:Key="BackNavigationButtonToolbarItemViewImageSource"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconBack}"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Size="16" />

    <Style x:Key="BackNavigationButtonToolbarItemViewStyle" TargetType="telerikMauiControls:BackNavigationButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}">
        <Setter Property="ImageSource" Value="{StaticResource BackNavigationButtonToolbarItemViewImageSource}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Selected" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikMauiControls:BackNavigationButtonToolbarItemView" BasedOn="{StaticResource BackNavigationButtonToolbarItemViewStyle}" />

    <Style x:Key="ScrollForwardButtonToolbarItemViewStyle" TargetType="telerikMauiControls:ScrollForwardButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style TargetType="telerikMauiControls:ScrollForwardButtonToolbarItemView" BasedOn="{StaticResource ScrollForwardButtonToolbarItemViewStyle}" />

    <Style x:Key="ScrollForwardButtonToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:ScrollForwardButtonToolbarItemView" BasedOn="{StaticResource ScrollForwardButtonToolbarItemViewStyle}">
        <Setter Property="ImageSource" Value="{StaticResource ScrollForwardButtonToolbarItemViewImageSourceHorizontal}" />
    </Style>
    <Style x:Key="ScrollForwardButtonToolbarItemViewStyleVertical" TargetType="telerikMauiControls:ScrollForwardButtonToolbarItemView" BasedOn="{StaticResource ScrollForwardButtonToolbarItemViewStyle}">
        <Setter Property="ImageSource" Value="{StaticResource ScrollForwardButtonToolbarItemViewImageSourceVertical}" />
    </Style>

    <Style x:Key="ScrollBackwardButtonToolbarItemViewStyle" TargetType="telerikMauiControls:ScrollBackwardButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style TargetType="telerikMauiControls:ScrollBackwardButtonToolbarItemView" BasedOn="{StaticResource ScrollBackwardButtonToolbarItemViewStyle}" />

    <Style x:Key="ScrollBackwardButtonToolbarItemViewStyleHorizontal" TargetType="telerikMauiControls:ScrollBackwardButtonToolbarItemView" BasedOn="{StaticResource ScrollBackwardButtonToolbarItemViewStyle}">
        <Setter Property="ImageSource" Value="{StaticResource ScrollBackwardButtonToolbarItemViewImageSourceHorizontal}" />
    </Style>
    <Style x:Key="ScrollBackwardButtonToolbarItemViewStyleVertical" TargetType="telerikMauiControls:ScrollBackwardButtonToolbarItemView" BasedOn="{StaticResource ScrollBackwardButtonToolbarItemViewStyle}">
        <Setter Property="ImageSource" Value="{StaticResource ScrollBackwardButtonToolbarItemViewImageSourceVertical}" />
    </Style>

    <ControlTemplate x:Key="RadToolbarControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       Content="{TemplateBinding Content}" />
    </ControlTemplate>

    <ControlTemplate x:Key="RadToolbarControlTemplateHorizontal">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <Grid ColumnDefinitions="Auto, *, Auto">
                <HorizontalStackLayout Grid.Column="0">
                    <telerikMauiControls:BackNavigationButtonToolbarItemView Toolbar="{TemplateBinding}"
                                                                             Orientation="{TemplateBinding Orientation}"
                                                                             Style="{TemplateBinding BackNavigationButtonStyle}"
                                                                             ControlTemplate="{TemplateBinding BackNavigationButtonTemplate}"
                                                                             Command="{TemplateBinding NavigateBackCommand}"
                                                                             IsVisible="{TemplateBinding IsBackNavigationButtonVisible}" />
                    <BoxView WidthRequest="{TemplateBinding ItemSpacing}"
                             HeightRequest="1"
                             IsVisible="{TemplateBinding IsBackNavigationButtonVisible}"
                             Style="{StaticResource ToolbarSpacingBoxViewStyle}" />
                    <telerikMauiControls:ScrollBackwardButtonToolbarItemView Toolbar="{TemplateBinding}"
                                                                             Orientation="{TemplateBinding Orientation}"
                                                                             Style="{TemplateBinding ScrollBackwardButtonStyle}"
                                                                             ControlTemplate="{TemplateBinding ScrollBackwardButtonTemplate}"
                                                                             Command="{TemplateBinding ScrollBackwardCommand}"
                                                                             IsVisible="{TemplateBinding AreScrollButtonsVisible}" />
                    <BoxView WidthRequest="{TemplateBinding ItemSpacing}"
                             HeightRequest="1"
                             IsVisible="{TemplateBinding AreScrollButtonsVisible}"
                             Style="{StaticResource ToolbarSpacingBoxViewStyle}" />
                </HorizontalStackLayout>
                <ContentPresenter Grid.Column="1" />
                <HorizontalStackLayout Grid.Column="2">
                    <BoxView WidthRequest="{TemplateBinding ItemSpacing}"
                             HeightRequest="1"
                             IsVisible="{TemplateBinding AreScrollButtonsVisible}"
                             Style="{StaticResource ToolbarSpacingBoxViewStyle}" />
                    <telerikMauiControls:ScrollForwardButtonToolbarItemView Toolbar="{TemplateBinding}"
                                                                            Orientation="{TemplateBinding Orientation}"
                                                                            Style="{TemplateBinding ScrollForwardButtonStyle}"
                                                                            ControlTemplate="{TemplateBinding ScrollForwardButtonTemplate}"
                                                                            Command="{TemplateBinding ScrollForwardCommand}"
                                                                            IsVisible="{TemplateBinding AreScrollButtonsVisible}" />
                    <BoxView WidthRequest="{TemplateBinding ItemSpacing}"
                             HeightRequest="1"
                             IsVisible="{TemplateBinding IsOverflowMenuButtonVisible}"
                             Style="{StaticResource ToolbarSpacingBoxViewStyle}" />
                    <telerikMauiControls:OverflowMenuButtonToolbarItemView Toolbar="{TemplateBinding}"
                                                                           Orientation="{TemplateBinding Orientation}"
                                                                           Style="{TemplateBinding OverflowMenuButtonStyle}"
                                                                           ControlTemplate="{TemplateBinding OverflowMenuButtonTemplate}"
                                                                           ItemsSource="{TemplateBinding OverflowItems}"
                                                                           IsVisible="{TemplateBinding IsOverflowMenuButtonVisible}" />
                </HorizontalStackLayout>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="RadToolbarControlTemplateVertical">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <Grid RowDefinitions="Auto, *, Auto">
                <VerticalStackLayout Grid.Row="0">
                    <telerikMauiControls:BackNavigationButtonToolbarItemView Toolbar="{TemplateBinding}"
                                                                             Orientation="{TemplateBinding Orientation}"
                                                                             Style="{TemplateBinding BackNavigationButtonStyle}"
                                                                             ControlTemplate="{TemplateBinding BackNavigationButtonTemplate}"
                                                                             Command="{TemplateBinding NavigateBackCommand}"
                                                                             IsVisible="{TemplateBinding IsBackNavigationButtonVisible}" />
                    <BoxView WidthRequest="1"
                             HeightRequest="{TemplateBinding ItemSpacing}"
                             IsVisible="{TemplateBinding IsBackNavigationButtonVisible}"
                             Style="{StaticResource ToolbarSpacingBoxViewStyle}" />
                    <telerikMauiControls:ScrollBackwardButtonToolbarItemView Toolbar="{TemplateBinding}"
                                                                             Orientation="{TemplateBinding Orientation}"
                                                                             Style="{TemplateBinding ScrollBackwardButtonStyle}"
                                                                             ControlTemplate="{TemplateBinding ScrollBackwardButtonTemplate}"
                                                                             Command="{TemplateBinding ScrollBackwardCommand}"
                                                                             IsVisible="{TemplateBinding AreScrollButtonsVisible}" />
                    <BoxView WidthRequest="1"
                             HeightRequest="{TemplateBinding ItemSpacing}"
                             IsVisible="{TemplateBinding AreScrollButtonsVisible}"
                             Style="{StaticResource ToolbarSpacingBoxViewStyle}" />
                </VerticalStackLayout>
                <ContentPresenter Grid.Row="1" />
                <VerticalStackLayout Grid.Row="2">
                    <BoxView WidthRequest="1"
                             HeightRequest="{TemplateBinding ItemSpacing}"
                             IsVisible="{TemplateBinding AreScrollButtonsVisible}"
                             Style="{StaticResource ToolbarSpacingBoxViewStyle}" />
                    <telerikMauiControls:ScrollForwardButtonToolbarItemView Toolbar="{TemplateBinding}"
                                                                            Orientation="{TemplateBinding Orientation}"
                                                                            Style="{TemplateBinding ScrollForwardButtonStyle}"
                                                                            ControlTemplate="{TemplateBinding ScrollForwardButtonTemplate}"
                                                                            Command="{TemplateBinding ScrollForwardCommand}"
                                                                            IsVisible="{TemplateBinding AreScrollButtonsVisible}" />
                    <BoxView WidthRequest="1"
                             HeightRequest="{TemplateBinding ItemSpacing}"
                             IsVisible="{TemplateBinding IsOverflowMenuButtonVisible}"
                             Style="{StaticResource ToolbarSpacingBoxViewStyle}" />
                    <telerikMauiControls:OverflowMenuButtonToolbarItemView Toolbar="{TemplateBinding}"
                                                                           Orientation="{TemplateBinding Orientation}"
                                                                           Style="{TemplateBinding OverflowMenuButtonStyle}"
                                                                           ControlTemplate="{TemplateBinding OverflowMenuButtonTemplate}"
                                                                           ItemsSource="{TemplateBinding OverflowItems}"
                                                                           IsVisible="{TemplateBinding IsOverflowMenuButtonVisible}" />
                </VerticalStackLayout>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="RadToolbarStyle" TargetType="telerikMauiControls:RadToolbar">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadToolbarBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ContentPadding" Value="{OnPlatform WinUI='8, 2', MacCatalyst='12, 9', iOS='16, 3', Android='16, 4'}" />
        <Setter Property="ItemSpacing" Value="{OnPlatform Default=8, WinUI=0}" />
        <Setter Property="LineSpacing" Value="{OnPlatform Default=8, WinUI=4}" />
    </Style>

    <Style x:Key="RadToolbarStyleHorizontal" TargetType="telerikMauiControls:RadToolbar" BasedOn="{StaticResource RadToolbarStyle}" />

    <Style x:Key="RadToolbarStyleVertical" TargetType="telerikMauiControls:RadToolbar" BasedOn="{StaticResource RadToolbarStyle}">
        <Setter Property="ContentPadding" Value="{OnPlatform WinUI='2, 8', MacCatalyst='9, 12', iOS='3, 16', Android='4, 16'}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadToolbar" BasedOn="{StaticResource RadToolbarStyle}" />
</ResourceDictionary>