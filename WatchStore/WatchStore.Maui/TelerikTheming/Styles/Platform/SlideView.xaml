<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:slideView="clr-namespace:Telerik.Maui.Controls.SlideView;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.SlideView">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <ControlTemplate x:Key="SlideViewIndicatorItemControlTemplate">
        <Grid WidthRequest="{OnPlatform Default=12, Android=24}"
              HeightRequest="{OnPlatform Default=12, Android=24}">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState x:Name="MouseOver">
                        <VisualState.Setters>
                            <Setter TargetName="ellipse" Property="Ellipse.Fill" Value="{DynamicResource RadSlideViewEllipseMouseOverFill}" />
                            <Setter TargetName="ellipse" Property="Ellipse.WidthRequest" Value="{OnPlatform Default=6, Android=8, iOS=4}" />
                            <Setter TargetName="ellipse" Property="Ellipse.HeightRequest" Value="{OnPlatform Default=6, Android=8, iOS=4}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Selected">
                        <VisualState.Setters>
                            <Setter TargetName="ellipse" Property="Ellipse.Fill" Value="{DynamicResource RadOnAppSurfaceBrush}" />
                            <Setter TargetName="ellipse" Property="Ellipse.WidthRequest" Value="{OnPlatform Default=6, Android=8, iOS=4}" />
                            <Setter TargetName="ellipse" Property="Ellipse.HeightRequest" Value="{OnPlatform Default=6, Android=8, iOS=4}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="SelectedMouseOver">
                        <VisualState.Setters>
                            <Setter TargetName="ellipse" Property="Ellipse.Fill" Value="{DynamicResource RadSlideViewEllipseMouseOverFill}" />
                            <Setter TargetName="ellipse" Property="Ellipse.WidthRequest" Value="{OnPlatform Default=6, Android=8, iOS=4}" />
                            <Setter TargetName="ellipse" Property="Ellipse.HeightRequest" Value="{OnPlatform Default=6, Android=8, iOS=4}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter TargetName="ellipse" Property="Ellipse.Fill" Value="{DynamicResource RadSlideViewEllipseDisabledFill}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <Ellipse x:Name="ellipse"
                     WidthRequest="{OnPlatform Default=4, Android=8}"
                     HeightRequest="{OnPlatform Default=4, Android=8}"
                     HorizontalOptions="Center"
                     VerticalOptions="Center"
                     Fill="{DynamicResource RadSlideViewEllipseFill}"
                     AutomationId="RadSlideViewIndicatorItem" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="SlideViewIndicatorItemStyle" TargetType="slideView:SlideViewIndicatorItem">
        <Setter Property="ControlTemplate" Value="{StaticResource SlideViewIndicatorItemControlTemplate}" />
    </Style>
    <Style TargetType="slideView:SlideViewIndicatorItem" BasedOn="{StaticResource SlideViewIndicatorItemStyle}" />

    <Style x:Key="SlideViewIndicatorNavigationButtonStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=8, Android=10, WinUI=10}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha70}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="telerikMauiControls:ButtonUtils.Visibility" Value="{Binding Source={RelativeSource AncestorType={x:Type slideView:SlideViewIndicator}}, Path=NavigationButtonsVisibility}}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SlideViewIndicatorNavigationButtonStyleHorizontal" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SlideViewIndicatorNavigationButtonStyle}">
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=20, Android=32}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=12, Android=24}" />
    </Style>

    <Style x:Key="SlideViewIndicatorNavigationButtonStyleVertical" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SlideViewIndicatorNavigationButtonStyle}">
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=12, Android=24}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=20, Android=32}" />
    </Style>

    <Style x:Key="SlideViewIndicatorContentStyleWinUI" TargetType="slideView:SlideViewIndicatorContent">
        <Setter Property="MinimumWidthRequest" Value="1" />
        <Setter Property="MinimumHeightRequest" Value="1" />
    </Style>

    <ControlTemplate x:Key="RadSlideViewIndicatorControlTemplateHorizontal">
        <Grid ColumnDefinitions="Auto, *, Auto">
            <telerikMauiControls:RadButton Command="{Binding NavigateToPreviousItemCommand, Source={RelativeSource Mode=TemplatedParent}}"
                                           Text="{OnPlatform Default={x:Static telerikMaui:TelerikFont.IconLeftArrow}, WinUI={x:Static telerikMaui:TelerikFont.IconLeftDir}}"
                                           Style="{StaticResource SlideViewIndicatorNavigationButtonStyleHorizontal}"
                                           AutomationId="RadSlideViewIndicatorNavigateToPreviousItemButton" />
            <telerikMauiControls:RadLayout Grid.Column="1" HorizontalOptions="Center">
                <slideView:SlideViewIndicatorContent ItemsSource="{Binding ItemsSource, Source={RelativeSource Mode=TemplatedParent}}"
                                                     MaxVisibleItems="{Binding MaxVisibleItems, Source={RelativeSource Mode=TemplatedParent}}"
                                                     CurrentIndex="{Binding CurrentIndex, Source={RelativeSource Mode=TemplatedParent}}"
                                                     AnimationDuration="{Binding AnimationDuration, Source={RelativeSource Mode=TemplatedParent}}"
                                                     AnimationEasing="{Binding AnimationEasing, Source={RelativeSource Mode=TemplatedParent}}"
                                                     NavigateOnItemTap="{Binding NavigateOnItemTap, Source={RelativeSource Mode=TemplatedParent}}"
                                                     IsEnabled="{Binding IsEnabled, Source={RelativeSource Mode=TemplatedParent}}"
                                                     Orientation="Horizontal"
                                                     VerticalOptions="Center"
                                                     Style="{OnPlatform WinUI={StaticResource SlideViewIndicatorContentStyleWinUI}}"
                                                     AutomationId="RadSlideViewIndicatorContent" />
            </telerikMauiControls:RadLayout>
            <telerikMauiControls:RadButton Command="{Binding NavigateToNextItemCommand, Source={RelativeSource Mode=TemplatedParent}}"
                                           Text="{OnPlatform Default={x:Static telerikMaui:TelerikFont.IconRightArrow}, WinUI={x:Static telerikMaui:TelerikFont.IconRightDir}}"
                                           Grid.Column="2"
                                           Style="{StaticResource SlideViewIndicatorNavigationButtonStyleHorizontal}"
                                           AutomationId="RadSlideViewIndicatorNavigateToNextItemButton" />
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="RadSlideViewIndicatorControlTemplateVertical">
        <Grid RowDefinitions="Auto, *, Auto">
            <telerikMauiControls:RadButton Command="{Binding NavigateToPreviousItemCommand, Source={RelativeSource Mode=TemplatedParent}}"
                                           Text="{OnPlatform Default={x:Static telerikMaui:TelerikFont.IconUpArrow}, WinUI={x:Static telerikMaui:TelerikFont.IconUpDir}}"
                                           Style="{StaticResource SlideViewIndicatorNavigationButtonStyleVertical}"
                                           AutomationId="RadSlideViewIndicatorNavigateToPreviousItemButton" />
            <telerikMauiControls:RadLayout Grid.Row="1" VerticalOptions="Center">
                <slideView:SlideViewIndicatorContent ItemsSource="{Binding ItemsSource, Source={RelativeSource Mode=TemplatedParent}}"
                                                     MaxVisibleItems="{Binding MaxVisibleItems, Source={RelativeSource Mode=TemplatedParent}}"
                                                     CurrentIndex="{Binding CurrentIndex, Source={RelativeSource Mode=TemplatedParent}}"
                                                     AnimationDuration="{Binding AnimationDuration, Source={RelativeSource Mode=TemplatedParent}}"
                                                     AnimationEasing="{Binding AnimationEasing, Source={RelativeSource Mode=TemplatedParent}}"
                                                     NavigateOnItemTap="{Binding NavigateOnItemTap, Source={RelativeSource Mode=TemplatedParent}}"
                                                     IsEnabled="{Binding IsEnabled, Source={RelativeSource Mode=TemplatedParent}}"
                                                     Orientation="Vertical"
                                                     HorizontalOptions="Center"
                                                     Style="{OnPlatform WinUI={StaticResource SlideViewIndicatorContentStyleWinUI}}"
                                                     AutomationId="RadSlideViewIndicatorContent" />
            </telerikMauiControls:RadLayout>
            <telerikMauiControls:RadButton Command="{Binding NavigateToNextItemCommand, Source={RelativeSource Mode=TemplatedParent}}"
                                           Text="{OnPlatform Default={x:Static telerikMaui:TelerikFont.IconDownArrow}, WinUI={x:Static telerikMaui:TelerikFont.IconDownDir}}"
                                           Grid.Row="2"
                                           Style="{StaticResource SlideViewIndicatorNavigationButtonStyleVertical}"
                                           AutomationId="RadSlideViewIndicatorNavigateToNextItemButton" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="SlideViewIndicatorStyle" TargetType="slideView:SlideViewIndicator">
        <Setter Property="ControlTemplate" Value="{StaticResource RadSlideViewIndicatorControlTemplateHorizontal}" />
        <Style.Triggers>
            <Trigger TargetType="slideView:SlideViewIndicator" Property="Orientation" Value="Vertical">
                <Setter Property="ControlTemplate" Value="{DynamicResource RadSlideViewIndicatorControlTemplateVertical}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style TargetType="slideView:SlideViewIndicator" BasedOn="{StaticResource SlideViewIndicatorStyle}" />

    <Style x:Key="TouchableViewStyle" TargetType="VisualElement">
        <Setter Property="BackgroundColor" Value="Transparent" />
    </Style>

    <Style x:Key="TouchableViewStyleHorizontal" TargetType="VisualElement" BasedOn="{StaticResource TouchableViewStyle}">
        <Setter Property="MinimumWidthRequest" Value="40" />
    </Style>

    <Style x:Key="TouchableViewStyleVertical" TargetType="VisualElement" BasedOn="{StaticResource TouchableViewStyle}">
        <Setter Property="MinimumHeightRequest" Value="40" />
    </Style>

    <Style x:Key="SlideViewNavigationButtonStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=8, Android=12}" />
        <Setter Property="FontFamily" Value="{Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=10, WinUI=14}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha70}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSlideViewNavigationButtonBackgroundColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="telerikMauiControls:ButtonUtils.Visibility" Value="{Binding NavigationButtonsVisibility, Source={RelativeSource Mode=TemplatedParent}}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha50}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SlideViewNavigationButtonStyleHorizontal" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SlideViewNavigationButtonStyle}">
        <Setter Property="WidthRequest" Value="{OnPlatform Default=16, iOS=44, Android=40}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=38, iOS=44, Android=40}" />
    </Style>

    <Style x:Key="SlideViewNavigateToPreviousItemButtonStyleHorizontal" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SlideViewNavigationButtonStyleHorizontal}">
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconLeftDir}" />
        <Setter Property="Margin" Value="2, 0, 0, 0" />
    </Style>

    <Style x:Key="SlideViewNavigateToNextItemButtonStyleHorizontal" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SlideViewNavigationButtonStyleHorizontal}">
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconRightDir}" />
        <Setter Property="Margin" Value="0, 0, 2, 0" />
    </Style>

    <Style x:Key="SlideViewNavigationButtonStyleVertical" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SlideViewNavigationButtonStyle}">
        <Setter Property="WidthRequest" Value="{OnPlatform Default=38, iOS=44, Android=40}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=16, iOS=44, Android=40}" />
    </Style>

    <Style x:Key="SlideViewNavigateToPreviousItemButtonStyleVertical" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SlideViewNavigationButtonStyleVertical}">
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconUpDir}" />
        <Setter Property="Margin" Value="0, 2, 0, 0" />
    </Style>

    <Style x:Key="SlideViewNavigateToNextItemButtonStyleVertical" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SlideViewNavigationButtonStyleVertical}">
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconDownDir}" />
        <Setter Property="Margin" Value="0, 0, 0, 2" />
    </Style>

    <Style x:Key="SlideViewIndicatorStyleHorizontal" TargetType="slideView:SlideViewIndicator" BasedOn="{StaticResource SlideViewIndicatorStyle}">
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="End" />
        <Setter Property="Grid.Row" Value="2" />
        <Setter Property="Margin" Value="7" />
    </Style>

    <Style x:Key="SlideViewIndicatorStyleVertical" TargetType="slideView:SlideViewIndicator" BasedOn="{StaticResource SlideViewIndicatorStyle}">
        <Setter Property="HorizontalOptions" Value="End" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="Orientation" Value="Vertical" />
        <Setter Property="Grid.Column" Value="2" />
        <Setter Property="Margin" Value="7" />
    </Style>

    <ControlTemplate x:Key="RadSlideViewControlTemplateHorizontal">
        <Grid BackgroundColor="Transparent"
              IsClippedToBounds="True"
              RowDefinitions="Auto, *, Auto">
            <slideView:SlideViewContent x:Name="PART_SlideViewContent"
                                        Items="{Binding Items, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadSlideView}}}"
                                        Spacing="{Binding Spacing, Source={RelativeSource Mode=TemplatedParent}}"
                                        HasLooping="{Binding HasLooping, Source={RelativeSource Mode=TemplatedParent}}"
                                        Orientation="Horizontal"
                                        Grid.Row="1"
                                        AutomationId="RadSlideViewContent" />
            <StackLayout Style="{OnIdiom Phone={StaticResource TouchableViewStyleHorizontal}, Tablet={StaticResource TouchableViewStyleHorizontal}}"
                         Grid.Row="1"
                         HorizontalOptions="Start"
                         VerticalOptions="Center">
                <View.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NavigateToPreviousItemCommand, Source={RelativeSource Mode=TemplatedParent}}" />
                </View.GestureRecognizers>
                <telerikMauiControls:RadButton Command="{Binding NavigateToPreviousItemCommand, Source={RelativeSource Mode=TemplatedParent}}"
                                               Style="{Binding ActualNavigateToPreviousItemButtonStyle, Source={RelativeSource Mode=TemplatedParent}}" 
                                               AutomationId="RadSlideViewNavigateToPreviousItemButton" />
            </StackLayout>
            <StackLayout Style="{OnIdiom Phone={StaticResource TouchableViewStyleHorizontal}, Tablet={StaticResource TouchableViewStyleHorizontal}}"
                         HorizontalOptions="End"
                         VerticalOptions="Center"
                         Grid.Row="1">
                <View.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NavigateToNextItemCommand, Source={RelativeSource Mode=TemplatedParent}}" />
                </View.GestureRecognizers>
                <telerikMauiControls:RadButton Command="{Binding NavigateToNextItemCommand, Source={RelativeSource Mode=TemplatedParent}}"
                                               Style="{Binding ActualNavigateToNextItemButtonStyle, Source={RelativeSource Mode=TemplatedParent}}" 
                                               AutomationId="RadSlideViewNavigateToNextItemButton" />
            </StackLayout>
            <slideView:SlideViewIndicator ItemsSource="{Binding IndicatorItemsSource, Source={RelativeSource Mode=TemplatedParent}}"
                                          CurrentIndex="{Binding CurrentIndex, Source={RelativeSource Mode=TemplatedParent}}"
                                          HasLooping="{Binding HasLooping, Source={RelativeSource Mode=TemplatedParent}}"
                                          Style="{Binding ActualIndicatorStyle, Source={RelativeSource Mode=TemplatedParent}}"
                                          IsEnabled="{Binding IsEnabled, Source={RelativeSource Mode=TemplatedParent}}"
                                          AutomationId="RadSlideViewIndicator" />
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="RadSlideViewControlTemplateVertical">
        <Grid BackgroundColor="Transparent"
              IsClippedToBounds="True"
              ColumnDefinitions="Auto, *, Auto">
            <slideView:SlideViewContent x:Name="PART_SlideViewContent"
                                        Items="{Binding Items, Source={RelativeSource Mode=TemplatedParent}}"
                                        Spacing="{Binding Spacing, Source={RelativeSource Mode=TemplatedParent}}"
                                        HasLooping="{Binding HasLooping, Source={RelativeSource Mode=TemplatedParent}}"
                                        Orientation="Vertical"
                                        Grid.Column="1"
                                        AutomationId="RadSlideViewContent" />
            <StackLayout Style="{OnIdiom Phone={StaticResource TouchableViewStyleVertical}, Tablet={StaticResource TouchableViewStyleVertical}}"
                         Grid.Column="1"
                         HorizontalOptions="Center"
                         VerticalOptions="Start">
                <View.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NavigateToPreviousItemCommand, Source={RelativeSource Mode=TemplatedParent}}" />
                </View.GestureRecognizers>
                <telerikMauiControls:RadButton Command="{Binding NavigateToPreviousItemCommand, Source={RelativeSource Mode=TemplatedParent}}"
                                               Style="{Binding ActualNavigateToPreviousItemButtonStyle, Source={RelativeSource Mode=TemplatedParent}}" 
                                               AutomationId="RadSlideViewNavigateToPreviousItemButton" />
            </StackLayout>
            <StackLayout Style="{OnIdiom Phone={StaticResource TouchableViewStyleVertical}, Tablet={StaticResource TouchableViewStyleVertical}}"
                         Grid.Column="1"
                         HorizontalOptions="Center"
                         VerticalOptions="End">
                <View.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NavigateToNextItemCommand, Source={RelativeSource Mode=TemplatedParent}}" />
                </View.GestureRecognizers>
                <telerikMauiControls:RadButton Command="{Binding NavigateToNextItemCommand, Source={RelativeSource Mode=TemplatedParent}}"
                                               Style="{Binding ActualNavigateToNextItemButtonStyle, Source={RelativeSource Mode=TemplatedParent}}" 
                                               AutomationId="RadSlideViewNavigateToNextItemButton" />
            </StackLayout>
            <slideView:SlideViewIndicator ItemsSource="{Binding IndicatorItemsSource, Source={RelativeSource Mode=TemplatedParent}}"
                                          CurrentIndex="{Binding CurrentIndex, Source={RelativeSource Mode=TemplatedParent}}"
                                          HasLooping="{Binding HasLooping, Source={RelativeSource Mode=TemplatedParent}}"
                                          Style="{Binding ActualIndicatorStyle, Source={RelativeSource Mode=TemplatedParent}}"
                                          IsEnabled="{Binding IsEnabled, Source={RelativeSource Mode=TemplatedParent}}" 
                                          AutomationId="RadSlideViewIndicator" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="RadSlideViewStyle" TargetType="telerikMauiControls:RadSlideView">
        <Setter Property="ControlTemplate" Value="{StaticResource RadSlideViewControlTemplateHorizontal}" />
        <Setter Property="NavigateToPreviousItemButtonStyle" Value="{StaticResource SlideViewNavigateToPreviousItemButtonStyleHorizontal}" />
        <Setter Property="NavigateToNextItemButtonStyle" Value="{StaticResource SlideViewNavigateToNextItemButtonStyleHorizontal}" />
        <Style.Triggers>
            <Trigger TargetType="telerikMauiControls:RadSlideView" Property="Orientation" Value="Vertical">
                <Setter Property="ControlTemplate" Value="{StaticResource RadSlideViewControlTemplateVertical}" />
                <Setter Property="NavigateToPreviousItemButtonStyle" Value="{StaticResource SlideViewNavigateToPreviousItemButtonStyleVertical}" />
                <Setter Property="NavigateToNextItemButtonStyle" Value="{StaticResource SlideViewNavigateToNextItemButtonStyleVertical}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style TargetType="telerikMauiControls:RadSlideView" BasedOn="{StaticResource RadSlideViewStyle}" />
</ResourceDictionary>