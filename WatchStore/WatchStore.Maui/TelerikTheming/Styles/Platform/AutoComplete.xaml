<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikAutoComplete="clr-namespace:Telerik.Maui.Controls.AutoComplete;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.AutoComplete">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <Style x:Key="AutoCompleteTextInputStyle" TargetType="telerikMauiControls:RadTextInput">
        <Setter Property="Padding" Value="{OnPlatform Android='8, 12, 8, 13', iOS=8, MacCatalyst=6, WinUI='10, 5.5'}" />
        <Setter Property="IsSpellCheckEnabled" Value="False" />
    </Style>

    <VisualStateGroupList x:Key="AutoCompleteClearButtonVisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver" />
            <VisualState Name="Pressed" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="AutoCompleteClearButtonVisualStates_iOS_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha14}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="AutoCompleteClearButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource ClearButtonPointerOverBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha62}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource ClearButtonPressedBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="AutoCompleteClearButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=15, Android=14, WinUI=8}" />
        <Setter Property="Content" Value="{OnPlatform Default='&#xe851;', WinUI='&#xe80a;'}" />
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="{OnPlatform Android='8, 2, 4, 2', iOS='4, 2, 12, 2', MacCatalyst='4, 0, 8, 0', WinUI=6}" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, Android='0, 0, 8, 0', WinUI='0, 0, 5, 0'}" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="Shadow" Value="{x:Null}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Default={StaticResource AutoCompleteClearButtonVisualStates_iOS_MacCatalyst},
                                   Android={StaticResource AutoCompleteClearButtonVisualStates_Android},
                                   WinUI={StaticResource AutoCompleteClearButtonVisualStates_WinUI}}" />
    </Style>

    <Style x:Key="AutoCompleteSuggestionItemLabelStyle_MacCatalyst" TargetType="telerikMauiControls:RadHighlightLabel">
        <Style.Triggers>
            <DataTrigger TargetType="telerikMauiControls:RadHighlightLabel"
                         Binding="{Binding IsMouseOver, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadCollectionViewItemView}}}" 
                         Value="True">
                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                <Setter Property="HighlightTextColor" Value="{DynamicResource RadOnPrimaryColor}" />
            </DataTrigger>
            <DataTrigger TargetType="telerikMauiControls:RadHighlightLabel"
                         Binding="{Binding IsCurrent, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadCollectionViewItemView}}}" 
                         Value="True">
                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                <Setter Property="HighlightTextColor" Value="{DynamicResource RadOnPrimaryColor}" />
            </DataTrigger>
            <MultiTrigger TargetType="telerikMauiControls:RadHighlightLabel">
                <MultiTrigger.Conditions>
                    <BindingCondition Binding="{Binding IsCurrent, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadCollectionViewItemView}}}" Value="True" />
                    <BindingCondition Binding="{Binding IsMouseOver, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadCollectionViewItemView}}}" Value="True" />
                </MultiTrigger.Conditions>
                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                <Setter Property="HighlightTextColor" Value="{DynamicResource RadOnPrimaryColor}" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <DataTemplate x:Key="RadAutoComplete_ItemTemplate">
        <telerikMauiControls:RadBorder MinimumWidthRequest="{Binding Width, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}">
            <telerikMauiControls:RadHighlightLabel x:Name="label"
                                                   telerikMauiControls:LabelUtils.TextPath="{Binding TextSearchPath, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                   MinimumHeightRequest="{OnPlatform Default=36, Android=32, MacCatalyst=22}"
                                                   FontSize="{OnPlatform Default=14, MacCatalyst=13, iOS=17}"
                                                   HighlightText="{Binding SearchText, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                   HighlightTextColor="{Binding SuggestionItemHighlightTextColor, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                   TextColor="{DynamicResource RadOnAppSurfaceColor}"
                                                   Padding="{OnPlatform Android='8, 7', iOS='28, 8, 0, 8', MacCatalyst='15, 1, 0, 1', WinUI='12, 8, 0, 8'}"
                                                   VerticalTextAlignment="Center"
                                                   VerticalOptions="Center"
                                                   Style="{OnPlatform MacCatalyst={StaticResource AutoCompleteSuggestionItemLabelStyle_MacCatalyst}}" />
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <Style x:Key="AutoCompleteTokenClearButtonStyle_WinUI" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0, 0, 10, 0" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver" />
                    <VisualState x:Name="Pressed" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <ControlTemplate x:Key="AutoCompleteSuggestionItemControlTemplate">
        <ContentPresenter />
    </ControlTemplate>

    <Style x:Key="AutoCompleteSuggestionItemStyle" TargetType="telerikMauiControls:RadCollectionViewItemView">
        <Setter Property="ControlTemplate" Value="{StaticResource AutoCompleteSuggestionItemControlTemplate}" />
        <Setter Property="MinimumHeightRequest" Value="0" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="{DynamicResource InputDropDownItemBorderColor}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, iOS='0, 0, 0, 1', WinUI=2}" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, WinUI='0, 0, 3, 0'}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, MacCatalyst=6, WinUI=4}" />
        <Setter Property="AutomationId" Value="AutoCompleteSuggestionItem" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="MouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadCollectionViewItemView.BackgroundColor" Value="{DynamicResource RadAutoCompleteItemHighlightedBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Focused">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadCollectionViewItemView.BackgroundColor" Value="{DynamicResource RadAutoCompleteItemHighlightedBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="FocusedMouseOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadCollectionViewItemView.BackgroundColor" Value="{DynamicResource RadAutoCompleteItemHighlightedBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <DataTemplate x:Key="RadAutoComplete_TokenTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource ChipBackgroundColor}"
                                       CornerRadius="{OnPlatform Default=4, Android=16, iOS=8}"
                                       Margin="{OnPlatform Default='2, 4', WinUI='2, 3'}"
                                       VerticalOptions="Center">
            <telerikMauiControls:RadDockLayout>
                <OnPlatform x:TypeArguments="View">
                    <On Platform="WinUI">
                        <telerikMauiControls:RadTemplatedButton Content="{x:Static telerikMaui:TelerikFont.IconCancel}"
                                                                Command="{Binding RemoveTokenCommand, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                                CommandParameter="{Binding BindingContext, Source={RelativeSource Self}}"
                                                                Style="{DynamicResource AutoCompleteTokenClearButtonStyle_WinUI}"
                                                                VerticalOptions="Center"
                                                                telerikMauiControls:RadDockLayout.Dock="Right"
                                                                AutomationId="TokenCloseButton">
                            <telerikMauiControls:RadTemplatedButton.Triggers>
                                <DataTrigger TargetType="{x:Type telerikMauiControls:RadTemplatedButton}" Binding="{Binding IsFocused, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}" Value="False">
                                    <Setter Property="telerikMauiControls:RadTemplatedButton.IsVisible" Value="False" />
                                </DataTrigger>
                            </telerikMauiControls:RadTemplatedButton.Triggers>
                        </telerikMauiControls:RadTemplatedButton>
                    </On>
                    <OnPlatform.Default>
                        <Label Text="{x:Static telerikMaui:TelerikFont.IconCancel}"
                               FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                               FontSize="9"
                               TextColor="{DynamicResource RadSubtleColor}"
                               Padding="{OnPlatform Android='0, 6, 12, 6', iOS='0, 7, 14, 7', MacCatalyst='5, 0, 0, 0'}"
                               Margin="{OnPlatform Default=0, Android='-10, 0, 0, 0'}"
                               VerticalOptions="Center"
                               telerikMauiControls:RadDockLayout.Dock="{OnPlatform Default=Right, MacCatalyst=Left}"
                               AutomationId="TokenCloseButton">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding RemoveTokenCommand, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                      CommandParameter="{Binding BindingContext, Source={RelativeSource Self}}" />
                            </Label.GestureRecognizers>
                            <Label.Triggers>
                                <DataTrigger TargetType="Label" Binding="{Binding IsFocused, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}" Value="False">
                                    <Setter Property="Opacity" Value="0" />
                                    <Setter Property="IsVisible" Value="{OnPlatform Default=False, MacCatalyst=True}" />
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>
                    </OnPlatform.Default>
                </OnPlatform>
                <telerikAutoComplete:AutoCompleteTokenLabel TextPath="{Binding TextSearchPath, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                            DisplayTextFormatter="{Binding DisplayTextFormatter, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
                                                            TextColor="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, Android={DynamicResource RadOnAppSurfaceColorAlpha87}}"
                                                            FontSize="{OnPlatform Default=14, iOS=13, MacCatalyst=13}"
                                                            Margin="{OnPlatform Android='16, 6', iOS='8, 6, 8, 7', MacCatalyst='5, 3, 20, 3', WinUI='10, 3, 8, 3'}"
                                                            VerticalOptions="Center"
                                                            AutomationId="TokenLabel" />
            </telerikMauiControls:RadDockLayout>
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="RadAutoComplete_ShowMoreTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource ChipBackgroundColor}"
                                       CornerRadius="{OnPlatform Default=4, Android=16, iOS=8}"
                                       Margin="{OnPlatform Default='2, 4', WinUI='2, 3'}"
                                       VerticalOptions="Center">
            <Label Text="{Binding StringFormat='+{0}'}"
                   TextColor="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, Android={DynamicResource RadOnAppSurfaceColorAlpha87}}"
                   FontSize="{OnPlatform Default=14, iOS=13, MacCatalyst=13}"
                   Margin="{OnPlatform Android='16, 6', iOS='8, 6, 8, 7', MacCatalyst='5, 3, 20, 3', WinUI='10, 3, 8, 3'}"
                   VerticalOptions="Center" />
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="RadAutoComplete_NoResultsTemplate">
        <Label Text="{Binding NoResultsMessage, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadAutoComplete}}}"
               TextColor="{DynamicResource RadOnAppSurfaceColor}"
               FontSize="{OnPlatform Default=14, iOS=17, MacCatalyst=13}"
               Margin="{OnPlatform Android='8, 7', iOS='28, 8, 0, 8', MacCatalyst='15, 3, 0, 3', WinUI='14, 10, 0, 11'}"
               AutomationId="RadAutoCompleteNoResultsLabel" />
    </DataTemplate>

    <VisualStateGroupList x:Key="RadAutoComplete_VisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Focused" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadAutoComplete.TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadAutoComplete.PlaceholderColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadAutoComplete.BackgroundColor" Value="{DynamicResource RadEntryDisabledBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadAutoComplete.BorderBrush" Value="{DynamicResource RadEntryDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadAutoComplete_VisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Focused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadAutoComplete.BorderBrush" Value="{DynamicResource RadEntryFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadAutoComplete.BorderThickness" Value="0, 0, 0, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadAutoComplete.TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadAutoComplete.PlaceholderColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadAutoComplete.BorderBrush" Value="{DynamicResource RadEntryDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadAutoComplete_VisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadAutoComplete.BackgroundColor" Value="{DynamicResource RadSurfaceAltColorDarken2Alpha50}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadAutoComplete.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadAutoComplete.BorderBrush" Value="{DynamicResource RadEntryFocusedBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadAutoComplete.TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadAutoComplete.PlaceholderColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadAutoComplete.BackgroundColor" Value="{DynamicResource RadEntryDisabledBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadAutoComplete.BorderBrush" Value="{DynamicResource RadEntryDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <ControlTemplate x:Key="RadAutoComplete_ControlTemplate">
        <telerikMauiControls:RadWrapLayout x:Name="PART_WrapLayout"
                                           BackgroundColor="Transparent"
                                           StretchLastChild="True"
                                           AutomationId="RadAutoCompleteViewWrapLayout">
            <telerikMauiControls:RadDockLayout x:Name="PART_DockLayout">
                <telerikMauiControls:RadTemplatedButton x:Name="PART_ClearButton"
                                                        IsEnabled="{TemplateBinding IsEnabled}"
                                                        Command="{TemplateBinding ClearTextCommand}"
                                                        Style="{TemplateBinding ActualClearButtonStyle}"
                                                        telerikMauiControls:RadDockLayout.Dock="Right"
                                                        AutomationId="RadAutoCompleteClearButton" />
                <telerikMauiControls:RadTextInput x:Name="PART_Input"
                                                  Style="{TemplateBinding ActualTextInputStyle}"
                                                  IsEnabled="{TemplateBinding IsEnabled}"
                                                  FontFamily="{TemplateBinding FontFamily}"
                                                  FontSize="{TemplateBinding FontSize}"
                                                  FontAttributes="{TemplateBinding FontAttributes}"
                                                  Text="{TemplateBinding Text, Mode=TwoWay}"
                                                  TextColor="{TemplateBinding TextColor}"
                                                  PlaceholderColor="{TemplateBinding PlaceholderColor}"
                                                  Keyboard="{TemplateBinding Keyboard}"
                                                  AutomationId="RadAutoCompleteTextInput" />
            </telerikMauiControls:RadDockLayout>
        </telerikMauiControls:RadWrapLayout>
    </ControlTemplate>

    <Style x:Key="RadAutoCompleteStyle" TargetType="telerikMauiControls:RadAutoComplete">
        <Setter Property="ControlTemplate" Value="{StaticResource RadAutoComplete_ControlTemplate}" />
        <Setter Property="PlaceholderColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadEntryBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadEntryBorderBrush}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=1, Android='0, 0, 0, 1'}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Android='4, 4, 0, 0', iOS=10, MacCatalyst=6, WinUI=4}" />
        <Setter Property="TextInputStyle" Value="{StaticResource AutoCompleteTextInputStyle}" />
        <Setter Property="ClearButtonStyle" Value="{StaticResource AutoCompleteClearButtonStyle}" />
        <Setter Property="SuggestionViewBackgroundColor" Value="{DynamicResource InputDropDownBackgroundColor}" />
        <Setter Property="SuggestionViewBorderColor" Value="{DynamicResource InputDropDownBorderColor}" />
        <Setter Property="SuggestionViewCornerRadius" Value="{OnPlatform Default=8, Android=4, iOS=12}" />
        <Setter Property="SuggestionViewBorderThickness" Value="1" />
        <Setter Property="SuggestionItemHighlightTextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="SuggestionItemTemplate" Value="{StaticResource RadAutoComplete_ItemTemplate}" />
        <Setter Property="TokenTemplate" Value="{StaticResource RadAutoComplete_TokenTemplate}" />
        <Setter Property="ShowMoreTemplate" Value="{StaticResource RadAutoComplete_ShowMoreTemplate}" />
        <Setter Property="NoResultsTemplate" Value="{StaticResource RadAutoComplete_NoResultsTemplate}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Default={StaticResource RadAutoComplete_VisualStates},
                                   Android={StaticResource RadAutoComplete_VisualStates_Android},
                                   WinUI={StaticResource RadAutoComplete_VisualStates_WinUI}}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadAutoComplete" BasedOn="{StaticResource RadAutoCompleteStyle}" />
</ResourceDictionary>