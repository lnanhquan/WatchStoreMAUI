<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    x:Class="Telerik.Theming.Platform.Styles.DataPager"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikDataPager="clr-namespace:Telerik.Maui.Controls.DataPager;assembly=Telerik.Maui.Controls"
                    xmlns:telerikTheming="clr-namespace:Telerik.Maui.Controls.Theming;assembly=Telerik.Maui.Controls">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->
    
    <telerikMauiControls:DictionaryConverter x:Key="DataPagerTextToFontFamilyConverter" FallbackValue="{Null}">
        <telerikMauiControls:DictionaryConverterItem Key="{x:Static telerikMaui:TelerikFont.IconGoToFirstPage}" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <telerikMauiControls:DictionaryConverterItem Key="{x:Static telerikMaui:TelerikFont.IconGoToPreviousPage}" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <telerikMauiControls:DictionaryConverterItem Key="{x:Static telerikMaui:TelerikFont.IconGoToNextPage}" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <telerikMauiControls:DictionaryConverterItem Key="{x:Static telerikMaui:TelerikFont.IconGoToLastPage}" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <telerikMauiControls:DictionaryConverterItem Key="{x:Static telerikMaui:TelerikFont.IconEllipsis}" Value="{x:Static telerikMaui:TelerikFont.Name}" />
    </telerikMauiControls:DictionaryConverter>

    <Style x:Key="DataPagerContentStyle" TargetType="telerikDataPager:DataPagerContent">
        <Setter Property="PageIndex" Value="{TemplateBinding PageIndex}" />
        <Setter Property="PageCount" Value="{TemplateBinding PageCount}" />
        <Setter Property="MinNumericButtonsCount" Value="{TemplateBinding MinNumericButtonsCount}" />
        <Setter Property="MaxNumericButtonsCount" Value="{TemplateBinding MaxNumericButtonsCount}" />
        <Setter Property="EllipsisMode" Value="{TemplateBinding EllipsisMode}" />
        <Setter Property="DisplayMode" Value="{TemplateBinding DisplayMode}" />
        <Setter Property="AdaptiveDisplayModes" Value="{TemplateBinding AdaptiveDisplayModes}" />
        <Setter Property="ItemSpacing" Value="{TemplateBinding ItemSpacing}" />
    </Style>

    <Style x:Key="DataPagerNumericButtonsViewStyle" TargetType="telerikDataPager:DataPagerNumericButtonsView">
        <Setter Property="PageIndex" Value="{TemplateBinding PageIndex}" />
        <Setter Property="NumericButtonsStyle" Value="{TemplateBinding ActualNumericButtonsStyle}" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="ItemSpacing" Value="{TemplateBinding ItemSpacing}" />
    </Style>

    <VisualStateGroupList x:Key="DataPagerButtonVisualStates_Mobile">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal">
                <VisualState.Setters>
                    <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                    <Setter Property="telerikDataPager:DataPagerButton.Background" Value="Transparent" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed" />
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadPrimaryBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="SelectedDisabled">
                <VisualState.Setters>
                    <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadDataPagerSelectedDisabledBackground}" />
                    <Setter Property="ToolTipProperties.Text" Value="{Null}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{OnPlatform Default={DynamicResource RadButtonDisabledTextColor}, iOS={DynamicResource RadOnAppSurfaceColorAlpha40}}" />
                    <Setter Property="ToolTipProperties.Text" Value="{Null}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="DataPagerButtonVisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadDataPagerButtonPointerOverBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed" />
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadPrimaryBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="SelectedDisabled">
                <VisualState.Setters>
                    <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadDataPagerSelectedDisabledBackground}" />
                    <Setter Property="ToolTipProperties.Text" Value="{Null}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="ToolTipProperties.Text" Value="{Null}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="DataPagerButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadDataPagerButtonPointerOverBackground}" />
                    <Setter Property="telerikDataPager:DataPagerButton.BorderBrush" Value="{DynamicResource RadBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed" />
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadPrimaryBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="SelectedDisabled">
                <VisualState.Setters>
                    <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikDataPager:DataPagerButton.Background" Value="{DynamicResource RadDataPagerSelectedDisabledBackground}" />
                    <Setter Property="ToolTipProperties.Text" Value="{Null}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikDataPager:DataPagerButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="ToolTipProperties.Text" Value="{Null}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <ControlTemplate x:Key="DataPagerButtonControlTemplate">
        <telerikMauiControls:RadEffectsView x:Name="PART_EffectsView"
                                            BackgroundColor="{TemplateBinding BackgroundColor}"
                                            Background="{TemplateBinding Background}"
                                            BorderColor="{TemplateBinding BorderColor}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="{TemplateBinding CornerRadius, Converter={x:Static telerikMauiControls:CornerRadiusToThicknessConverter.Instance}}"
                                            Padding="{TemplateBinding Padding}"
                                            Margin="{OnPlatform Default=0, Android=8, WinUI=4}">
            <ContentPresenter telerikMauiControls:ContentPresenterUtils.Content="{TemplateBinding ActualContent}" />
        </telerikMauiControls:RadEffectsView>
    </ControlTemplate>

    <Style x:Key="DataPagerButtonStyle" TargetType="telerikDataPager:DataPagerButton">
        <Setter Property="ControlTemplate" Value="{StaticResource DataPagerButtonControlTemplate}" />
        <Setter Property="telerikTheming:RadEffects.RippleMode" Value="Pulse" />
        <Setter Property="telerikTheming:RadEffects.RippleColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha12}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=8, MacCatalyst=5}" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{Binding Content, Source={RelativeSource Mode=Self}, Converter={StaticResource DataPagerTextToFontFamilyConverter}}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, WinUI=1}" />
        <Setter Property="Padding" Value="{OnPlatform Default='4, 0', iOS='12, 0'}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=48, iOS=44, MacCatalyst=20, WinUI=40}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=44, MacCatalyst=20, WinUI=40}" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="ToolTipProperties.Text" Value="{OnPlatform Default={Null}, WinUI={Binding CommandParameter, Source={RelativeSource Mode=Self}, Converter={x:Static telerikMauiControls:ArithmeticConverter.Plus1Converter}}}" />
        <Setter Property="Shadow" Value="{x:Null}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Default={StaticResource DataPagerButtonVisualStates_Mobile},
                                   MacCatalyst={StaticResource DataPagerButtonVisualStates_MacCatalyst},
                                   WinUI={StaticResource DataPagerButtonVisualStates_WinUI}}" />
    </Style>

    <Style x:Key="DataPagerNavigateToFirstPageButtonStyle" TargetType="telerikDataPager:DataPagerButton" BasedOn="{StaticResource DataPagerButtonStyle}">
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconGoToFirstPage}" />
        <Setter Property="Command" Value="{TemplateBinding NavigateToFirstPageCommand}" />
        <Setter Property="CommandParameter" Value="{x:Int32 0}" />
        <Setter Property="telerikDataPager:DataPagerContent.ItemType" Value="FirstPageButton" />
        <Setter Property="AutomationId" Value="NavigateToFirstPageButton" />
    </Style>

    <Style x:Key="DataPagerNavigateToPreviousPageButtonStyle" TargetType="telerikDataPager:DataPagerButton" BasedOn="{StaticResource DataPagerButtonStyle}">
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconGoToPreviousPage}" />
        <Setter Property="Command" Value="{TemplateBinding NavigateToPreviousPageCommand}" />
        <Setter Property="CommandParameter" Value="{TemplateBinding PageIndex, Converter={x:Static telerikMauiControls:ArithmeticConverter.Minus1Converter}}" />
        <Setter Property="telerikDataPager:DataPagerContent.ItemType" Value="PrevPageButton" />
        <Setter Property="AutomationId" Value="NavigateToPreviousPageButton" />
    </Style>

    <Style x:Key="DataPagerNavigateToNextPageButtonStyle" TargetType="telerikDataPager:DataPagerButton" BasedOn="{StaticResource DataPagerButtonStyle}">
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconGoToNextPage}" />
        <Setter Property="Command" Value="{TemplateBinding NavigateToNextPageCommand}" />
        <Setter Property="CommandParameter" Value="{TemplateBinding PageIndex, Converter={x:Static telerikMauiControls:ArithmeticConverter.Plus1Converter}}" />
        <Setter Property="telerikDataPager:DataPagerContent.ItemType" Value="NextPageButton" />
        <Setter Property="AutomationId" Value="NavigateToNextPageButton" />
    </Style>

    <Style x:Key="DataPagerNavigateToLastPageButtonStyle" TargetType="telerikDataPager:DataPagerButton" BasedOn="{StaticResource DataPagerButtonStyle}">
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconGoToLastPage}" />
        <Setter Property="Command" Value="{TemplateBinding NavigateToLastPageCommand}" />
        <Setter Property="CommandParameter" Value="{TemplateBinding PageCount, Converter={x:Static telerikMauiControls:ArithmeticConverter.Minus1Converter}}" />
        <Setter Property="telerikDataPager:DataPagerContent.ItemType" Value="LastPageButton" />
        <Setter Property="AutomationId" Value="NavigateToLastPageButton" />
    </Style>

    <Style x:Key="DataPagerNumericButtonStyle" TargetType="telerikDataPager:DataPagerButton" BasedOn="{StaticResource DataPagerButtonStyle}">
        <Setter Property="Command" Value="{Binding NavigateToPageCommand, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadDataPager}}}" />
    </Style>

    <Style x:Key="DataPagerComboBoxStyle" TargetType="telerikMauiControls:RadComboBox">
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=48, Android=68, iOS=120}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataPagerComboBoxBackgroundColor}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="IsClearButtonVisible" Value="False" />
    </Style>

    <Style x:Key="DataPagerNavigationComboBoxStyle" TargetType="telerikMauiControls:RadComboBox" BasedOn="{StaticResource DataPagerComboBoxStyle}">
        <Setter Property="telerikDataPager:DataPagerContent.ItemType" Value="NavigationComboBox" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=72, iOS=71, MacCatalyst=58, WinUI=88}" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="IsFilteringEnabled" Value="True" />
        <Setter Property="ItemsSource" Value="{TemplateBinding PageCount, Converter={x:Static telerikMauiControls:CountToListConverter.Instance}}" />
        <Setter Property="AutomationId" Value="NavigationComboBox" />
    </Style>

    <Style x:Key="DataPagerPageSizesViewLabelStyle" TargetType="Label">
        <Setter Property="Text" Value="{telerikMauiControls:Localize DataPager_PageSizesView_LabelText}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Label.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataPagerPageSizesViewComboBoxStyle" TargetType="telerikMauiControls:RadComboBox" BasedOn="{StaticResource DataPagerComboBoxStyle}">
        <Setter Property="Margin" Value="8, 0, 0, 0" />
        <Setter Property="ItemsSource" Value="{TemplateBinding DataPager.PageSizes}" />
        <Setter Property="AutomationId" Value="PageSizesViewComboBox" />
    </Style>

    <ControlTemplate x:Key="DataPagerPageSizesViewControlTemplate">
        <HorizontalStackLayout>
            <Label Style="{TemplateBinding ActualLabelStyle}" />
            <telerikMauiControls:RadComboBox SelectedItem="{TemplateBinding DataPager.PageSize, Mode=TwoWay}"
                                             Style="{TemplateBinding ActualComboBoxStyle}" />
        </HorizontalStackLayout>
    </ControlTemplate>

    <Style x:Key="DataPagerPageSizesViewStyle" TargetType="telerikDataPager:DataPagerPageSizesView">
        <Setter Property="ControlTemplate" Value="{StaticResource DataPagerPageSizesViewControlTemplate}" />
        <Setter Property="DataPager" Value="{TemplateBinding}" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="LabelStyle" Value="{DynamicResource DataPagerPageSizesViewLabelStyle}" />
        <Setter Property="ComboBoxStyle" Value="{DynamicResource DataPagerPageSizesViewComboBoxStyle}" />
    </Style>

    <Style x:Key="DataPagerNavigationViewLabelsStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Label.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataPagerNavigationViewComboBoxStyle" TargetType="telerikMauiControls:RadComboBox" BasedOn="{StaticResource DataPagerComboBoxStyle}">
        <Setter Property="Margin" Value="{OnPlatform Default='8, 0', WinUI='4, 0'}" />
        <Setter Property="IsFilteringEnabled" Value="True" />
        <Setter Property="ItemsSource" Value="{TemplateBinding DataPager.PageCount, Converter={x:Static telerikMauiControls:CountToListConverter.Instance}}" />
        <Setter Property="AutomationId" Value="NavigationViewComboBox" />
    </Style>

    <ControlTemplate x:Key="DataPagerNavigationViewControlTemplate">
        <HorizontalStackLayout>
            <Label Text="{telerikMauiControls:Localize DataPager_NavigationView_PageNumberText}"
                   Style="{TemplateBinding ActualLabelsStyle}" />
            <telerikMauiControls:RadComboBox SelectedIndex="{TemplateBinding DataPager.PageIndex, Mode=TwoWay}"
                                             Style="{TemplateBinding ActualComboBoxStyle}" />
            <Label Text="{TemplateBinding DataPager.PageCount, StringFormat={telerikMauiControls:Localize DataPager_NavigationView_OfPageCountText}}"
                   AutomationId="NavigationViewPageCountLabel"
                   Style="{TemplateBinding ActualLabelsStyle}" />
        </HorizontalStackLayout>
    </ControlTemplate>

    <Style x:Key="DataPagerNavigationViewStyle" TargetType="telerikDataPager:DataPagerNavigationView">
        <Setter Property="ControlTemplate" Value="{StaticResource DataPagerNavigationViewControlTemplate}" />
        <Setter Property="DataPager" Value="{TemplateBinding}" />
        <Setter Property="HorizontalOptions" Value="End" />
        <Setter Property="LabelsStyle" Value="{DynamicResource DataPagerNavigationViewLabelsStyle}" />
        <Setter Property="ComboBoxStyle" Value="{DynamicResource DataPagerNavigationViewComboBoxStyle}" />
    </Style>

    <Style x:Key="DataPagerRootBorderStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BackgroundColor" Value="{TemplateBinding BackgroundColor}" />
        <Setter Property="Background" Value="{TemplateBinding Background}" />
        <Setter Property="BorderColor" Value="{TemplateBinding BorderColor}" />
        <Setter Property="BorderBrush" Value="{TemplateBinding BorderBrush}" />
        <Setter Property="BorderThickness" Value="{TemplateBinding BorderThickness}" />
        <Setter Property="CornerRadius" Value="{TemplateBinding CornerRadius}" />
        <Setter Property="Padding" Value="{TemplateBinding ContentPadding}" />
    </Style>

    <ControlTemplate x:Key="RadDataPagerControlTemplate">
        <telerikMauiControls:RadBorder Style="{StaticResource DataPagerRootBorderStyle}">
            <telerikDataPager:DataPagerContent Style="{TemplateBinding ActualDataPagerContentStyle}">
                <telerikDataPager:DataPagerButton Style="{TemplateBinding ActualNavigateToFirstPageButtonStyle}" />
                <telerikDataPager:DataPagerButton Style="{TemplateBinding ActualNavigateToPreviousPageButtonStyle}" />
                <telerikDataPager:DataPagerNumericButtonsView Style="{TemplateBinding ActualNumericButtonsViewStyle}" />
                <telerikMauiControls:RadComboBox Style="{TemplateBinding ActualNavigationComboBoxStyle}" 
                                                 SelectedIndex="{TemplateBinding PageIndex, Mode=TwoWay}" />
                <telerikDataPager:DataPagerButton Style="{TemplateBinding ActualNavigateToNextPageButtonStyle}" />
                <telerikDataPager:DataPagerButton Style="{TemplateBinding ActualNavigateToLastPageButtonStyle}" />
                <telerikDataPager:DataPagerPageSizesView Style="{TemplateBinding PageSizesViewStyle}" />
                <telerikDataPager:DataPagerNavigationView Style="{TemplateBinding NavigationViewStyle}" />
            </telerikDataPager:DataPagerContent>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="RadDataPagerStyle" TargetType="telerikMauiControls:RadDataPager">
        <Setter Property="ControlTemplate" Value="{StaticResource RadDataPagerControlTemplate}" />
        <Setter Property="Background" Value="{DynamicResource RadDataPagerBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadDataPagerBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ContentPadding" Value="{OnPlatform Android='16, 4', iOS='16, 3', MacCatalyst='12, 8', WinUI='8, 3'}" />
        <Setter Property="ItemSpacing" Value="16" />
        <Setter Property="HeightRequest" Value="{OnPlatform Android=56, iOS=50, MacCatalyst=38, WinUI=48}" />
        <Setter Property="NavigateToFirstPageButtonStyle" Value="{DynamicResource DataPagerNavigateToFirstPageButtonStyle}" />
        <Setter Property="NavigateToPreviousPageButtonStyle" Value="{DynamicResource DataPagerNavigateToPreviousPageButtonStyle}" />
        <Setter Property="NavigateToNextPageButtonStyle" Value="{DynamicResource DataPagerNavigateToNextPageButtonStyle}" />
        <Setter Property="NavigateToLastPageButtonStyle" Value="{DynamicResource DataPagerNavigateToLastPageButtonStyle}" />
        <Setter Property="NumericButtonStyle" Value="{DynamicResource DataPagerNumericButtonStyle}" />
        <Setter Property="NavigationComboBoxStyle" Value="{DynamicResource DataPagerNavigationComboBoxStyle}" />
        <Setter Property="PageSizesViewStyle" Value="{DynamicResource DataPagerPageSizesViewStyle}" />
        <Setter Property="NavigationViewStyle" Value="{DynamicResource DataPagerNavigationViewStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadDataPager" BasedOn="{StaticResource RadDataPagerStyle}" />
</ResourceDictionary>
