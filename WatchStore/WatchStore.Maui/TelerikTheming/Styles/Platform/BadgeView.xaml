<?xml version="1.0" encoding="UTF-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikBadgeView="clr-namespace:Telerik.Maui.Controls.BadgeView;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.BadgeView">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <ControlTemplate x:Key="RadBadgeViewBadgeTemplate">
        <telerikMauiControls:RadBorder BorderThickness="{TemplateBinding BorderThickness}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       BackgroundColor="{TemplateBinding BackgroundColor}"
                                       MinimumHeightRequest="{TemplateBinding MinimumHeightRequest}"
                                       MinimumWidthRequest="{TemplateBinding MinimumWidthRequest}">
            <Label x:Name="PART_BadgeContent"
                   Text="{TemplateBinding Text}"
                   TextColor="{TemplateBinding TextColor}"
                   Margin="{TemplateBinding TextMargin}"
                   FontSize="{TemplateBinding FontSize}"
                   FontFamily="{TemplateBinding FontFamily}"
                   FontAttributes="{TemplateBinding FontAttributes}"
                   HorizontalTextAlignment="Center"
                   VerticalTextAlignment="Center"
                   AutomationId="RadBadgeLabel" />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style TargetType="telerikBadgeView:Badge">
        <Setter Property="ControlTemplate" Value="{StaticResource RadBadgeViewBadgeTemplate}" />
        <Setter Property="TextColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="Red" />
    </Style>

    <Style x:Key="RadBadgeViewStyle" TargetType="telerikMauiControls:RadBadgeView">
        <Setter Property="BadgeTextColor" Value="{DynamicResource RadAppSurfaceColor}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadBadgeView" BasedOn="{StaticResource RadBadgeViewStyle}" />
</ResourceDictionary>