<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.TabView">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <ControlTemplate x:Key="TabViewHeaderControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       AutomationId="RadTabViewHeader">
            <ContentPresenter />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="TabViewHeaderStyle" TargetType="telerikMauiControls:TabViewHeader">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadTabViewHeaderBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadTabViewBorderColor}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=1, Android='0, 0, 0, 1', iOS='0, 0, 0, 1', WinUI='1, 1, 1, 0'}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, MacCatalyst=4}" />
        <Setter Property="HorizontalOptions" Value="{OnPlatform Default=Fill, MacCatalyst=Center}" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>

    <Style x:Key="TabViewBottomHeaderStyle" TargetType="telerikMauiControls:TabViewHeader" BasedOn="{StaticResource TabViewHeaderStyle}">
        <Setter Property="BorderThickness" Value="{OnPlatform Default=1, Android='0, 1, 0, 0', iOS='0, 1, 0, 0', WinUI='1, 0, 1, 1'}" />
    </Style>

    <Style x:Key="TabViewLeftHeaderStyle" TargetType="telerikMauiControls:TabViewHeader" BasedOn="{StaticResource TabViewHeaderStyle}">
        <Setter Property="BorderThickness" Value="{OnPlatform Default=1, Android='0, 0, 1, 0', iOS='0, 0, 1, 0', WinUI='1, 1, 0, 1'}" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="VerticalOptions" Value="{OnPlatform Default=Fill, MacCatalyst=Center}" />
    </Style>

    <Style x:Key="TabViewRightHeaderStyle" TargetType="telerikMauiControls:TabViewHeader" BasedOn="{StaticResource TabViewHeaderStyle}">
        <Setter Property="BorderThickness" Value="{OnPlatform Default=1, Android='1, 0, 0, 0', iOS='1, 0, 0, 0', WinUI='0, 1, 1, 1'}" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="VerticalOptions" Value="{OnPlatform Default=Fill, MacCatalyst=Center}" />
    </Style>

    <VisualStateGroupList x:Key="TabViewHeaderItemVisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderColor" Value="{DynamicResource RadPrimaryColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="TabViewHeaderItemVisualStates_iOS">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="TabViewHeaderItemVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadTabViewHeaderItemSelectedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="TabViewHeaderItemVisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadTabViewHeaderItemSelectedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.BorderThickness" Value="1" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <ControlTemplate x:Key="TabViewHeaderItemControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       AutomationId="RadTabViewHeaderItem">
            <ContentPresenter />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="TabViewHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem">
        <Setter Property="ControlTemplate" Value="{StaticResource TabViewHeaderItemControlTemplate}" />
        <Setter Property="TextColor" Value="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, Android={DynamicResource RadSubtleColor}, iOS={DynamicResource RadSubtleColor}}" />
        <Setter Property="BorderColor" Value="{OnPlatform Default=Transparent, WinUI={DynamicResource RadTabViewBorderColor}, MacCatalyst={DynamicResource RadTabViewBorderColor}}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, Android='0, 0, 0, 2', WinUI='0, 0, 1, 0'}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, MacCatalyst=4}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default='10, 15', MacCatalyst='20, 5'}" />
        <Setter Property="ImageSpacing" Value="4" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Android={StaticResource TabViewHeaderItemVisualStates_Android},
                                   iOS={StaticResource TabViewHeaderItemVisualStates_iOS},
                                   WinUI={StaticResource TabViewHeaderItemVisualStates_WinUI},
                                   MacCatalyst={StaticResource TabViewHeaderItemVisualStates_MacCatalyst}}" />
    </Style>

    <Style x:Key="TabViewBottomHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem" BasedOn="{StaticResource TabViewHeaderItemStyle}">
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, Android='0, 2, 0, 0', WinUI='0, 0, 1, 0'}" />
    </Style>

    <Style x:Key="TabViewLeftHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem" BasedOn="{StaticResource TabViewHeaderItemStyle}">
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, Android='0, 0, 2, 0', WinUI='0, 0, 0, 1'}" />
        <Setter Property="VerticalOptions" Value="Start" />
    </Style>

    <Style x:Key="TabViewRightHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem" BasedOn="{StaticResource TabViewHeaderItemStyle}">
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, Android='2, 0, 0, 0', WinUI='0, 0, 0, 1'}" />
        <Setter Property="VerticalOptions" Value="Start" />
    </Style>

    <ControlTemplate x:Key="TabViewContentControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       AutomationId="RadTabViewContent">
            <ContentPresenter />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="TabViewContentStyle" TargetType="telerikMauiControls:TabViewContent">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadTabViewBorderColor}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, WinUI=1, MacCatalyst=1}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, MacCatalyst=4}" />
    </Style>

    <ControlTemplate x:Key="RadTabViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <ContentPresenter />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="RadTabViewStyle" TargetType="telerikMauiControls:RadTabView">
        <Setter Property="HeaderTemplate" Value="{StaticResource TabViewHeaderControlTemplate}" />
        <Setter Property="HeaderStyle" Value="{StaticResource TabViewHeaderStyle}" />
        <Setter Property="HeaderItemTemplate" Value="{StaticResource TabViewHeaderItemControlTemplate}" />
        <Setter Property="HeaderItemStyle" Value="{StaticResource TabViewHeaderItemStyle}" />
        <Setter Property="ContentTemplate" Value="{StaticResource TabViewContentControlTemplate}" />
        <Setter Property="ContentStyle" Value="{StaticResource TabViewContentStyle}" />
        <Style.Triggers>
            <Trigger TargetType="telerikMauiControls:RadTabView" Property="HeaderPosition" Value="Bottom">
                <Setter Property="HeaderStyle" Value="{DynamicResource TabViewBottomHeaderStyle}" />
                <Setter Property="HeaderItemStyle" Value="{DynamicResource TabViewBottomHeaderItemStyle}" />
            </Trigger>
            <Trigger TargetType="telerikMauiControls:RadTabView" Property="HeaderPosition" Value="Left">
                <Setter Property="HeaderStyle" Value="{DynamicResource TabViewLeftHeaderStyle}" />
                <Setter Property="HeaderItemStyle" Value="{DynamicResource TabViewLeftHeaderItemStyle}" />
            </Trigger>
            <Trigger TargetType="telerikMauiControls:RadTabView" Property="HeaderPosition" Value="Right">
                <Setter Property="HeaderStyle" Value="{DynamicResource TabViewRightHeaderStyle}" />
                <Setter Property="HeaderItemStyle" Value="{DynamicResource TabViewRightHeaderItemStyle}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style TargetType="telerikMauiControls:RadTabView" BasedOn="{StaticResource RadTabViewStyle}" />
</ResourceDictionary>
