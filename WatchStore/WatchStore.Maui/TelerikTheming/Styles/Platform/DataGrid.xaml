<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMauiCore="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikDataGrid="clr-namespace:Telerik.Maui.Controls.DataGrid;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.DataGrid">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <telerikMauiControls:AreEqualToBoolConverter x:Key="AreEqualToBoolConverter" />
    <telerikMauiControls:IsOfTypeConverter x:Key="IsColumnConverter" Type="{x:Type telerikDataGrid:DataGridColumn}" />
    <telerikMauiControls:NullOrEmptyToBoolConverter x:Key="NullOrEmptyToBoolConverter" />

    <Shadow x:Key="DataGridFilterControlShadow_WinUI" Offset="0, 4" Radius="8" Brush="#24000000" />

    <DataTemplate x:Key="DataGridColumnHeaderDragVisualTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource RadPrimaryColor}"
                                       CornerRadius="{OnPlatform Default=2, WinUI=4}"
                                       MinimumWidthRequest="{Binding ActualWidth}">
            <Label Text="{Binding HeaderText, FallbackValue='dragging...'}"
                   TextColor="{DynamicResource RadOnPrimaryColor}"
                   Margin="{OnPlatform Default='10, 8', Android=6}" />
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <Style x:Key="DataGridTextOnlyButtonStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderWidth" Value="0" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.BackgroundColor" Value="{DynamicResource RadDataGridCloseButtonPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataGridCloseButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource DataGridTextOnlyButtonStyle}">
        <Setter Property="FontFamily" Value="Arial" />
        <Setter Property="FontAttributes" Value="{OnPlatform Default=None, Android=Bold, iOS=Bold}" />
        <Setter Property="Text" Value="&#x2715;" />
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="WidthRequest" Value="{OnPlatform Default=40, MacCatalyst=20, WinUI=22}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=40, MacCatalyst=20, WinUI=22}" />
    </Style>

    <Style x:Key="DataGridGroupingPanelItemCloseButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource DataGridCloseButtonStyle}">
        <Setter Property="FontSize" Value="{OnPlatform MacCatalyst=18, WinUI=14, iOS=12, Android=10}" />
        <Setter Property="FontAttributes" Value="{OnPlatform Default=None, Android=Bold, iOS=Bold}" />
        <Setter Property="WidthRequest" Value="{OnPlatform Default=36, Android=26, iOS=26}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=16, Android=30, iOS=30}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadPrimaryColorAlpha90}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <DataTemplate x:Key="DataGridGroupingPanelItemTemplate">
        <telerikMauiControls:RadBorder Background="{DynamicResource RadDataGridGroupingPanelItemBackground}"
                                       BorderBrush="{OnPlatform Default={DynamicResource RadPrimaryColor}, WinUI={DynamicResource RadButtonBorderBrush}}"
                                       BorderThickness="{OnPlatform Default=1, MacCatalyst=0}"
                                       Margin="{OnPlatform Android='0, 6', WinUI='0, 2'}"
                                       CornerRadius="{OnPlatform Default=2, WinUI=4}">
            <HorizontalStackLayout>
                <Label Text="{Binding DisplayContent}"
                       TextColor="{DynamicResource RadOnAppSurfaceColor}"
                       FontSize="{OnPlatform Default=14, iOS=17, MacCatalyst=17}"
                       VerticalOptions="Center"
                       Margin="{OnPlatform Default='10, 8, 2, 8', Android='7 ,0, 2, 0', MacCatalyst='14, 0'}" />
                <telerikMauiControls:RadButton Command="{Binding RemoveCommand, Source={RelativeSource AncestorType={Type telerikDataGrid:DataGridGroupingPanel}}}"
                                               CommandParameter="{Binding}"
                                               Style="{StaticResource DataGridGroupingPanelItemCloseButtonStyle}"
                                               VerticalContentAlignment="Center"
                                               Margin="{OnPlatform Default='2, 0', MacCatalyst='4, 0'}" />
            </HorizontalStackLayout>
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <Style x:Key="DataGridCollectionViewStyle_iOS" TargetType="VisualElement">
        <Setter Property="InputTransparent" Value="{Binding DataGrid.DraggedObject, Source={RelativeSource AncestorType={Type telerikDataGrid:DataGridGroupingPanel}}, Converter={StaticResource IsColumnConverter}}" />
        <Setter Property="HeightRequest" Value="36" />
    </Style>

    <ControlTemplate x:Key="DataGridGroupingPanelControlTemplate">
        <Grid ColumnDefinitions="Auto, *" BackgroundColor="Transparent">
            <Label Text="{TemplateBinding ActualHeaderText}"
                   FontSize="{OnPlatform Default=14, iOS=17, MacCatalyst=17}"
                   TextColor="{DynamicResource RadOnAppSurfaceColor}"
                   VerticalOptions="Center"
                   Margin="{OnPlatform iOS='18, 9, 4, 9', WinUI='11, 10.5', Android='8, 12', MacCatalyst='8, 0, 4, 0'}" />
            <CollectionView ItemTemplate="{Binding DataGrid.GroupingPanelItemTemplate, Source={RelativeSource AncestorType={Type telerikDataGrid:DataGridGroupingPanel}}, TargetNullValue={StaticResource DataGridGroupingPanelItemTemplate}}"
                            ItemsSource="{TemplateBinding DataGrid.GroupDescriptors}"
                            CanReorderItems="True"
                            Grid.Column="1"
                            Margin="{OnPlatform Default=0, iOS='0, 4', MacCatalyst='0, 4'}"
                            Style="{OnPlatform iOS={StaticResource DataGridCollectionViewStyle_iOS}, MacCatalyst={StaticResource DataGridCollectionViewStyle_iOS}}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal"
                                       ItemSpacing="{OnPlatform Default=4, Android=6, WinUI=2}" />
                </CollectionView.ItemsLayout>
            </CollectionView>
        </Grid>
    </ControlTemplate>

    <Style TargetType="telerikDataGrid:DataGridGroupingPanel">
        <Setter Property="ControlTemplate" Value="{StaticResource DataGridGroupingPanelControlTemplate}" />
    </Style>

    <ControlTemplate x:Key="DataGridServicePanelControlTemplate">
        <telerikDataGrid:DataGridGroupingPanel DataGrid="{TemplateBinding DataGrid}" />
    </ControlTemplate>

    <Style TargetType="telerikDataGrid:DataGridServicePanel">
        <Setter Property="Background" Value="{DynamicResource RadDataGridGroupingPanelBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderAltColor}" />
    </Style>

    <DataTemplate x:Key="DataGridColumnReorderIndicatorTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource RadPrimaryColor}"
                                       WidthRequest="4"
                                       HorizontalOptions="Start"
                                       InputTransparent="True" />
    </DataTemplate>

    <DataTemplate x:Key="DataGridRowDetailsTemplate">
        <Label Text="{Binding}" Padding="15" />
    </DataTemplate>

    <Style x:Key="DataGridSearchPanelEntryToolbarItemViewEntryStyle" TargetType="telerikMauiControls:RadEntry">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Placeholder" Value="{telerikMauiControls:Localize DataGrid_Search_SearchEntryPlaceholder}" />
        <Setter Property="ReturnType" Value="Search" />
        <Setter Property="ClearButtonVisibility" Value="WhileEditing" />
        <Setter Property="ReserveSpaceForErrorView" Value="False" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="DataGridSearchPanelEntryToolbarItemViewEntryStyle_Desktop" TargetType="telerikMauiControls:RadEntry" BasedOn="{StaticResource DataGridSearchPanelEntryToolbarItemViewEntryStyle}">
        <Setter Property="WidthRequest" Value="200" />
    </Style>

    <Style x:Key="DataGridSearchPanelEntryToolbarItemViewStyle" TargetType="telerikMauiControls:EntryToolbarItemView">
        <Setter Property="EntryStyle" Value="{OnPlatform Default={StaticResource DataGridSearchPanelEntryToolbarItemViewEntryStyle}, MacCatalyst={StaticResource DataGridSearchPanelEntryToolbarItemViewEntryStyle_Desktop}, WinUI={StaticResource DataGridSearchPanelEntryToolbarItemViewEntryStyle_Desktop}}" />
        <Setter Property="Padding" Value="{OnPlatform Default=0, WinUI=2}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default=0, WinUI=2, MacCatalyst=2}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=222, iOS=38, WinUI=40, MacCatalyst=20}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=38, WinUI=40, MacCatalyst=20}" />
        <Setter Property="HorizontalOptions" Value="{OnPlatform Default=Fill, MacCatalyst=Start, WinUI=Start}" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="DataGridSearchPanelCloseButtonToolbarItemViewStyle" TargetType="telerikMauiControls:ButtonToolbarItemView">
        <Setter Property="HorizontalOptions" Value="End" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="DataGridSearchPanelSearchOptionsDropDownButtonToolbarItemViewStyle" TargetType="telerikMauiControls:DropDownButtonToolbarItemView">
        <Setter Property="Padding" Value="{OnPlatform Default=0, WinUI='3, 4', Android=8}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default=8, MacCatalyst=2, WinUI='8, 6'}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=14, iOS=17, MacCatalyst=13}" />
        <Setter Property="ToolTipProperties.Text" Value="{OnPlatform Default={x:Null}, MacCatalyst={Binding Source={RelativeSource Mode=Self}, Path=Text}, WinUI={Binding Source={RelativeSource Mode=Self}, Path=Text}}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, WinUI=1}" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="DisplayOptions" Value="Image" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=48, iOS=38, MacCatalyst=20, WinUI=40}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=38, MacCatalyst=20, WinUI=40}" />
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform Android=0, iOS=3, MacCatalyst=10, WinUI=2}" />
        <Setter Property="HorizontalOptions" Value="{OnPlatform Default=Start, Android=End, iOS=End}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadDataGridSearchPanelOptionsButtonPointerOverBackgroundColor}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Opened">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataGridSearchPanelSearchOptionsDropDownSeparatorStyle" TargetType="BoxView">
        <Setter Property="HeightRequest" Value="2" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBorderColor}" />
    </Style>

    <Style x:Key="DataGridSearchPanelRadioButtonStyle_Mobile" TargetType="RadioButton">
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <Grid ColumnDefinitions="Auto, *"
                      ColumnSpacing="4">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CheckedStates">
                                <VisualState x:Name="Checked">
                                    <VisualState.Setters>
                                        <Setter TargetName="check" Property="Label.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Unchecked" />
                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>
                    <Label x:Name="check"
                           Text="{x:Static telerikMauiCore:TelerikFont.IconOk}"
                           TextColor="Transparent"
                           FontFamily="{x:Static telerikMauiCore:TelerikFont.Name}"
                           FontSize="16"
                           WidthRequest="44"
                           HeightRequest="44"
                           HorizontalTextAlignment="Center"
                           VerticalTextAlignment="Center" />
                    <ContentPresenter Grid.Column="1"
                                      VerticalOptions="{TemplateBinding VerticalOptions}" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>

    <Style x:Key="DataGridSearchPanelRadioButtonStyle_Desktop" TargetType="RadioButton">
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=0, WinUI=38}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=14, MacCatalyst=13}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Padding" Value="{OnPlatform Default=0, MacCatalyst='6, 0'}" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, WinUI='4, 0, 0, 0'}" />
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <Grid ColumnDefinitions="Auto, *"
                      ColumnSpacing="{OnPlatform Default=6, WinUI=10}">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CheckedStates">
                                <VisualState x:Name="Checked">
                                    <VisualState.Setters>
                                        <Setter TargetName="outerEllipse" Property="Ellipse.Fill" Value="{DynamicResource RadPrimaryColor}" />
                                        <Setter TargetName="outerEllipse" Property="Ellipse.Stroke" Value="{DynamicResource RadPrimaryColor}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Unchecked" />
                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>
                    <Grid VerticalOptions="Center">
                        <Ellipse x:Name="outerEllipse"
                                 Stroke="{DynamicResource RadioButtonEllipseStroke}"
                                 Fill="{DynamicResource RadioButtonEllipseFill}"
                                 WidthRequest="{OnPlatform Default=16, WinUI=20}"
                                 HeightRequest="{OnPlatform Default=16, WinUI=20}"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center" />
                        <Ellipse x:Name="check"
                                 IsVisible="{TemplateBinding IsChecked}"
                                 Fill="{DynamicResource RadioButtonEllipseFill}"
                                 WidthRequest="{OnPlatform Default=6, WinUI=12}"
                                 HeightRequest="{OnPlatform Default=6, WinUI=12}"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center" />
                    </Grid>
                    <ContentPresenter Grid.Column="1"
                                      VerticalOptions="Center" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>

    <Style x:Key="DataGridSearchPanelRadioButtonStyle" TargetType="RadioButton" BasedOn="{OnPlatform Default={StaticResource DataGridSearchPanelRadioButtonStyle_Mobile}, MacCatalyst={StaticResource DataGridSearchPanelRadioButtonStyle_Desktop}, WinUI={StaticResource DataGridSearchPanelRadioButtonStyle_Desktop}}" />

    <ControlTemplate x:Key="DataGridSearchPanelControlTemplate">
        <telerikMauiControls:RadToolbar BindingContext="{TemplateBinding DataGrid.SearchSettings}"
                                        BackgroundColor="{DynamicResource RadDataGridSearchPanelBackgroundColor}"
                                        BorderBrush="{DynamicResource RadBorderAltColor}"
                                        BorderThickness="1, 1, 1, 0"
                                        ContentPadding="{OnPlatform Android='16, 0', iOS='16, 3', MacCatalyst='12, 4', WinUI='8, 2'}"
                                        OverflowMode="Clip">
            <telerikMauiControls:EntryToolbarItem PlacementOptions="ToolStrip"
                                                  Text="{Binding IntermediateSearchText}"
                                                  telerikMauiControls:EntryExtensions.CompletedCommand="{Binding SearchEntryCompletedCommand}"
                                                  Style="{StaticResource DataGridSearchPanelEntryToolbarItemViewStyle}" />
            <telerikMauiControls:OptionsButtonToolbarItem Style="{StaticResource DataGridSearchPanelSearchOptionsDropDownButtonToolbarItemViewStyle}">
                <telerikMauiControls:OptionsButtonToolbarItem.ImageSource>
                    <FontImageSource Glyph="{x:Static telerikMauiCore:TelerikFont.IconDotsVert}"
                                     FontFamily="{x:Static telerikMauiCore:TelerikFont.Name}"
                                     Size="{OnPlatform Default=16, MacCatalyst=12}" />
                </telerikMauiControls:OptionsButtonToolbarItem.ImageSource>
                <telerikMauiControls:OptionsButtonToolbarItem.DropDownContentTemplate>
                    <ControlTemplate>
                        <telerikMauiControls:DropDownButtonToolbarItemViewContent BackgroundColor="{DynamicResource RadSurfaceAltColor}"
                                                                                  BorderBrush="{DynamicResource RadBorderAltColor}"
                                                                                  ContentPadding="{OnPlatform MacCatalyst='5, 10'}">
                            <telerikMauiControls:DropDownButtonToolbarItemViewContent.Shadow>
                                <Shadow Offset="{OnPlatform Default='0, 0', MacCatalyst='0, 2', WinUI='0, 4'}"
                                        Radius="{OnPlatform Default=0, MacCatalyst=12, WinUI=8}"
                                        Brush="{OnPlatform Default=Transparent, MacCatalyst=#33000000, WinUI=#24000000}" />
                            </telerikMauiControls:DropDownButtonToolbarItemViewContent.Shadow>
                            <Grid RowDefinitions="Auto, Auto, Auto, Auto, Auto"
                                  RowSpacing="{OnPlatform MacCatalyst=8}">
                                <Grid ColumnDefinitions="Auto, *"
                                      ColumnSpacing="{OnPlatform Android=14, iOS=16, MacCatalyst=6, WinUI=7}"
                                      Padding="{OnPlatform Android=10, iOS='10, 11, 22, 11', MacCatalyst='7.5, 0', WinUI='0, 10'}"
                                      Margin="{OnPlatform WinUI='12, 0'}"
                                      telerikMauiControls:RadCheckBox.ToggleOnTap="{Binding Source={x:Reference caseSensitiveCheckBox}}">
                                    <telerikMauiControls:RadCheckBox x:Name="caseSensitiveCheckBox"
                                                                     IsChecked="{Binding CaseSensitive}" />
                                    <Label Grid.Column="1"
                                           Text="{telerikMauiControls:Localize DataGrid_Search_CaseSensitive}"
                                           TextColor="{DynamicResource RadOnAppSurfaceColor}"
                                           FontSize="{OnPlatform iOS=17, MacCatalyst=13}" />
                                </Grid>
                                <BoxView Grid.Row="1"
                                         Style="{StaticResource DataGridSearchPanelSearchOptionsDropDownSeparatorStyle}" />
                                <Grid Grid.Row="2"
                                      RowDefinitions="Auto, Auto, Auto"
                                      RowSpacing="{OnPlatform MacCatalyst=8}"
                                      Padding="{OnPlatform WinUI='8, 0'}">
                                    <RadioButton IsChecked="{Binding TextMatchMode, Converter={StaticResource AreEqualToBoolConverter}, ConverterParameter={x:Static telerikDataGrid:DataGridSearchTextMatchMode.Contains}}"
                                                 Content="{telerikMauiControls:Localize DataGrid_Search_Contains}"
                                                 Style="{StaticResource DataGridSearchPanelRadioButtonStyle}" />
                                    <RadioButton Grid.Row="1"
                                                 IsChecked="{Binding TextMatchMode, Converter={StaticResource AreEqualToBoolConverter}, ConverterParameter={x:Static telerikDataGrid:DataGridSearchTextMatchMode.StartsWith}}"
                                                 Content="{telerikMauiControls:Localize DataGrid_Search_StartsWith}"
                                                 Style="{StaticResource DataGridSearchPanelRadioButtonStyle}" />
                                    <RadioButton Grid.Row="2"
                                                 IsChecked="{Binding TextMatchMode, Converter={StaticResource AreEqualToBoolConverter}, ConverterParameter={x:Static telerikDataGrid:DataGridSearchTextMatchMode.WholeWord}}"
                                                 Content="{telerikMauiControls:Localize DataGrid_Search_WholeWord}"
                                                 Style="{StaticResource DataGridSearchPanelRadioButtonStyle}" />
                                </Grid>
                                <BoxView Grid.Row="3"
                                         Style="{StaticResource DataGridSearchPanelSearchOptionsDropDownSeparatorStyle}" />
                                <Grid Grid.Row="4"
                                      ColumnDefinitions="Auto, *"
                                      ColumnSpacing="{OnPlatform Android=14, iOS=16, MacCatalyst=6, WinUI=7}"
                                      Padding="{OnPlatform Android=10, iOS='10, 11, 22, 11', MacCatalyst='7.5, 0', WinUI='0, 10'}"
                                      Margin="{OnPlatform WinUI='12, 0'}"
                                      telerikMauiControls:RadCheckBox.ToggleOnTap="{Binding Source={x:Reference applyFilterCheckBox}}">
                                    <telerikMauiControls:RadCheckBox x:Name="applyFilterCheckBox"
                                                                     IsChecked="{Binding ApplyFilter}" />
                                    <Label Grid.Column="1"
                                           Text="{telerikMauiControls:Localize DataGrid_Search_ApplyFilter}"
                                           TextColor="{DynamicResource RadOnAppSurfaceColor}"
                                           FontSize="{OnPlatform iOS=17, MacCatalyst=13}" />
                                </Grid>
                            </Grid>
                        </telerikMauiControls:DropDownButtonToolbarItemViewContent>
                    </ControlTemplate>
                </telerikMauiControls:OptionsButtonToolbarItem.DropDownContentTemplate>
            </telerikMauiControls:OptionsButtonToolbarItem>
            <telerikMauiControls:LabelToolbarItem Text="{Binding ResultsSummary}"
                                                  IsVisible="{OnPlatform Android=False, iOS=False}" />
            <telerikMauiControls:ButtonToolbarItem PlacementOptions="ToolStrip"
                                                   Command="{Binding CloseSearchPanelCommand}"
                                                   IsVisible="{Binding IsCloseSearchPanelButtonVisible}"
                                                   Style="{StaticResource DataGridSearchPanelCloseButtonToolbarItemViewStyle}">
                <telerikMauiControls:LabelToolbarItem.ImageSource>
                    <FontImageSource Glyph="{x:Static telerikMauiCore:TelerikFont.IconCross}"
                                     FontFamily="{x:Static telerikMauiCore:TelerikFont.Name}"
                                     Size="16" />
                </telerikMauiControls:LabelToolbarItem.ImageSource>
            </telerikMauiControls:ButtonToolbarItem>
        </telerikMauiControls:RadToolbar>
    </ControlTemplate>

    <Style x:Key="DataGridFilterControlApplyFilterButtonStyle_Mobile" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource DataGridTextOnlyButtonStyle}">
        <Setter Property="TextTransform" Value="{OnPlatform Default=None, Android=Uppercase}" />
        <Setter Property="Padding" Value="{OnPlatform Default='30, 8', iOS='24, 12'}" />
    </Style>

    <Style x:Key="DataGridFilterControlApplyFilterButtonStyle_MacCatalyst" TargetType="telerikMauiControls:RadButton">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="Padding" Value="8, 2, 8, 2" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadPrimaryButtonDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataGridFilterControlApplyFilterButtonStyle_WinUI" TargetType="telerikMauiControls:RadButton">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.BackgroundColor" Value="{DynamicResource RadPrimaryColorAlpha90}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.BackgroundColor" Value="{DynamicResource RadPrimaryColorAlpha80}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadPrimaryButtonDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DataGridFilterControlApplyFilterButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{OnPlatform Default={StaticResource DataGridFilterControlApplyFilterButtonStyle_Mobile}, MacCatalyst={StaticResource DataGridFilterControlApplyFilterButtonStyle_MacCatalyst}, WinUI={StaticResource DataGridFilterControlApplyFilterButtonStyle_WinUI}}" />

    <Style x:Key="DataGridFilterControlResetFilterButtonStyle_Mobile" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource DataGridTextOnlyButtonStyle}">
        <Setter Property="TextTransform" Value="{OnPlatform Default=None, Android=Uppercase}" />
        <Setter Property="Padding" Value="{OnPlatform Default='30, 8', iOS='24, 12'}" />
    </Style>

    <Style x:Key="DataGridFilterControlResetFilterButtonStyle_MacCatalyst" TargetType="telerikMauiControls:RadButton">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderWidth" Value="0.5" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="Padding" Value="8, 2" />
        <Setter Property="Shadow">
            <Setter.Value>
                <Shadow Offset="0, 1" Radius="1.5" Brush="#33000000" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DataGridFilterControlResetFilterButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{OnPlatform Android={StaticResource DataGridFilterControlResetFilterButtonStyle_Mobile}, iOS={StaticResource DataGridFilterControlResetFilterButtonStyle_Mobile}, MacCatalyst={StaticResource DataGridFilterControlResetFilterButtonStyle_MacCatalyst}}" />

    <Style x:Key="DataGridFilterControlMatchCaseButtonStyle_iOS" TargetType="telerikMauiControls:RadToggleButton">
        <Setter Property="Background" Value="{DynamicResource RadDataGridFilterControlMatchCaseButtonBackgroundColor}" />
    </Style>

    <Style x:Key="DataGridFilterControlMatchCaseButtonStyle" TargetType="telerikMauiControls:RadToggleButton" BasedOn="{OnPlatform iOS={StaticResource DataGridFilterControlMatchCaseButtonStyle_iOS}}">
        <Setter Property="Content" Value="{x:Static telerikMauiCore:TelerikFont.IconMatchCase}" />
        <Setter Property="FontFamily" Value="{x:Static telerikMauiCore:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="WidthRequest" Value="{OnPlatform Default=44, iOS=36, MacCatalyst=30, WinUI=32}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=44, iOS=36, MacCatalyst=30, WinUI=32}" />
    </Style>

    <Style x:Key="DataGridFilterControlBorderStyle_Mobile" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataGridFilterControlBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderAltColor}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="DataGridFilterControlRootBorderStyle_Mobile" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource DataGridFilterControlBorderStyle_Mobile}">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="{OnPlatform Default=0, iOS='0, 8'}" />
    </Style>

    <Style x:Key="DataGridFilterControlSeparatorStyle_Mobile" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBorderAltColor}" />
        <Setter Property="HeightRequest" Value="1" />
    </Style>

    <Style x:Key="DataGridFilterControlTextOnlyButtonStyle_Mobile" TargetType="telerikMauiControls:RadButton">
        <Setter Property="TextTransform" Value="{OnPlatform Default=None, Android=Uppercase}" />
        <Setter Property="Padding" Value="{OnPlatform Default='30, 8', iOS='24, 12'}" />
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
    </Style>

    <Style x:Key="DataGridFilterControlFrameStyle_Desktop" TargetType="Frame">
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, WinUI=8}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataGridFilterControlBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderAltColor}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HasShadow" Value="{OnPlatform Default=True, MacCatalyst=False}" />
    </Style>

    <ControlTemplate x:Key="DataGridBooleanFilterControlControlTemplate_Mobile">
        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlRootBorderStyle_Mobile}">
            <Grid RowDefinitions="*, Auto, Auto">
                <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                  IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                  VerticalOptions="Start"
                                                                  Margin="{OnPlatform Android='16, 12', iOS=16}" />
                <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                               HorizontalOptions="End"
                                               VerticalOptions="Start"
                                               Style="{StaticResource DataGridCloseButtonStyle}" />
                <telerikMauiControls:RadBorder Grid.Row="1"
                                               Style="{StaticResource DataGridFilterControlSeparatorStyle_Mobile}" />
                <HorizontalStackLayout Grid.Row="2"
                                       HorizontalOptions="End"
                                       VerticalOptions="Center"
                                       Margin="{OnPlatform Android='0, 8, 0, 0', iOS='0, 10, 0, 0'}">
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                                   Command="{TemplateBinding ResetFilterCommand}"
                                                   Style="{StaticResource DataGridFilterControlResetFilterButtonStyle}"
                                                   Margin="4, 0" />
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                                   Command="{TemplateBinding ApplyFilterCommand}"
                                                   Style="{StaticResource DataGridFilterControlApplyFilterButtonStyle}"
                                                   Margin="4, 0, 10, 0" />
                </HorizontalStackLayout>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="DataGridBooleanFilterControlControlTemplate_Desktop">
        <Grid WidthRequest="{OnPlatform MacCatalyst=300, WinUI=232}"
              BackgroundColor="Transparent"
              Shadow="{OnPlatform WinUI={StaticResource DataGridFilterControlShadow_WinUI}}">
            <Frame Style="{StaticResource DataGridFilterControlFrameStyle_Desktop}" />
            <Grid RowDefinitions="*, Auto"
                  RowSpacing="4"
                  ColumnDefinitions="{OnPlatform MacCatalyst='*, Auto', WinUI='*, *'}"
                  ColumnSpacing="4"
                  Margin="12">
                <telerikDataGrid:DataGridDistinctValuesFilterView Grid.ColumnSpan="2"
                                                                  DistinctValues="{TemplateBinding DistinctValues}"
                                                                  IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                  Margin="{OnPlatform MacCatalyst='0, 0, 0, 10', WinUI='0, 0, 0, 8'}" />
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Grid.Column="{OnPlatform MacCatalyst=1}"
                                               Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                               Command="{TemplateBinding ApplyFilterCommand}"
                                               Style="{StaticResource DataGridFilterControlApplyFilterButtonStyle}"
                                               Margin="{OnPlatform MacCatalyst='0, 8, 0, 0'}"
                                               HorizontalOptions="{OnPlatform MacCatalyst=End}" />
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Grid.Column="{OnPlatform WinUI=1}"
                                               Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                               Command="{TemplateBinding ResetFilterCommand}"
                                               Style="{StaticResource DataGridFilterControlResetFilterButtonStyle}"
                                               Margin="{OnPlatform MacCatalyst='0, 8, 8, 0'}"
                                               HorizontalOptions="{OnPlatform MacCatalyst=End}" />
            </Grid>
            <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                           HorizontalOptions="End"
                                           VerticalOptions="Start"
                                           Style="{StaticResource DataGridCloseButtonStyle}"
                                           Margin="{OnPlatform MacCatalyst='0, 12, 12, 0', WinUI='0, 10, 12, 0'}" />
        </Grid>
    </ControlTemplate>

    <Style TargetType="telerikDataGrid:DataGridBooleanFilterControl">
        <Setter Property="ControlTemplate"
                Value="{OnPlatform Android={StaticResource DataGridBooleanFilterControlControlTemplate_Mobile},
                                   iOS={StaticResource DataGridBooleanFilterControlControlTemplate_Mobile},
                                   MacCatalyst={StaticResource DataGridBooleanFilterControlControlTemplate_Desktop},
                                   WinUI={StaticResource DataGridBooleanFilterControlControlTemplate_Desktop}}" />
    </Style>

    <ControlTemplate x:Key="DataGridDateTimeFilterControlControlTemplate_Mobile">
        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlRootBorderStyle_Mobile}">
            <Grid RowDefinitions="*, Auto, Auto">
                <ScrollView>
                    <VerticalStackLayout>
                        <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                          IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                          Margin="{OnPlatform Android='16, 12', iOS=16}" />
                        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlSeparatorStyle_Mobile}" />
                        <VerticalStackLayout Margin="{OnPlatform Android=16, iOS='16, 24'}">
                            <Label Text="{telerikMauiControls:Localize DataGrid_Filter_ShowRowsWithValueThat}"
                                   FontAttributes="Bold"
                                   Margin="{OnPlatform Android='0, 0, 0, 12', iOS='0, 0, 0, 8'}" />
                            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding FirstDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             Margin="{OnPlatform Android='0, 4', iOS='0, 8'}" />
                            <telerikMauiControls:RadDatePicker Date="{TemplateBinding FirstDescriptorValue, Mode=TwoWay}"
                                                               MinimumDate="{TemplateBinding ActualMinimumDate}"
                                                               MaximumDate="{TemplateBinding ActualMaximumDate}"
                                                               Margin="{OnPlatform Android='0, 4', iOS='0, 8'}" />
                            <telerikMauiControls:RadSegmentedControl ItemsSource="{TemplateBinding LogicalOperators}"
                                                                     SelectedIndex="{TemplateBinding LogicalOperatorIndex, Mode=TwoWay}"
                                                                     HeightRequest="{OnPlatform Android={Binding Height, Source={Reference picker}}}"
                                                                     Margin="{OnPlatform Android='0, 12'}" />
                            <telerikMauiControls:RadComboBox x:Name="picker"
                                                             ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding SecondDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             Margin="{OnPlatform iOS='0, 8'}" />
                            <telerikMauiControls:RadDatePicker Date="{TemplateBinding SecondDescriptorValue, Mode=TwoWay}"
                                                               MinimumDate="{TemplateBinding ActualMinimumDate}"
                                                               MaximumDate="{TemplateBinding ActualMaximumDate}"
                                                               Margin="{OnPlatform Android='0, 4', iOS='0, 8'}" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </ScrollView>
                <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                               HorizontalOptions="End"
                                               VerticalOptions="Start"
                                               Style="{StaticResource DataGridCloseButtonStyle}" />
                <telerikMauiControls:RadBorder Grid.Row="1"
                                               Style="{StaticResource DataGridFilterControlSeparatorStyle_Mobile}" />
                <HorizontalStackLayout Grid.Row="2"
                                       HorizontalOptions="End"
                                       VerticalOptions="Center"
                                       Margin="{OnPlatform Android='0, 8, 0, 0', iOS='0, 10, 0, 0'}">
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                                   Command="{TemplateBinding ResetFilterCommand}"
                                                   Style="{StaticResource DataGridFilterControlResetFilterButtonStyle}"
                                                   Margin="4, 0" />
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                                   Command="{TemplateBinding ApplyFilterCommand}"
                                                   Style="{StaticResource DataGridFilterControlApplyFilterButtonStyle}"
                                                   Margin="4, 0, 10, 0" />
                </HorizontalStackLayout>

            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="DataGridDateTimeFilterControlControlTemplate_Desktop">
        <Grid WidthRequest="{OnPlatform MacCatalyst=300, WinUI=232}"
              BackgroundColor="Transparent"
              Shadow="{OnPlatform WinUI={StaticResource DataGridFilterControlShadow_WinUI}}">
            <Frame Style="{StaticResource DataGridFilterControlFrameStyle_Desktop}" />
            <Grid RowDefinitions="*, Auto"
                  RowSpacing="4"
                  ColumnDefinitions="{OnPlatform MacCatalyst='*, Auto', WinUI='*, *'}"
                  ColumnSpacing="4"
                  Margin="12">
                <StackLayout Grid.ColumnSpan="2">
                    <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                      IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                      Margin="{OnPlatform MacCatalyst='0, 0, 0, 8', WinUI='0, 0, 0, 6'}" />
                    <Label Text="{telerikMauiControls:Localize DataGrid_Filter_ShowRowsWithValueThat}"
                           FontAttributes="Bold"
                           Margin="{OnPlatform MacCatalyst='0, 7', WinUI='0, 0, 0, 2'}" />
                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                     SelectedItem="{TemplateBinding FirstDescriptorOperator, Mode=TwoWay}"
                                                     IsClearButtonVisible="False"
                                                     Margin="0, 4" />
                    <telerikMauiControls:RadDatePicker Date="{TemplateBinding FirstDescriptorValue, Mode=TwoWay}"
                                                       MinimumDate="{TemplateBinding ActualMinimumDate}"
                                                       MaximumDate="{TemplateBinding ActualMaximumDate}"
                                                       Margin="0, 4" />
                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding LogicalOperators}"
                                                     SelectedItem="{TemplateBinding LogicalOperator, Mode=TwoWay}"
                                                     IsClearButtonVisible="False"
                                                     Margin="0, 4" />
                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                     SelectedItem="{TemplateBinding SecondDescriptorOperator, Mode=TwoWay}"
                                                     IsClearButtonVisible="False"
                                                     Margin="0, 4" />
                    <telerikMauiControls:RadDatePicker Date="{TemplateBinding SecondDescriptorValue, Mode=TwoWay}"
                                                       MinimumDate="{TemplateBinding ActualMinimumDate}"
                                                       MaximumDate="{TemplateBinding ActualMaximumDate}"
                                                       Margin="0, 4" />
                </StackLayout>
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Grid.Column="{OnPlatform MacCatalyst=1}"
                                               Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                               Command="{TemplateBinding ApplyFilterCommand}"
                                               Style="{StaticResource DataGridFilterControlApplyFilterButtonStyle}"
                                               HorizontalOptions="{OnPlatform MacCatalyst=End}"
                                               Margin="{OnPlatform MacCatalyst='0, 8, 0, 0'}" />
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Grid.Column="{OnPlatform WinUI=1}"
                                               Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                               Command="{TemplateBinding ResetFilterCommand}"
                                               Style="{StaticResource DataGridFilterControlResetFilterButtonStyle}"
                                               Margin="{OnPlatform MacCatalyst='0, 8, 8, 0'}"
                                               HorizontalOptions="{OnPlatform MacCatalyst=End}" />
            </Grid>
            <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                           HorizontalOptions="End"
                                           VerticalOptions="Start"
                                           Style="{StaticResource DataGridCloseButtonStyle}"
                                           Margin="{OnPlatform MacCatalyst='0, 12, 12, 0', WinUI='0, 10, 12, 0'}" />
        </Grid>
    </ControlTemplate>

    <Style TargetType="telerikDataGrid:DataGridDateTimeFilterControl">
        <Setter Property="ControlTemplate"
                Value="{OnPlatform Android={StaticResource DataGridDateTimeFilterControlControlTemplate_Mobile},
                                   iOS={StaticResource DataGridDateTimeFilterControlControlTemplate_Mobile},
                                   MacCatalyst={StaticResource DataGridDateTimeFilterControlControlTemplate_Desktop},
                                   WinUI={StaticResource DataGridDateTimeFilterControlControlTemplate_Desktop}}" />
    </Style>

    <ControlTemplate x:Key="DataGridNumericFilterControlControlTemplate_Mobile">
        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlRootBorderStyle_Mobile}">
            <Grid RowDefinitions="*, Auto, Auto">
                <ScrollView>
                    <VerticalStackLayout>
                        <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                          IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                          Margin="{OnPlatform Android='16, 12', iOS=16}" />
                        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlSeparatorStyle_Mobile}" />
                        <VerticalStackLayout Margin="{OnPlatform Android=16, iOS='16, 24'}">
                            <Label Text="{telerikMauiControls:Localize DataGrid_Filter_ShowRowsWithValueThat}"
                                   FontAttributes="Bold"
                                   Margin="{OnPlatform Android='0, 0, 0, 12', iOS='0, 0, 0, 8'}" />
                            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding FirstDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             Margin="{OnPlatform Android='0, 4', iOS='0, 8'}" />
                            <telerikMauiControls:RadNumericInput Value="{TemplateBinding FirstDescriptorValue, Mode=TwoWay}"
                                                                 Margin="{OnPlatform Android='0, 4', iOS='0, 8'}" />
                            <telerikMauiControls:RadSegmentedControl ItemsSource="{TemplateBinding LogicalOperators}"
                                                                     SelectedIndex="{TemplateBinding LogicalOperatorIndex, Mode=TwoWay}"
                                                                     HeightRequest="{OnPlatform Android={Binding Height, Source={Reference picker}}}"
                                                                     Margin="{OnPlatform Android='0, 12'}" />
                            <telerikMauiControls:RadComboBox x:Name="picker"
                                                             ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding SecondDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             Margin="{OnPlatform iOS='0, 8'}" />
                            <telerikMauiControls:RadNumericInput Value="{TemplateBinding SecondDescriptorValue, Mode=TwoWay}"
                                                                 Margin="{OnPlatform Android='0, 4', iOS='0, 8'}" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </ScrollView>
                <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                               HorizontalOptions="End"
                                               VerticalOptions="Start"
                                               Style="{StaticResource DataGridCloseButtonStyle}" />
                <telerikMauiControls:RadBorder Grid.Row="1"
                                               Style="{StaticResource DataGridFilterControlSeparatorStyle_Mobile}" />
                <HorizontalStackLayout Grid.Row="2"
                                       HorizontalOptions="End"
                                       VerticalOptions="Center"
                                       Margin="{OnPlatform Android='0, 8, 0, 0', iOS='0, 10, 0, 0'}">
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                                   Command="{TemplateBinding ResetFilterCommand}"
                                                   Style="{StaticResource DataGridFilterControlResetFilterButtonStyle}"
                                                   Margin="4, 0" />
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                                   Command="{TemplateBinding ApplyFilterCommand}"
                                                   Style="{StaticResource DataGridFilterControlApplyFilterButtonStyle}"
                                                   Margin="4, 0, 10, 0" />
                </HorizontalStackLayout>

            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="DataGridNumericFilterControlControlTemplate_Desktop">
        <Grid WidthRequest="{OnPlatform MacCatalyst=300, WinUI=232}"
              BackgroundColor="Transparent"
              Shadow="{OnPlatform WinUI={StaticResource DataGridFilterControlShadow_WinUI}}">
            <Frame Style="{StaticResource DataGridFilterControlFrameStyle_Desktop}" />
            <Grid RowDefinitions="*, Auto"
                  RowSpacing="4"
                  ColumnDefinitions="{OnPlatform MacCatalyst='*, Auto', WinUI='*, *'}"
                  ColumnSpacing="4"
                  Margin="12">
                <StackLayout Grid.ColumnSpan="2">
                    <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                      IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                      Margin="{OnPlatform MacCatalyst='0, 0, 0, 8', WinUI='0, 0, 0, 6'}" />
                    <Label Text="{telerikMauiControls:Localize DataGrid_Filter_ShowRowsWithValueThat}"
                           FontAttributes="Bold"
                           Margin="{OnPlatform MacCatalyst='0, 7', WinUI='0, 0, 0, 4'}" />
                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                     SelectedItem="{TemplateBinding FirstDescriptorOperator, Mode=TwoWay}"
                                                     IsClearButtonVisible="False"
                                                     Margin="0, 4" />
                    <telerikMauiControls:RadNumericInput Value="{TemplateBinding FirstDescriptorValue, Mode=TwoWay}"
                                                         Margin="0, 4" />
                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding LogicalOperators}"
                                                     SelectedItem="{TemplateBinding LogicalOperator, Mode=TwoWay}"
                                                     IsClearButtonVisible="False"
                                                     Margin="0, 4" />
                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                     SelectedItem="{TemplateBinding SecondDescriptorOperator, Mode=TwoWay}"
                                                     IsClearButtonVisible="False"
                                                     Margin="0, 4" />
                    <telerikMauiControls:RadNumericInput Value="{TemplateBinding SecondDescriptorValue, Mode=TwoWay}"
                                                         Margin="0, 4" />
                </StackLayout>
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Grid.Column="{OnPlatform MacCatalyst=1}"
                                               Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                               Command="{TemplateBinding ApplyFilterCommand}"
                                               Style="{StaticResource DataGridFilterControlApplyFilterButtonStyle}"
                                               HorizontalOptions="{OnPlatform MacCatalyst=End}"
                                               Margin="{OnPlatform MacCatalyst='0, 8, 0, 0'}" />
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Grid.Column="{OnPlatform WinUI=1}"
                                               Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                               Command="{TemplateBinding ResetFilterCommand}"
                                               Style="{StaticResource DataGridFilterControlResetFilterButtonStyle}"
                                               Margin="{OnPlatform MacCatalyst='0, 8, 8, 0'}"
                                               HorizontalOptions="{OnPlatform MacCatalyst=End}" />
            </Grid>
            <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                           HorizontalOptions="End"
                                           VerticalOptions="Start"
                                           Style="{StaticResource DataGridCloseButtonStyle}"
                                           Margin="{OnPlatform MacCatalyst='0, 12, 12, 0', WinUI='0, 10, 12, 0'}" />
        </Grid>
    </ControlTemplate>

    <Style TargetType="telerikDataGrid:DataGridNumericFilterControl">
        <Setter Property="ControlTemplate"
                Value="{OnPlatform Android={StaticResource DataGridNumericFilterControlControlTemplate_Mobile},
                                   iOS={StaticResource DataGridNumericFilterControlControlTemplate_Mobile},
                                   MacCatalyst={StaticResource DataGridNumericFilterControlControlTemplate_Desktop},
                                   WinUI={StaticResource DataGridNumericFilterControlControlTemplate_Desktop}}" />
    </Style>

    <ControlTemplate x:Key="DataGridTextFilterControlControlTemplate_Mobile">
        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlRootBorderStyle_Mobile}">
            <Grid RowDefinitions="*, Auto, Auto">
                <ScrollView>
                    <VerticalStackLayout>
                        <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                          IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                          Margin="{OnPlatform Android='16, 12', iOS=16}" />
                        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlSeparatorStyle_Mobile}" />
                        <VerticalStackLayout Margin="{OnPlatform Android=16, iOS='16, 24'}">
                            <Label Text="{telerikMauiControls:Localize DataGrid_Filter_ShowRowsWithValueThat}"
                                   FontAttributes="Bold"
                                   Margin="{OnPlatform Android='0, 0, 0, 12', iOS='0, 0, 0, 8'}" />
                            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding FirstDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             Margin="{OnPlatform Android='0, 4', iOS='0, 8'}" />
                            <Grid ColumnDefinitions="*, Auto"
                                  ColumnSpacing="{OnPlatform Android=8, iOS=10}"
                                  Padding="{OnPlatform Android='0, 4', iOS='0, 8'}">
                                <telerikMauiControls:RadEntry Text="{TemplateBinding FirstDescriptorValue, Mode=TwoWay}"
                                                              Placeholder="{telerikMauiControls:Localize DataGrid_FilterWatermarkText}" />
                                <telerikMauiControls:RadToggleButton Grid.Column="1"
                                                                     IsToggled="{TemplateBinding IsFirstDescriptorCaseSensitive}"
                                                                     Style="{StaticResource DataGridFilterControlMatchCaseButtonStyle}"
                                                                     HorizontalOptions="End"
                                                                     VerticalOptions="Center" />
                            </Grid>
                            <telerikMauiControls:RadSegmentedControl ItemsSource="{TemplateBinding LogicalOperators}"
                                                                     SelectedIndex="{TemplateBinding LogicalOperatorIndex, Mode=TwoWay}"
                                                                     HeightRequest="{OnPlatform Android={Binding Height, Source={Reference picker}}}"
                                                                     Margin="{OnPlatform Android='0, 12'}" />
                            <telerikMauiControls:RadComboBox x:Name="picker"
                                                             ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding SecondDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             Margin="{OnPlatform iOS='0, 8'}" />
                            <Grid ColumnDefinitions="*, Auto"
                                  ColumnSpacing="{OnPlatform Android=8, iOS=10}"
                                  Padding="{OnPlatform Android='0, 4', iOS='0, 8'}">
                                <telerikMauiControls:RadEntry Text="{TemplateBinding SecondDescriptorValue, Mode=TwoWay}"
                                                              Placeholder="{telerikMauiControls:Localize DataGrid_FilterWatermarkText}" />
                                <telerikMauiControls:RadToggleButton Grid.Column="1"
                                                                     IsToggled="{TemplateBinding IsSecondDescriptorCaseSensitive}"
                                                                     Style="{StaticResource DataGridFilterControlMatchCaseButtonStyle}"
                                                                     HorizontalOptions="End"
                                                                     VerticalOptions="Center" />
                            </Grid>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </ScrollView>
                <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                               HorizontalOptions="End"
                                               VerticalOptions="Start"
                                               Style="{StaticResource DataGridCloseButtonStyle}" />
                <telerikMauiControls:RadBorder Grid.Row="1"
                                               Style="{StaticResource DataGridFilterControlSeparatorStyle_Mobile}" />
                <HorizontalStackLayout Grid.Row="2"
                                       HorizontalOptions="End"
                                       VerticalOptions="Center"
                                       Margin="{OnPlatform Android='0, 8, 0, 0', iOS='0, 10, 0, 0'}">
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                                   Command="{TemplateBinding ResetFilterCommand}"
                                                   Style="{StaticResource DataGridFilterControlResetFilterButtonStyle}"
                                                   Margin="4, 0" />
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                                   Command="{TemplateBinding ApplyFilterCommand}"
                                                   Style="{StaticResource DataGridFilterControlApplyFilterButtonStyle}"
                                                   Margin="4, 0, 10, 0" />
                </HorizontalStackLayout>

            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="DataGridTextFilterControlControlTemplate_Desktop">
        <Grid WidthRequest="{OnPlatform MacCatalyst=300, WinUI=232}"
              BackgroundColor="Transparent"
              Shadow="{OnPlatform WinUI={StaticResource DataGridFilterControlShadow_WinUI}}">
            <Frame Style="{StaticResource DataGridFilterControlFrameStyle_Desktop}" />
            <Grid RowDefinitions="*, Auto"
                  RowSpacing="4"
                  ColumnDefinitions="{OnPlatform MacCatalyst='*, Auto', WinUI='*, *'}"
                  ColumnSpacing="4"
                  Margin="12">
                <StackLayout Grid.ColumnSpan="2">
                    <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                      IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                      Margin="0, 0, 0, 8" />
                    <Label Text="{telerikMauiControls:Localize DataGrid_Filter_ShowRowsWithValueThat}"
                           FontAttributes="Bold"
                           Margin="{OnPlatform MacCatalyst='0, 7', WinUI='0, 0, 0, 4'}" />
                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                     SelectedItem="{TemplateBinding FirstDescriptorOperator, Mode=TwoWay}"
                                                     IsClearButtonVisible="False"
                                                     Margin="0, 4" />
                    <Grid ColumnDefinitions="*, Auto"
                          ColumnSpacing="5"
                          Padding="0, 4">
                        <telerikMauiControls:RadEntry Text="{TemplateBinding FirstDescriptorValue, Mode=TwoWay}"
                                                      Placeholder="{telerikMauiControls:Localize DataGrid_FilterWatermarkText}"
                                                      ReserveSpaceForErrorView="false" />
                        <telerikMauiControls:RadToggleButton Grid.Column="1"
                                                             IsToggled="{TemplateBinding IsFirstDescriptorCaseSensitive}"
                                                             Style="{StaticResource DataGridFilterControlMatchCaseButtonStyle}"
                                                             HorizontalOptions="End"
                                                             VerticalOptions="Center" />
                    </Grid>
                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding LogicalOperators}"
                                                     SelectedItem="{TemplateBinding LogicalOperator, Mode=TwoWay}"
                                                     IsClearButtonVisible="False"
                                                     Margin="0, 4" />
                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                     SelectedItem="{TemplateBinding SecondDescriptorOperator, Mode=TwoWay}"
                                                     IsClearButtonVisible="False"
                                                     Margin="0, 4" />
                    <Grid ColumnDefinitions="*, Auto"
                          ColumnSpacing="5"
                          Padding="0, 4">
                        <telerikMauiControls:RadEntry Text="{TemplateBinding SecondDescriptorValue, Mode=TwoWay}"
                                                      Placeholder="{telerikMauiControls:Localize DataGrid_FilterWatermarkText}"
                                                      ReserveSpaceForErrorView="false" />
                        <telerikMauiControls:RadToggleButton Grid.Column="1"
                                                             IsToggled="{TemplateBinding IsSecondDescriptorCaseSensitive}"
                                                             Style="{StaticResource DataGridFilterControlMatchCaseButtonStyle}"
                                                             HorizontalOptions="End"
                                                             VerticalOptions="Center" />
                    </Grid>
                </StackLayout>
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Grid.Column="{OnPlatform MacCatalyst=1}"
                                               Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                               Command="{TemplateBinding ApplyFilterCommand}"
                                               Style="{StaticResource DataGridFilterControlApplyFilterButtonStyle}"
                                               HorizontalOptions="{OnPlatform MacCatalyst=End}"
                                               Margin="{OnPlatform MacCatalyst='0, 8, 0, 0'}" />
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Grid.Column="{OnPlatform WinUI=1}"
                                               Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                               Command="{TemplateBinding ResetFilterCommand}"
                                               Style="{StaticResource DataGridFilterControlResetFilterButtonStyle}"
                                               Margin="{OnPlatform MacCatalyst='0, 8, 8, 0'}"
                                               HorizontalOptions="{OnPlatform MacCatalyst=End}" />
            </Grid>
            <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                           HorizontalOptions="End"
                                           VerticalOptions="Start"
                                           Style="{StaticResource DataGridCloseButtonStyle}"
                                           Margin="{OnPlatform MacCatalyst='0, 12, 12, 0', WinUI='0, 10, 12, 0'}" />
        </Grid>
    </ControlTemplate>

    <Style TargetType="telerikDataGrid:DataGridTextFilterControl" ApplyToDerivedTypes="True">
        <Setter Property="ControlTemplate"
                Value="{OnPlatform Android={StaticResource DataGridTextFilterControlControlTemplate_Mobile},
                                   iOS={StaticResource DataGridTextFilterControlControlTemplate_Mobile},
                                   MacCatalyst={StaticResource DataGridTextFilterControlControlTemplate_Desktop},
                                   WinUI={StaticResource DataGridTextFilterControlControlTemplate_Desktop}}" />
    </Style>

    <ControlTemplate x:Key="DataGridTimeFilterControlControlTemplate_Mobile">
        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlRootBorderStyle_Mobile}">
            <Grid RowDefinitions="*, Auto, Auto">
                <ScrollView>
                    <VerticalStackLayout>
                        <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                          IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                          Margin="{OnPlatform Android='16, 12', iOS=16}" />
                        <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlSeparatorStyle_Mobile}" />
                        <VerticalStackLayout Margin="{OnPlatform Android=16, iOS='16, 24'}">
                            <Label Text="{telerikMauiControls:Localize DataGrid_Filter_ShowRowsWithValueThat}"
                                   FontAttributes="Bold"
                                   Margin="{OnPlatform Android='0, 0, 0, 12', iOS='0, 0, 0, 8'}" />
                            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding FirstDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             Margin="{OnPlatform Android='0, 4', iOS='0, 8'}" />
                            <telerikMauiControls:RadTimePicker Time="{TemplateBinding FirstDescriptorValue, Mode=TwoWay}"
                                                               Margin="{OnPlatform Android='0, 4', iOS='0, 8'}" />
                            <telerikMauiControls:RadSegmentedControl ItemsSource="{TemplateBinding LogicalOperators}"
                                                                     SelectedIndex="{TemplateBinding LogicalOperatorIndex, Mode=TwoWay}"
                                                                     HeightRequest="{OnPlatform Android={Binding Height, Source={Reference picker}}}"
                                                                     Margin="{OnPlatform Android='0, 12'}" />
                            <telerikMauiControls:RadComboBox x:Name="picker"
                                                             ItemsSource="{TemplateBinding DescriptorOperators}"
                                                             SelectedItem="{TemplateBinding SecondDescriptorOperator, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             Margin="{OnPlatform iOS='0, 8'}" />
                            <telerikMauiControls:RadTimePicker Time="{TemplateBinding SecondDescriptorValue, Mode=TwoWay}"
                                                               Margin="{OnPlatform Android='0, 4', iOS='0, 8'}" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </ScrollView>
                <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                               HorizontalOptions="End"
                                               VerticalOptions="Start"
                                               Style="{StaticResource DataGridCloseButtonStyle}" />
                <telerikMauiControls:RadBorder Grid.Row="1"
                                               Style="{StaticResource DataGridFilterControlSeparatorStyle_Mobile}" />
                <HorizontalStackLayout Grid.Row="2"
                                       HorizontalOptions="End"
                                       VerticalOptions="Center"
                                       Margin="{OnPlatform Android='0, 8, 0, 0', iOS='0, 10, 0, 0'}">
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                                   Command="{TemplateBinding ResetFilterCommand}"
                                                   Style="{StaticResource DataGridFilterControlResetFilterButtonStyle}"
                                                   Margin="4, 0" />
                    <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                                   Command="{TemplateBinding ApplyFilterCommand}"
                                                   Style="{StaticResource DataGridFilterControlApplyFilterButtonStyle}"
                                                   Margin="4, 0, 10, 0" />
                </HorizontalStackLayout>

            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="DataGridTimeFilterControlControlTemplate_Desktop">
        <Grid WidthRequest="{OnPlatform MacCatalyst=300, WinUI=232}"
              BackgroundColor="Transparent"
              Shadow="{OnPlatform WinUI={StaticResource DataGridFilterControlShadow_WinUI}}">
            <Frame Style="{StaticResource DataGridFilterControlFrameStyle_Desktop}" />
            <Grid RowDefinitions="*, Auto"
                  RowSpacing="4"
                  ColumnDefinitions="{OnPlatform MacCatalyst='*, Auto', WinUI='*, *'}"
                  ColumnSpacing="4"
                  Margin="12">
                <StackLayout Grid.ColumnSpan="2">
                    <telerikDataGrid:DataGridDistinctValuesFilterView DistinctValues="{TemplateBinding DistinctValues}"
                                                                      IsVisible="{TemplateBinding DistinctValues, Converter={StaticResource NullOrEmptyToBoolConverter}}"
                                                                      Margin="0, 0, 0, 8" />
                    <Label Text="{telerikMauiControls:Localize DataGrid_Filter_ShowRowsWithValueThat}"
                           FontAttributes="Bold"
                           Margin="{OnPlatform MacCatalyst='0, 7', WinUI='0, 0, 0, 4'}" />
                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                     SelectedItem="{TemplateBinding FirstDescriptorOperator, Mode=TwoWay}"
                                                     IsClearButtonVisible="False"
                                                     Margin="0, 4" />
                    <telerikMauiControls:RadTimePicker Time="{TemplateBinding FirstDescriptorValue, Mode=TwoWay}"
                                                       Margin="0, 4" />
                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding LogicalOperators}"
                                                     SelectedItem="{TemplateBinding LogicalOperator, Mode=TwoWay}"
                                                     IsClearButtonVisible="False"
                                                     Margin="0, 4" />
                    <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding DescriptorOperators}"
                                                     SelectedItem="{TemplateBinding SecondDescriptorOperator, Mode=TwoWay}"
                                                     IsClearButtonVisible="False"
                                                     Margin="0, 4" />
                    <telerikMauiControls:RadTimePicker Time="{TemplateBinding SecondDescriptorValue, Mode=TwoWay}"
                                                       Margin="0, 4" />
                </StackLayout>
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Grid.Column="{OnPlatform MacCatalyst=1}"
                                               Text="{telerikMauiControls:Localize DataGrid_Filter_ApplyFilter}"
                                               Command="{TemplateBinding ApplyFilterCommand}"
                                               Style="{StaticResource DataGridFilterControlApplyFilterButtonStyle}"
                                               HorizontalOptions="{OnPlatform MacCatalyst=End}"
                                               Margin="{OnPlatform MacCatalyst='0, 8, 0, 0'}" />
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Grid.Column="{OnPlatform WinUI=1}"
                                               Text="{telerikMauiControls:Localize DataGrid_Filter_ResetFilter}"
                                               Command="{TemplateBinding ResetFilterCommand}"
                                               Style="{StaticResource DataGridFilterControlResetFilterButtonStyle}"
                                               Margin="{OnPlatform MacCatalyst='0, 8, 8, 0'}"
                                               HorizontalOptions="{OnPlatform MacCatalyst=End}" />
            </Grid>
            <telerikMauiControls:RadButton Command="{TemplateBinding CloseCommand}"
                                           HorizontalOptions="End"
                                           VerticalOptions="Start"
                                           Style="{StaticResource DataGridCloseButtonStyle}"
                                           Margin="{OnPlatform MacCatalyst='0, 12, 12, 0', WinUI='0, 10, 12, 0'}" />
        </Grid>
    </ControlTemplate>

    <Style TargetType="telerikDataGrid:DataGridTimeFilterControl">
        <Setter Property="ControlTemplate"
                Value="{OnPlatform Android={StaticResource DataGridTimeFilterControlControlTemplate_Mobile},
                                   iOS={StaticResource DataGridTimeFilterControlControlTemplate_Mobile},
                                   MacCatalyst={StaticResource DataGridTimeFilterControlControlTemplate_Desktop},
                                   WinUI={StaticResource DataGridTimeFilterControlControlTemplate_Desktop}}" />
    </Style>

    <DataTemplate x:Key="DataGridFilterControlDistinctValueItemTemplate_Mobile">
        <VerticalStackLayout>
            <HorizontalStackLayout telerikMauiControls:RadCheckBox.ToggleOnTap="{Binding Source={x:Reference checkBox}}"
                                   Margin="{OnPlatform Android='12, 7', iOS='8, 12'}">
                <telerikMauiControls:RadCheckBox x:Name="checkBox"
                                               IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                <Label Text="{Binding Text}"
                       Margin="{OnPlatform Android='6, 0, 0, 0', iOS='8, 0, 0, 0'}"
                       VerticalOptions="Center" />
            </HorizontalStackLayout>
            <telerikMauiControls:RadBorder Style="{StaticResource DataGridFilterControlSeparatorStyle_Mobile}" />
        </VerticalStackLayout>
    </DataTemplate>

    <DataTemplate x:Key="DataGridFilterControlDistinctValueItemTemplate_Desktop">
        <Grid>
            <HorizontalStackLayout telerikMauiControls:RadCheckBox.ToggleOnTap="{Binding Source={x:Reference checkBox}}"
                                   Margin="5">
                <telerikMauiControls:RadCheckBox x:Name="checkBox"
                                               IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                <Label Text="{Binding Text}"
                       Margin="{OnPlatform MacCatalyst='4, 0, 0, 0', WinUI='8, 0, 0, 0'}" />
            </HorizontalStackLayout>
        </Grid>
    </DataTemplate>

    <ControlTemplate x:Key="DataGridFilterControlDistinctValuesViewControlTemplate_Mobile">
        <Grid RowDefinitions="{OnPlatform Android='Auto, *', iOS='Auto, Auto'}"
              RowSpacing="{OnPlatform Android=14, iOS=20}">
            <HorizontalStackLayout telerikMauiControls:RadCheckBox.ToggleOnTap="{Binding Source={x:Reference checkBox}}">
                <telerikMauiControls:RadCheckBox x:Name="checkBox"
                                                 IsChecked="{TemplateBinding SelectAll, Mode=TwoWay}" />
                <Label Text="{telerikMauiControls:Localize DataGrid_DistinctValues_SelectAll}"
                       FontAttributes="Bold"
                       Margin="{OnPlatform Android='12, 0, 0, 0', iOS='8, 0, 0, 0'}"
                       VerticalOptions="Center" />
            </HorizontalStackLayout>
            <telerikMauiControls:RadBorder IsClippedToBounds="True"
                                           Grid.Row="1"
                                           Style="{StaticResource DataGridFilterControlBorderStyle_Mobile}">
                <telerikMauiControls:RadItemsControl Background="Transparent"
                                                     ItemsSource="{TemplateBinding DistinctValues}"
                                                     ItemTemplate="{StaticResource DataGridFilterControlDistinctValueItemTemplate_Mobile}"
                                                     MinimumHeightRequest="{TemplateBinding DistinctValuesMinimumHeight}"
                                                     MaximumHeightRequest="{TemplateBinding DistinctValuesMaximumHeight}" />
            </telerikMauiControls:RadBorder>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="DataGridFilterControlDistinctValuesViewControlTemplate_Desktop">
        <Grid RowDefinitions="Auto, Auto"
              RowSpacing="{OnPlatform MacCatalyst=8, WinUI=7}">
            <HorizontalStackLayout telerikMauiControls:RadCheckBox.ToggleOnTap="{Binding Source={x:Reference checkBox}}">
                <telerikMauiControls:RadCheckBox x:Name="checkBox"
                                                 IsChecked="{TemplateBinding SelectAll, Mode=TwoWay}" />
                <Label Text="{telerikMauiControls:Localize DataGrid_DistinctValues_SelectAll}"
                       FontAttributes="Bold"
                       Margin="{OnPlatform MacCatalyst='4, 0, 0, 0', WinUI='8, 0, 0, 0'}" />
            </HorizontalStackLayout>
            <Grid Grid.Row="1"
                  IsClippedToBounds="True">
                <Frame Style="{StaticResource DataGridFilterControlFrameStyle_Desktop}"
                       BackgroundColor="{DynamicResource RadSurfaceAltColor}" />
                <telerikMauiControls:RadItemsControl Background="Transparent"
                                                     ItemsSource="{TemplateBinding DistinctValues}"
                                                     ItemTemplate="{StaticResource DataGridFilterControlDistinctValueItemTemplate_Desktop}"
                                                     MinimumHeightRequest="{TemplateBinding DistinctValuesMinimumHeight}"
                                                     MaximumHeightRequest="{TemplateBinding DistinctValuesMaximumHeight}"
                                                     Margin="6, 3" />
            </Grid>
        </Grid>
    </ControlTemplate>

    <Style TargetType="telerikDataGrid:DataGridDistinctValuesFilterView">
        <Setter Property="ControlTemplate"
                Value="{OnPlatform Android={StaticResource DataGridFilterControlDistinctValuesViewControlTemplate_Mobile},
                                   iOS={StaticResource DataGridFilterControlDistinctValuesViewControlTemplate_Mobile},
                                   MacCatalyst={StaticResource DataGridFilterControlDistinctValuesViewControlTemplate_Desktop},
                                   WinUI={StaticResource DataGridFilterControlDistinctValuesViewControlTemplate_Desktop}}" />
    </Style>

    <Style TargetType="telerikDataGrid:DataGridSearchPanel">
        <Setter Property="ControlTemplate" Value="{StaticResource DataGridSearchPanelControlTemplate}" />
    </Style>

    <Style x:Key="DataGridMouseHoverStyle" TargetType="telerikDataGrid:DataGridMouseHoverAppearance">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataGridPointerOverBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderAltColor}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, MacCatalyst=1, WinUI=1}" />
    </Style>

    <Style x:Key="DataGridSelectionStyle" TargetType="telerikDataGrid:DataGridSelectionAppearance">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataGridSelectionBackgroundColor}" />
        <Setter Property="SearchMatchBackgroundColor" Value="{DynamicResource RadDataGridSearchMatchBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadDataGridSelectionBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="DataGridCurrentCellStyle" TargetType="telerikDataGrid:DataGridCurrentCellAppearance">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="{DynamicResource RadDataGridCurrentCellBorderColor}" />
        <Setter Property="BorderThickness" Value="2" />
    </Style>

    <Style x:Key="DataGridColumnHeaderStyle" TargetType="telerikDataGrid:DataGridColumnHeaderAppearance">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataGridColumnHeaderBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderAltColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HoverBackgroundColor" Value="{DynamicResource RadDataGridPointerOverBackgroundColor}" />
        <Setter Property="IndicatorColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="FilterIndicatorTextColor" Value="{DynamicResource RadDataGridFilterIndicatorTextColor}" />
        <Setter Property="FilterIndicatorActiveTextColor" Value="{DynamicResource RadDataGridFilterIndicatorTextColor}" />
        <Setter Property="SortIndicatorColor" Value="{DynamicResource RadDataGridSortIndicatorColor}" />
    </Style>

    <Style x:Key="DataGridColumnFooterStyle" TargetType="telerikDataGrid:DataGridColumnFooterAppearance">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataGridColumnFooterBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderAltColor}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="DataGridGroupHeaderStyle" TargetType="telerikDataGrid:DataGridGroupHeaderAppearance">
        <Setter Property="ButtonTextColor" Value="{DynamicResource RadDataGridGroupHeaderButtonTextColor}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="AggregatesTextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataGridGroupHeaderBackgroundColor}" />
        <Setter Property="SearchMatchBackgroundColor" Value="{DynamicResource RadDataGridSearchMatchBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderAltColor}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="DataGridGroupFooterStyle" TargetType="telerikDataGrid:DataGridGroupFooterAppearance">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataGridGroupFooterBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderAltColor}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="DataGridFrozenColumnsSplitterStyle" TargetType="telerikDataGrid:DataGridFrozenColumnsSplitterAppearance">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataGridFrozenColumnsSplitterBackgroundColor}" />
        <Setter Property="Width" Value="4" />
    </Style>

    <Style x:Key="DataGridLoadOnDemandRowStyle" TargetType="telerikDataGrid:DataGridLoadOnDemandRowAppearance">
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="IndicatorAnimationColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataGridLoadOnDemandRowBackgroundColor}" />
    </Style>

    <Style x:Key="DataGridTypedColumnBaseStyle" TargetType="telerikDataGrid:DataGridTypedColumn" ApplyToDerivedTypes="True">
        <Setter Property="HeaderStyle" Value="{StaticResource DataGridColumnHeaderStyle}" />
        <Setter Property="FooterStyle" Value="{StaticResource DataGridColumnFooterStyle}" />
        <Setter Property="CellContentStyle">
            <Style TargetType="telerikDataGrid:DataGridTextCellAppearance">
                <Setter Property="FontSize" Value="{OnPlatform Default=14, iOS=17, MacCatalyst=17}" />
                <Setter Property="TextMargin" Value="{OnPlatform Android='8, 13', iOS='18, 14', MacCatalyst=10, WinUI=12}" />
                <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                <Setter Property="HoverTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                <Setter Property="SelectedTextColor" Value="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, Android={DynamicResource RadPrimaryColor}, iOS={DynamicResource RadPrimaryColor}}" />
                <Setter Property="SearchMatchTextColor" Value="{DynamicResource RadErrorColor}" />
            </Style>
        </Setter>
        <Setter Property="CellDecorationStyle">
            <Style TargetType="telerikDataGrid:DataGridCellDecorationAppearance">
                <Setter Property="SearchMatchBackgroundColor" Value="{DynamicResource RadDataGridSearchMatchBackgroundColor}" />
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridTextColumn" ApplyToDerivedTypes="True" BasedOn="{StaticResource DataGridTypedColumnBaseStyle}">
        <Setter Property="CellEditorStyle">
            <Style TargetType="telerikMauiControls:RadEntry">
                <Setter Property="FontSize" Value="{OnPlatform Default=14, iOS=17}" />
                <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                <Setter Property="CornerRadius" Value="{OnPlatform Default=6, WinUI=4}" />
                <Setter Property="Margin" Value="{OnPlatform Android='2, 2.5', iOS='10, 4', MacCatalyst=2, WinUI='3, 4, 3, 5'}" />
                <Setter Property="ReserveSpaceForErrorView" Value="False" />
                <Setter Property="VerticalOptions" Value="Center" />
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridBooleanColumn" ApplyToDerivedTypes="True" BasedOn="{StaticResource DataGridTypedColumnBaseStyle}">
        <Setter Property="CellEditorStyle">
            <Style TargetType="CheckBox">
                <Setter Property="Color" Value="{DynamicResource RadPrimaryColor}" />
                <Setter Property="Margin" Value="{OnPlatform Default=0, iOS='8, 0', MacCatalyst='8, 0', WinUI='7, 0, 0, 0'}" />
                <Setter Property="HorizontalOptions" Value="Start" />
                <Setter Property="VerticalOptions" Value="Center" />
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridComboBoxColumn" ApplyToDerivedTypes="True" BasedOn="{StaticResource DataGridTypedColumnBaseStyle}">
        <Setter Property="CellEditorStyle">
            <Style TargetType="telerikMauiControls:RadComboBox">
                <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                <Setter Property="Margin" Value="{OnPlatform Android='2, 2.5', iOS='10, 4', MacCatalyst=2, WinUI='3, 4, 3, 5'}" />
                <Setter Property="OpenOnFocus" Value="False" />
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="TextInputStyle">
                    <Style TargetType="telerikMauiControls:RadTextInput">
                        <Setter Property="Padding" Value="{OnPlatform Android='8, 7, 8, 13', iOS=8, MacCatalyst=6, WinUI='10, 5.5'}" />
                    </Style>
                </Setter>
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridDateColumn" ApplyToDerivedTypes="True" BasedOn="{StaticResource DataGridTypedColumnBaseStyle}">
        <Setter Property="CellEditorStyle">
            <Style TargetType="telerikMauiControls:RadDatePicker">
                <Setter Property="MinimumDate" Value="1900,1,1" />
                <Setter Property="Margin" Value="{OnPlatform Android='2, 2.5', iOS='10, 4', MacCatalyst=2, WinUI='3, 4, 3, 5'}" />
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="DisplayLabelStyle">
                    <Style TargetType="Label">
                        <Setter Property="FontSize" Value="{OnPlatform Default=14, iOS=17}" />
                        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        <Setter Property="Margin" Value="{OnPlatform Android='7, 0, 2, 3', iOS=8, MacCatalyst='6, 8', WinUI='8, 3, 9, 5'}" />
                    </Style>
                </Setter>
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridTimeColumn" ApplyToDerivedTypes="True" BasedOn="{StaticResource DataGridTypedColumnBaseStyle}">
        <Setter Property="CellEditorStyle">
            <Style TargetType="telerikMauiControls:RadTimePicker">
                <Setter Property="Margin" Value="{OnPlatform Android='2, 2.5', iOS='10, 4', MacCatalyst=2, WinUI='3, 4, 3, 5'}" />
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="DisplayStringFormat" Value="HH:mm" />
                <Setter Property="DisplayLabelStyle">
                    <Style TargetType="Label">
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                        <Setter Property="Margin" Value="{OnPlatform Android='7, 0, 2, 3', iOS=8, MacCatalyst='6, 8', WinUI='8, 3, 9, 5'}" />
                    </Style>
                </Setter>
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridNumericalColumn" ApplyToDerivedTypes="True" BasedOn="{StaticResource DataGridTypedColumnBaseStyle}">
        <Setter Property="CellEditorStyle">
            <Style TargetType="telerikMauiControls:RadNumericInput">
                <Setter Property="Margin" Value="{OnPlatform Android='2, 2.5', iOS='10, 4', MacCatalyst=2, WinUI='3, 4, 3, 5'}" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="TextInputStyle">
                    <Style TargetType="telerikMauiControls:NumericInputTextInput">
                        <Setter Property="FontSize" Value="{OnPlatform Default=14, iOS=17}" />
                        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                    </Style>
                </Setter>
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridToggleRowDetailsColumn">
        <Setter Property="HeaderStyle" Value="{StaticResource DataGridColumnHeaderStyle}" />
        <Setter Property="FooterStyle" Value="{StaticResource DataGridColumnFooterStyle}" />
        <Setter Property="CellContentStyle">
            <Style TargetType="telerikDataGrid:DataGridToggleRowDetailsCellAppearance">
                <Setter Property="ButtonFontSize" Value="{OnPlatform Default=14, iOS=17}" />
                <Setter Property="ButtonMargin" Value="{OnPlatform Android='8, 13', iOS='18, 14', MacCatalyst=10, WinUI=12}" />
                <Setter Property="ButtonTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                <Setter Property="HoverButtonTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                <Setter Property="SelectedButtonTextColor" Value="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, Android={DynamicResource RadPrimaryColor}, iOS={DynamicResource RadPrimaryColor}}" />
            </Style>
        </Setter>
    </Style>

    <Style TargetType="telerikDataGrid:DataGridTemplateColumn">
        <Setter Property="HeaderStyle" Value="{StaticResource DataGridColumnHeaderStyle}" />
        <Setter Property="FooterStyle" Value="{StaticResource DataGridColumnFooterStyle}" />
    </Style>

    <Style x:Key="RadDataGridStyle" TargetType="telerikMauiControls:RadDataGrid">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataGridBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="GridLinesColor" Value="{DynamicResource RadBorderAltColor}" />
        <Setter Property="MouseHoverStyle" Value="{StaticResource DataGridMouseHoverStyle}" />
        <Setter Property="SelectionStyle" Value="{StaticResource DataGridSelectionStyle}" />
        <Setter Property="CurrentCellStyle" Value="{StaticResource DataGridCurrentCellStyle}" />
        <Setter Property="GroupHeaderStyle" Value="{StaticResource DataGridGroupHeaderStyle}" />
        <Setter Property="GroupFooterStyle" Value="{StaticResource DataGridGroupFooterStyle}" />
        <Setter Property="GroupingPanelItemTemplate" Value="{StaticResource DataGridGroupingPanelItemTemplate}" />
        <Setter Property="FrozenColumnsSplitterStyle" Value="{StaticResource DataGridFrozenColumnsSplitterStyle}" />
        <Setter Property="LoadOnDemandRowStyle" Value="{StaticResource DataGridLoadOnDemandRowStyle}" />
        <Setter Property="ColumnHeaderDragVisualTemplate" Value="{StaticResource DataGridColumnHeaderDragVisualTemplate}" />
        <Setter Property="ColumnReorderIndicatorTemplate" Value="{StaticResource DataGridColumnReorderIndicatorTemplate}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadDataGrid" BasedOn="{StaticResource RadDataGridStyle}" />
</ResourceDictionary>