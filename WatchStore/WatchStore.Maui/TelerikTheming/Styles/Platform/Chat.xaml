<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikChat="clr-namespace:Telerik.Maui.Controls.Chat;assembly=Telerik.Maui.Controls"
                    xmlns:telerikCollectionView="clr-namespace:Telerik.Maui.Controls.CollectionView;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMauiDataControls="clr-namespace:Telerik.Maui.Controls.DataControls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikTheming="clr-namespace:Telerik.Maui.Controls.Theming;assembly=Telerik.Maui.Controls"
                    xmlns:telerikToolbar="clr-namespace:Telerik.Maui.Controls.Toolbar;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.Chat">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <!-- Dependencies: Entry.xaml, Toolbar.xaml -->

    <Style x:Key="ContentViewTemplateBorderStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BackgroundColor" Value="{TemplateBinding BackgroundColor}" />
        <Setter Property="Background" Value="{TemplateBinding Background}" />
        <Setter Property="BorderColor" Value="{TemplateBinding BorderColor}" />
        <Setter Property="BorderBrush" Value="{TemplateBinding BorderBrush}" />
        <Setter Property="BorderThickness" Value="{TemplateBinding BorderThickness}" />
        <Setter Property="CornerRadius" Value="{TemplateBinding CornerRadius}" />
        <Setter Property="Padding" Value="{TemplateBinding ContentPadding}" />
    </Style>

    <ControlTemplate x:Key="VanillaControlTemplate">
        <ContentPresenter />
    </ControlTemplate>

    <telerikMauiControls:BoolToValueConverter x:Key="ChatBoolToValueConverter" TrueValue="-1" FalseValue="0" />
    <telerikMauiControls:InvertedNullToBooleanConverter x:Key="ChatInvertedNullToBooleanConverter" />
    <telerikMauiControls:NullOrEmptyToBoolConverter x:Key="ChatNullOrEmptyToBoolConverter" />
    <telerikMauiControls:MultiBooleanConverter x:Key="ChatPickerMultiBooleanConverter" LogicalOperator="Or" />
    <telerikMauiControls:StringToBoolConverter x:Key="ChatPickerStringToBoolConverter" />

    <DataTemplate x:Key="ChatTemplateNotAvailableTemplate">
        <Label Text="N/A" TextColor="{DynamicResource RadErrorColor}" />
    </DataTemplate>

    <DataTemplate x:Key="ChatCardActionTemplate">
        <telerikChat:CardActionView x:Name="view"
                                    telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Source={x:Reference view}, Path=Parent.ActualStyleClass}"
                                    Text="{Binding Text}"
                                    Command="{Binding Command}"
                                    HeightRequest="{OnPlatform Android=36, iOS=44, MacCatalyst=28, WinUI=32}" />
    </DataTemplate>

    <Style x:Key="ChatCardLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="ChatBasicCardViewBaseStyle" TargetType="telerikChat:BasicCardView">
        <Setter Property="ContentBackgroundColor" Value="{DynamicResource RadChatPickerBackgroundColor}" />
        <Setter Property="TitleStyle" Value="{StaticResource ChatCardLabelStyle}" />
        <Setter Property="SubtitleStyle" Value="{StaticResource ChatCardLabelStyle}" />
        <Setter Property="DescriptionStyle" Value="{StaticResource ChatCardLabelStyle}" />
    </Style>

    <ControlTemplate x:Key="ChatBasicCardViewControlTemplate">
        <telerikMauiControls:RadBorder x:Name="PART_MainContainer"
                                       BorderBrush="{DynamicResource RadBorderBrush}"
                                       BorderThickness="1"
                                       CornerRadius="4"
                                       WidthRequest="280"
                                       AutomationId="RadChatBasicCard">
            <Grid RowDefinitions="Auto, Auto">
                <VerticalStackLayout Padding="16, 12">
                    <Label Text="{TemplateBinding Title}"
                           Style="{TemplateBinding ActualTitleStyle}"
                           IsVisible="{TemplateBinding Title, Converter={StaticResource ChatPickerStringToBoolConverter}}"
                           AutomationId="RadChatCardTitle" />
                    <Label Text="{TemplateBinding Subtitle}"
                           Style="{TemplateBinding ActualSubtitleStyle}"
                           IsVisible="{TemplateBinding Subtitle, Converter={StaticResource ChatPickerStringToBoolConverter}}"
                           AutomationId="RadChatCardSubtitle" />
                    <Label Text="{TemplateBinding Description}"
                           Style="{TemplateBinding ActualDescriptionStyle}"
                           IsVisible="{TemplateBinding Description, Converter={StaticResource ChatPickerStringToBoolConverter}}"
                           AutomationId="RadChatCardDescription" />
                </VerticalStackLayout>
                <telerikMauiControls:RadBorder Grid.Row="1"
                                               BorderColor="{DynamicResource RadBorderColor}"
                                               BorderThickness="0, 1, 0, 0">
                    <telerikChat:CardActionsListView ItemsSource="{TemplateBinding Actions}"
                                                     telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                                     ItemTemplate="{StaticResource ChatCardActionTemplate}"
                                                     AutomationId="RadChatCardSelectButton" />
                </telerikMauiControls:RadBorder>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style TargetType="telerikChat:BasicCardView" BasedOn="{StaticResource ChatBasicCardViewBaseStyle}" ApplyToDerivedTypes="True">
        <Setter Property="ControlTemplate" Value="{DynamicResource ChatBasicCardViewControlTemplate}" />
    </Style>

    <ControlTemplate x:Key="ChatImageCardViewControlTemplate">
        <telerikMauiControls:RadBorder x:Name="PART_MainContainer"
                                       BorderBrush="{DynamicResource RadBorderBrush}"
                                       BorderThickness="1"
                                       CornerRadius="4"
                                       WidthRequest="280"
                                       AutomationId="RadChatImageCard">
            <Grid RowDefinitions="Auto, *, Auto">
                <Image Source="{TemplateBinding Image}"
                       VerticalOptions="FillAndExpand"
                       Aspect="Fill"
                       MaximumHeightRequest="160" />
                <Grid Grid.Row="1"
                      Padding="16, 12"
                      RowDefinitions="Auto, *"
                      RowSpacing="12">
                    <VerticalStackLayout>
                        <Label Text="{TemplateBinding Title}"
                               Style="{TemplateBinding ActualTitleStyle}"
                               IsVisible="{TemplateBinding Title, Converter={StaticResource ChatPickerStringToBoolConverter}}" />
                        <Label Text="{TemplateBinding Subtitle}"
                               Style="{TemplateBinding ActualSubtitleStyle}"
                               IsVisible="{TemplateBinding Subtitle, Converter={StaticResource ChatPickerStringToBoolConverter}}" />
                    </VerticalStackLayout>
                    <Label Grid.Row="1"
                           Text="{TemplateBinding Description}"
                           LineBreakMode="WordWrap"
                           MaxLines="4"
                           Style="{TemplateBinding ActualDescriptionStyle}"
                           IsVisible="{TemplateBinding Description, Converter={StaticResource ChatPickerStringToBoolConverter}}" />
                </Grid>
                <telerikMauiControls:RadBorder Grid.Row="2"
                                               BorderColor="{DynamicResource RadBorderColor}"
                                               BorderThickness="0, 1, 0, 0">
                    <telerikChat:CardActionsListView telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                                     ItemsSource="{TemplateBinding Actions}"
                                                     ItemTemplate="{StaticResource ChatCardActionTemplate}" />
                </telerikMauiControls:RadBorder>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style TargetType="telerikChat:ImageCardView" BasedOn="{StaticResource ChatBasicCardViewBaseStyle}" ApplyToDerivedTypes="True">
        <Setter Property="ControlTemplate" Value="{DynamicResource ChatImageCardViewControlTemplate}" />
    </Style>

    <ControlTemplate x:Key="ChatCardActionViewControlTemplate">
        <Label Text="{TemplateBinding Text}"
               TextColor="{TemplateBinding TextColor}"
               HorizontalOptions="Center"
               VerticalOptions="Center">
            <Label.GestureRecognizers>
                <TapGestureRecognizer Command="{TemplateBinding Command}" />
            </Label.GestureRecognizers>
        </Label>
    </ControlTemplate>

    <Style TargetType="telerikChat:CardActionView">
        <Setter Property="ControlTemplate" Value="{StaticResource ChatCardActionViewControlTemplate}" />
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
    </Style>

    <Style x:Key="ChatPickerItemStyle" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadChatPickerBackgroundColor}" />
        <Setter Property="Padding" Value="{OnPlatform Android='16, 15', iOS='16, 13.5', MacCatalyst='6, 2.5', WinUI='16, 10.5'}" />
    </Style>

    <Style x:Key="ChatPickerSelectedItemStyle" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer" BasedOn="{StaticResource ChatPickerItemStyle}">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadChatPickerSelectedItemBackgroundColor}" />
    </Style>

    <ControlTemplate x:Key="ChatTimePickerControlTemplate">
        <telerikChat:TimePickerListView telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                        ItemsSource="{TemplateBinding Items}"
                                        ItemStyle="{StaticResource ChatPickerItemStyle}"
                                        SelectedItem="{TemplateBinding SelectedItem, Mode=TwoWay}"
                                        SelectedItemStyle="{StaticResource ChatPickerSelectedItemStyle}"
                                        AutomationId="RadChatTimePicker">
            <telerikChat:TimePickerListView.ItemTemplate>
                <DataTemplate>
                    <telerikChat:TimePickerItemLabel Time="{Binding Time}"
                                                     TimeFormat="{TemplateBinding TimeFormat}"
                                                     telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                                     VerticalOptions="Center"
                                                     AutomationId="RadChatTimePickerItem" />
                </DataTemplate>
            </telerikChat:TimePickerListView.ItemTemplate>
        </telerikChat:TimePickerListView>
    </ControlTemplate>

    <Style TargetType="telerikChat:TimePickerView">
        <Setter Property="ControlTemplate" Value="{StaticResource ChatTimePickerControlTemplate}" />
    </Style>

    <DataTemplate x:Key="ChatItemPickerTemplate">
        <telerikChat:ItemPickerView x:Name="view"
                                    WidthRequest="{OnPlatform MacCatalyst=260, WinUI=296}"
                                    HorizontalOptions="{OnIdiom Default=Fill, Desktop=Start}"
                                    ItemsSource="{Binding ItemsSource}"
                                    ItemStyle="{StaticResource ChatPickerItemStyle}"
                                    SelectionMode="{Binding SelectionMode}"
                                    SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                                    SelectedItems="{Binding SelectedItems}"
                                    SelectedItemStyle="{StaticResource ChatPickerSelectedItemStyle}"
                                    AutomationId="RadChatItemPicker" />
    </DataTemplate>

    <DataTemplate x:Key="ChatCardPickerTemplate">
        <telerikChat:CardPickerView x:Name="view"
                                    ItemsSource="{Binding Cards}"
                                    SelectionMode="None"
                                    AutomationId="RadChatBasicCardsList" />
    </DataTemplate>

    <DataTemplate x:Key="ChatDatePickerTemplate">
        <telerikChat:DatePickerView x:Name="view"
                                    BackgroundColor="{DynamicResource RadChatPickerBackgroundColor}"
                                    BorderThickness="0"
                                    CornerRadius="0"
                                    MinDate="{Binding MinDate}"
                                    MaxDate="{Binding MaxDate}"
                                    SelectedDate="{Binding SelectedDate, Mode=TwoWay}"
                                    HorizontalOptions="{OnIdiom Default=Fill, Desktop=Start}"
                                    VerticalOptions="{OnIdiom Default=Fill, Desktop=Start}"
                                    AutomationId="RadChatDatePicker" />
    </DataTemplate>

    <DataTemplate x:Key="ChatTimePickerTemplate">
        <telerikChat:TimePickerView x:Name="view"
                                    WidthRequest="{OnPlatform MacCatalyst=260, WinUI=296}"
                                    HorizontalOptions="{OnIdiom Default=Fill, Desktop=Start}"
                                    StartTime="{Binding StartTime}"
                                    EndTime="{Binding EndTime}"
                                    TimeInterval="{Binding TimeInterval}"
                                    SelectedValue="{Binding SelectedValue, Mode=TwoWay}" />
    </DataTemplate>

    <DataTemplate x:Key="ChatBasicCardTemplate">
        <telerikChat:BasicCardView x:Name="view"
                                   Title="{Binding Title}"
                                   Subtitle="{Binding Subtitle}"
                                   Description="{Binding Description}"
                                   Actions="{Binding Actions}"
                                   Margin="10, 10, 0, 10" />
    </DataTemplate>

    <DataTemplate x:Key="ChatImageCardTemplate">
        <telerikChat:ImageCardView x:Name="view"
                                   Image="{Binding Image}"
                                   Title="{Binding Title}"
                                   Subtitle="{Binding Subtitle}"
                                   Description="{Binding Description}"
                                   Actions="{Binding Actions}"
                                   Margin="10, 10, 0, 10" />
    </DataTemplate>

    <Style x:Key="ChatPickerButtonStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="{OnPlatform Android='8, 8.5', iOS='8, 13.5', MacCatalyst='8, 1.5', WinUI='8, 7'}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <Setter.Value>
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Disabled">
                            <VisualState.Setters>
                                <Setter Property="Opacity" Value="0.5" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="RadChatPickerControlTemplate">
        <telerikMauiControls:RadBorder x:Name="BorderVisual"
                                       BackgroundColor="{DynamicResource RadChatPickerBackgroundColor}"
                                       BorderColor="{DynamicResource RadBorderColor}"
                                       BorderThickness="1"
                                       CornerRadius="{OnPlatform Default=0, MacCatalyst=4}"
                                       HorizontalOptions="{OnIdiom Default=Fill, Desktop=Start}"
                                       Margin="{OnPlatform Default='0, 0, 0, 10', WinUI='0, 0, 0, 12'}">
            <Grid RowDefinitions="Auto, *, Auto">
                <telerikMauiControls:RadBorder x:Name="PART_Header"
                                               IsVisible="{TemplateBinding HeaderText, Converter={StaticResource ChatPickerStringToBoolConverter}}"
                                               BorderColor="{DynamicResource RadBorderColor}"
                                               BorderThickness="0, 0, 0, 1"
                                               Padding="{OnPlatform Android='16, 15', iOS='16, 13.5', MacCatalyst='6, 7.5', WinUI='16, 14'}">
                    <Label x:Name="PART_HeaderLabel"
                           Text="{TemplateBinding HeaderText}"
                           LineBreakMode="WordWrap"
                           IsVisible="{TemplateBinding HeaderText, Converter={StaticResource ChatPickerStringToBoolConverter}}" />
                </telerikMauiControls:RadBorder>
                <ContentPresenter Grid.Row="1" />
                <telerikMauiControls:RadBorder x:Name="PART_Footer"
                                               Grid.Row="2"
                                               BorderColor="{DynamicResource RadBorderColor}"
                                               BorderThickness="0, 1, 0, 0"
                                               Padding="{OnPlatform Android='8, 6', iOS='8, 0', MacCatalyst='0, 6', WinUI='8, 6.5'}">
                    <telerikMauiControls:RadBorder.IsVisible>
                        <MultiBinding Converter="{StaticResource ChatPickerMultiBooleanConverter}">
                            <Binding Path="IsCancelButtonVisible" Source="{RelativeSource TemplatedParent}" />
                            <Binding Path="IsOkButtonVisible" Source="{RelativeSource TemplatedParent}" />
                        </MultiBinding>
                    </telerikMauiControls:RadBorder.IsVisible>
                    <Grid ColumnDefinitions="{OnIdiom Desktop='*, *', Phone='Auto, Auto'}"
                          HorizontalOptions="{OnIdiom Desktop=Fill, Phone=End}"
                          ColumnSpacing="{OnIdiom Desktop=0, Phone=8}">
                        <telerikMauiControls:RadButton x:Name="PART_CancelButton"
                                                       Grid.Column="{OnPlatform Default=0, WinUI=1}"
                                                       telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                                       Text="Cancel"
                                                       Style="{StaticResource ChatPickerButtonStyle}"
                                                       IsVisible="{TemplateBinding IsCancelButtonVisible}"
                                                       Command="{TemplateBinding ActualCancelCommand}"
                                                       AutomationId="RadChatPickerCancelButton" />
                        <telerikMauiControls:RadButton x:Name="PART_OkButton"
                                                       Grid.Column="{OnPlatform Default=1, WinUI=0}"
                                                       telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                                       Text="Ok"
                                                       Style="{StaticResource ChatPickerButtonStyle}"
                                                       IsVisible="{TemplateBinding IsOkButtonVisible}"
                                                       Command="{TemplateBinding ActualOkCommand}"
                                                       AutomationId="RadChatPickerOkButton" />
                    </Grid>
                </telerikMauiControls:RadBorder>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <telerikChat:ChatPickerTemplateSelector x:Key="ChatPickerTemplateSelector"
                                            ItemPickerTemplate="{StaticResource ChatItemPickerTemplate}"
                                            DatePickerTemplate="{StaticResource ChatDatePickerTemplate}"
                                            TimePickerTemplate="{StaticResource ChatTimePickerTemplate}"
                                            CardPickerTemplate="{StaticResource ChatCardPickerTemplate}" />

    <Style x:Key="RadChatPickerStyle" TargetType="telerikChat:RadChatPicker">
        <Setter Property="ControlTemplate" Value="{StaticResource RadChatPickerControlTemplate}" />
        <Setter Property="TemplateSelector" Value="{StaticResource ChatPickerTemplateSelector}" />
        <Setter Property="HeaderTextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="HeaderBackgroundColor" Value="{DynamicResource RadChatPickerHeaderBackgroundColor}" />
        <Setter Property="FooterBackgroundColor" Value="{DynamicResource RadChatPickerFooterBackgroundColor}" />
        <Setter Property="OkButtonTextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="CancelButtonTextColor" Value="{DynamicResource RadPrimaryColor}" />
    </Style>
    <Style TargetType="telerikChat:RadChatPicker" BasedOn="{StaticResource RadChatPickerStyle}" />

    <Style x:Key="ChatMessageImageStyle" TargetType="Image">
        <Setter Property="WidthRequest" Value="{OnPlatform Default=24, WinUI=32}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=24, WinUI=32}" />
        <Setter Property="VerticalOptions" Value="Start" />
        <Setter Property="Aspect" Value="AspectFit" />
        <Setter Property="Source" Value="{Binding Item.Author.Avatar, Source={RelativeSource Mode=TemplatedParent}}" />
    </Style>

    <Style x:Key="ChatIncomingMessageImageStyle" TargetType="Image" BasedOn="{StaticResource ChatMessageImageStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="Margin" Value="{OnPlatform Default='0, 8, 0, 0', MacCatalyst='12, 8, 0, 0', WinUI='10, 10, 0, 0'}" />
    </Style>

    <Style x:Key="ChatIncomingTextMessageViewStyle" TargetType="telerikChat:ChatIncomingTextMessageView">
        <Setter Property="LabelStyle">
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
            </Style>
        </Setter>
        <Setter Property="BorderStyle">
            <Style TargetType="telerikMauiControls:RadBorder">
                <Setter Property="BackgroundColor" Value="{DynamicResource RadChatIncomingTextMessageBackgroundColor}" />
            </Style>
        </Setter>
    </Style>
    <Style TargetType="telerikChat:ChatIncomingTextMessageView" BasedOn="{StaticResource ChatIncomingTextMessageViewStyle}" ApplyToDerivedTypes="True" />

    <Style x:Key="ChatOutgoingTextMessageViewStyle" TargetType="telerikChat:ChatOutgoingTextMessageView">
        <Setter Property="LabelStyle">
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
            </Style>
        </Setter>
        <Setter Property="BorderStyle">
            <Style TargetType="telerikMauiControls:RadBorder">
                <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
            </Style>
        </Setter>
    </Style>
    <Style TargetType="telerikChat:ChatOutgoingTextMessageView" BasedOn="{StaticResource ChatOutgoingTextMessageViewStyle}" ApplyToDerivedTypes="True" />

    <DataTemplate x:Key="ChatOutgoingSingleTextMessageTemplate">
        <telerikChat:ChatOutgoingSingleTextMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                       Item="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="ChatOutgoingFirstTextMessageTemplate">
        <telerikChat:ChatOutgoingFirstTextMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                      Item="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="ChatOutgoingMiddleTextMessageTemplate">
        <telerikChat:ChatOutgoingMiddleTextMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                       Item="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="ChatOutgoingLastTextMessageTemplate">
        <telerikChat:ChatOutgoingLastTextMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                     Item="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="ChatIncomingSingleTextMessageTemplate">
        <telerikChat:ChatIncomingSingleTextMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                       Item="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="ChatIncomingFirstTextMessageTemplate">
        <telerikChat:ChatIncomingFirstTextMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                      Item="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="ChatIncomingMiddleTextMessageTemplate">
        <telerikChat:ChatIncomingMiddleTextMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                       Item="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="ChatIncomingLastTextMessageTemplate">
        <telerikChat:ChatIncomingLastTextMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                     Item="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="ChatPickerTemplate">
        <telerikChat:RadChatPicker telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                   Context="{Binding Context}"
                                   HeaderText="{Binding HeaderText}"
                                   IsOkButtonVisible="False"
                                   IsCancelButtonVisible="False"
                                   AutomationId="RadChatPicker" />
    </DataTemplate>

    <ControlTemplate x:Key="ChatTimeBreakViewControlTemplate">
        <Grid ColumnDefinitions="*, Auto, *"
              Padding="{OnPlatform Default='8, 10', WinUI='8, 12'}"
              Margin="{OnPlatform Default='-10, 10', WinUI='-10, 12'}">
            <BoxView x:Name="PART_LeftBorder"
                     BackgroundColor="{TemplateBinding Stroke}"
                     HeightRequest="{TemplateBinding StrokeThickness}"
                     VerticalOptions="Center" />
            <Label x:Name="PART_Label"
                   Grid.Column="1"
                   Text="{TemplateBinding Text}"
                   FontSize="{TemplateBinding FontSize}"
                   FontFamily="{TemplateBinding FontFamily}"
                   FontAttributes="{TemplateBinding FontAttributes}"
                   VerticalOptions="Center"
                   Margin="{OnPlatform Default='8, 0', WinUI='10, 0', MacCatalyst='10, 0'}" 
                   AutomationId="RadChatTimeBreakMessage" />
            <BoxView x:Name="PART_RightBorder"
                     Grid.Column="2"
                     BackgroundColor="{TemplateBinding Stroke}"
                     HeightRequest="{TemplateBinding StrokeThickness}"
                     VerticalOptions="Center" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="ChatTimeBreakViewStyle" TargetType="telerikChat:TimeBreakView">
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="Stroke" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="StrokeThickness" Value="1" />
    </Style>
    <Style TargetType="telerikChat:TimeBreakView" BasedOn="{StaticResource ChatTimeBreakViewStyle}" />

    <DataTemplate x:Key="ChatTimeBreakTemplate">
        <telerikChat:TimeBreakView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                   Text="{Binding Text}" />
    </DataTemplate>

    <Style x:Key="ChatSuggestedActionItemStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadChatSuggestedActionItemBackgroundColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, WinUI=2, iOS=8, Android=16}" />
        <Setter Property="Padding" Value="{OnPlatform Default='10, 1.5', WinUI='10, 8', iOS='8, 5.5', Android='16, 6.5'}" />
    </Style>

    <DataTemplate x:Key="ChatSuggestedActionsTemplate">
        <telerikChat:SuggestedActionsView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                          ItemsSource="{Binding Actions}"
                                          SelectionMode="None"
                                          Orientation="Horizontal">
            <telerikChat:SuggestedActionsView.ItemsLayoutTemplate>
                <DataTemplate>
                    <HorizontalStackLayout Padding="{OnPlatform Default='0, 10', WinUI=10, MacCatalyst='12, 10'}"
                                           Spacing="4" />
                </DataTemplate>
            </telerikChat:SuggestedActionsView.ItemsLayoutTemplate>
            <telerikChat:SuggestedActionsView.ItemTemplate>
                <DataTemplate>
                    <telerikMauiControls:RadButton Text="{Binding Text}"
                                                   Command="{Binding Command}"
                                                   Style="{StaticResource ChatSuggestedActionItemStyle}" 
                                                   AutomationId="RadChatSuggestedActionButton" />
                </DataTemplate>
            </telerikChat:SuggestedActionsView.ItemTemplate>
        </telerikChat:SuggestedActionsView>
    </DataTemplate>
    <telerikChat:ChatItemTemplateSelector x:Key="ChatItemTemplateSelector"
                                          SuggestedActionsTemplate="{StaticResource ChatSuggestedActionsTemplate}" />

    <ControlTemplate x:Key="ChatTypingIndicatorControlTemplate">
        <Grid RowSpacing="4"
              ColumnSpacing="4"
              Padding="{OnPlatform Default='0, 10', WinUI='0, 12'}"
              RowDefinitions="Auto, Auto"
              ColumnDefinitions="Auto, Auto">
            <Image Source="{TemplateBinding ActualImage}"
                   Grid.RowSpan="2"
                   Margin="0, 2"
                   WidthRequest="{OnPlatform Default=24, WinUI=32}"
                   HeightRequest="{OnPlatform Default=24, WinUI=32}"
                   VerticalOptions="Center"
                   Aspect="AspectFit" />
            <Label x:Name="PART_TypingIndicatorLabel"
                   Grid.Column="1" 
                   Text="{TemplateBinding ActualText}"
                   FontSize="{TemplateBinding FontSize}"
                   FontFamily="{TemplateBinding FontFamily}"
                   FontAttributes="{TemplateBinding FontAttributes}" />
            <telerikChat:TypingIndicatorDots x:Name="PART_TypingIndicatorDots"
                                             telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                             IsTyping="{TemplateBinding ActualIsTyping}"
                                             Grid.Row="1"
                                             Grid.Column="1"
                                             AutomationId="RadChatTypingIndicator" />
        </Grid>
    </ControlTemplate>

    <VisualStateGroupList x:Key="ChatEntryVisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="MouseOver" />
            <VisualState Name="Focused" />
            <VisualState Name="Disabled" />
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="ChatTypingIndicatorStyle" TargetType="telerikChat:TypingIndicator">
        <Setter Property="ControlTemplate" Value="{DynamicResource ChatTypingIndicatorControlTemplate}" />
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="DotsColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>
    <Style TargetType="telerikChat:TypingIndicator" BasedOn="{StaticResource ChatTypingIndicatorStyle}" />

    <FontImageSource x:Key="ChatSendMessageButtonImageSource"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconSendMessage}"
                     Color="{DynamicResource RadOnAppSurfaceColor}"
                     Size="{OnPlatform Default=24, WinUI=22}" />

    <Style TargetType="telerikChat:ChatEntry" BasedOn="{StaticResource RadEntryStyle}" />

    <FontImageSource x:Key="ChatInputAreaMoreButtonAttachIconImageSource"
                     FontFamily="{Static telerikMaui:TelerikFont.Name}"
                     Glyph="{Static telerikMaui:TelerikFont.IconFile}"
                     Size="16" />

    <FontImageSource x:Key="ChatInputAreaMoreButtonImageIconImageSource"
                     FontFamily="{Static telerikMaui:TelerikFont.Name}"
                     Glyph="{Static telerikMaui:TelerikFont.IconImage}"
                     Size="16" />

    <FontImageSource x:Key="ChatInputAreaMoreButtonCameraIconImageSource"
                     FontFamily="{Static telerikMaui:TelerikFont.Name}"
                     Glyph="{Static telerikMaui:TelerikFont.IconCamera}"
                     Size="16" />

    <VisualStateGroupList x:Key="ChatMoreButtonActionItemViewVisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatMoreButtonActionItemView.BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                    <Setter Property="telerikChat:ChatMoreButtonActionItemView.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatMoreButtonActionItemView.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatMoreButtonActionItemViewVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatMoreButtonActionItemView.BackgroundColor" Value="{DynamicResource RadChatMoreButtonActionPointerOverBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatMoreButtonActionItemView.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="ChatMoreButtonActionItemViewStyle" TargetType="telerikChat:ChatMoreButtonActionItemView">
        <Setter Property="ContentPadding" Value="{OnPlatform Default=0, iOS='6, 0', Android=10, WinUI='4, 0'}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="HorizontalContentOptions" Value="Start" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform iOS=40, MacCatalyst=24, WinUI=36}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform iOS=145, MacCatalyst=136, WinUI=148}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, WinUI=4}" />
        <Setter Property="ImageSpacing" Value="{OnPlatform Default=6, WinUI=8}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Default={StaticResource ChatMoreButtonActionItemViewVisualStates},
                                                                                   WinUI={StaticResource ChatMoreButtonActionItemViewVisualStates_WinUI}}" />
    </Style>
    <Style TargetType="telerikChat:ChatMoreButtonActionItemView" BasedOn="{StaticResource ChatMoreButtonActionItemViewStyle}" />

    <DataTemplate x:Key="ChatMoreButtonContentItemTemplate">
        <telerikChat:ChatMoreButtonActionItemView ImageSource="{Binding ImageSource}"
                                                  Text="{Binding Text}"
                                                  Command="{Binding Command}"
                                                  CommandParameter="{Binding CommandParameter}"
                                                  Padding="{OnPlatform Default='8, 0', WinUI=0}" />
    </DataTemplate>

    <Style x:Key="ChatMoreButtonDropDownContentStyle" TargetType="telerikMauiControls:DropDownButtonToolbarItemViewContent">
        <Setter Property="BackgroundColor" Value="{DynamicResource InputDropDownBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, iOS=12, MacCatalyst=10, WinUI=8}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default=4, MacCatalyst='4, 8'}" />
    </Style>

    <Style x:Key="ChatMoreButtonToolbarItemStyle" TargetType="telerikChat:ChatMoreButtonToolbarItemView">
        <Setter Property="DropDownVerticalOffset" Value="{OnPlatform iOS=-2, Android=-3, WinUI=-2, MacCatalyst=-9}" />
        <Setter Property="DropDownPlacement" Value="Top" />
        <Setter Property="HorizontalContentOptions" Value="Center" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="WidthRequest" Value="{OnPlatform Android=40, iOS=36, MacCatalyst=22, WinUI=32}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Android=40, iOS=36, MacCatalyst=22, WinUI=32}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=8, MacCatalyst=5}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default='8, 0, 0, 0', MacCatalyst='8, 0, 0, 0', WinUI='5, 0, 0 ,0'}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=16, MacCatalyst=12}" />
        <Setter Property="ItemTemplate" Value="{StaticResource ChatMoreButtonContentItemTemplate}" />
        <Setter Property="DropDownContentStyle" Value="{StaticResource ChatMoreButtonDropDownContentStyle}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadChatActionButtonPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Opened">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                            <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikChat:ChatMoreButtonActionItemView.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style TargetType="telerikChat:ChatMoreButtonToolbarItemView" BasedOn="{StaticResource ChatMoreButtonToolbarItemStyle}" />

    <ControlTemplate x:Key="ChatInputAreaMoreButtonViewControlTemplate">
        <telerikMauiControls:RadToolbar OverflowMode="Clip" ControlTemplate="{StaticResource VanillaControlTemplate}">
            <telerikChat:ChatMoreButtonToolbarItem ItemsSource="{Binding ActualMoreButtonActions, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadChat}}}"
                                                   ItemStyle="{StaticResource ChatMoreButtonActionItemViewStyle}"
                                                   Style="{Binding ActualMoreButtonStyle, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadChat}}}">
                <telerikChat:ChatMoreButtonToolbarItem.ImageSource>
                    <FontImageSource Glyph="{Static telerikMaui:TelerikFont.IconPlus}"
                                     FontFamily="{Static telerikMaui:TelerikFont.Name}"
                                     Size="{OnPlatform Default=16, MacCatalyst=12}" />
                </telerikChat:ChatMoreButtonToolbarItem.ImageSource>
            </telerikChat:ChatMoreButtonToolbarItem>
        </telerikMauiControls:RadToolbar>
    </ControlTemplate>
    <Style x:Key="ChatInputAreaMoreButtonViewStyle" TargetType="telerikChat:ChatInputAreaMoreButtonView">
        <Setter Property="ControlTemplate" Value="{StaticResource ChatInputAreaMoreButtonViewControlTemplate}" />
    </Style>
    <Style TargetType="telerikChat:ChatInputAreaMoreButtonView" BasedOn="{StaticResource ChatInputAreaMoreButtonViewStyle}" />

    <telerikChat:ChatFileExtensionToImageSourceConverter x:Key="ChatFileExtensionToImageSourceConverter">
        <telerikChat:ChatFileExtensionToImageSourceConverter.FallbackValue>
            <FontImageSource FontFamily="{Static telerikMaui:TelerikFont.Name}"
                             Glyph="{Static telerikMaui:TelerikFont.IconFile}"
                             Color="{DynamicResource RadOnAppSurfaceColor}"
                             Size="{OnPlatform Default=16, MacCatalyst=12}" />
        </telerikChat:ChatFileExtensionToImageSourceConverter.FallbackValue>
        <telerikChat:ChatFileExtensionToImageSourceConverterItem FileExtensions=".pptx">
            <telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
                <FontImageSource FontFamily="{Static telerikMaui:TelerikFont.Name}"
                                 Glyph="{Static telerikMaui:TelerikFont.IconFile}"
                                 Color="{DynamicResource RadOnAppSurfaceColor}"
                                 Size="{OnPlatform Default=16, MacCatalyst=12}" />
            </telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
        </telerikChat:ChatFileExtensionToImageSourceConverterItem>
        <telerikChat:ChatFileExtensionToImageSourceConverterItem FileExtensions=".doc, .docx">
            <telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
                <FontImageSource FontFamily="{Static telerikMaui:TelerikFont.Name}"
                                 Glyph="{Static telerikMaui:TelerikFont.IconFileWord}"
                                 Color="{DynamicResource RadOnAppSurfaceColor}"
                                 Size="{OnPlatform Default=16, MacCatalyst=12}" />
            </telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
        </telerikChat:ChatFileExtensionToImageSourceConverterItem>
        <telerikChat:ChatFileExtensionToImageSourceConverterItem FileExtensions=".pdf">
            <telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
                <FontImageSource FontFamily="{Static telerikMaui:TelerikFont.Name}"
                                 Glyph="{Static telerikMaui:TelerikFont.IconFilePdf}"
                                 Color="{DynamicResource RadOnAppSurfaceColor}"
                                 Size="{OnPlatform Default=16, MacCatalyst=12}" />
            </telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
        </telerikChat:ChatFileExtensionToImageSourceConverterItem>
        <telerikChat:ChatFileExtensionToImageSourceConverterItem FileExtensions=".png, .bmp, .jpg, .jpeg">
            <telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
                <FontImageSource FontFamily="{Static telerikMaui:TelerikFont.Name}"
                                 Glyph="{Static telerikMaui:TelerikFont.IconFileImage}"
                                 Color="{DynamicResource RadOnAppSurfaceColor}"
                                 Size="{OnPlatform Default=16, MacCatalyst=12}" />
            </telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
        </telerikChat:ChatFileExtensionToImageSourceConverterItem>
    </telerikChat:ChatFileExtensionToImageSourceConverter>

    <telerikChat:ChatFileExtensionToImageSourceConverter x:Key="ChatOutgoingAttachmentsFileExtensionToImageSourceConverter">
        <telerikChat:ChatFileExtensionToImageSourceConverter.FallbackValue>
            <FontImageSource FontFamily="{Static telerikMaui:TelerikFont.Name}"
                             Glyph="{Static telerikMaui:TelerikFont.IconFile}"
                             Color="{DynamicResource RadOnPrimaryColor}"
                             Size="{OnPlatform Default=16, MacCatalyst=12}" />
        </telerikChat:ChatFileExtensionToImageSourceConverter.FallbackValue>
        <telerikChat:ChatFileExtensionToImageSourceConverterItem FileExtensions=".pptx">
            <telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
                <FontImageSource FontFamily="{Static telerikMaui:TelerikFont.Name}"
                                 Glyph="{Static telerikMaui:TelerikFont.IconFile}"
                                 Color="{DynamicResource RadOnPrimaryColor}"
                                 Size="{OnPlatform Default=16, MacCatalyst=12}" />
            </telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
        </telerikChat:ChatFileExtensionToImageSourceConverterItem>
        <telerikChat:ChatFileExtensionToImageSourceConverterItem FileExtensions=".doc, .docx">
            <telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
                <FontImageSource FontFamily="{Static telerikMaui:TelerikFont.Name}"
                                 Glyph="{Static telerikMaui:TelerikFont.IconFileWord}"
                                 Color="{DynamicResource RadOnPrimaryColor}"
                                 Size="{OnPlatform Default=16, MacCatalyst=12}" />
            </telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
        </telerikChat:ChatFileExtensionToImageSourceConverterItem>
        <telerikChat:ChatFileExtensionToImageSourceConverterItem FileExtensions=".pdf">
            <telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
                <FontImageSource FontFamily="{Static telerikMaui:TelerikFont.Name}"
                                 Glyph="{Static telerikMaui:TelerikFont.IconFilePdf}"
                                 Color="{DynamicResource RadOnPrimaryColor}"
                                 Size="{OnPlatform Default=16, MacCatalyst=12}" />
            </telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
        </telerikChat:ChatFileExtensionToImageSourceConverterItem>
        <telerikChat:ChatFileExtensionToImageSourceConverterItem FileExtensions=".png, .bmp, .jpg, .jpeg">
            <telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
                <FontImageSource FontFamily="{Static telerikMaui:TelerikFont.Name}"
                                 Glyph="{Static telerikMaui:TelerikFont.IconFileImage}"
                                 Color="{DynamicResource RadOnPrimaryColor}"
                                 Size="{OnPlatform Default=16, MacCatalyst=12}" />
            </telerikChat:ChatFileExtensionToImageSourceConverterItem.ImageSource>
        </telerikChat:ChatFileExtensionToImageSourceConverterItem>
    </telerikChat:ChatFileExtensionToImageSourceConverter>

    <Style x:Key="AttachmentImageStyleBase" TargetType="Image">
        <Setter Property="WidthRequest" Value="16" />
        <Setter Property="HeightRequest" Value="16" />
    </Style>

    <Style x:Key="AttachmentTitleStyleBase" TargetType="Label">
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="LineBreakMode" Value="TailTruncation" />
        <Setter Property="Grid.Column" Value="1" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="AttachmentSubtitleStyleBase" TargetType="Label">
        <Setter Property="Grid.Row" Value="1" />
        <Setter Property="Grid.Column" Value="1" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="ChatInputAreaAttachedFileImageStyle" TargetType="Image" BasedOn="{StaticResource AttachmentImageStyleBase}">
        <Setter Property="Margin" Value="4, 0, 0, 0" />
        <Setter Property="VerticalOptions" Value="End" />
        <Setter Property="Source" Value="{Binding AttachedFile.FileName, Source={RelativeSource Mode=TemplatedParent}, Converter={StaticResource ChatFileExtensionToImageSourceConverter}}" />
    </Style>

    <Style x:Key="ChatInputAreaAttachedFileTitleStyle" TargetType="Label" BasedOn="{StaticResource AttachmentTitleStyleBase}">
        <Setter Property="Text" Value="{Binding AttachedFile.FileName, Source={RelativeSource Mode=TemplatedParent}}" />
    </Style>

    <Style x:Key="ChatInputAreaAttachedFileSubtitleStyle" TargetType="Label" BasedOn="{StaticResource AttachmentSubtitleStyleBase}">
        <Setter Property="Text" Value="{Binding AttachedFile.FileSize, Source={RelativeSource Mode=TemplatedParent}, Converter={Static telerikMauiControls:HumanReadableFileSizeConverter.Instance}}" />
    </Style>

    <VisualStateGroupList x:Key="ChatInputAreaAttachedFileRemoveButton_VisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadSubtleColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RemoveButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatInputAreaAttachedFileRemoveButton_VisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadSubtleColor}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RemoveButtonPointerOverBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha45}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RemoveButtonPressedBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="ChatInputAreaAttachedFileRemoveButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="Command" Value="{Binding RemoveAttachedFileCommand, Source={RelativeSource AncestorType={Type telerikChat:ChatInputAreaAttachedFilesView}}}" />
        <Setter Property="CommandParameter" Value="{TemplateBinding AttachedFile}" />
        <Setter Property="Grid.Column" Value="2" />
        <Setter Property="Grid.RowSpan" Value="2" />
        <Setter Property="WidthRequest" Value="{OnPlatform Default=22, Android=20, iOS=24}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=22, Android=20, iOS=24}" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=19, Android=20, MacCatalyst=18, WinUI=10}" />
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="{OnPlatform Default=0, WinUI=4}" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Margin" Value="4, 0, 0, 0" />
        <Setter Property="Content" Value="{OnPlatform Default={x:Static telerikMaui:TelerikFont.IconCancel2}, WinUI={x:Static telerikMaui:TelerikFont.IconCancel}}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Default={StaticResource ChatInputAreaAttachedFileRemoveButton_VisualStates}, WinUI={StaticResource ChatInputAreaAttachedFileRemoveButton_VisualStates_WinUI}}" />
    </Style>

    <ControlTemplate x:Key="ChatInputAreaAttachedFileItemViewControlTemplate">
        <telerikMauiControls:RadBorder Style="{StaticResource ContentViewTemplateBorderStyle}">
            <Grid RowDefinitions="Auto, Auto" 
                  ColumnDefinitions="Auto, *, Auto"
                  ColumnSpacing="6"
                  RowSpacing="{OnPlatform Default=2, iOS=4}">
                <Image Style="{TemplateBinding ActualImageStyle}" />
                <Label Style="{TemplateBinding ActualTitleStyle}" />
                <Label Style="{TemplateBinding ActualSubtitleStyle}" />
                <telerikMauiControls:RadTemplatedButton Style="{TemplateBinding ActualRemoveButtonStyle}" VerticalOptions="{OnPlatform Default=Start, MacCatalyst=Center}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="ChatInputAreaAttachedFileItemViewStyle" TargetType="telerikChat:ChatInputAreaAttachedFileItemView">
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default=8, WinUI='8, 4'}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadChatInputAreaAttachedFileItemViewBackgroundColor}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=64, MacCatalyst=52, WinUI=56}" />
        <Setter Property="ControlTemplate" Value="{StaticResource ChatInputAreaAttachedFileItemViewControlTemplate}" />
        <Setter Property="ImageStyle" Value="{StaticResource ChatInputAreaAttachedFileImageStyle}" />
        <Setter Property="TitleStyle" Value="{StaticResource ChatInputAreaAttachedFileTitleStyle}" />
        <Setter Property="SubtitleStyle" Value="{StaticResource ChatInputAreaAttachedFileSubtitleStyle}" />
        <Setter Property="RemoveButtonStyle" Value="{StaticResource ChatInputAreaAttachedFileRemoveButtonStyle}" />
    </Style>
    <Style TargetType="telerikChat:ChatInputAreaAttachedFileItemView" BasedOn="{StaticResource ChatInputAreaAttachedFileItemViewStyle}" />

    <DataTemplate x:Key="AttachedFileItemTemplate">
        <telerikChat:ChatInputAreaAttachedFileItemView AttachedFile="{Binding}" />
    </DataTemplate>

    <ControlTemplate x:Key="ChatInputAreaAttachedFilesViewControlTemplate">
        <telerikMauiControls:RadCollectionView x:Name="collectionView"
                                               ItemTemplate="{StaticResource AttachedFileItemTemplate}"
                                               ItemsSource="{TemplateBinding AttachedFiles}"
                                               SelectionMode="None">
            <telerikMauiControls:RadCollectionView.ItemsLayout>
                <telerikCollectionView:CollectionViewGridLayout SpanCount="{Binding Parent.ActualNumberOfItemsPerRow, Source={Reference collectionView}}" 
                                                                HorizontalItemSpacing="8"
                                                                VerticalItemSpacing="8" />
            </telerikMauiControls:RadCollectionView.ItemsLayout>
            <telerikMauiControls:RadCollectionView.ItemViewStyle>
                <Style TargetType="telerikMauiControls:RadCollectionViewItemView">
                    <Setter Property="CornerRadius" Value="8" />
                    <Setter Property="BorderColor" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="ControlTemplate" Value="{StaticResource VanillaControlTemplate}" />
                    <Setter Property="VisualStateManager.VisualStateGroups">
                        <VisualStateGroupList>
                            <VisualStateGroup Name="CommonStates">
                                <VisualState Name="Normal" />
                                <VisualState Name="PointerOver" />
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Property="Opacity" Value="0.5" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </Setter>
                </Style>
            </telerikMauiControls:RadCollectionView.ItemViewStyle>
        </telerikMauiControls:RadCollectionView>
    </ControlTemplate>

    <Style x:Key="ChatInputAreaAttachedFilesViewStyle" TargetType="telerikChat:ChatInputAreaAttachedFilesView">
        <Setter Property="AttachedFiles" Value="{TemplateBinding AttachedFiles}" />
        <Setter Property="RemoveAttachedFileCommand" Value="{Binding ActualRemoveAttachedFileCommand, Source={RelativeSource AncestorType={Type telerikMauiControls:RadChat}}}" />
        <Setter Property="Grid.ColumnSpan" Value="3" />
        <Setter Property="Grid.ColumnSpacing" Value="20" />
    </Style>
    <Style TargetType="telerikChat:ChatInputAreaAttachedFilesView" BasedOn="{StaticResource ChatInputAreaAttachedFilesViewStyle}" />

    <Style x:Key="ChatInputAreaSpeechToTextButtonStyle" TargetType="telerikMauiControls:RadSpeechToTextButton">
        <Setter Property="IsVisible" Value="{Binding IsSpeechToTextButtonVisible, Source={RelativeSource AncestorType={Type telerikMauiControls:RadChat}}}" />
        <Setter Property="Grid.Row" Value="2" />
        <Setter Property="Grid.Column" Value="2" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=40, iOS=36, MacCatalyst=22, WinUI=32}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=40, iOS=36, MacCatalyst=22, WinUI=32}" />
        <Setter Property="SpeechRecognizedCommand" Value="{TemplateBinding SpeechRecognizedCommand}" />
        <Setter Property="WidthRequest" Value="{OnPlatform Android=40, iOS=36, MacCatalyst=20, WinUI=32}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Android=40, iOS=36, MacCatalyst=20, WinUI=32}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=50, MacCatalyst=5, WinUI=4}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                            <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="{DynamicResource RadChatActionButtonPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Listening">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                            <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="ListeningPointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                            <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="ListeningPressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                            <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Error">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnErrorColor}" />
                            <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="{DynamicResource RadErrorColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <ControlTemplate x:Key="ChatInputAreaControlTemplate">
        <Grid BackgroundColor="{TemplateBinding BackgroundColor}"
              Padding="{TemplateBinding ContentPadding}"
              telerikMauiControls:KeyboardHelper.IsTranslationPivot="True">
            <telerikMauiControls:RadBorder BorderColor="{TemplateBinding BorderColor}"
                                           BorderBrush="{TemplateBinding BorderBrush}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="{TemplateBinding CornerRadius}"
                                           Padding="8">
                <Grid RowDefinitions="Auto, Auto"
                      RowSpacing="4">
                    <telerikChat:ChatInputAreaAttachedFilesView Style="{TemplateBinding AttachedFilesViewStyle}" />
                    <telerikChat:RadAnimatedGrid x:Name="PART_AnimatedGrid"
                                                 Grid.Row="1"
                                                 RowDefinitions="Auto, Auto, Auto"
                                                 ColumnDefinitions="Auto, *, Auto, Auto">
                        <telerikChat:ChatInputAreaMoreButtonView Grid.Row="2"
                                                                 HorizontalOptions="Start"
                                                                 IsVisible="{Binding IsMoreButtonVisible, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadChat}}}"
                                                                 telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                                                 AutomationId="RadChatInputAreaMoreButton" />
                        <telerikChat:ChatEntry x:Name="PART_ChatEntry"
                                               Grid.Row="{OnIdiom Default=2, Desktop=1}"
                                               Grid.Column="{OnIdiom Default=1, Desktop=0}"
                                               Grid.ColumnSpan="{OnIdiom Default=1, Desktop=4}"
                                               telerikChat:RadAnimatedGrid.Row="1"
                                               telerikChat:RadAnimatedGrid.Column="0"
                                               telerikChat:RadAnimatedGrid.ColumnSpan="4"
                                               Margin="{OnIdiom Default=0, Desktop='0, 0, 0, 4'}"
                                               BorderThickness="0"
                                               BorderBrush="Transparent"
                                               BackgroundColor="Transparent"
                                               MinimumHeightRequest="{OnPlatform Default=36, WinUI=32, MacCatalyst=28}"
                                               VerticalOptions="Fill"
                                               VisualStateManager.VisualStateGroups="{StaticResource ChatEntryVisualStates}"
                                               ClearButtonVisibility="Never"
                                               telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                               ReserveSpaceForErrorView="False"
                                               Keyboard="Chat"
                                               Text="{TemplateBinding Message, Mode=TwoWay}"
                                               ReturnCommand="{TemplateBinding SendMessageCommand}"
                                               telerikMauiControls:KeyboardHelper.IsTranslationSource="True"
                                               AutomationId="RadChatEntry" />
                        <telerikMauiControls:RadSpeechToTextButton x:Name="PART_SpeechToTextButton"
                                                                   telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                                                   Style="{Binding ActualSpeechToTextButtonStyle, Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadChat}}}"
                                                                   AutomationId="RadChatSpeechToTextButton" />
                        <telerikMauiControls:RadTemplatedButton Grid.Row="2"
                                                                Grid.Column="3"
                                                                telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                                                Padding="0"
                                                                Margin="4, 0, 0, 0"
                                                                Command="{TemplateBinding SendMessageCommand}"
                                                                BorderThickness="0"
                                                                BorderBrush="Transparent"
                                                                Background="{DynamicResource RadPrimaryBrush}"
                                                                VerticalTextAlignment="Center"
                                                                HorizontalTextAlignment="Center"
                                                                WidthRequest="{OnPlatform Android=40, iOS=36, MacCatalyst=22, WinUI=32}"
                                                                HeightRequest="{OnPlatform Android=40, iOS=36, MacCatalyst=22, WinUI=32}"
                                                                CornerRadius="{OnPlatform Default=20, iOS=18, MacCatalyst=5, WinUI=4}"
                                                                Content="{Binding SendMessageButtonImage.Glyph, Source={RelativeSource Mode=TemplatedParent}, FallbackValue={x:Static telerikMaui:TelerikFont.IconSendMessage}}"
                                                                FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                                                                FontSize="{OnPlatform Default=16, MacCatalyst=12}"
                                                                TextColor="{DynamicResource RadOnPrimaryColor}"
                                                                AutomationId="RadChatSendMessageButton">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroupList>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Property="Background" Value="{DynamicResource RadPrimaryHoverBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Property="Background" Value="{DynamicResource RadPrimaryActiveBrush}" />
                                                <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Property="Background" Value="Transparent" />
                                                <Setter Property="TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateGroupList>
                            </VisualStateManager.VisualStateGroups>
                        </telerikMauiControls:RadTemplatedButton>
                    </telerikChat:RadAnimatedGrid>
                </Grid>
            </telerikMauiControls:RadBorder>
        </Grid>
    </ControlTemplate>

    <Style x:Key="ChatInputAreaStyle" TargetType="telerikChat:ChatInputArea">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadChatInputAreaBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default='8', iOS='10, 8', WinUI=12}" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="ControlTemplate" Value="{StaticResource ChatInputAreaControlTemplate}" />
    </Style>
    <Style TargetType="telerikChat:ChatInputArea" BasedOn="{StaticResource ChatInputAreaStyle}" />

    <ControlTemplate x:Key="RadChatControlTemplate">
        <Grid IsClippedToBounds="True">
            <Grid telerikMauiControls:KeyboardHelper.IsTranslationTarget="True"
                  IsClippedToBounds="True"
                  RowSpacing="2"
                  RowDefinitions="*, Auto">
                <Grid RowDefinitions="*, Auto"
                      Margin="{OnPlatform Default='10, 10, 10, 0', WinUI='0, 10, 0, 0', MacCatalyst='0, 10, 0, 0'}">
                    <telerikChat:RadChatListView x:Name="PART_ChatListView"
                                                 telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                                 AutoScrollMode="{TemplateBinding AutoScrollMode}"
                                                 ScrollMediator="{TemplateBinding ActualScrollMediator}"
                                                 ItemTemplate="{TemplateBinding ItemTemplateSelector}"
                                                 ItemsSource="{TemplateBinding Items}"
                                                 AutomationId="RadChatListView" />
                    <telerikMauiControls:RadContentView telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                                        Grid.Row="1"
                                                        VerticalOptions="End"
                                                        Content="{TemplateBinding TypingIndicator}"
                                                        HeightRequest="{TemplateBinding TypingIndicator.IsVisible, Converter={StaticResource ChatBoolToValueConverter}}"
                                                        Padding="{OnPlatform Default=0, WinUI='10, 0', MacCatalyst='12, 0'}" />
                    <telerikMauiControls:RadContentView telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                                        VerticalOptions="End"
                                                        Content="{TemplateBinding Picker}"
                                                        HeightRequest="{TemplateBinding Picker.IsVisible, Converter={StaticResource ChatBoolToValueConverter}}"
                                                        MaximumHeightRequest="{Binding Height, Source={x:Reference PART_ChatListView}}"
                                                        Padding="{OnPlatform Default=0, WinUI='10, 0', MacCatalyst='12, 0'}" />
                </Grid>
                <telerikChat:ChatInputArea x:Name="PART_ChatInputArea"
                                           telerikMauiControls:StyleHelper.InheritedStyleClass="{TemplateBinding ActualStyleClass}"
                                           Grid.Row="1" 
                                           Style="{TemplateBinding InputAreaStyle}"
                                           Message="{TemplateBinding Message, Mode=TwoWay}"
                                           SendMessageCommand="{TemplateBinding ActualSendMessageCommand}"
                                           SendMessageButtonImage="{TemplateBinding SendMessageButtonImage}"
                                           AttachedFiles="{TemplateBinding AttachedFiles}"
                                           AutomationId="RadChatInputArea" />
            </Grid>
        </Grid>
    </ControlTemplate>

    <Style x:Key="ChatMessageAttachmentDropDownContentStyle" TargetType="telerikMauiControls:DropDownButtonToolbarItemViewContent">
        <Setter Property="BackgroundColor" Value="{DynamicResource InputDropDownBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, iOS=12, MacCatalyst=10, WinUI=8}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default=4, MacCatalyst='4, 8'}" />
    </Style>

    <VisualStateGroupList x:Key="ChatIncomingMessageAttachmentsDropDownMenuButtonVisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver" />
            <VisualState Name="Opened" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatIncomingMessageAttachmentsDropDownMenuButtonVisualStates_iOS">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver" />
            <VisualState Name="Opened">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatIncomingMessageAttachmentsDropDownMenuButtonVisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Opened">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatIncomingMessageAttachmentsDropDownMenuButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Opened">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.BackgroundColor" Value="{DynamicResource RadBaseActiveColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatOutgoingMessageAttachmentsDropDownMenuButtonVisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver" />
            <VisualState Name="Opened" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha60}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatOutgoingMessageAttachmentsDropDownMenuButtonVisualStates_iOS">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver" />
            <VisualState Name="Opened">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha60}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha60}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatOutgoingMessageAttachmentsDropDownMenuButtonVisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.BackgroundColor" Value="{DynamicResource RadPrimaryHoverColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Opened">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha60}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha60}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatOutgoingMessageAttachmentsDropDownMenuButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.BackgroundColor" Value="{DynamicResource RadPrimaryHoverColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Opened">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.BackgroundColor" Value="{DynamicResource RadPrimaryActiveColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikChat:ChatDropDownMenuButtonToolbarItemView.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha60}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="ChatMessageAttachmentDropDownMenuButtonStyle" TargetType="telerikChat:ChatDropDownMenuButtonToolbarItemView">
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=30, MacCatalyst=0, WinUI=32}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=30, MacCatalyst=20, WinUI=32}" />
        <Setter Property="Margin" Value="16, 0, 0, 0" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="DropDownContentStyle" Value="{StaticResource ChatMessageAttachmentDropDownContentStyle}" />
    </Style>

    <Style x:Key="IncomingChatMessageAttachmentDropDownMenuButtonStyle" TargetType="telerikChat:ChatDropDownMenuButtonToolbarItemView" BasedOn="{StaticResource ChatMessageAttachmentDropDownMenuButtonStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Android={StaticResource ChatIncomingMessageAttachmentsDropDownMenuButtonVisualStates_Android},
                                                                                   iOS={StaticResource ChatIncomingMessageAttachmentsDropDownMenuButtonVisualStates_iOS},
                                                                                   MacCatalyst={StaticResource ChatIncomingMessageAttachmentsDropDownMenuButtonVisualStates_MacCatalyst},
                                                                                   WinUI={StaticResource ChatIncomingMessageAttachmentsDropDownMenuButtonVisualStates_WinUI}}" />
    </Style>

    <Style x:Key="OutgoingChatMessageAttachmentDropDownMenuButtonStyle" TargetType="telerikChat:ChatDropDownMenuButtonToolbarItemView" BasedOn="{StaticResource ChatMessageAttachmentDropDownMenuButtonStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Android={StaticResource ChatOutgoingMessageAttachmentsDropDownMenuButtonVisualStates_Android},
                                                                                   iOS={StaticResource ChatOutgoingMessageAttachmentsDropDownMenuButtonVisualStates_iOS},
                                                                                   MacCatalyst={StaticResource ChatOutgoingMessageAttachmentsDropDownMenuButtonVisualStates_MacCatalyst},
                                                                                   WinUI={StaticResource ChatOutgoingMessageAttachmentsDropDownMenuButtonVisualStates_WinUI}}" />
    </Style>

    <FontImageSource x:Key="ChatMessageAttachmentToolbarButtonImageSource"
                     FontFamily="{Static telerikMaui:TelerikFont.Name}"
                     Glyph="{Static telerikMaui:TelerikFont.IconDotsVert}"
                     Size="{OnPlatform Default=16, MacCatalyst=12}" />
    <FontImageSource x:Key="ChatMessageAttachmentDownloadIconImageSource"
                     FontFamily="{Static telerikMaui:TelerikFont.Name}"
                     Glyph="{Static telerikMaui:TelerikFont.IconDownload}"
                     Size="{OnPlatform Default=16, MacCatalyst=12}" />
    <FontImageSource x:Key="ChatMessageAttachmentShareIconImageSource"
                     FontFamily="{Static telerikMaui:TelerikFont.Name}"
                     Glyph="{Static telerikMaui:TelerikFont.IconShare}"
                     Size="{OnPlatform Default=16, MacCatalyst=12}"/>

    <VisualStateGroupList x:Key="ChatMessageAttachmentButtonToolbarItemViewVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:ButtonToolbarItemView.BackgroundColor" Value="{DynamicResource RadChatMoreButtonActionPointerOverBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:ButtonToolbarItemView.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatMessageAttachmentButtonToolbarItemViewVisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:ButtonToolbarItemView.BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                    <Setter Property="telerikMauiControls:ButtonToolbarItemView.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:ButtonToolbarItemView.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="ChatMessageAttachmentButtonToolbarItemViewBaseStyle" TargetType="telerikMauiControls:ButtonToolbarItemView" >
        <Setter Property="ContentPadding" Value="{OnPlatform Android='10, 0', iOS='16, 0', MacCatalyst='8, 0', WinUI='8, 0'}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=40, iOS=40, MacCatalyst=24, WinUI=36}" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="HorizontalContentOptions" Value="Start" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, WinUI=4}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="ImageSpacing" Value="8" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Android={StaticResource ChatMessageAttachmentButtonToolbarItemViewVisualStates}, 
                                                                                   iOS={StaticResource ChatMessageAttachmentButtonToolbarItemViewVisualStates},
                                                                                   MacCatalyst={StaticResource ChatMessageAttachmentButtonToolbarItemViewVisualStates},
                                                                                   WinUI={StaticResource ChatMessageAttachmentButtonToolbarItemViewVisualStates_WinUI}}" />
    </Style>

    <ControlTemplate x:Key="ChatMessageAttachmentActionsViewControlTemplate">
        <telerikMauiControls:RadToolbar OverflowMode="Clip" ControlTemplate="{StaticResource VanillaControlTemplate}">
            <telerikChat:ChatDropDownMenuButtonToolbarItem ImageSource="{StaticResource ChatMessageAttachmentToolbarButtonImageSource}"
                                                           Style="{Binding ActualMenuButtonStyle, Source={RelativeSource AncestorType={x:Type telerikChat:ChatMessageAttachmentActionsView}}}">
                <telerikMauiControls:ButtonToolbarItem ImageSource="{StaticResource ChatMessageAttachmentDownloadIconImageSource}"
                                                       Text="{telerikMauiControls:Localize Chat_MessageAttachmentAction_Download}"
                                                       Command="{Binding ActualDownloadAttachmentsCommand, Source={RelativeSource AncestorType={Type telerikMauiControls:RadChat}}}"
                                                       CommandParameter="{Binding Attachment, Source={RelativeSource AncestorType={Type telerikChat:ChatMessageAttachmentActionsView}}}"
                                                       Style="{StaticResource ChatMessageAttachmentButtonToolbarItemViewBaseStyle}" />
                <telerikMauiControls:ButtonToolbarItem ImageSource="{StaticResource ChatMessageAttachmentShareIconImageSource}"
                                                       Text="{telerikMauiControls:Localize Chat_MessageAttachmentAction_Share}"
                                                       Command="{Binding ActualShareAttachmentsCommand, Source={RelativeSource AncestorType={Type telerikMauiControls:RadChat}}}"
                                                       CommandParameter="{Binding Attachment, Source={RelativeSource AncestorType={Type telerikChat:ChatMessageAttachmentActionsView}}}"
                                                       Style="{StaticResource ChatMessageAttachmentButtonToolbarItemViewBaseStyle}" />
            </telerikChat:ChatDropDownMenuButtonToolbarItem>
        </telerikMauiControls:RadToolbar>
    </ControlTemplate>

    <Style x:Key="ChatMessageAttachmentActionsViewStyleBase" TargetType="telerikChat:ChatMessageAttachmentActionsView">
        <Setter Property="Attachment" Value="{Binding Attachment, Source={RelativeSource Mode=TemplatedParent}}" />
        <Setter Property="Grid.RowSpan" Value="2" />
        <Setter Property="Grid.Column" Value="2" />
    </Style>

    <Style TargetType="telerikChat:ChatMessageAttachmentActionsView" BasedOn="{StaticResource ChatMessageAttachmentActionsViewStyleBase}">
        <Setter Property="ControlTemplate" Value="{StaticResource ChatMessageAttachmentActionsViewControlTemplate}" />
    </Style>

    <Style x:Key="ChatMessageAttachmentTitleStyleBase" TargetType="Label" BasedOn="{StaticResource AttachmentTitleStyleBase}">
        <Setter Property="Text" Value="{Binding Attachment.FileName, Source={RelativeSource Mode=TemplatedParent}}" />
    </Style>

    <Style x:Key="ChatMessageAttachmentSubtitleStyleBase" TargetType="Label" BasedOn="{StaticResource AttachmentSubtitleStyleBase}">
        <Setter Property="Text" Value="{Binding Attachment.FileSize, Source={RelativeSource Mode=TemplatedParent}, Converter={Static telerikMauiControls:HumanReadableFileSizeConverter.Instance}}" />
    </Style>

    <ControlTemplate x:Key="ChatMessageAttachmentViewControlTemplate">
        <Grid RowDefinitions="Auto, Auto"
              ColumnDefinitions="Auto, *, Auto"
              RowSpacing="3"
              ColumnSpacing="6">
            <Image Style="{TemplateBinding ActualImageStyle}" />
            <Label Style="{TemplateBinding ActualTitleStyle}" />
            <Label Style="{TemplateBinding ActualSubtitleStyle}" />
            <telerikChat:ChatMessageAttachmentActionsView Style="{TemplateBinding ActualActionsViewStyle}" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="ChatMessageAttachmentViewStyle" TargetType="telerikChat:ChatMessageAttachmentView">
        <Setter Property="ControlTemplate" Value="{StaticResource ChatMessageAttachmentViewControlTemplate}" />
    </Style>

    <Style TargetType="telerikChat:ChatMessageAttachmentView" BasedOn="{StaticResource ChatMessageAttachmentViewStyle}" ApplyToDerivedTypes="True" />

    <Style x:Key="IncomingAttachmentImageStyle" TargetType="Image" BasedOn="{StaticResource AttachmentImageStyleBase}">
        <Setter Property="Source" Value="{Binding Attachment.FileName, Source={RelativeSource Mode=TemplatedParent}, Converter={StaticResource ChatFileExtensionToImageSourceConverter}}" />
    </Style>

    <Style x:Key="IncomingAttachmentTitleStyle" TargetType="Label" BasedOn="{StaticResource ChatMessageAttachmentTitleStyleBase}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="IncomingAttachmentSubtitleStyle" TargetType="Label" BasedOn="{StaticResource ChatMessageAttachmentSubtitleStyleBase}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="IncomingAttachmentActionsViewStyle" TargetType="telerikChat:ChatMessageAttachmentActionsView" BasedOn="{StaticResource ChatMessageAttachmentActionsViewStyleBase}">
        <Setter Property="MenuButtonStyle" Value="{StaticResource IncomingChatMessageAttachmentDropDownMenuButtonStyle}" />
    </Style>

    <Style x:Key="IncomingMessageAttachmentStyle" TargetType="telerikChat:ChatMessageAttachmentView" BasedOn="{StaticResource ChatMessageAttachmentViewStyle}">
        <Setter Property="ImageStyle" Value="{StaticResource IncomingAttachmentImageStyle}" />
        <Setter Property="TitleStyle" Value="{StaticResource IncomingAttachmentTitleStyle}" />
        <Setter Property="SubtitleStyle" Value="{StaticResource IncomingAttachmentSubtitleStyle}" />
        <Setter Property="ActionsViewStyle" Value="{StaticResource IncomingAttachmentActionsViewStyle}" />
    </Style>

    <VisualStateGroupList x:Key="ChatIncomingMessageAttachmentsDownloadAllButtonVisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatIncomingMessageAttachmentsDownloadAllButtonVisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatIncomingMessageAttachmentsDownloadAllButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnBaseColorAlpha61}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.BorderBrush" Value="{DynamicResource RadToggleButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseActiveBrush}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.BorderBrush" Value="{DynamicResource RadToggleButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatOutgoingMessageAttachmentsDownloadAllButtonVisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha80}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha50}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatOutgoingMessageAttachmentsDownloadAllButtonVisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha50}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="ChatOutgoingMessageAttachmentsDownloadAllButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadPrimaryHoverBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha80}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadPrimaryActiveBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha50}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <telerikMauiControls:ArithmeticComparisonConverter x:Key="GreaterThan1Converter" Operator=">" SecondOperand="1" />

    <DataTemplate x:Key="MessageAttachmentDataTemplate">
        <telerikChat:ChatMessageAttachmentView Attachment="{Binding}" 
                                               Style="{Binding ActualAttachmentStyle, Source={RelativeSource AncestorType={x:Type telerikChat:ChatMessageAttachmentsListView}}}" />
    </DataTemplate>

    <ControlTemplate x:Key="ChatMessageAttachmentsListViewControlTemplate">
        <VerticalStackLayout>
            <VerticalStackLayout BindableLayout.ItemTemplate="{StaticResource MessageAttachmentDataTemplate}"
                                 BindableLayout.ItemsSource="{TemplateBinding Item.Attachments}"
                                 Spacing="{OnPlatform Android=2, iOS=6, MacCatalyst=8, WinUI=8}" />
            <telerikMauiControls:RadTemplatedButton Content="{telerikMauiControls:Localize Chat_MessageAttachmentsListView_DownloadAll}" 
                                                    Command="{TemplateBinding Item.Chat.ActualDownloadAttachmentsCommand}"
                                                    CommandParameter="{Binding Item.Attachments, Source={RelativeSource Mode=TemplatedParent}}"
                                                    Style="{TemplateBinding ActualDownloadAllButtonStyle}"
                                                    IsVisible="{Binding Item.Attachments.Count, Source={RelativeSource Mode=TemplatedParent}, Converter={StaticResource GreaterThan1Converter}, TargetNullValue=False, FallbackValue=False}" />
        </VerticalStackLayout>
    </ControlTemplate>

    <Style x:Key="ChatMessageAttachmentsListViewStyle" TargetType="telerikChat:ChatMessageAttachmentsListView">
        <Setter Property="ControlTemplate" Value="{StaticResource ChatMessageAttachmentsListViewControlTemplate}" />
    </Style>

    <Style TargetType="telerikChat:ChatMessageAttachmentsListView" BasedOn="{StaticResource ChatMessageAttachmentsListViewStyle}" />

    <Style x:Key="IncomingMessageBorderStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadChatIncomingMessageBackgroundColor}" />
        <Setter Property="Padding" Value="{OnPlatform Default='10, 8', WinUI='8'}" />
        <Setter Property="Margin" Value="{OnPlatform Default='28, 2, 0, 2', MacCatalyst='39, 2, 0, 2', WinUI='46, 2, 0, 2'}" />
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>

    <Style x:Key="MessageLabelStyle" TargetType="Label">
        <Setter Property="LineBreakMode" Value="WordWrap" />
    </Style>

    <Style x:Key="IncomingMessageLabelStyle" TargetType="Label" BasedOn="{StaticResource MessageLabelStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadChatIncomingMessageTextColor}" />
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>

    <ControlTemplate x:Key="ChatIncomingFirstAndSingleTextMessageViewControlTemplate">
        <Grid ColumnDefinitions="{OnIdiom Default='8*, 2*', Desktop='7*, 3*'}">
            <telerikMauiControls:RadLayout>
                <Image Style="{TemplateBinding ActualImageStyle}" />
                <telerikMauiControls:RadBorder Style="{TemplateBinding ActualBorderStyle}">
                    <Label Text="{TemplateBinding Item.Text}" Style="{TemplateBinding ActualLabelStyle}" />
                </telerikMauiControls:RadBorder>
            </telerikMauiControls:RadLayout>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="ChatIncomingFirstAndSingleAttachmentsMessageViewControlTemplate">
        <Grid ColumnDefinitions="{OnIdiom Default='8*, 2*', Desktop='7*, 3*'}">
            <telerikMauiControls:RadLayout>
                <Image Style="{TemplateBinding ActualImageStyle}" />
                <telerikMauiControls:RadBorder Style="{TemplateBinding ActualBorderStyle}">
                    <VerticalStackLayout Spacing="{OnPlatform Default=12, WinUI=16}">
                        <Label Text="{TemplateBinding Item.Text}" Style="{TemplateBinding ActualLabelStyle}" IsVisible="{TemplateBinding Item.Text, Converter={StaticResource ChatNullOrEmptyToBoolConverter}}" />
                        <telerikChat:ChatMessageAttachmentsListView Item="{TemplateBinding Item}" Style="{TemplateBinding ActualAttachmentsListViewStyle}" />
                    </VerticalStackLayout>
                </telerikMauiControls:RadBorder>
            </telerikMauiControls:RadLayout>
        </Grid>
    </ControlTemplate>

    <Style x:Key="IncomingFirstMessageBorderStyle" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource IncomingMessageBorderStyle}">
        <Setter Property="CornerRadius" Value="8, 8, 8, 0" />
        <Setter Property="Margin" Value="{OnPlatform Default='28, 8, 0, 2', MacCatalyst='39, 8, 0, 2', WinUI='46, 10, 0, 2'}" />
    </Style>

    <DataTemplate x:Key="ChatIncomingFirstAttachmentsMessageTemplate">
        <telerikMauiControls:RadLayout>
            <telerikChat:ChatIncomingFirstAttachmentsMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                                 Item="{Binding}" />
        </telerikMauiControls:RadLayout>
    </DataTemplate>

    <ControlTemplate x:Key="ChatIncomingMiddleAndLastTextMessageViewControlTemplate">
        <Grid ColumnDefinitions="{OnIdiom Default='8*, 2*', Desktop='7*, 3*'}">
            <telerikMauiControls:RadBorder Style="{TemplateBinding ActualBorderStyle}">
                <Label Text="{TemplateBinding Item.Text}" Style="{TemplateBinding ActualLabelStyle}" />
            </telerikMauiControls:RadBorder>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="ChatIncomingMiddleAndLastAttachmentsMessageViewControlTemplate">
        <Grid ColumnDefinitions="{OnIdiom Default='8*, 2*', Desktop='7*, 3*'}">
            <telerikMauiControls:RadBorder Style="{TemplateBinding ActualBorderStyle}">
                <VerticalStackLayout Spacing="{OnPlatform Default=12, WinUI=16}">
                    <Label Text="{TemplateBinding Item.Text}" Style="{TemplateBinding ActualLabelStyle}"  IsVisible="{TemplateBinding Item.Text, Converter={StaticResource ChatNullOrEmptyToBoolConverter}}" />
                    <telerikChat:ChatMessageAttachmentsListView Item="{TemplateBinding Item}" Style="{TemplateBinding ActualAttachmentsListViewStyle}" />
                </VerticalStackLayout>
            </telerikMauiControls:RadBorder>
        </Grid>
    </ControlTemplate>

    <Style x:Key="IncomingMiddleMessageBorderStyle" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource IncomingMessageBorderStyle}">
        <Setter Property="CornerRadius" Value="0, 8, 8, 0" />
    </Style>

    <DataTemplate x:Key="ChatIncomingMiddleAttachmentsMessageTemplate">
        <telerikMauiControls:RadLayout>
            <telerikChat:ChatIncomingMiddleAttachmentsMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                                  Item="{Binding}" />
        </telerikMauiControls:RadLayout>
    </DataTemplate>

    <Style x:Key="IncomingLastMessageBorderStyle" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource IncomingMessageBorderStyle}">
        <Setter Property="CornerRadius" Value="0, 8, 8, 8" />
    </Style>

    <DataTemplate x:Key="ChatIncomingLastAttachmentsMessageTemplate">
        <telerikMauiControls:RadLayout>
            <telerikChat:ChatIncomingLastAttachmentsMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                                Item="{Binding}" />
        </telerikMauiControls:RadLayout>
    </DataTemplate>

    <Style x:Key="IncomingSingleMessageBorderStyle" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource IncomingMessageBorderStyle}">
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Margin" Value="{OnPlatform Default='28, 8, 0, 2', MacCatalyst='39, 8, 0, 2', WinUI='46, 10, 0, 2'}" />
    </Style>

    <DataTemplate x:Key="ChatIncomingSingleAttachmentsMessageTemplate">
        <telerikMauiControls:RadLayout>
            <telerikChat:ChatIncomingSingleAttachmentsMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                                  Item="{Binding}" />
        </telerikMauiControls:RadLayout>
    </DataTemplate>

    <Style x:Key="OutgoingAttachmentImageStyle" TargetType="Image" BasedOn="{StaticResource AttachmentImageStyleBase}">
        <Setter Property="Source" Value="{Binding Attachment.FileName, Source={RelativeSource Mode=TemplatedParent}, Converter={StaticResource ChatOutgoingAttachmentsFileExtensionToImageSourceConverter}}" />
    </Style>

    <Style x:Key="OutgoingAttachmentTitleStyle" TargetType="Label" BasedOn="{StaticResource ChatMessageAttachmentTitleStyleBase}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
    </Style>

    <Style x:Key="OutgoingAttachmentSubtitleStyle" TargetType="Label" BasedOn="{StaticResource ChatMessageAttachmentSubtitleStyleBase}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
    </Style>

    <Style x:Key="OutgoingAttachmentActionsViewStyle" TargetType="telerikChat:ChatMessageAttachmentActionsView" BasedOn="{StaticResource ChatMessageAttachmentActionsViewStyleBase}">
        <Setter Property="MenuButtonStyle" Value="{StaticResource OutgoingChatMessageAttachmentDropDownMenuButtonStyle}" />
    </Style>

    <Style x:Key="OutgoingMessageAttachmentStyle" TargetType="telerikChat:ChatMessageAttachmentView" BasedOn="{StaticResource ChatMessageAttachmentViewStyle}">
        <Setter Property="ImageStyle" Value="{StaticResource OutgoingAttachmentImageStyle}" />
        <Setter Property="TitleStyle" Value="{StaticResource OutgoingAttachmentTitleStyle}" />
        <Setter Property="SubtitleStyle" Value="{StaticResource OutgoingAttachmentSubtitleStyle}" />
        <Setter Property="ActionsViewStyle" Value="{StaticResource OutgoingAttachmentActionsViewStyle}" />
    </Style>

    <Style x:Key="OutgoingMessageBorderStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadChatOutgoingMessageBackgroundColor}" />
        <Setter Property="Margin" Value="{OnPlatform Default='0, 2, 8, 2', MacCatalyst='0, 2, 10, 2', WinUI='0, 2, 12, 2'}" />
        <Setter Property="Padding" Value="{OnPlatform Default='10, 8', WinUI='8'}" />
        <Setter Property="HorizontalOptions" Value="End" />
    </Style>

    <Style x:Key="OutgoingMessageLabelStyle" TargetType="Label" BasedOn="{StaticResource MessageLabelStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadChatOutgoingMessageTextColor}" />
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>

    <DataTemplate x:Key="AttachmentsDownloadAllButtonTemplate">
        <Grid ColumnDefinitions="Auto, *"
              ColumnSpacing="6">
            <Label FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                   FontSize="{OnPlatform Default=16, MacCatalyst=14}"
                   TextColor="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=TextColor}"
                   Text="{x:Static telerikMaui:TelerikFont.IconDownload}"
                   HorizontalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}},     Path=HorizontalTextAlignment}"
                   VerticalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}},     Path=VerticalTextAlignment}" />
            <Label Grid.Column="1"
                   FontFamily="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=FontFamily}"
                   FontSize="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=FontSize}"
                   FontAttributes="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=FontAttributes}"
                   TextColor="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=TextColor}"
                   Text="{Binding}"
                   HorizontalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=HorizontalTextAlignment}"
                   VerticalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=VerticalTextAlignment}" />
        </Grid>
    </DataTemplate>

    <Style x:Key="AttachmentsDownloadAllButtonStyleBase" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="telerikTheming:RadEffects.RippleMode" Value="Pulse" />
        <Setter Property="telerikTheming:RadEffects.RippleColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha12}" />
        <Setter Property="ContentTemplate" Value="{StaticResource AttachmentsDownloadAllButtonTemplate}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=18, MacCatalyst=5}" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="Padding" Value="{OnPlatform Default='8, 4', Android='6, 4, 8, 4'}" />
        <Setter Property="Margin" Value="{OnPlatform Default='0, 20, 0, 4', Android='0, 16, 0, 4'}" />
    </Style>

    <Style x:Key="IncomingAttachmentsDownloadAllButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource AttachmentsDownloadAllButtonStyleBase}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Default={StaticResource ChatIncomingMessageAttachmentsDownloadAllButtonVisualStates},
                                                                                   Android={StaticResource ChatIncomingMessageAttachmentsDownloadAllButtonVisualStates_Android},
                                                                                   WinUI={StaticResource ChatIncomingMessageAttachmentsDownloadAllButtonVisualStates_WinUI}}" />
    </Style>

    <Style x:Key="OutgoingAttachmentsDownloadAllButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource AttachmentsDownloadAllButtonStyleBase}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Default={StaticResource ChatOutgoingMessageAttachmentsDownloadAllButtonVisualStates},
                                                                                   Android={StaticResource ChatOutgoingMessageAttachmentsDownloadAllButtonVisualStates_Android},
                                                                                   WinUI={StaticResource ChatOutgoingMessageAttachmentsDownloadAllButtonVisualStates_WinUI}}" />
    </Style>

    <Style x:Key="IncomingAttachmentsListViewStyle" TargetType="telerikChat:ChatMessageAttachmentsListView">
        <Setter Property="AttachmentStyle" Value="{StaticResource IncomingMessageAttachmentStyle}" />
        <Setter Property="DownloadAllButtonStyle" Value="{StaticResource IncomingAttachmentsDownloadAllButtonStyle}" />
    </Style>

    <Style x:Key="OutgoingAttachmentsListViewStyle" TargetType="telerikChat:ChatMessageAttachmentsListView">
        <Setter Property="AttachmentStyle" Value="{StaticResource OutgoingMessageAttachmentStyle}" />
        <Setter Property="DownloadAllButtonStyle" Value="{StaticResource OutgoingAttachmentsDownloadAllButtonStyle}" />
    </Style>

    <ControlTemplate x:Key="ChatOutgoingFirstAndSingleTextMessageViewControlTemplate">
        <Grid ColumnDefinitions="{OnIdiom Default='2*, 8*', Desktop='3*, 7*'}">
            <telerikMauiControls:RadLayout Grid.Column="1">
                <telerikMauiControls:RadBorder Style="{TemplateBinding ActualBorderStyle}">
                    <Label Text="{TemplateBinding Item.Text}" Style="{TemplateBinding ActualLabelStyle}" />
                </telerikMauiControls:RadBorder>
            </telerikMauiControls:RadLayout>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="ChatOutgoingFirstAndSingleAttachmentsMessageViewControlTemplate">
        <Grid ColumnDefinitions="{OnIdiom Default='2*, 8*', Desktop='3*, 7*'}">
            <telerikMauiControls:RadLayout Grid.Column="1">
                <telerikMauiControls:RadBorder Style="{TemplateBinding ActualBorderStyle}">
                    <VerticalStackLayout Spacing="{OnPlatform Default=12, WinUI=16}">
                        <Label Text="{TemplateBinding Item.Text}" Style="{TemplateBinding ActualLabelStyle}" IsVisible="{TemplateBinding Item.Text, Converter={StaticResource ChatNullOrEmptyToBoolConverter}}"/>
                        <telerikChat:ChatMessageAttachmentsListView Item="{TemplateBinding Item}" Style="{TemplateBinding ActualAttachmentsListViewStyle}" />
                    </VerticalStackLayout>
                </telerikMauiControls:RadBorder>
            </telerikMauiControls:RadLayout>
        </Grid>
    </ControlTemplate>

    <Style x:Key="OutgoingFirstMessageBorderStyle" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource OutgoingMessageBorderStyle}">
        <Setter Property="CornerRadius" Value="8, 8, 0, 8" />
        <Setter Property="Margin" Value="{OnPlatform Default='0, 8, 8, 2', MacCatalyst='0, 8, 10, 2', WinUI='0, 10, 12, 2'}" />
    </Style>

    <DataTemplate x:Key="ChatOutgoingFirstAttachmentsMessageTemplate">
        <telerikMauiControls:RadLayout>
            <telerikChat:ChatOutgoingFirstAttachmentsMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                                 Item="{Binding}" />
        </telerikMauiControls:RadLayout>
    </DataTemplate>

    <ControlTemplate x:Key="ChatOutgoingMiddleAndLastTextMessageViewControlTemplate">
        <Grid ColumnDefinitions="{OnIdiom Default='2*, 8*', Desktop='3*, 7*'}">
            <telerikMauiControls:RadBorder Grid.Column="1" Style="{TemplateBinding ActualBorderStyle}">
                <Label Text="{TemplateBinding Item.Text}" Style="{TemplateBinding ActualLabelStyle}" />
            </telerikMauiControls:RadBorder>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="ChatOutgoingMiddleAndLastAttachmentsMessageViewControlTemplate">
        <Grid ColumnDefinitions="{OnIdiom Default='2*, 8*', Desktop='3*, 7*'}">
            <telerikMauiControls:RadBorder Grid.Column="1" Style="{TemplateBinding ActualBorderStyle}">
                <VerticalStackLayout Spacing="{OnPlatform Default=12, WinUI=16}">
                    <Label Text="{TemplateBinding Item.Text}" Style="{TemplateBinding ActualLabelStyle}"  IsVisible="{TemplateBinding Item.Text, Converter={StaticResource ChatNullOrEmptyToBoolConverter}}" />
                    <telerikChat:ChatMessageAttachmentsListView Item="{TemplateBinding Item}" Style="{TemplateBinding ActualAttachmentsListViewStyle}" />
                </VerticalStackLayout>
            </telerikMauiControls:RadBorder>
        </Grid>
    </ControlTemplate>

    <Style x:Key="OutgoingMiddleMessageBorderStyle" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource OutgoingMessageBorderStyle}">
        <Setter Property="CornerRadius" Value="8, 0, 0, 8" />
    </Style>

    <DataTemplate x:Key="ChatOutgoingMiddleAttachmentsMessageTemplate">
        <telerikMauiControls:RadLayout>
            <telerikChat:ChatOutgoingMiddleAttachmentsMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                                  Item="{Binding}" />
        </telerikMauiControls:RadLayout>
    </DataTemplate>

    <Style x:Key="OutgoingLastMessageBorderStyle" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource OutgoingMessageBorderStyle}">
        <Setter Property="CornerRadius" Value="8, 0, 8, 8" />
    </Style>

    <DataTemplate x:Key="ChatOutgoingLastAttachmentsMessageTemplate">
        <telerikMauiControls:RadLayout>
            <telerikChat:ChatOutgoingLastAttachmentsMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                                Item="{Binding}" />
        </telerikMauiControls:RadLayout>
    </DataTemplate>

    <Style x:Key="OutgoingSingleMessageBorderStyle" TargetType="telerikMauiControls:RadBorder" BasedOn="{StaticResource OutgoingMessageBorderStyle}">
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Margin" Value="{OnPlatform Default='0, 8, 8, 2', MacCatalyst='0, 8, 10, 2', WinUI='0, 10, 12, 2'}" />
    </Style>

    <DataTemplate x:Key="ChatOutgoingSingleAttachmentsMessageTemplate">
        <telerikMauiControls:RadLayout>
            <telerikChat:ChatOutgoingSingleAttachmentsMessageView telerikMauiControls:StyleHelper.InheritedStyleClass="{Binding Chat.ActualStyleClass}"
                                                                  Item="{Binding}" />
        </telerikMauiControls:RadLayout>
    </DataTemplate>

    <Style x:Key="RadChatStyle" TargetType="telerikMauiControls:RadChat">
        <Setter Property="MinimumWidthRequest" Value="{OnIdiom Desktop=375, Default=-1}" />
        <Setter Property="ItemTemplateSelector" Value="{StaticResource ChatItemTemplateSelector}" />
        <Setter Property="SpeechToTextButtonStyle" Value="{StaticResource ChatInputAreaSpeechToTextButtonStyle}" />
        <Setter Property="Background" Value="{DynamicResource RadChatBackgroundColor}" />
        <Setter Property="SendMessageButtonImage" Value="{StaticResource ChatSendMessageButtonImageSource}" />
        <Setter Property="MoreButtonStyle" Value="{StaticResource ChatMoreButtonToolbarItemStyle}" />
    </Style>

    <Style TargetType="telerikMauiControls:RadChat" BasedOn="{StaticResource RadChatStyle}" />

    <Style x:Key="ChatIncomingAttachmentsMessageViewStyleBase" TargetType="telerikChat:ChatIncomingAttachmentsMessageView">
        <Setter Property="LabelStyle" Value="{StaticResource IncomingMessageLabelStyle}" />
        <Setter Property="AttachmentsListViewStyle" Value="{StaticResource IncomingAttachmentsListViewStyle}" />
    </Style>

    <Style x:Key="ChatIncomingSingleAttachmentsMessageViewStyle" TargetType="telerikChat:ChatIncomingSingleAttachmentsMessageView" BasedOn="{StaticResource ChatIncomingAttachmentsMessageViewStyleBase}">
        <Setter Property="BorderStyle" Value="{StaticResource IncomingSingleMessageBorderStyle}" />
    </Style>
    <Style TargetType="telerikChat:ChatIncomingSingleAttachmentsMessageView" BasedOn="{StaticResource ChatIncomingSingleAttachmentsMessageViewStyle}" ApplyToDerivedTypes="True" />

    <Style x:Key="ChatIncomingFirstAttachmentsMessageViewStyle" TargetType="telerikChat:ChatIncomingFirstAttachmentsMessageView" BasedOn="{StaticResource ChatIncomingAttachmentsMessageViewStyleBase}">
        <Setter Property="BorderStyle" Value="{StaticResource IncomingFirstMessageBorderStyle}" />
    </Style>
    <Style TargetType="telerikChat:ChatIncomingFirstAttachmentsMessageView" BasedOn="{StaticResource ChatIncomingFirstAttachmentsMessageViewStyle}" ApplyToDerivedTypes="True" />

    <Style x:Key="ChatIncomingMiddleAttachmentsMessageViewStyle" TargetType="telerikChat:ChatIncomingMiddleAttachmentsMessageView" BasedOn="{StaticResource ChatIncomingAttachmentsMessageViewStyleBase}">
        <Setter Property="BorderStyle" Value="{StaticResource IncomingMiddleMessageBorderStyle}" />
    </Style>
    <Style TargetType="telerikChat:ChatIncomingMiddleAttachmentsMessageView" BasedOn="{StaticResource ChatIncomingMiddleAttachmentsMessageViewStyle}" ApplyToDerivedTypes="True" />

    <Style x:Key="ChatIncomingLastAttachmentsMessageViewStyle" TargetType="telerikChat:ChatIncomingLastAttachmentsMessageView" BasedOn="{StaticResource ChatIncomingAttachmentsMessageViewStyleBase}">
        <Setter Property="BorderStyle" Value="{StaticResource IncomingLastMessageBorderStyle}" />
    </Style>
    <Style TargetType="telerikChat:ChatIncomingLastAttachmentsMessageView" BasedOn="{StaticResource ChatIncomingLastAttachmentsMessageViewStyle}" ApplyToDerivedTypes="True" />

    <Style x:Key="ChatOutgoingAttachmentsMessageViewStyleBase" TargetType="telerikChat:ChatOutgoingAttachmentsMessageView">
        <Setter Property="LabelStyle" Value="{StaticResource OutgoingMessageLabelStyle}" />
        <Setter Property="AttachmentsListViewStyle" Value="{StaticResource OutgoingAttachmentsListViewStyle}" />
    </Style>

    <Style x:Key="ChatOutgoingSingleAttachmentsMessageViewStyle" TargetType="telerikChat:ChatOutgoingSingleAttachmentsMessageView" BasedOn="{StaticResource ChatOutgoingAttachmentsMessageViewStyleBase}">
        <Setter Property="BorderStyle" Value="{StaticResource OutgoingSingleMessageBorderStyle}" />
    </Style>
    <Style TargetType="telerikChat:ChatOutgoingSingleAttachmentsMessageView" BasedOn="{StaticResource ChatOutgoingSingleAttachmentsMessageViewStyle}" ApplyToDerivedTypes="True" />

    <Style x:Key="ChatOutgoingFirstAttachmentsMessageViewStyle" TargetType="telerikChat:ChatOutgoingFirstAttachmentsMessageView" BasedOn="{StaticResource ChatOutgoingAttachmentsMessageViewStyleBase}">
        <Setter Property="BorderStyle" Value="{StaticResource OutgoingFirstMessageBorderStyle}" />
    </Style>
    <Style TargetType="telerikChat:ChatOutgoingFirstAttachmentsMessageView" BasedOn="{StaticResource ChatOutgoingFirstAttachmentsMessageViewStyle}" ApplyToDerivedTypes="True" />

    <Style x:Key="ChatOutgoingMiddleAttachmentsMessageViewStyle" TargetType="telerikChat:ChatOutgoingMiddleAttachmentsMessageView" BasedOn="{StaticResource ChatOutgoingAttachmentsMessageViewStyleBase}">
        <Setter Property="BorderStyle" Value="{StaticResource OutgoingMiddleMessageBorderStyle}" />
    </Style>
    <Style TargetType="telerikChat:ChatOutgoingMiddleAttachmentsMessageView" BasedOn="{StaticResource ChatOutgoingMiddleAttachmentsMessageViewStyle}" ApplyToDerivedTypes="True" />

    <Style x:Key="ChatOutgoingLastAttachmentsMessageViewStyle" TargetType="telerikChat:ChatOutgoingLastAttachmentsMessageView" BasedOn="{StaticResource ChatOutgoingAttachmentsMessageViewStyleBase}">
        <Setter Property="BorderStyle" Value="{StaticResource OutgoingLastMessageBorderStyle}" />
    </Style>
    <Style TargetType="telerikChat:ChatOutgoingLastAttachmentsMessageView" BasedOn="{StaticResource ChatOutgoingLastAttachmentsMessageViewStyle}" ApplyToDerivedTypes="True" />
</ResourceDictionary>
