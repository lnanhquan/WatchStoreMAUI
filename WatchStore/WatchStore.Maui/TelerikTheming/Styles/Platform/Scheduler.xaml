<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMauiDataControls="clr-namespace:Telerik.Maui.Controls.DataControls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikScheduler="clr-namespace:Telerik.Maui.Controls.Scheduler;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.Scheduler">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <telerikMauiControls:InvertedBooleanConverter x:Key="InvertedBooleanConverter" />
    <telerikMauiControls:StringToBoolConverter x:Key="StringToBoolConverter" />
    <telerikMauiControls:MultiBooleanConverter x:Key="MultiBooleanConverter" LogicalOperator="Or" />
    <telerikScheduler:ProgressToRotationConverter x:Key="ProgressToRotationConverter" Rotation="-180" />
    <telerikScheduler:ViewDefinitionToBoolConverter x:Key="ViewDefinitionToBoolConverter" />
    <telerikScheduler:FormattedSubjectConverter x:Key="FormattedSubjectConverter" />

    <DataTemplate x:Key="SchedulerLabelTemplate">
        <Label Text="{Binding Text}" />
    </DataTemplate>

    <DataTemplate x:Key="SchedulerLineTemplate">
        <telerikScheduler:SchedulerBoxView />
    </DataTemplate>

    <DataTemplate x:Key="SchedulerAppointmentTemplate">
        <telerikScheduler:AppointmentView Subject="{Binding Occurrence.Appointment.Subject}" />
    </DataTemplate>

    <DataTemplate x:Key="SchedulerMonthViewMoreIndicatorTemplate">
        <Label />
    </DataTemplate>

    <DataTemplate x:Key="SchedulerAgendaMonthTemplate">
        <telerikMauiControls:RadBorder BorderColor="{Binding BorderColor, Source={x:RelativeSource AncestorType={Type telerikMauiControls:RadScheduler}}}"
                                       BorderThickness="{OnPlatform Default=0, MacCatalyst='0, 1', WinUI='0, 1'}">
            <Label Text="{Binding FormattedMonth}"
                   FontSize="{OnPlatform Default=14, iOS=15, MacCatalyst=15}"
                   FontAttributes="Bold"
                   TextColor="{DynamicResource RadOnAppSurfaceColor}"
                   Margin="{OnPlatform Android='8, 4', iOS=8, MacCatalyst='4.5', WinUI='12, 8'}" />
        </telerikMauiControls:RadBorder>
    </DataTemplate>

    <DataTemplate x:Key="SchedulerAgendaWeekTemplate">
        <Label Text="{Binding FormattedWeek}"
               FontSize="{OnPlatform Default=14, Android=12}"
               Margin="{OnPlatform Android='8, 2', iOS='8, 4', MacCatalyst='4.5', WinUI='12, 4'}"
               TextColor="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, iOS={DynamicResource RadSubtleColor}}" />
    </DataTemplate>

    <DataTemplate x:Key="SchedulerAgendaDayTemplate">
        <Grid RowDefinitions="Auto, Auto">
            <Label Text="{Binding FormattedDay}"
                   FontSize="14"
                   Margin="{OnPlatform Default='8, 8, 8, 4', MacCatalyst='4.5, 8', WinUI='12, 8, 12, 4'}"
                   TextColor="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, iOS={DynamicResource RadOnAppSurfaceColorAlpha60}}" />
            <telerikScheduler:SchedulerBoxView Grid.Row="1"
                                               BackgroundColor="{Binding BorderColor, Source={x:RelativeSource AncestorType={Type telerikMauiControls:RadScheduler}}}"
                                               HeightRequest="1"
                                               Margin="{OnPlatform Default='108, 0, 0, 0', MacCatalyst='108, 0, 0, 4.5', Android='72, 0, 0, 0', iOS='72, 0, 0, 0'}" />
        </Grid>
        
    </DataTemplate>

    <DataTemplate x:Key="SchedulerAgendaAllDayDurationTemplate">
        <Label Text="{telerikMauiControls:Localize Scheduler_AgendaViewDefinition_AllDay}"
               FontSize="{OnPlatform Default=14, Android=12, iOS=12}"
               HorizontalTextAlignment="End"
               VerticalOptions="Center"
               WidthRequest="{OnPlatform Default=100, Android=64, iOS=64}"
               TextColor="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, iOS={DynamicResource RadSubtleColor}, Android={DynamicResource RadSubtleColor}}" />
    </DataTemplate>

    <DataTemplate x:Key="SchedulerAgendaStartEndDurationTemplate">
        <Label WidthRequest="{OnPlatform Default=100, Android=64, iOS=64}"
               HorizontalTextAlignment="End"
               VerticalOptions="Center"
               TextColor="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, iOS={DynamicResource RadSubtleColor}, Android={DynamicResource RadSubtleColor}}">
            <Label.FormattedText>
                <FormattedString>
                    <Span Text="{Binding FormattedStartTime}"
                          FontSize="{OnPlatform Default=14, Android=12, iOS=12}" />
                    <Span Text="{OnPlatform Default='&#x200B; - &#x200B;', Android='&#x200B;&#x0a;&#x200B;', iOS='&#x200B;&#x0a;&#x200B;'}"
                          FontSize="{OnPlatform Default=14, Android=12, iOS=12}" />
                    <Span Text="{Binding FormattedEndTime}"
                          TextColor="{DynamicResource LabelDisabledTextColor}"
                          FontSize="{OnPlatform Default=14, Android=12, iOS=12}" />
                </FormattedString>
            </Label.FormattedText>
        </Label>
    </DataTemplate>

    <DataTemplate x:Key="SchedulerAgendaStartOnlyDurationTemplate">
        <Label Text="{Binding FormattedStartTime}"
               FontSize="{OnPlatform Default=14, Android=12, iOS=12}"
               VerticalOptions="Center"
               HorizontalTextAlignment="End"
               WidthRequest="{OnPlatform Default=100, Android=64, iOS=64}"
               TextColor="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, iOS={DynamicResource RadSubtleColor}, Android={DynamicResource RadSubtleColor}}" />
    </DataTemplate>

    <DataTemplate x:Key="SchedulerAgendaEndOnlyDurationTemplate">
        <Label WidthRequest="{OnPlatform Default=100, Android=64, iOS=64}"
               HorizontalTextAlignment="End"
               VerticalOptions="Center"
               TextColor="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, iOS={DynamicResource RadSubtleColor}, Android={DynamicResource RadSubtleColor}}">
            <Label.FormattedText>
                <FormattedString>
                    <Span Text="{telerikMauiControls:Localize Scheduler_AgendaViewDefinition_Until}"
                          FontSize="{OnPlatform Default=14, Android=12, iOS=12}" />
                    <Span Text="{OnPlatform Default='&#x200B; &#x200B;', Android='&#x200B;&#x0a;&#x200B;', iOS='&#x200B;&#x0a;&#x200B;'}"
                          FontSize="{OnPlatform Default=14, Android=12, iOS=12}" />
                    <Span Text="{Binding FormattedEndTime}"
                          TextColor="{DynamicResource LabelDisabledTextColor}"
                          FontSize="{OnPlatform Default=14, Android=12, iOS=12}" />
                </FormattedString>
            </Label.FormattedText>
        </Label>
    </DataTemplate>

    <DataTemplate x:Key="SchedulerAgendaAppointmentTemplate">
        <telerikScheduler:AppointmentView HeightRequest="{OnPlatform Android=36, iOS=40, MacCatalyst=20}"
                                          Margin="{OnPlatform Default='8, 2, 2, 4', Android='8, 4, 20, 4', iOS='8, 4, 20, 4', WinUI='8, 5, 4, 5'}">
            <telerikScheduler:AppointmentView.Subject>
                <MultiBinding Converter="{StaticResource FormattedSubjectConverter}">
                    <Binding Path="." />
                    <Binding Path="Occurrence.Appointment.Subject" />
                </MultiBinding>
            </telerikScheduler:AppointmentView.Subject>
        </telerikScheduler:AppointmentView>
    </DataTemplate>

    <Style x:Key="SchedulerHeaderBorderStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadSchedulerBorderColor}" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="Padding" Value="{OnPlatform Android='8, 5, 0, 5', iOS='8, 0, 0, 0', WinUI='12, 8', MacCatalyst=8}" />
    </Style>

    <Style x:Key="SchedulerHeaderLabelStyle" TargetType="Label">
        <Setter Property="Text" Value="{TemplateBinding HeaderText}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="HorizontalTextAlignment" Value="{OnPlatform Default=Start, MacCatalyst=End}" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="LineBreakMode" Value="NoWrap" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, MacCatalyst='0, 0, 4, 0', WinUI='0, 0, 8, 0'}" />
    </Style>

    <Style x:Key="SchedulerHeaderLabelStyle_Mobile" TargetType="Label" BasedOn="{StaticResource SchedulerHeaderLabelStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
    </Style>

    <Style x:Key="SchedulerHeaderIndicatorLabelStyle" TargetType="Label">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconDownArrow}" />
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="LineBreakMode" Value="NoWrap" />
        <Setter Property="Rotation" Value="{Binding AnimationProgress, Source={RelativeSource AncestorType={x:Type telerikScheduler:SchedulerExpander}}, Converter={StaticResource ProgressToRotationConverter}}" />
        <Setter Property="WidthRequest" Value="{OnPlatform Default=40, iOS=44}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=40, iOS=44}" />
    </Style>

    <Style x:Key="SchedulerBaseButtonStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderWidth" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=20, Android=40, iOS=44}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=20, Android=40, iOS=44}" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="PointerOver" />
                    <VisualState Name="Pressed" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Button.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SchedulerBaseButtonStyle_WinUI" TargetType="telerikMauiControls:RadButton">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="MinimumWidthRequest" Value="32" />
        <Setter Property="MinimumHeightRequest" Value="32" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="SchedulerTodayButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SchedulerBaseButtonStyle}">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconCalendarDay}" />
        <Setter Property="FontSize" Value="16" />
    </Style>

    <Style x:Key="SchedulerTodayButtonStyle_MacCatalyst" TargetType="telerikMauiControls:RadButton">
        <Setter Property="Text" Value="{telerikMauiControls:Localize Scheduler_Today_Header_Text}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderWidth" Value="1" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="Padding" Value="10, 2" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="SchedulerTodayButtonStyle_WinUI" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SchedulerBaseButtonStyle_WinUI}">
        <Setter Property="Text" Value="{telerikMauiControls:Localize Scheduler_Today_Header_Text}" />
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="Padding" Value="10, 5" />
    </Style>

    <Style x:Key="SchedulerAddAppointmentButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{OnPlatform Default={StaticResource SchedulerBaseButtonStyle}, WinUI={StaticResource SchedulerBaseButtonStyle_WinUI}}">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconAdd}" />
        <Setter Property="IsVisible" Value="{Binding ActiveViewDefinition, Source={RelativeSource TemplatedParent}, Converter={StaticResource ViewDefinitionToBoolConverter}, ConverterParameter='AgendaViewDefinition'}" />
    </Style>

    <Style x:Key="SchedulerIncreaseVisibleRangeButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{OnPlatform Default={StaticResource SchedulerBaseButtonStyle}, WinUI={StaticResource SchedulerBaseButtonStyle_WinUI}}">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="Text" Value="{OnPlatform Default={x:Static telerikMaui:TelerikFont.IconRightArrow}, WinUI={x:Static telerikMaui:TelerikFont.IconForward}}" />
        <Setter Property="IsVisible" Value="{Binding ActiveViewDefinition, Source={RelativeSource TemplatedParent}, Converter={StaticResource ViewDefinitionToBoolConverter}, ConverterParameter='DayViewDefinition,WeekViewDefinition,MultidayViewDefinition,MonthViewDefinition'}" />
    </Style>

    <Style x:Key="SchedulerDecreaseVisibleRangeButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{OnPlatform Default={StaticResource SchedulerBaseButtonStyle}, WinUI={StaticResource SchedulerBaseButtonStyle_WinUI}}">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="Text" Value="{OnPlatform Default={x:Static telerikMaui:TelerikFont.IconLeftArrow}, WinUI={x:Static telerikMaui:TelerikFont.IconBack}}" />
        <Setter Property="IsVisible" Value="{Binding ActiveViewDefinition, Source={RelativeSource TemplatedParent}, Converter={StaticResource ViewDefinitionToBoolConverter}, ConverterParameter='DayViewDefinition,WeekViewDefinition,MultidayViewDefinition,MonthViewDefinition'}" />
    </Style>

    <Style x:Key="SchedulerDatePickerButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{OnPlatform Default={StaticResource SchedulerBaseButtonStyle}, WinUI={StaticResource SchedulerBaseButtonStyle_WinUI}}">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconCalendarMonth}" />
        <Setter Property="FontSize" Value="16" />
    </Style>

    <ControlTemplate x:Key="SchedulerDatePickerControlTemplate">
        <telerikScheduler:SchedulerNavigationButton x:Name="PART_ToggleButton"
                                                    Style="{TemplateBinding ActualButtonStyle}">
            <telerikMauiControls:RadPopup.Popup>
                <telerikMauiControls:RadPopup VerticalOffset="{OnPlatform Default=0, MacCatalyst=8, WinUI=8}">
                    <telerikMauiControls:RadCalendar />
                </telerikMauiControls:RadPopup>
            </telerikMauiControls:RadPopup.Popup>
        </telerikScheduler:SchedulerNavigationButton>
    </ControlTemplate>

    <Style TargetType="telerikScheduler:SchedulerDatePicker">
        <Setter Property="ButtonStyle" Value="{StaticResource SchedulerDatePickerButtonStyle}" />
        <Setter Property="ControlTemplate" Value="{DynamicResource SchedulerDatePickerControlTemplate}" />
    </Style>

    <Style x:Key="SchedulerDropDownButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{OnPlatform Default={StaticResource SchedulerBaseButtonStyle}, WinUI={StaticResource SchedulerBaseButtonStyle_WinUI}}">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconDotsVert}" />
        <Setter Property="HorizontalOptions" Value="{OnPlatform Default=Center, Android=Fill}" />
        <Setter Property="VerticalOptions" Value="{OnPlatform Default=Center, Android=Fill}" />
    </Style>

    <ControlTemplate x:Key="SchedulerDropDownButtonControlTemplate">
        <Button x:Name="PART_Button"
                Style="{TemplateBinding ActualButtonStyle}">
            <telerikMauiControls:RadPopup.Popup>
                <telerikMauiControls:RadPopup AnimationType="None"
                                              VerticalOffset="{OnPlatform Default=0, Android=4}">
                    <telerikMauiControls:RadBorder BackgroundColor="{DynamicResource RadSurfaceAltColor}"
                                                   BorderColor="{DynamicResource RadBorderColor}"
                                                   BorderThickness="1"
                                                   CornerRadius="{OnPlatform Android=4, iOS=8}"
                                                   Padding="{OnPlatform Android='0, 8'}">
                        <telerikMauiDataControls:NonVirtualizedItemsControl DisplayMemberPath="Title"
                                                                            SelectionMode="Single"
                                                                            CanUnselectSingleItem="False"
                                                                            MinimumWidthRequest="{OnPlatform Android=120, iOS=180}"
                                                                            ItemStyle="{TemplateBinding ItemStyle}"
                                                                            SelectedItemStyle="{TemplateBinding SelectedItemStyle}" />
                    </telerikMauiControls:RadBorder>
                </telerikMauiControls:RadPopup>
            </telerikMauiControls:RadPopup.Popup>
        </Button>
    </ControlTemplate>

    <Style TargetType="telerikScheduler:SchedulerDropDownButton">
        <Setter Property="ButtonStyle" Value="{DynamicResource SchedulerDropDownButtonStyle}" />
        <Setter Property="ControlTemplate" Value="{DynamicResource SchedulerDropDownButtonControlTemplate}" />
    </Style>

    <Style x:Key="SchedulerViewDefinitionHeaderItemStyle_Android" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer">
        <Setter Property="MinimumHeightRequest" Value="32" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Padding" Value="8, 0, 4, 0" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
    </Style>

    <Style x:Key="SchedulerActiveViewDefinitionHeaderItemStyle_Android" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer" BasedOn="{StaticResource SchedulerViewDefinitionHeaderItemStyle_Android}">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerActiveViewDefinitionHeaderItemBackgroundColor}" />
    </Style>

    <Style x:Key="SchedulerViewDefinitionHeaderItemStyle_iOS" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer">
        <Setter Property="MinimumHeightRequest" Value="44" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="Padding" Value="16, 8" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
    </Style>

    <Style x:Key="SchedulerActiveViewDefinitionHeaderItemStyle_iOS" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer" BasedOn="{StaticResource SchedulerViewDefinitionHeaderItemStyle_iOS}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerActiveViewDefinitionHeaderItemBackgroundColor}" />
    </Style>

    <Style x:Key="SchedulerViewDefinitionHeaderItemStyle_MacCatalyst" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="Padding" Value="10, 2" />
        <Setter Property="Margin" Value="1, 0" />
        <Setter Property="TextMargin" Value="0" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="PointerOver" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SchedulerActiveViewDefinitionHeaderItemStyle_MacCatalyst" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer" BasedOn="{StaticResource SchedulerViewDefinitionHeaderItemStyle_MacCatalyst}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerActiveViewDefinitionHeaderItemBackgroundColor}" />
    </Style>

    <Style x:Key="SchedulerViewDefinitionHeaderItemStyle_WinUI" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer">
        <Setter Property="MinimumWidthRequest" Value="80" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Margin" Value="2, 0" />
        <Setter Property="TextMargin" Value="0" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.BackgroundColor" Value="{DynamicResource RadSchedulerViewDefinitionHeaderItemPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SchedulerActiveViewDefinitionHeaderItemStyle_WinUI" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer" BasedOn="{StaticResource SchedulerViewDefinitionHeaderItemStyle_WinUI}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerActiveViewDefinitionHeaderItemBackgroundColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.BackgroundColor" Value="{DynamicResource RadPrimaryColorAlpha90}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SchedulerResizeHandleStyle" TargetType="Rectangle">
        <Setter Property="Fill" Value="{DynamicResource RadSchedulerResizeHandleFill}" />
        <Setter Property="RadiusX" Value="4" />
        <Setter Property="RadiusY" Value="4" />
        <Setter Property="MaximumHeightRequest" Value="8" />
        <Setter Property="MaximumWidthRequest" Value="58" />
    </Style>

    <ControlTemplate x:Key="SchedulerExpanderContentControlTemplate">
        <Grid Padding="{OnPlatform Android='0, 0, 0, 4', iOS='0, 0, 0, 8'}">
            <Grid RowDefinitions="*, Auto"
                  BackgroundColor="{Binding BackgroundColor, Source={x:RelativeSource AncestorType={Type telerikScheduler:SchedulerExpander}}}">
                <ContentPresenter />
                <telerikMauiControls:RadLayout x:Name="PART_ResizeHandle"
                                               Grid.Row="1"
                                               HeightRequest="24">
                    <Rectangle Style="{TemplateBinding ActualResizeHandleStyle}" />
                </telerikMauiControls:RadLayout>
            </Grid>
        </Grid>
    </ControlTemplate>

    <Style TargetType="telerikScheduler:SchedulerExpander">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="ResizeHandleStyle" Value="{StaticResource SchedulerResizeHandleStyle}" />
    </Style>

    <ControlTemplate x:Key="SchedulerEmptyViewControlTemplate">
        <Label HorizontalTextAlignment="Center"
               VerticalTextAlignment="Center"
               FontSize="{OnPlatform Default=24, iOS=22, MacCatalyst=17}"
               Text="{telerikMauiControls:Localize Scheduler_Empty_ViewDefinition_Text}"
               TextColor="{DynamicResource RadOnAppSurfaceColor}" />
    </ControlTemplate>

    <Style TargetType="telerikScheduler:SchedulerEmptyView">
        <Setter Property="ControlTemplate" Value="{StaticResource SchedulerEmptyViewControlTemplate}" />
    </Style>

    <Style TargetType="telerikScheduler:AppointmentView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerAppointmentBackgroundColor}" />
        <Setter Property="SubjectTextColor" Value="{DynamicResource RadSchedulerAppointmentSubjectTextColor}" />
        <Setter Property="DeleteButtonTextColor" Value="{DynamicResource RadSchedulerAppointmentDeleteButtonTextColor}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=5, MacCatalyst=5}" />
    </Style>

    <Style TargetType="telerikScheduler:SchedulerCurrentTimeIndicator">
        <Setter Property="Color" Value="{DynamicResource RadPrimaryColor}" />
    </Style>

    <Style x:Key="SchedulerViewLineStyle" TargetType="telerikScheduler:SchedulerBoxView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerBorderColor}" />
    </Style>

    <Style x:Key="SchedulerViewDayLabelBaseStyle" TargetType="Label">
        <Setter Property="HorizontalTextAlignment" Value="{OnPlatform Default=Start, Android=Center, iOS=Center}" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="LineBreakMode" Value="{OnPlatform Default=NoWrap, Android=WordWrap, iOS=WordWrap}" />
        <Setter Property="Margin" Value="{OnPlatform Default='4, 0, 0, 0', Android=0, iOS=0}" />
    </Style>

    <Style x:Key="SchedulerMultidayViewDayLabelStyle" TargetType="Label" BasedOn="{StaticResource SchedulerViewDayLabelBaseStyle}">
        <Setter Property="TextColor" Value="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, WinUI={DynamicResource RadSubtleColor}}" />
    </Style>

    <Style x:Key="SchedulerMultidayViewTodayLabelStyle" TargetType="Label" BasedOn="{StaticResource SchedulerViewDayLabelBaseStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="FontAttributes" Value="Bold" />
    </Style>

    <Style x:Key="SchedulerMonthViewDayLabelStyle" TargetType="Label" BasedOn="{StaticResource SchedulerViewDayLabelBaseStyle}">
        <Setter Property="VerticalTextAlignment" Value="Start" />
        <Setter Property="LineBreakMode" Value="NoWrap" />
        <Setter Property="Padding" Value="{OnPlatform Default='4, 6', Android='8, 4', iOS='8, 4'}" />
    </Style>

    <Style x:Key="SchedulerMonthViewDayLabelStyle_MacCatalyst" TargetType="Label" BasedOn="{StaticResource SchedulerMonthViewDayLabelStyle}">
        <Setter Property="FontSize" Value="12" />
    </Style>

    <Style x:Key="SchedulerMonthViewNormalLabelStyle" TargetType="Label" BasedOn="{OnPlatform Default={StaticResource SchedulerMonthViewDayLabelStyle}, MacCatalyst={StaticResource SchedulerMonthViewDayLabelStyle_MacCatalyst}}"/>

    <Style x:Key="SchedulerMonthViewTodayLabelStyle" TargetType="Label" BasedOn="{OnPlatform Default={StaticResource SchedulerMonthViewDayLabelStyle}, MacCatalyst={StaticResource SchedulerMonthViewDayLabelStyle_MacCatalyst}}">
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="FontAttributes" Value="Bold" />
    </Style>

    <Style x:Key="SchedulerMonthViewDayNameLabelStyle" TargetType="Label" BasedOn="{StaticResource SchedulerMonthViewDayLabelStyle}">
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="LineBreakMode" Value="NoWrap" />
        <Setter Property="Padding" Value="4, 0" />
    </Style>

    <Style x:Key="SchedulerMonthViewDayNameLabelStyle_MacCatalyst" TargetType="Label" BasedOn="{StaticResource SchedulerMonthViewDayNameLabelStyle}">
        <Setter Property="FontSize" Value="13" />
    </Style>

    <Style x:Key="SchedulerMonthViewDayNameNormalLabelStyle" TargetType="Label" BasedOn="{OnPlatform Default={StaticResource SchedulerMonthViewDayNameLabelStyle}, MacCatalyst={StaticResource SchedulerMonthViewDayNameLabelStyle_MacCatalyst}}" />

    <Style x:Key="SchedulerMonthViewDayNameTodayLabelStyle" TargetType="Label" BasedOn="{OnPlatform Default={StaticResource SchedulerMonthViewDayNameLabelStyle}, MacCatalyst={StaticResource SchedulerMonthViewDayNameLabelStyle_MacCatalyst}}">
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="FontAttributes" Value="Bold" />
    </Style>

    <Style x:Key="SchedulerMultidayViewTimeRulerLabelStyle" TargetType="Label">
        <Setter Property="FontSize" Value="{OnPlatform Default=12, MacCatalyst=13}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="LineBreakMode" Value="NoWrap" />
        <Setter Property="Margin" Value="{OnPlatform Default='4, 0', MacCatalyst='2, 0'}" />
        <Setter Property="HorizontalTextAlignment" Value="End" />
    </Style>

    <Style x:Key="SchedulerSpecialSlotStyle" TargetType="telerikScheduler:SchedulerBoxView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerSpecialSlotColor}" />
    </Style>

    <Style x:Key="SchedulerReadOnlySpecialSlotStyle" TargetType="telerikScheduler:SchedulerBoxView">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerReadOnlySpecialSlotColor}" />
    </Style>

    <telerikScheduler:SpecialSlotStyleSelector x:Key="SchedulerSpecialSlotStyleSelector"
                                               NormalStyle="{StaticResource SchedulerSpecialSlotStyle}"
                                               ReadOnlyStyle="{StaticResource SchedulerReadOnlySpecialSlotStyle}" />

    <telerikScheduler:DayStyleSelector x:Key="SchedulerViewDayStyleSelector"
                                       NormalLabelStyle="{StaticResource SchedulerMultidayViewDayLabelStyle}"
                                       TodayLabelStyle="{StaticResource SchedulerMultidayViewTodayLabelStyle}" />

    <telerikScheduler:DayStyleSelector x:Key="SchedulerMonthViewDayStyleSelector"
                                       NormalLabelStyle="{OnPlatform Default={StaticResource SchedulerMonthViewDayLabelStyle}, MacCatalyst={StaticResource SchedulerMonthViewDayLabelStyle_MacCatalyst}}"
                                       TodayLabelStyle="{StaticResource SchedulerMonthViewTodayLabelStyle}" />

    <telerikScheduler:DayStyleSelector x:Key="SchedulerMonthViewDayNameStyleSelector"
                                       NormalLabelStyle="{OnPlatform Default={StaticResource SchedulerMonthViewDayNameLabelStyle}, MacCatalyst={StaticResource SchedulerMonthViewDayNameLabelStyle}}"
                                       TodayLabelStyle="{StaticResource SchedulerMonthViewDayNameTodayLabelStyle}" />

    <Style x:Key="SchedulerMonthViewMoreIndicatorStyle" TargetType="Label">
        <Setter Property="FontFamily" Value="{Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=12, Android=16, iOS=16}" />
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconDots3}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Padding" Value="4, 0" />
        <Setter Property="Margin" Value="0, 0, 0, 4" />
        <Setter Property="HorizontalTextAlignment" Value="{OnPlatform Default=Center, Android=Start, iOS=Start}" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
    </Style>

    <Style TargetType="telerikMauiControls:MultidayViewDefinitionBase" ApplyToDerivedTypes="True">
        <Setter Property="MajorTickLineStyle" Value="{StaticResource SchedulerViewLineStyle}" />
        <Setter Property="MinorTickLineStyle" Value="{StaticResource SchedulerViewLineStyle}" />
        <Setter Property="VerticalLineStyle" Value="{StaticResource SchedulerViewLineStyle}" />
        <Setter Property="HorizontalLineStyle" Value="{StaticResource SchedulerViewLineStyle}" />
        <Setter Property="DayStyleSelector" Value="{StaticResource SchedulerViewDayStyleSelector}" />
        <Setter Property="TimeRulerLabelStyle" Value="{StaticResource SchedulerMultidayViewTimeRulerLabelStyle}" />
        <Setter Property="SpecialSlotStyleSelector" Value="{StaticResource SchedulerSpecialSlotStyleSelector}" />
    </Style>

    <Style TargetType="telerikMauiControls:MonthViewDefinition">
        <Setter Property="VerticalLineStyle" Value="{StaticResource SchedulerViewLineStyle}" />
        <Setter Property="HorizontalLineStyle" Value="{StaticResource SchedulerViewLineStyle}" />
        <Setter Property="DayStyleSelector" Value="{StaticResource SchedulerMonthViewDayStyleSelector}" />
        <Setter Property="DayNameStyleSelector" Value="{StaticResource SchedulerMonthViewDayNameStyleSelector}" />
        <Setter Property="DayMoreAppointmentsIndicatorStyle" Value="{StaticResource SchedulerMonthViewMoreIndicatorStyle}" />
    </Style>

    <Style x:Key="SchedulerAgendaViewInitializationBusyIndicatorStyle" TargetType="telerikMauiControls:RadBusyIndicator">
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="AnimationType" Value="Animation9" />
        <Setter Property="AnimationContentColor" Value="{DynamicResource RadPrimaryColor}" />
    </Style>

    <Style x:Key="SchedulerAgendaViewLoadingProgressIndicatorStyle" TargetType="telerikMauiControls:RadLinearProgressBar">
        <Setter Property="VerticalOptions" Value="Start" />
        <Setter Property="IsIndeterminate" Value="True" />
        <Setter Property="HeightRequest" Value="2" />
        <Setter Property="InputTransparent" Value="True" />
        <Setter Property="ProgressFill" Value="{DynamicResource RadPrimaryBrush}" />
    </Style>

    <Style x:Key="SchedulerAgendaViewCollectionViewItemStyle" TargetType="telerikMauiControls:RadCollectionViewItemView">
        <Setter Property="ControlTemplate">
            <Setter.Value>
                <ControlTemplate>
                    <ContentPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="MinimumHeightRequest" Value="0" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="MouseOver" />
                    <VisualState Name="Focused" />
                    <VisualState Name="FocusedSelected" />
                    <VisualState Name="FocusedMouseOver" />
                    <VisualState Name="Disabled" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <telerikScheduler:AgendaAppointmentDurationTemplateSelector x:Key="AgendaAppointmentDurationTemplateSelector"
                                                                AllDayDurationTemplate="{StaticResource SchedulerAgendaAllDayDurationTemplate}"
                                                                StartOnlyDurationTemplate="{StaticResource SchedulerAgendaStartOnlyDurationTemplate}"
                                                                StartEndDurationTemplate="{StaticResource SchedulerAgendaStartEndDurationTemplate}"
                                                                EndOnlyDurationTemplate="{StaticResource SchedulerAgendaEndOnlyDurationTemplate}" />

    <Style x:Key="SchedulerAgendaViewStyle" TargetType="telerikMauiControls:AgendaViewDefinition">
        <Setter Property="InitializationBusyIndicatorStyle" Value="{StaticResource SchedulerAgendaViewInitializationBusyIndicatorStyle}" />
        <Setter Property="LoadingProgressIndicatorStyle" Value="{StaticResource SchedulerAgendaViewLoadingProgressIndicatorStyle}" />
        <Setter Property="DayTemplate" Value="{StaticResource SchedulerAgendaDayTemplate}" />
        <Setter Property="WeekTemplate" Value="{StaticResource SchedulerAgendaWeekTemplate}" />
        <Setter Property="MonthTemplate" Value="{StaticResource SchedulerAgendaMonthTemplate}" />
        <Setter Property="AppointmentDurationTemplate" Value="{StaticResource AgendaAppointmentDurationTemplateSelector}" />
    </Style>
    <Style TargetType="telerikMauiControls:AgendaViewDefinition" BasedOn="{StaticResource SchedulerAgendaViewStyle}" />

    <Shadow x:Key="SchedulerDialogShadow"
            Offset="0, 2"
            Radius="{OnPlatform Default=0, MacCatalyst=12, WinUI=21}"
            Brush="{OnPlatform Default=Transparent, MacCatalyst=#33000000, WinUI=#38000000}" />

    <Style x:Key="SchedulerDialogHeaderStyle" TargetType="Label">
        <Setter Property="FontAttributes" Value="{OnPlatform Default=Bold, WinUI=None}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="HorizontalTextAlignment" Value="{OnPlatform Default=Start, MacCatalyst=Center}" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="LineBreakMode" Value="TailTruncation" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, MacCatalyst='16, 0, 0, 0', WinUI='0, 0, 12, 0'}" />
    </Style>

    <Style x:Key="SchedulerDialogHeaderStyle_Mobile" TargetType="Label" BasedOn="{StaticResource SchedulerDialogHeaderStyle}">
        <Setter Property="FontSize" Value="{OnPlatform Default=14, iOS=17}" />
        <Setter Property="Padding" Value="16, 4" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="SchedulerDialogCancelButtonStyle_Mobile" TargetType="telerikMauiControls:RadButton">
        <Setter Property="FontSize" Value="{OnPlatform Default=14, iOS=17}" />
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <Style x:Key="SchedulerDialogCancelButtonStyle_MacCatalyst" TargetType="telerikMauiControls:RadButton">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderWidth" Value="0.5" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="Padding" Value="8, 2" />
        <Setter Property="Shadow">
            <Setter.Value>
                <Shadow Offset="0, 1" Radius="1.5" Brush="#33000000" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SchedulerDialogCancelButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{OnPlatform Android={StaticResource SchedulerDialogCancelButtonStyle_Mobile}, iOS={StaticResource SchedulerDialogCancelButtonStyle_Mobile}, MacCatalyst={StaticResource SchedulerDialogCancelButtonStyle_MacCatalyst}}">
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=65, WinUI=120}" />
    </Style>

    <Style x:Key="SchedulerDialogConfirmButtonStyle_Mobile" TargetType="telerikMauiControls:RadButton">
        <Setter Property="FontSize" Value="{OnPlatform Default=14, iOS=17}" />
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <Style x:Key="SchedulerDialogConfirmButtonStyle_MacCatalyst" TargetType="telerikMauiControls:RadButton">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="Padding" Value="8, 2" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadPrimaryButtonDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SchedulerDialogConfirmButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{OnPlatform Android={StaticResource SchedulerDialogConfirmButtonStyle_Mobile}, iOS={StaticResource SchedulerDialogConfirmButtonStyle_Mobile}, MacCatalyst={StaticResource SchedulerDialogConfirmButtonStyle_MacCatalyst}}">
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=0, MacCatalyst=50, WinUI=120}" />
    </Style>

    <Style x:Key="SchedulerDialogCloseButtonBaseStyle" TargetType="telerikMauiControls:RadButton">
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="SchedulerDialogCloseButtonStyle_MacCatalyst" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SchedulerDialogCloseButtonBaseStyle}">
        <Setter Property="FontSize" Value="6" />
        <Setter Property="TextColor" Value="#000000" />
        <Setter Property="BackgroundColor" Value="#FF5F57" />
        <Setter Property="BorderColor" Value="#1F000000" />
        <Setter Property="BorderWidth" Value="0.5" />
        <Setter Property="WidthRequest" Value="12" />
        <Setter Property="HeightRequest" Value="12" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.Text" Value="{x:Static telerikMaui:TelerikFont.IconCancel}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.Text" Value="{x:Static telerikMaui:TelerikFont.IconCancel}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SchedulerDialogCloseButtonStyle_WinUI" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SchedulerDialogCloseButtonBaseStyle}">
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconCancel}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BorderWidth" Value="0" />
        <Setter Property="WidthRequest" Value="46" />
        <Setter Property="HeightRequest" Value="32" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadOnErrorColor}" />
                            <Setter Property="telerikMauiControls:RadButton.BackgroundColor" Value="{DynamicResource RadErrorColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadButton.TextColor" Value="{DynamicResource RadOnErrorColor}" />
                            <Setter Property="telerikMauiControls:RadButton.BackgroundColor" Value="{DynamicResource RadErrorColor}" />
                            <Setter Property="telerikMauiControls:RadButton.Opacity" Value="0.9" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SchedulerChoiceDialogActionButtonStyle_Mobile" TargetType="telerikMauiControls:RadButton">
        <Setter Property="FontSize" Value="{OnPlatform Default=14, iOS=20}" />
        <Setter Property="TextColor" Value="{OnPlatform Default={DynamicResource RadSubtleColor}, iOS={DynamicResource RadPrimaryColor}}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerChoiceDialogButtonBackgroundColor}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, iOS=14}" />
        <Setter Property="Padding" Value="{OnPlatform Default=0, Android='16, 0'}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=48, iOS=56}" />
        <Setter Property="HorizontalContentAlignment" Value="{OnPlatform Default=Center, Android=Start}" />
    </Style>

    <Style x:Key="SchedulerChoiceDialogDestructiveActionButtonStyle_Mobile" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource SchedulerChoiceDialogActionButtonStyle_Mobile}">
        <Setter Property="TextColor" Value="{OnPlatform Default={DynamicResource RadSubtleColor}, iOS={DynamicResource RadErrorColor}}" />
    </Style>

    <Style x:Key="SchedulerDialogLabelStyle" TargetType="Label">
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=0, MacCatalyst=100, WinUI=80}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=14, iOS=17}" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="SchedulerEditAppointmentDialogLabelStyle_Mobile" TargetType="Label" BasedOn="{StaticResource SchedulerDialogLabelStyle}">
        <Setter Property="WidthRequest" Value="85" />
    </Style>

    <Style x:Key="SchedulerEditRecurrenceDialogLabelStyle_Mobile" TargetType="Label" BasedOn="{StaticResource SchedulerDialogLabelStyle}">
        <Setter Property="WidthRequest" Value="55" />
    </Style>

    <Style x:Key="SchedulerDialogSeparatorStyle_Mobile" TargetType="telerikScheduler:SchedulerBoxView">
        <Setter Property="Grid.ColumnSpan" Value="2" />
        <Setter Property="HeightRequest" Value="1" />
        <Setter Property="BackgroundColor" Value="{Binding BorderColor, Source={RelativeSource AncestorType={x:Type telerikScheduler:SchedulerDialog}}}" />
        <Setter Property="Margin" Value="0, 4" />
    </Style>

    <Style x:Key="SchedulerDialogRadioButtonStyle_Mobile" TargetType="RadioButton">
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <Grid ColumnDefinitions="Auto, *"
                      ColumnSpacing="4">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CheckedStates">
                                <VisualState x:Name="Checked">
                                    <VisualState.Setters>
                                        <Setter TargetName="check" Property="Label.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Unchecked" />
                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>
                    <Label x:Name="check"
                           Text="{x:Static telerikMaui:TelerikFont.IconOk}"
                           TextColor="Transparent"
                           FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                           FontSize="16"
                           WidthRequest="44"
                           HeightRequest="44"
                           HorizontalTextAlignment="Center"
                           VerticalTextAlignment="Center" />
                    <ContentPresenter Grid.Column="1" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>

    <Style x:Key="SchedulerDialogRadioButtonStyle_Desktop" TargetType="RadioButton">
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=0, WinUI=38}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <Grid ColumnDefinitions="Auto, *"
                      ColumnSpacing="{OnPlatform Default=6, WinUI=10}">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CheckedStates">
                                <VisualState x:Name="Checked">
                                    <VisualState.Setters>
                                        <Setter TargetName="outerEllipse" Property="Ellipse.Fill" Value="{DynamicResource RadPrimaryBrush}" />
                                        <Setter TargetName="outerEllipse" Property="Ellipse.Stroke" Value="{DynamicResource RadPrimaryBrush}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Unchecked" />
                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>
                    <Grid VerticalOptions="Center">
                        <Ellipse x:Name="outerEllipse"
                                 Stroke="{DynamicResource RadioButtonEllipseStroke}"
                                 Fill="{DynamicResource RadioButtonEllipseFill}"
                                 WidthRequest="{OnPlatform Default=16, WinUI=20}"
                                 HeightRequest="{OnPlatform Default=16, WinUI=20}"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center" />
                        <Ellipse x:Name="check"
                                 IsVisible="{TemplateBinding IsChecked}"
                                 Fill="{DynamicResource RadioButtonEllipseFill}"
                                 WidthRequest="{OnPlatform Default=6, WinUI=12}"
                                 HeightRequest="{OnPlatform Default=6, WinUI=12}"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center" />
                    </Grid>
                    <ContentPresenter Grid.Column="1"
                                      VerticalOptions="Center" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>

    <Style x:Key="SchedulerDialogSwitchStyle" TargetType="Switch">
        <Setter Property="OnColor" Value="{DynamicResource SwitchToggledOffOnColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="On">
                        <VisualState.Setters>
                            <Setter Property="Switch.OnColor" Value="{DynamicResource SwitchToggledOnOnColor}" />
                            <Setter Property="Switch.ThumbColor" Value="{DynamicResource SwitchToggledOnThumbColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Off">
                        <VisualState.Setters>
                            <Setter Property="Switch.OnColor" Value="{DynamicResource SwitchToggledOffOnColor}" />
                            <Setter Property="Switch.ThumbColor" Value="{DynamicResource SwitchToggledOffThumbColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SchedulerEditRecurrenceDialogNumericInputStyle" TargetType="telerikMauiControls:RadNumericInput">
        <Setter Property="Minimum" Value="1" />
        <Setter Property="Maximum" Value="99" />
        <Setter Property="StringFormat" Value="{}{0:N0}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=0, MacCatalyst=70, WinUI=145}" />
    </Style>

    <Style x:Key="SchedulerEditRecurrenceDialogNumericInputStyle_Mobile" TargetType="telerikMauiControls:RadNumericInput" BasedOn="{StaticResource SchedulerEditRecurrenceDialogNumericInputStyle}">
        <Setter Property="WidthRequest" Value="{OnPlatform Default=193, iOS=189}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=1, Android=0}" />
    </Style>

    <Style x:Key="SchedulerEditRecurrenceDialogDayOfWeekItemStyle_Mobile" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerDialogFooterBackgroundColor}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=36, Android=44}" />
        <Setter Property="WidthRequest" Value="{OnPlatform Default=42, Android=49}" />
        <Setter Property="TextMargin" Value="0" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="PointerOver" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.BackgroundColor" Value="{DynamicResource RadSchedulerDialogFooterBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SchedulerEditRecurrenceDialogDayOfWeekSelectedItemStyle_Mobile" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer" BasedOn="{StaticResource SchedulerEditRecurrenceDialogDayOfWeekItemStyle_Mobile}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
    </Style>

    <Style x:Key="SchedulerEditRecurrenceDialogDayOfWeekItemStyle_MacCatalyst" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="Padding" Value="10, 2" />
        <Setter Property="Margin" Value="0, 0, 4, 0" />
        <Setter Property="TextMargin" Value="0" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="PointerOver" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
                            <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SchedulerEditRecurrenceDialogDayOfWeekSelectedItemStyle_MacCatalyst" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer" BasedOn="{StaticResource SchedulerEditRecurrenceDialogDayOfWeekItemStyle_MacCatalyst}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadPrimaryColor}" />
    </Style>

    <Style x:Key="SchedulerEditRecurrenceDialogDayOfWeekItemStyle_WinUI" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer">
        <Setter Property="MinimumHeightRequest" Value="32" />
        <Setter Property="MinimumWidthRequest" Value="40" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadBaseColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Margin" Value="0, 0, 4, 0" />
        <Setter Property="TextMargin" Value="0" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.BackgroundColor" Value="{DynamicResource RadBaseActiveColor}" />
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SchedulerEditRecurrenceDialogDayOfWeekSelectedItemStyle_WinUI" TargetType="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer" BasedOn="{StaticResource SchedulerEditRecurrenceDialogDayOfWeekItemStyle_WinUI}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.BackgroundColor" Value="{DynamicResource RadPrimaryColorAlpha90}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                            <Setter Property="telerikMauiDataControls:NonVirtualizedItemsControlItemContainer.BackgroundColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha38}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <ControlTemplate x:Key="SchedulerPreviewAppointmentDialogControlTemplate_Mobile">
        <Grid RowDefinitions="{OnPlatform Default='40, Auto, *, 56', iOS='44, Auto, *, 52'}"
              BindingContext="{TemplateBinding BindingContext}"
              Padding="0, 8, 0, 0">
            <Grid ColumnDefinitions="Auto, *, Auto">
                <telerikMauiControls:RadButton Command="{Binding BackCommand}"
                                               FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                                               FontSize="16"
                                               Text="{x:Static telerikMaui:TelerikFont.IconLeftArrow}"
                                               TextColor="{DynamicResource RadPrimaryColor}"
                                               Background="Transparent"
                                               WidthRequest="44"
                                               HeightRequest="44" />
                <Label Grid.Column="1"
                       Text="{Binding Occurrence.Appointment.Subject}"
                       TextColor="{DynamicResource RadOnAppSurfaceColor}"
                       FontSize="{OnPlatform Android=14, iOS=17}"
                       FontAttributes="Bold"
                       LineBreakMode="TailTruncation"
                       VerticalOptions="Center" />
                <telerikMauiControls:RadButton Grid.Column="2"
                                               Command="{Binding EditCommand}"
                                               IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                               Text="{telerikMauiControls:Localize Scheduler_EditAppointmentPreview_Edit}"
                                               TextColor="{DynamicResource RadPrimaryColor}"
                                               Background="Transparent"
                                               MinimumWidthRequest="60" />
            </Grid>
            <Grid Grid.Row="1"
                  RowDefinitions="Auto, Auto, Auto"
                  RowSpacing="8"
                  Padding="16, 10, 16, 0">
                <Label Text="{Binding Duration}"
                       TextColor="{DynamicResource RadOnAppSurfaceColor}"
                       FontSize="{OnPlatform Android=14, iOS=17}" />
                <Label Grid.Row="1"
                       Text="{Binding Summary}"
                       TextColor="{DynamicResource RadOnAppSurfaceColor}"
                       FontSize="12" />
                <Label Grid.Row="2"
                       Text="{Binding Description}"
                       TextColor="{DynamicResource RadOnAppSurfaceColor}"
                       FontSize="{OnPlatform Android=14, iOS=17}"/>
            </Grid>
            <Grid Grid.Row="3"
                  BackgroundColor="{DynamicResource RadSchedulerDialogFooterBackgroundColor}"
                  Margin="0, 8, 0, 0">
                <telerikScheduler:SchedulerBoxView HeightRequest="1"
                                                   BackgroundColor="{Binding BorderColor, Source={RelativeSource AncestorType={x:Type telerikScheduler:SchedulerDialog}}}"
                                                   VerticalOptions="Start" />
                <telerikMauiControls:RadButton Grid.Row="4"
                                               IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                               Text="{telerikMauiControls:Localize Scheduler_EditAppointmentPreview_Delete_Appointment}"
                                               TextColor="{DynamicResource RadPrimaryColor}"
                                               Background="Transparent"
                                               Command="{Binding DeleteCommand}" />
            </Grid>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="SchedulerEditRecurrenceDialogControlTemplate_Mobile">
        <Grid RowDefinitions="{OnPlatform Default='40, *, 56', iOS='44, *, 52'}"
              Padding="0, 8, 0, 0">
            <Label Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Header}"
                   Style="{StaticResource SchedulerDialogHeaderStyle_Mobile}" />
            <Grid Grid.Row="1"
                  RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, *"
                  ColumnDefinitions="Auto, *"
                  BindingContext="{TemplateBinding BindingContext}"
                  Padding="16, 4, 16, 0">
                <Label Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Start}"
                       Style="{StaticResource SchedulerEditRecurrenceDialogLabelStyle_Mobile}" />
                <telerikMauiControls:RadDatePicker Grid.Column="1"
                                                   Date="{Binding Start, Mode=TwoWay}"
                                                   WidthRequest="{OnPlatform Android=156, iOS=154}"
                                                   HorizontalOptions="End"
                                                   BorderThickness="{OnPlatform Android=0}" />
                <telerikScheduler:SchedulerBoxView Grid.Row="1"
                                                   Style="{StaticResource SchedulerDialogSeparatorStyle_Mobile}" />
                <Label Grid.Row="2"
                       Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat}"
                       Style="{StaticResource SchedulerEditRecurrenceDialogLabelStyle_Mobile}" />
                <telerikMauiControls:RadComboBox Grid.Row="2"
                                                 Grid.Column="1"
                                                 SelectedItem="{Binding RepeatOption, Mode=TwoWay}"
                                                 ItemsSource="{Binding RepeatOptions}"
                                                 IsClearButtonVisible="False"
                                                 HorizontalOptions="End"
                                                 WidthRequest="{OnPlatform Android=156, iOS=154}"
                                                 BorderThickness="{OnPlatform Android=0}" />
                <telerikScheduler:SchedulerBoxView Grid.Row="3"
                                                   Style="{StaticResource SchedulerDialogSeparatorStyle_Mobile}" />
                <Label Grid.Row="4"
                       Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Every}"
                       Style="{StaticResource SchedulerEditRecurrenceDialogLabelStyle_Mobile}" />
                <Grid Grid.Row="4"
                      Grid.Column="1"
                      ColumnDefinitions="*, Auto"
                      ColumnSpacing="{OnPlatform Default=8, iOS=4}"
                      HorizontalOptions="End">
                    <telerikMauiControls:RadNumericInput IsVisible="{Binding RepeatOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Daily}}"
                                                         Style="{StaticResource SchedulerEditRecurrenceDialogNumericInputStyle_Mobile}"
                                                         Value="{Binding DaysInterval, Mode=TwoWay}" />
                    <telerikMauiControls:RadNumericInput IsVisible="{Binding RepeatOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Weekly}}"
                                                         Style="{StaticResource SchedulerEditRecurrenceDialogNumericInputStyle_Mobile}"
                                                         Value="{Binding WeeksInterval, Mode=TwoWay}" />
                    <telerikMauiControls:RadNumericInput IsVisible="{Binding RepeatOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Monthly}}"
                                                         Style="{StaticResource SchedulerEditRecurrenceDialogNumericInputStyle_Mobile}"
                                                         Value="{Binding MonthsInterval, Mode=TwoWay}"/>
                    <telerikMauiControls:RadComboBox IsVisible="{Binding RepeatOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Yearly}}"
                                                     SelectedIndex="{Binding SelectedMonth}"
                                                     ItemsSource="{Binding Months}"
                                                     IsClearButtonVisible="False"
                                                     WidthRequest="{OnPlatform Android=156, iOS=154}"
                                                     BorderThickness="{OnPlatform Android=0}"
                                                     Margin="{OnPlatform Default='0, 0, -8, 0', iOS='0, 0, -4, 0'}" />
                    <Label Grid.Column="1"
                           Text="{Binding RepeatOptionUnitStr}"
                           TextColor="{DynamicResource RadOnAppSurfaceColor}"
                           FontSize="{OnPlatform Android=14, iOS=17}"
                           VerticalOptions="Center" />
                </Grid>
                <telerikScheduler:SchedulerBoxView Grid.Row="5"
                                                   Style="{StaticResource SchedulerDialogSeparatorStyle_Mobile}" />
                <Grid Grid.Row="6"
                      Grid.ColumnSpan="2"
                      RowDefinitions="Auto, Auto, Auto"
                      ColumnDefinitions="Auto, *">
                    <Grid.IsVisible>
                        <MultiBinding Converter="{StaticResource MultiBooleanConverter}">
                            <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Weekly}" />
                            <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Monthly}" />
                            <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Yearly}" />
                        </MultiBinding>
                    </Grid.IsVisible>
                    <Label Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_On}"
                           Style="{StaticResource SchedulerEditRecurrenceDialogLabelStyle_Mobile}" />
                    <Grid Grid.Column="1"
                          ColumnDefinitions="44, Auto"
                          ColumnSpacing="4"
                          HorizontalOptions="End">
                        <Grid.IsVisible>
                            <MultiBinding Converter="{StaticResource MultiBooleanConverter}">
                                <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Monthly}" />
                                <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Yearly}" />
                            </MultiBinding>
                        </Grid.IsVisible>
                        <RadioButton IsChecked="{Binding DayOfMonthOptionEnabled}"
                                     InputTransparent="{Binding DayOfMonthOptionEnabled}"
                                     Style="{StaticResource SchedulerDialogRadioButtonStyle_Mobile}"
                                     VerticalOptions="Center"
                                     GroupName="RecurrenceRepeatChoice" />
                        <Grid Grid.Column="1"
                              ColumnDefinitions="*, Auto"
                              ColumnSpacing="4"
                              WidthRequest="{OnPlatform Android=258, iOS=263}">
                            <telerikMauiControls:RadNumericInput IsEnabled="{Binding DayOfMonthOptionEnabled}"
                                                                 Style="{StaticResource SchedulerEditRecurrenceDialogNumericInputStyle_Mobile}"
                                                                 WidthRequest="{OnPlatform Android=192}"
                                                                 Maximum="31"
                                                                 Value="{Binding DayOfMonth, Mode=TwoWay}"
                                                                 HorizontalOptions="{OnPlatform Android=Start}"
                                                                 VerticalOptions="Center" />
                            <Label Grid.Column="1"
                                   Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Day}"
                                   TextColor="{DynamicResource RadOnAppSurfaceColor}"
                                   FontSize="{OnPlatform Android=14, iOS=17}"
                                   VerticalOptions="Center" />
                        </Grid>
                    </Grid>
                    <Grid Grid.Column="1"
                          Grid.Row="1"
                          ColumnDefinitions="44, Auto"
                          ColumnSpacing="4"
                          Margin="0, 4, 0, 0"
                          HorizontalOptions="End">
                        <Grid.IsVisible>
                            <MultiBinding Converter="{StaticResource MultiBooleanConverter}">
                                <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Monthly}" />
                                <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Yearly}" />
                            </MultiBinding>
                        </Grid.IsVisible>
                        <RadioButton IsChecked="{Binding WeekOfMonthOptionEnabled}"
                                     InputTransparent="{Binding WeekOfMonthOptionEnabled}"
                                     Style="{StaticResource SchedulerDialogRadioButtonStyle_Mobile}"
                                     VerticalOptions="Center"
                                     GroupName="RecurrenceRepeatChoice" />
                        <Grid Grid.Column="1"
                              ColumnDefinitions="Auto, Auto"
                              ColumnSpacing="4"
                              WidthRequest="{OnPlatform Android=258, iOS=263}">
                            <telerikMauiControls:RadComboBox IsEnabled="{Binding DayOfMonthOptionEnabled, Converter={StaticResource InvertedBooleanConverter}}"
                                                             SelectedIndex="{Binding SelectedWeekOfMonth, Mode=TwoWay}"
                                                             ItemsSource="{Binding WeeksOfMonth}"
                                                             IsClearButtonVisible="False"
                                                             WidthRequest="{OnPlatform Android=97, iOS=105}"
                                                             VerticalOptions="Center"
                                                             BorderThickness="{OnPlatform Android=0}" />
                            <telerikMauiControls:RadComboBox Grid.Column="1"
                                                             IsEnabled="{Binding DayOfMonthOptionEnabled, Converter={StaticResource InvertedBooleanConverter}}"
                                                             SelectedItem="{Binding SelectedRecurrenceDay, Mode=TwoWay}"
                                                             ItemsSource="{Binding RecurrenceDaysOptions, Mode=TwoWay}"
                                                             IsClearButtonVisible="False"
                                                             WidthRequest="{OnPlatform Android=155, iOS=154}"
                                                             VerticalOptions="Center"
                                                             BorderThickness="{OnPlatform Android=0}" />
                        </Grid>
                    </Grid>
                    <telerikMauiControls:RadBorder Grid.Column="1"
                                                   IsVisible="{Binding RepeatOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Weekly}}"
                                                   BackgroundColor="{DynamicResource RadSchedulerDialogFooterBackgroundColor}"
                                                   CornerRadius="{OnPlatform Android=4, iOS=10}"
                                                   HorizontalOptions="End">
                        <telerikMauiDataControls:NonVirtualizedItemsControl ItemsSource="{Binding DaysOfWeek, Mode=TwoWay}"
                                                                            ItemStyle="{StaticResource SchedulerEditRecurrenceDialogDayOfWeekItemStyle_Mobile}"
                                                                            SelectedItemStyle="{StaticResource SchedulerEditRecurrenceDialogDayOfWeekSelectedItemStyle_Mobile}"
                                                                            SelectedItems="{Binding SelectedDaysOfWeek}"
                                                                            SelectedItem="{Binding SelectedDayOfWeek, Mode=TwoWay}"
                                                                            SelectionMode="Multiple"
                                                                            Orientation="Horizontal"
                                                                            HorizontalOptions="End" />
                    </telerikMauiControls:RadBorder>
                    <telerikScheduler:SchedulerBoxView Grid.Row="2"
                                                       Style="{StaticResource SchedulerDialogSeparatorStyle_Mobile}" />
                </Grid>
                <Grid Grid.Row="7"
                      Grid.ColumnSpan="2"
                      RowDefinitions="Auto, Auto"
                      ColumnDefinitions="Auto, *">
                    <Label Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_End}"
                           Style="{StaticResource SchedulerEditRecurrenceDialogLabelStyle_Mobile}" />
                    <telerikMauiControls:RadComboBox Grid.Column="1"
                                                     SelectedItem="{Binding EndOption, Mode=TwoWay}"
                                                     ItemsSource="{Binding EndOptions}"
                                                     IsClearButtonVisible="False"
                                                     WidthRequest="{OnPlatform Android=156, iOS=154}"
                                                     HorizontalOptions="End"
                                                     BorderThickness="{OnPlatform Android=0}" />
                    <Grid Grid.Row="1"
                          Grid.Column="1"
                          RowDefinitions="Auto, Auto"
                          HorizontalOptions="End">
                        <telerikMauiControls:RadDatePicker IsVisible="{Binding EndOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_End_On_This_Day}}"
                                                           Date="{Binding End, Mode=TwoWay}"
                                                           WidthRequest="{OnPlatform Android=156, iOS=154}"
                                                           Margin="0, 4, 0, 0"
                                                           BorderThickness="{OnPlatform Android=0}" />
                        <Grid Grid.Row="1"
                              IsVisible="{Binding EndOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_End_After}}"
                              ColumnDefinitions="*, Auto"
                              ColumnSpacing="{OnPlatform Default=8, iOS=4}"
                              Margin="0, 4, 0, 0">
                            <telerikMauiControls:RadNumericInput Style="{StaticResource SchedulerEditRecurrenceDialogNumericInputStyle_Mobile}"
                                                                 Maximum="{x:Static x:Double.MaxValue}"
                                                                 Value="{Binding MaxOccurrences, Mode=TwoWay}"
                                                                 WidthRequest="{OnPlatform Android=153, iOS=163}" />
                            <Label Grid.Column="1"
                                   Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_End_After_Occurrences}"
                                   TextColor="{DynamicResource RadOnAppSurfaceColor}"
                                   FontSize="{OnPlatform Android=14, iOS=17}"
                                   VerticalOptions="Center" />
                        </Grid>
                    </Grid>
                </Grid>
                <telerikScheduler:SchedulerBoxView Grid.Row="8"
                                                   Style="{StaticResource SchedulerDialogSeparatorStyle_Mobile}" />
                <Label Grid.Row="9"
                       Grid.ColumnSpan="2"
                       Text="{Binding Summary}"
                       TextColor="{DynamicResource RadOnAppSurfaceColor}"
                       FontSize="12"
                       Margin="0, 8, 0, 4" />
            </Grid>
            <Grid Grid.Row="2"
                  ColumnDefinitions="*, *"
                  BackgroundColor="{DynamicResource RadSchedulerDialogFooterBackgroundColor}"
                  Margin="0, 8, 0, 0">
                <telerikScheduler:SchedulerBoxView Grid.ColumnSpan="2"
                                                   HeightRequest="1"
                                                   BackgroundColor="{Binding BorderColor, Source={RelativeSource AncestorType={x:Type telerikScheduler:SchedulerDialog}}}"
                                                   VerticalOptions="Start" />
                <telerikMauiControls:RadButton Style="{StaticResource SchedulerDialogCancelButtonStyle_Mobile}"
                                               Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Cancel}"
                                               Command="{Binding CancelCommand}" />
                <telerikMauiControls:RadButton Grid.Column="1"
                                               Style="{StaticResource SchedulerDialogConfirmButtonStyle_Mobile}"
                                               Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Save}"
                                               Command="{Binding SaveCommand}" />
            </Grid>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="SchedulerEditRecurrenceDialogControlTemplate_Desktop">
        <Grid RowDefinitions="{OnPlatform Default='28, Auto, *, Auto, Auto', WinUI='32, Auto, *, Auto, Auto'}">
            <Grid ColumnDefinitions="{OnPlatform Default='*, Auto', MacCatalyst='Auto, *'}"
                  Padding="{OnPlatform Default='16, 0', WinUI='12, 0, 0, 0'}">
                <Label Grid.Column="{OnPlatform Default=0, MacCatalyst=1}"
                       Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Header}" 
                       Style="{StaticResource SchedulerDialogHeaderStyle}" />
                <telerikMauiControls:RadButton x:Name="PART_CloseButton"
                                               Grid.Column="{OnPlatform Default=1, MacCatalyst=0}"
                                               Style="{OnPlatform MacCatalyst={StaticResource SchedulerDialogCloseButtonStyle_MacCatalyst}, WinUI={StaticResource SchedulerDialogCloseButtonStyle_WinUI}}" />
            </Grid>
            <telerikScheduler:SchedulerBoxView Grid.Row="1"
                                               HeightRequest="1"
                                               BackgroundColor="{Binding BorderColor, Source={RelativeSource AncestorType={x:Type telerikScheduler:SchedulerDialog}}}"
                                               IsVisible="{OnPlatform Default=False, MacCatalyst=True}" />
            <Grid Grid.Row="2"
                  ColumnDefinitions="Auto, *"
                  ColumnSpacing="{OnPlatform Default=8, WinUI=24}"
                  RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto"
                  RowSpacing="8"
                  BindingContext="{TemplateBinding BindingContext}"
                  Padding="{OnPlatform Default='20, 20, 20, 0', WinUI='12, 24'}">
                <Label Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Start}"
                       Style="{StaticResource SchedulerDialogLabelStyle}" />
                <telerikMauiControls:RadDatePicker Grid.Column="1"
                                                   MinimumWidthRequest="{OnPlatform Default=140, WinUI=145}"
                                                   HorizontalOptions="Start"
                                                   Date="{Binding Start, Mode=TwoWay}" />
                <Label Grid.Row="1"
                       Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_End}"
                       Style="{StaticResource SchedulerDialogLabelStyle}" />
                <Grid Grid.Row="1"
                      Grid.Column="1"
                      ColumnDefinitions="Auto, Auto, Auto"
                      ColumnSpacing="{OnPlatform Default=4, WinUI=8}"
                      MinimumHeightRequest="{OnPlatform MacCatalyst=32}">
                    <telerikMauiControls:RadComboBox SelectedItem="{Binding EndOption, Mode=TwoWay}"
                                                     ItemsSource="{Binding EndOptions}"
                                                     MinimumWidthRequest="{OnPlatform Default=140, WinUI=145}"
                                                     IsClearButtonVisible="False" />
                    <telerikMauiControls:RadDatePicker Grid.Column="1"
                                                       IsVisible="{Binding EndOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_End_On_This_Day}}"
                                                       Date="{Binding End, Mode=TwoWay}"
                                                       MinimumWidthRequest="{OnPlatform Default=140, WinUI=145}" />
                    <telerikMauiControls:RadNumericInput Grid.Column="1"
                                                         Style="{StaticResource SchedulerEditRecurrenceDialogNumericInputStyle}"
                                                         HeightRequest="{OnPlatform MacCatalyst=31}"
                                                         IsVisible="{Binding EndOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_End_After}}"
                                                         Maximum="{x:Static x:Double.MaxValue}"
                                                         Value="{Binding MaxOccurrences, Mode=TwoWay}" />
                    <Label Grid.Column="2"
                           Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_End_After_Occurrences}"
                           IsVisible="{Binding EndOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_End_After}}"
                           VerticalOptions="Center" />
                </Grid>
                <Label Grid.Row="2"
                       Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat}"
                       Style="{StaticResource SchedulerDialogLabelStyle}" />
                <telerikMauiControls:RadComboBox Grid.Row="2"
                                                 Grid.Column="1"
                                                 SelectedItem="{Binding RepeatOption, Mode=TwoWay}"
                                                 ItemsSource="{Binding RepeatOptions}"
                                                 MinimumWidthRequest="{OnPlatform Default=140, WinUI=145}"
                                                 HorizontalOptions="Start"
                                                 IsClearButtonVisible="False" />
                <Label Grid.Row="3"
                       Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Every}"
                       Style="{StaticResource SchedulerDialogLabelStyle}" />
                <Grid Grid.Row="3"
                      Grid.Column="1"
                      ColumnDefinitions="Auto, *"
                      ColumnSpacing="{OnPlatform Default=4, WinUI=8}"
                      MinimumHeightRequest="{OnPlatform MacCatalyst=33, WinUI=34}">
                    <telerikMauiControls:RadNumericInput IsVisible="{Binding RepeatOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Daily}}"
                                                         Style="{StaticResource SchedulerEditRecurrenceDialogNumericInputStyle}"
                                                         Value="{Binding DaysInterval, Mode=TwoWay}" />
                    <telerikMauiControls:RadNumericInput IsVisible="{Binding RepeatOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Weekly}}"
                                                         Style="{StaticResource SchedulerEditRecurrenceDialogNumericInputStyle}"
                                                         Value="{Binding WeeksInterval, Mode=TwoWay}" />
                    <telerikMauiControls:RadNumericInput IsVisible="{Binding RepeatOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Monthly}}"
                                                         Style="{StaticResource SchedulerEditRecurrenceDialogNumericInputStyle}"
                                                         Value="{Binding MonthsInterval, Mode=TwoWay}" />
                    <telerikMauiControls:RadComboBox IsVisible="{Binding RepeatOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Yearly}}"
                                                     SelectedIndex="{Binding SelectedMonth}"
                                                     ItemsSource="{Binding Months}"
                                                     IsClearButtonVisible="False"
                                                     MinimumWidthRequest="{OnPlatform Default=140, WinUI=145}"
                                                     HorizontalOptions="Start" />
                    <Label Grid.Column="1"
                           Text="{Binding RepeatOptionUnitStr}"
                           VerticalOptions="Center" />
                </Grid>
                <Grid Grid.Row="4"
                      Grid.ColumnSpan="2"
                      ColumnDefinitions="Auto, *"
                      ColumnSpacing="{OnPlatform Default=8, WinUI=24}"
                      RowDefinitions="Auto, Auto">
                    <Label Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_On}"
                           Style="{StaticResource SchedulerDialogLabelStyle}">
                        <Label.IsVisible>
                            <MultiBinding Converter="{StaticResource MultiBooleanConverter}">
                                <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Weekly}" />
                                <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Monthly}" />
                                <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Yearly}" />
                            </MultiBinding>
                        </Label.IsVisible>
                    </Label>
                    <Grid Grid.Column="1"
                          ColumnDefinitions="{OnPlatform Default='Auto, 140, Auto', WinUI='Auto, 111, Auto'}"
                          ColumnSpacing="{OnPlatform Default=4, WinUI=0}"
                          MinimumHeightRequest="{OnPlatform MacCatalyst=33}">
                        <Grid.IsVisible>
                            <MultiBinding Converter="{StaticResource MultiBooleanConverter}">
                                <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Monthly}" />
                                <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Yearly}" />
                            </MultiBinding>
                        </Grid.IsVisible>
                        <RadioButton IsChecked="{Binding DayOfMonthOptionEnabled}"
                                     InputTransparent="{Binding DayOfMonthOptionEnabled}"
                                     MinimumWidthRequest="{OnPlatform WinUI=0}"
                                     Style="{StaticResource SchedulerDialogRadioButtonStyle_Desktop}"
                                     VerticalOptions="Center"
                                     GroupName="RecurrenceRepeatChoice" />
                        <telerikMauiControls:RadNumericInput Grid.Column="1"
                                                             IsEnabled="{Binding DayOfMonthOptionEnabled}"
                                                             Style="{StaticResource SchedulerEditRecurrenceDialogNumericInputStyle}"
                                                             MinimumWidthRequest="{OnPlatform WinUI=0}"
                                                             Maximum="31"
                                                             Value="{Binding DayOfMonth, Mode=TwoWay}"
                                                             VerticalOptions="Center" />
                        <Label Grid.Column="2"
                               Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Day}"
                               Margin="{OnPlatform WinUI='8, 0, 0, 0'}"
                               VerticalOptions="Center" />
                    </Grid>
                    <Grid Grid.Row="2"
                          Grid.Column="1"
                          ColumnDefinitions="{OnPlatform Default='Auto, 140, 140', WinUI='Auto, 111, 145'}"
                          ColumnSpacing="{OnPlatform Default=4, WinUI=0}"
                          Margin="0, 8, 0, 0"
                          MinimumHeightRequest="{OnPlatform MacCatalyst=33}">
                        <Grid.IsVisible>
                            <MultiBinding Converter="{StaticResource MultiBooleanConverter}">
                                <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Monthly}" />
                                <Binding Path="RepeatOption" Converter="{StaticResource StringToBoolConverter}" ConverterParameter="{telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Yearly}" />
                            </MultiBinding>
                        </Grid.IsVisible>
                        <RadioButton IsChecked="{Binding WeekOfMonthOptionEnabled}"
                                     InputTransparent="{Binding WeekOfMonthOptionEnabled}"
                                     MinimumWidthRequest="{OnPlatform WinUI=0}"
                                     Style="{StaticResource SchedulerDialogRadioButtonStyle_Desktop}"
                                     VerticalOptions="Center"
                                     GroupName="RecurrenceRepeatChoice" />
                        <telerikMauiControls:RadComboBox Grid.Column="1"
                                                         IsEnabled="{Binding DayOfMonthOptionEnabled, Converter={StaticResource InvertedBooleanConverter}}"
                                                         SelectedIndex="{Binding SelectedWeekOfMonth, Mode=TwoWay}"
                                                         ItemsSource="{Binding WeeksOfMonth}"
                                                         IsClearButtonVisible="False"
                                                         VerticalOptions="Center" />
                        <telerikMauiControls:RadComboBox Grid.Column="2"
                                                         IsEnabled="{Binding DayOfMonthOptionEnabled, Converter={StaticResource InvertedBooleanConverter}}"
                                                         SelectedItem="{Binding SelectedRecurrenceDay, Mode=TwoWay}"
                                                         ItemsSource="{Binding RecurrenceDaysOptions, Mode=TwoWay}"
                                                         IsClearButtonVisible="False"
                                                         Margin="{OnPlatform WinUI='8, 0, 0, 0'}"
                                                         VerticalOptions="Center" />
                    </Grid>
                    <telerikMauiDataControls:NonVirtualizedItemsControl Grid.Column="1"
                                                                        IsVisible="{Binding RepeatOption, Converter={StaticResource StringToBoolConverter}, ConverterParameter={telerikMauiControls:Localize Scheduler_EditRecurrenceDialog_Repeat_Weekly}}"
                                                                        ItemsSource="{Binding DaysOfWeek, Mode=TwoWay}"
                                                                        ItemStyle="{OnPlatform MacCatalyst={StaticResource SchedulerEditRecurrenceDialogDayOfWeekItemStyle_MacCatalyst}, WinUI={StaticResource SchedulerEditRecurrenceDialogDayOfWeekItemStyle_WinUI}}"
                                                                        SelectedItemStyle="{OnPlatform MacCatalyst={StaticResource SchedulerEditRecurrenceDialogDayOfWeekSelectedItemStyle_MacCatalyst}, WinUI={StaticResource SchedulerEditRecurrenceDialogDayOfWeekSelectedItemStyle_WinUI}}"
                                                                        SelectedItems="{Binding SelectedDaysOfWeek}"
                                                                        SelectedItem="{Binding SelectedDayOfWeek, Mode=TwoWay}"
                                                                        SelectionMode="Multiple"
                                                                        Orientation="Horizontal"
                                                                        MinimumHeightRequest="{OnPlatform MacCatalyst=33}" />
                </Grid>
                <Label Grid.Row="5"
                       Grid.Column="1"
                       Text="{Binding Summary}"
                       FontSize="{OnPlatform MacCatalyst=12}"
                       Margin="2, 0" />
            </Grid>
            <Grid Grid.Row="4"
                  Padding="{OnPlatform Default=20, WinUI=16}"
                  BackgroundColor="{DynamicResource RadSchedulerDialogFooterBackgroundColor}">
                <Grid HorizontalOptions="End"
                      ColumnDefinitions="Auto, Auto"
                      ColumnSpacing="8">
                    <telerikMauiControls:RadButton Grid.Column="{OnPlatform WinUI=1}"
                                                   Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Cancel}"
                                                   Style="{StaticResource SchedulerDialogCancelButtonStyle}"
                                                   Command="{Binding CancelCommand}" />
                    <telerikMauiControls:RadButton Grid.Column="{OnPlatform MacCatalyst=1}"
                                                   Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Save}"
                                                   Style="{StaticResource SchedulerDialogConfirmButtonStyle}"
                                                   Command="{Binding SaveCommand}" />
                </Grid>
            </Grid>
            <telerikScheduler:SchedulerBoxView Grid.Row="4"
                                               HeightRequest="1"
                                               BackgroundColor="{OnPlatform Default=Transparent, WinUI={DynamicResource RadBorderColor}}"
                                               IsVisible="{OnPlatform Default=False, WinUI=True}"
                                               VerticalOptions="Start" />
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="SchedulerEditAppointmentDialogControlTemplate_Mobile">
        <Grid RowDefinitions="{OnPlatform Default='40, *, 56', iOS='44, *, 52'}"
              Padding="0, 8, 0, 0">
            <Label Text="{Binding Header}"
                   Style="{StaticResource SchedulerDialogHeaderStyle_Mobile}" />
            <Grid Grid.Row="1"
                  RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, *"
                  ColumnDefinitions="Auto, *"
                  BindingContext="{TemplateBinding BindingContext}"
                  Padding="16, 4, 16, 0">
                <Label Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Subject}"
                       Style="{StaticResource SchedulerEditAppointmentDialogLabelStyle_Mobile}" />
                <telerikMauiControls:RadEntry Grid.Column="1"
                                              IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                              Text="{Binding Occurrence.Appointment.Subject, Mode=TwoWay}"
                                              BorderThickness="{OnPlatform Android=0}" />
                <telerikScheduler:SchedulerBoxView Grid.Row="1" 
                                                   Style="{StaticResource SchedulerDialogSeparatorStyle_Mobile}" />
                <Label Grid.Row="2"
                       Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_AllDay}"
                       Style="{StaticResource SchedulerEditAppointmentDialogLabelStyle_Mobile}" />
                <Switch Grid.Row="2"
                        Grid.Column="1"
                        HorizontalOptions="End"
                        IsToggled="{Binding IsAllDayAppointment, Mode=TwoWay}"
                        IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}">
                    <Switch.Style>
                        <Style TargetType="Switch" BasedOn="{StaticResource SchedulerDialogSwitchStyle}" />
                    </Switch.Style>
                </Switch>
                <telerikScheduler:SchedulerBoxView Grid.Row="3"
                                                   Style="{StaticResource SchedulerDialogSeparatorStyle_Mobile}" />
                <Label Grid.Row="4"
                       Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_StartTime}"
                       Style="{StaticResource SchedulerEditAppointmentDialogLabelStyle_Mobile}" />
                <HorizontalStackLayout Grid.Row="4"
                                       Grid.Column="1"
                                       Spacing="4"
                                       HorizontalOptions="End"
                                       IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}">
                    <telerikMauiControls:RadDatePicker Date="{Binding ActualStartDate, Mode=TwoWay}"
                                                       WidthRequest="102"
                                                       BorderThickness="{OnPlatform Android=0}" />
                    <telerikMauiControls:RadTimePicker Time="{Binding ActualStartTimeOfDay, Mode=TwoWay}"
                                                       WidthRequest="62"
                                                       IsVisible="{Binding IsAllDayAppointment, Converter={StaticResource InvertedBooleanConverter}}"
                                                       SpinnerFormat="H:mm"
                                                       MinuteStep="15"
                                                       BorderThickness="{OnPlatform Android=0}" />
                </HorizontalStackLayout>
                <telerikScheduler:SchedulerBoxView Grid.Row="5"
                                                   Style="{StaticResource SchedulerDialogSeparatorStyle_Mobile}" />
                <Label Grid.Row="6"
                       Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_EndTime}"
                       Style="{StaticResource SchedulerEditAppointmentDialogLabelStyle_Mobile}" />
                <HorizontalStackLayout Grid.Row="6"
                                       Grid.Column="1"
                                       Spacing="4"
                                       HorizontalOptions="End"
                                       IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}">
                    <telerikMauiControls:RadDatePicker MinimumDate="{Binding ActualStartDate}"
                                                       Date="{Binding ActualEndDate, Mode=TwoWay}"
                                                       WidthRequest="102"
                                                       BorderThickness="{OnPlatform Android=0}" />
                    <telerikMauiControls:RadTimePicker Time="{Binding ActualEndTimeOfDay, Mode=TwoWay}"
                                                       WidthRequest="62"
                                                       IsVisible="{Binding IsAllDayAppointment, Converter={StaticResource InvertedBooleanConverter}}"
                                                       SpinnerFormat="H:mm"
                                                       MinuteStep="15"
                                                       MinimumTime="{Binding MinimumEndTimeOfDay, Mode=TwoWay}"
                                                       BorderThickness="{OnPlatform Android=0}" />
                </HorizontalStackLayout>
                <telerikScheduler:SchedulerBoxView Grid.Row="7"
                                                   Style="{StaticResource SchedulerDialogSeparatorStyle_Mobile}" />
                <Label Grid.Row="8"
                       Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Repeat}"
                       IsVisible="{Binding IsExceptionOccurrence, Converter={StaticResource InvertedBooleanConverter}}"
                       Style="{StaticResource SchedulerEditAppointmentDialogLabelStyle_Mobile}" />
                <telerikMauiControls:RadComboBox Grid.Row="8"
                                                 Grid.Column="1"
                                                 HorizontalOptions="End"
                                                 IsClearButtonVisible="False"
                                                 SelectedItem="{Binding RepeatOption, Mode=TwoWay}"
                                                 IsVisible="{Binding IsExceptionOccurrence, Converter={StaticResource InvertedBooleanConverter}}"
                                                 IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                                 ItemsSource="{Binding RepeatOptions}"
                                                 WidthRequest="168"
                                                 DropDownHeight="{Binding RepeatOptionsDropDownHeight}"
                                                 BorderThickness="{OnPlatform Android=0}" />
                <Grid Grid.Row="9"
                      Grid.ColumnSpan="2"
                      IsVisible="{Binding IsExceptionOccurrence, Converter={StaticResource InvertedBooleanConverter}}">
                    <Label IsVisible="{Binding Summary, Converter={StaticResource StringToBoolConverter}}"
                           Text="{Binding Summary}"
                           TextColor="{DynamicResource RadOnAppSurfaceColor}"
                           FontSize="12"
                           Margin="0, 8, 0, 4" />
                </Grid>
                <telerikScheduler:SchedulerBoxView Grid.Row="10"
                                                   IsVisible="{Binding IsExceptionOccurrence, Converter={StaticResource InvertedBooleanConverter}}"
                                                   Style="{StaticResource SchedulerDialogSeparatorStyle_Mobile}" />
                <Editor x:Name="descriptionEditor"
                        Grid.Row="11"
                        Grid.ColumnSpan="2"
                        FontSize="{OnPlatform iOS=17}"
                        Text="{Binding Description, Mode=TwoWay}"
                        TextColor="{DynamicResource RadOnAppSurfaceColor}"
                        VerticalTextAlignment="Start"
                        Placeholder="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Description}"
                        PlaceholderColor="{DynamicResource RadSubtleColor}"
                        BackgroundColor="{DynamicResource RadAppSurfaceColor}" />
                <OnPlatform x:TypeArguments="View">
                    <On Platform="Android">
                        <Grid Grid.Row="11"
                              Grid.ColumnSpan="2">
                            <telerikMauiControls:RadBorder BorderBrush="{DynamicResource EditorBorderBrush}"
                                                           BorderThickness="0, 0, 0, 1"
                                                           CornerRadius="4, 4, 0, 0"
                                                           Margin="3, 0, 3, 8" />
                            <telerikMauiControls:RadBorder BorderBrush="{DynamicResource RadPrimaryColor}"
                                                           BorderThickness="0, 0, 0, 2"
                                                           CornerRadius="4, 4, 0, 0"
                                                           Margin="3, 0, 3, 7"
                                                           IsVisible="{Binding IsFocused, Source={x:Reference descriptionEditor}}" />
                        </Grid>
                    </On>
                </OnPlatform>
            </Grid>
            <Grid Grid.Row="2"
                  ColumnDefinitions="*, *"
                  BackgroundColor="{DynamicResource RadSchedulerDialogFooterBackgroundColor}"
                  Margin="0, 8, 0, 0">
                <telerikScheduler:SchedulerBoxView Grid.ColumnSpan="2"
                                                   HeightRequest="1"
                                                   BackgroundColor="{Binding BorderColor, Source={RelativeSource AncestorType={x:Type telerikScheduler:SchedulerDialog}}}"
                                                   VerticalOptions="Start" />
                <telerikMauiControls:RadButton Style="{StaticResource SchedulerDialogCancelButtonStyle_Mobile}"
                                               Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Cancel}"
                                               Command="{Binding CancelCommand}" />
                <telerikMauiControls:RadButton Grid.Column="1"
                                               Style="{StaticResource SchedulerDialogConfirmButtonStyle_Mobile}"
                                               Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Save}"
                                               IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                               Command="{Binding SaveCommand}" />
            </Grid>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="SchedulerEditAppointmentDialogControlTemplate_Desktop">
        <Grid RowDefinitions="{OnPlatform Default='28, Auto, *, Auto, Auto', WinUI='32, Auto, *, Auto, Auto'}">
            <Grid ColumnDefinitions="{OnPlatform Default='*, Auto', MacCatalyst='Auto, *'}"
                  Padding="{OnPlatform Default='16, 0', WinUI='12, 0, 0, 0'}">
                <Label Grid.Column="{OnPlatform Default=0, MacCatalyst=1}"
                       Text="{Binding Header}"
                       Style="{StaticResource SchedulerDialogHeaderStyle}" />
                <telerikMauiControls:RadButton x:Name="PART_CloseButton"
                                               Grid.Column="{OnPlatform Default=1, MacCatalyst=0}"
                                               Style="{OnPlatform MacCatalyst={StaticResource SchedulerDialogCloseButtonStyle_MacCatalyst}, WinUI={StaticResource SchedulerDialogCloseButtonStyle_WinUI}}" />
            </Grid>
            <telerikScheduler:SchedulerBoxView Grid.Row="1"
                                               HeightRequest="1"
                                               BackgroundColor="{Binding BorderColor, Source={RelativeSource AncestorType={x:Type telerikScheduler:SchedulerDialog}}}"
                                               IsVisible="{OnPlatform Default=False, MacCatalyst=True}" />
            <Grid Grid.Row="2"
                  ColumnDefinitions="Auto, *"
                  ColumnSpacing="{OnPlatform Default=8, WinUI=24}"
                  RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto"
                  RowSpacing="8"
                  BindingContext="{TemplateBinding BindingContext}"
                  Padding="{OnPlatform Default='20, 20, 20, 0', WinUI='12, 24'}">
                <Label Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Subject}"
                       Style="{StaticResource SchedulerDialogLabelStyle}" />
                <telerikMauiControls:RadEntry Grid.Column="1"
                                              IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                              InputTransparent="{Binding IsReadOnly}"
                                              Text="{Binding Occurrence.Appointment.Subject, Mode=TwoWay}"
                                              ReserveSpaceForErrorView="False"
                                              ClearButtonVisibility="Never" />
                <Label Grid.Row="1"
                       Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_StartTime}"
                       Style="{StaticResource SchedulerDialogLabelStyle}" />
                <HorizontalStackLayout Grid.Row="1"
                                       Grid.Column="1"
                                       Spacing="{OnPlatform Default=4, WinUI=8}"
                                       IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}">
                    <telerikMauiControls:RadDatePicker Date="{Binding ActualStartDate, Mode=TwoWay}"
                                                       MinimumWidthRequest="{OnPlatform Default=140, WinUI=145}" />
                    <telerikMauiControls:RadTimePicker Time="{Binding ActualStartTimeOfDay, Mode=TwoWay}"
                                                       IsVisible="{Binding IsAllDayAppointment, Converter={StaticResource InvertedBooleanConverter}}"
                                                       SpinnerFormat="H:mm"
                                                       MinuteStep="15"
                                                       MinimumWidthRequest="{OnPlatform Default=140, WinUI=145}" />
                </HorizontalStackLayout>
                <Label Grid.Row="2"
                       Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_EndTime}"
                       Style="{StaticResource SchedulerDialogLabelStyle}" />
                <HorizontalStackLayout Grid.Row="2"
                                       Grid.Column="1"
                                       Spacing="{OnPlatform Default=4, WinUI=8}"
                                       IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}">
                    <telerikMauiControls:RadDatePicker MinimumDate="{Binding ActualStartDate}"
                                                       Date="{Binding ActualEndDate, Mode=TwoWay}"
                                                       MinimumWidthRequest="{OnPlatform Default=140, WinUI=145}" />
                    <telerikMauiControls:RadTimePicker Time="{Binding ActualEndTimeOfDay, Mode=TwoWay}"
                                                       IsVisible="{Binding IsAllDayAppointment, Converter={StaticResource InvertedBooleanConverter}}"
                                                       SpinnerFormat="H:mm"
                                                       MinuteStep="15"
                                                       MinimumTime="{Binding MinimumEndTimeOfDay, Mode=TwoWay}"
                                                       MinimumWidthRequest="{OnPlatform Default=140, WinUI=145}" />
                </HorizontalStackLayout>
                <Label Grid.Row="3"
                       Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_AllDay}"
                       Style="{StaticResource SchedulerDialogLabelStyle}" />
                <Grid Grid.Row="3"
                      Grid.Column="1">
                    <Switch x:Name="allDaySwitch"
                            IsToggled="{Binding IsAllDayAppointment, Mode=TwoWay}"
                            IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}">
                        <Switch.Style>
                            <Style TargetType="Switch" BasedOn="{StaticResource SchedulerDialogSwitchStyle}" />
                        </Switch.Style>
                    </Switch>
                    <OnPlatform x:TypeArguments="View">
                        <On Platform="WinUI">
                            <telerikMauiControls:RadBorder HeightRequest="20"
                                                           WidthRequest="40"
                                                           CornerRadius="10"
                                                           BorderColor="{Binding OnColor, Source={x:Reference allDaySwitch}}"
                                                           BorderThickness="1"
                                                           HorizontalOptions="Start"
                                                           VerticalOptions="Center" />
                        </On>
                    </OnPlatform>
                </Grid>
                <Grid Grid.Row="4"
                      Grid.ColumnSpan="2"
                      ColumnDefinitions="Auto, *"
                      ColumnSpacing="{OnPlatform Default=8, WinUI=24}"
                      RowDefinitions="Auto, Auto"
                      RowSpacing="8"
                      IsVisible="{Binding IsExceptionOccurrence, Converter={StaticResource InvertedBooleanConverter}}">
                    <Label Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Repeat}"
                           Style="{StaticResource SchedulerDialogLabelStyle}" />
                    <telerikMauiControls:RadComboBox Grid.Column="1"
                                                     IsClearButtonVisible="False"
                                                     SelectedItem="{Binding RepeatOption, Mode=TwoWay}"
                                                     IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                                     ItemsSource="{Binding RepeatOptions}"
                                                     MinimumWidthRequest="{OnPlatform Default=140, WinUI=145}"
                                                     HorizontalOptions="Start" />
                    <Label Grid.Row="1"
                           Grid.Column="1"
                           IsVisible="{Binding Summary, Converter={StaticResource StringToBoolConverter}}"
                           Text="{Binding Summary}"
                           FontSize="{OnPlatform MacCatalyst=12}"
                           Margin="2, 0" />
                </Grid>
                <Label Grid.Row="5"
                       Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Description}"
                       Style="{StaticResource SchedulerDialogLabelStyle}"
                       VerticalOptions="Start" />
                <OnPlatform x:TypeArguments="View">
                    <On Platform="MacCatalyst">
                        <Grid Grid.Row="5"
                              Grid.Column="1">
                            <telerikMauiControls:RadBorder BorderBrush="{DynamicResource EditorBorderBrush}"
                                                           BorderThickness="1"
                                                           Padding="1, 0"
                                                           CornerRadius="6">
                                <Editor x:Name="descriptionEditor_MacCatalyst"
                                        TextColor="{DynamicResource RadOnAppSurfaceColor}"
                                        PlaceholderColor="{DynamicResource RadSubtleColor}"
                                        BackgroundColor="{DynamicResource RadEntryBackgroundColor}"
                                        IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                        Text="{Binding Description, Mode=TwoWay}"
                                        HeightRequest="68" />
                            </telerikMauiControls:RadBorder>
                            <Rectangle Stroke="{DynamicResource RadEntryFocusedBorderBrush}"
                                       StrokeThickness="3"
                                       RadiusX="6"
                                       RadiusY="6"
                                       IsVisible="{Binding IsFocused, Source={x:Reference descriptionEditor_MacCatalyst}}" />
                        </Grid>
                    </On>
                    <On Platform="WinUI">
                        <Grid Grid.Row="5"
                              Grid.Column="1">
                            <Editor x:Name="descriptionEditor_WinUI"
                                    TextColor="{DynamicResource RadOnAppSurfaceColor}"
                                    PlaceholderColor="{DynamicResource RadSubtleColor}"
                                    BackgroundColor="{DynamicResource RadEntryBackgroundColor}"
                                    IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                    Text="{Binding Description, Mode=TwoWay}"
                                    HeightRequest="70" />
                            <telerikMauiControls:RadBorder BorderBrush="{DynamicResource EditorBorderBrush}"
                                                           BorderThickness="1"
                                                           CornerRadius="4" />
                            <telerikMauiControls:RadBorder BorderBrush="{DynamicResource RadPrimaryColor}"
                                                           BorderThickness="0, 0, 0, 2"
                                                           CornerRadius="4"
                                                           IsVisible="{Binding IsFocused, Source={x:Reference descriptionEditor_WinUI}}" />
                        </Grid>
                    </On>
                </OnPlatform>
            </Grid>
            <Grid Grid.Row="3"
                  Padding="{OnPlatform Default=20, WinUI=16}"
                  BackgroundColor="{DynamicResource RadSchedulerDialogFooterBackgroundColor}">
                <Grid HorizontalOptions="End"
                      ColumnDefinitions="Auto, Auto"
                      ColumnSpacing="8">
                    <telerikMauiControls:RadButton Grid.Column="{OnPlatform WinUI=1}"
                                                   Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Cancel}"
                                                   Style="{StaticResource SchedulerDialogCancelButtonStyle}"
                                                   Command="{Binding CancelCommand}" />
                    <telerikMauiControls:RadButton Grid.Column="{OnPlatform MacCatalyst=1}"
                                                   Text="{telerikMauiControls:Localize Scheduler_EditAppointmentDialog_Save}"
                                                   Style="{StaticResource SchedulerDialogConfirmButtonStyle}"
                                                   IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                                   Command="{Binding SaveCommand}" />
                </Grid>
            </Grid>
            <telerikScheduler:SchedulerBoxView Grid.Row="3"
                                               HeightRequest="1"
                                               BackgroundColor="{OnPlatform Default=Transparent, WinUI={DynamicResource RadBorderColor}}"
                                               IsVisible="{OnPlatform Default=False, WinUI=True}"
                                               VerticalOptions="Start" />
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="SchedulerEditRecurrenceChoiceDialogControlTemplate_Mobile">
        <Grid BindingContext="{TemplateBinding BindingContext}">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding CancelCommand}" />
            </Grid.GestureRecognizers>
            <Grid RowDefinitions="Auto, Auto"
                  RowSpacing="{OnPlatform iOS=8}"
                  Padding="{OnPlatform iOS='8, 0, 8, 8'}"
                  VerticalOptions="End">
                <telerikMauiControls:RadBorder CornerRadius="{OnPlatform iOS=14}"
                                               BackgroundColor="{DynamicResource RadSchedulerDialogFooterBackgroundColor}">
                    <VerticalStackLayout>
                        <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize Scheduler_EditAppointmentPreview_Edit_Occurrence}"
                                                       Command="{Binding EditCommand}"
                                                       CommandParameter="{x:Boolean True}"
                                                       Style="{StaticResource SchedulerChoiceDialogDestructiveActionButtonStyle_Mobile}" />
                        <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize Scheduler_EditAppointmentPreview_Edit_Series}"
                                                       Command="{Binding EditCommand}"
                                                       CommandParameter="{x:Boolean False}"
                                                       Style="{StaticResource SchedulerChoiceDialogDestructiveActionButtonStyle_Mobile}" />
                    </VerticalStackLayout>
                </telerikMauiControls:RadBorder>
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Text="{telerikMauiControls:Localize Scheduler_EditAppointmentPreview_Cancel}"
                                               Command="{Binding CancelCommand}"
                                               Style="{StaticResource SchedulerChoiceDialogActionButtonStyle_Mobile}" />
            </Grid>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="SchedulerEditRecurrenceChoiceDialogControlTemplate_Desktop">
        <Grid RowDefinitions="{OnPlatform Default='28, Auto, *, Auto, Auto', WinUI='32, Auto, *, Auto, Auto'}">
            <Grid ColumnDefinitions="{OnPlatform Default='*, Auto', MacCatalyst='Auto, *'}"
                  Padding="{OnPlatform Default='16, 0', WinUI='12, 0, 0, 0'}">
                <Label Grid.Column="{OnPlatform Default=0, MacCatalyst=1}"
                       Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceChoiceDialog_Header}"
                       Style="{StaticResource SchedulerDialogHeaderStyle}" />
                <telerikMauiControls:RadButton x:Name="PART_CloseButton"
                                               Grid.Column="{OnPlatform Default=1, MacCatalyst=0}"
                                               Style="{OnPlatform MacCatalyst={StaticResource SchedulerDialogCloseButtonStyle_MacCatalyst}, WinUI={StaticResource SchedulerDialogCloseButtonStyle_WinUI}}" />
            </Grid>
            <telerikScheduler:SchedulerBoxView Grid.Row="1"
                                               HeightRequest="1"
                                               BackgroundColor="{Binding BorderColor, Source={RelativeSource AncestorType={x:Type telerikScheduler:SchedulerDialog}}}"
                                               IsVisible="{OnPlatform Default=False, MacCatalyst=True}" />
            <Grid Grid.Row="2"
                  RowDefinitions="Auto, Auto"
                  RowSpacing="{OnPlatform Default=8, WinUI=0}"
                  BindingContext="{TemplateBinding BindingContext}"
                  Padding="{OnPlatform Default='20, 20, 20, 0', WinUI='12, 16'}">
                <RadioButton x:Name="PART_OccurrenceChoice"
                             Content="{telerikMauiControls:Localize Scheduler_EditRecurrenceChoiceDialog_Edit_Occurrence}"
                             IsChecked="True"
                             Style="{StaticResource SchedulerDialogRadioButtonStyle_Desktop}"
                             GroupName="RecurrenceModeChoice" />
                <RadioButton Grid.Row="1"
                             Content="{telerikMauiControls:Localize Scheduler_EditRecurrenceChoiceDialog_Edit_Series}"
                             Style="{StaticResource SchedulerDialogRadioButtonStyle_Desktop}"
                             GroupName="RecurrenceModeChoice" />
            </Grid>
            <Grid Grid.Row="3"
                  Padding="{OnPlatform Default=20, WinUI='24, 16'}"
                  BackgroundColor="{DynamicResource RadSchedulerDialogFooterBackgroundColor}">
                <Grid HorizontalOptions="{OnPlatform MacCatalyst=End}"
                      ColumnDefinitions="{OnPlatform Default='Auto, Auto', WinUI='*, *'}"
                      ColumnSpacing="8">
                    <telerikMauiControls:RadButton Grid.Column="{OnPlatform WinUI=1}"
                                                   Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceChoiceDialog_Cancel}"
                                                   Style="{StaticResource SchedulerDialogCancelButtonStyle}"
                                                   Command="{Binding CancelCommand}" />
                    <telerikMauiControls:RadButton Grid.Column="{OnPlatform MacCatalyst=1}"
                                                   Text="{telerikMauiControls:Localize Scheduler_EditRecurrenceChoiceDialog_Edit}"
                                                   Style="{StaticResource SchedulerDialogConfirmButtonStyle}"
                                                   IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                                   Command="{Binding EditCommand}"
                                                   CommandParameter="{Binding Source={x:Reference PART_OccurrenceChoice}, Path=IsChecked}" />
                </Grid>
            </Grid>
            <telerikScheduler:SchedulerBoxView Grid.Row="3"
                                               HeightRequest="1"
                                               BackgroundColor="{OnPlatform Default=Transparent, WinUI={DynamicResource RadBorderColor}}"
                                               IsVisible="{OnPlatform Default=False, WinUI=True}"
                                               VerticalOptions="Start" />
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="SchedulerDeleteAppointmentChoiceDialogControlTemplate_Mobile">
        <Grid BindingContext="{TemplateBinding BindingContext}">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding CancelCommand}" />
            </Grid.GestureRecognizers>
            <Grid RowDefinitions="Auto, Auto"
                  Padding="{OnPlatform iOS='8, 0, 8, 8'}"
                  RowSpacing="{OnPlatform iOS=8}"
                  VerticalOptions="End">
                <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize Scheduler_EditAppointmentPreview_Delete_Appointment}"
                                               Command="{Binding DeleteCommand}"
                                               Style="{StaticResource SchedulerChoiceDialogDestructiveActionButtonStyle_Mobile}" />
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Text="{telerikMauiControls:Localize Scheduler_EditAppointmentPreview_Cancel}"
                                               Command="{Binding CancelCommand}"
                                               Style="{StaticResource SchedulerChoiceDialogActionButtonStyle_Mobile}" />
            </Grid>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="SchedulerDeleteAppointmentChoiceDialogControlTemplate_Desktop">
        <Grid RowDefinitions="{OnPlatform Default='28, Auto, *, Auto, Auto', WinUI='32, Auto, *, Auto, Auto'}">
            <Grid ColumnDefinitions="{OnPlatform Default='*, Auto', MacCatalyst='Auto, *'}"
                  Padding="{OnPlatform Default='16, 0', WinUI='12, 0, 0, 0'}">
                <Label Grid.Column="{OnPlatform Default=0, MacCatalyst=1}"
                       Text="{telerikMauiControls:Localize Scheduler_DeleteAppointmentChoiceDialog_Header}"
                       Style="{StaticResource SchedulerDialogHeaderStyle}" />
                <telerikMauiControls:RadButton x:Name="PART_CloseButton"
                                               Grid.Column="{OnPlatform Default=1, MacCatalyst=0}"
                                               Style="{OnPlatform MacCatalyst={StaticResource SchedulerDialogCloseButtonStyle_MacCatalyst}, WinUI={StaticResource SchedulerDialogCloseButtonStyle_WinUI}}" />
            </Grid>
            <telerikScheduler:SchedulerBoxView Grid.Row="1"
                                               HeightRequest="1"
                                               BackgroundColor="{Binding BorderColor, Source={RelativeSource AncestorType={x:Type telerikScheduler:SchedulerDialog}}}"
                                               IsVisible="{OnPlatform Default=False, MacCatalyst=True}" />
            <Grid Grid.Row="2"
                  BindingContext="{TemplateBinding BindingContext}"
                  Padding="{OnPlatform Default='20, 20, 20, 0', WinUI='12, 16'}">
                <Label Text="{Binding Question}"
                       VerticalOptions="Center" />
            </Grid>
            <Grid Grid.Row="3"
                  Padding="{OnPlatform Default=20, WinUI='24, 16'}"
                  BackgroundColor="{DynamicResource RadSchedulerDialogFooterBackgroundColor}">
                <Grid HorizontalOptions="{OnPlatform MacCatalyst=End}"
                      ColumnDefinitions="{OnPlatform Default='Auto, Auto', WinUI='*, *'}"
                      ColumnSpacing="8">
                    <telerikMauiControls:RadButton Grid.Column="{OnPlatform WinUI=1}"
                                                   Text="{telerikMauiControls:Localize Scheduler_DeleteAppointmentChoiceDialog_Cancel}"
                                                   Style="{StaticResource SchedulerDialogCancelButtonStyle}"
                                                   Command="{Binding CancelCommand}" />
                    <telerikMauiControls:RadButton Grid.Column="{OnPlatform MacCatalyst=1}"
                                                   Text="{telerikMauiControls:Localize Scheduler_DeleteAppointmentChoiceDialog_Delete}"
                                                   Style="{StaticResource SchedulerDialogConfirmButtonStyle}"
                                                   IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                                   Command="{Binding DeleteCommand}" />
                </Grid>
            </Grid>
            <telerikScheduler:SchedulerBoxView Grid.Row="3"
                                               HeightRequest="1"
                                               BackgroundColor="{OnPlatform Default=Transparent, WinUI={DynamicResource RadBorderColor}}"
                                               IsVisible="{OnPlatform Default=False, WinUI=True}"
                                               VerticalOptions="Start" />
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="SchedulerDeleteRecurrenceChoiceDialogControlTemplate_Mobile">
        <Grid BindingContext="{TemplateBinding BindingContext}">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding CancelCommand}" />
            </Grid.GestureRecognizers>
            <Grid RowDefinitions="Auto, Auto"
                  RowSpacing="{OnPlatform iOS=8}"
                  Padding="{OnPlatform iOS='8, 0, 8, 8'}"
                  VerticalOptions="End">
                <telerikMauiControls:RadBorder CornerRadius="{OnPlatform iOS=14}"
                                               BackgroundColor="{DynamicResource RadSchedulerDialogFooterBackgroundColor}">
                    <VerticalStackLayout>
                        <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize Scheduler_EditAppointmentPreview_Delete_Occurrence}"
                                                       Command="{Binding DeleteCommand}"
                                                       CommandParameter="{x:Boolean True}"
                                                       Style="{StaticResource SchedulerChoiceDialogDestructiveActionButtonStyle_Mobile}" />
                        <telerikMauiControls:RadButton Text="{telerikMauiControls:Localize Scheduler_EditAppointmentPreview_Delete_Series}"
                                                       Command="{Binding DeleteCommand}"
                                                       CommandParameter="{x:Boolean False}"
                                                       Style="{StaticResource SchedulerChoiceDialogDestructiveActionButtonStyle_Mobile}" />
                    </VerticalStackLayout>
                </telerikMauiControls:RadBorder>
                <telerikMauiControls:RadButton Grid.Row="1"
                                               Text="{telerikMauiControls:Localize Scheduler_EditAppointmentPreview_Cancel}"
                                               Command="{Binding CancelCommand}"
                                               Style="{StaticResource SchedulerChoiceDialogActionButtonStyle_Mobile}" />
            </Grid>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="SchedulerDeleteRecurrenceChoiceDialogControlTemplate_Desktop">
        <Grid RowDefinitions="{OnPlatform Default='28, Auto, *, Auto, Auto', WinUI='32, Auto, *, Auto, Auto'}">
            <Grid ColumnDefinitions="{OnPlatform Default='*, Auto', MacCatalyst='Auto, *'}"
                  Padding="{OnPlatform Default='16, 0', WinUI='12, 0, 0, 0'}">
                <Label Grid.Column="{OnPlatform Default=0, MacCatalyst=1}"
                       Text="{telerikMauiControls:Localize Scheduler_DeleteRecurrenceChoiceDialog_Header}"
                       Style="{StaticResource SchedulerDialogHeaderStyle}" />
                <telerikMauiControls:RadButton x:Name="PART_CloseButton"
                                               Grid.Column="{OnPlatform Default=1, MacCatalyst=0}"
                                               Style="{OnPlatform MacCatalyst={StaticResource SchedulerDialogCloseButtonStyle_MacCatalyst}, WinUI={StaticResource SchedulerDialogCloseButtonStyle_WinUI}}" />
            </Grid>
            <telerikScheduler:SchedulerBoxView Grid.Row="1"
                                               HeightRequest="1"
                                               BackgroundColor="{Binding BorderColor, Source={RelativeSource AncestorType={x:Type telerikScheduler:SchedulerDialog}}}"
                                               IsVisible="{OnPlatform Default=False, MacCatalyst=True}" />
            <Grid Grid.Row="2"
                  RowDefinitions="Auto, Auto"
                  RowSpacing="{OnPlatform Default=8, WinUI=0}"
                  BindingContext="{TemplateBinding BindingContext}"
                  Padding="{OnPlatform Default='20, 20, 20, 0', WinUI='12, 16'}">
                <RadioButton x:Name="PART_OccurrenceChoice"
                             Content="{telerikMauiControls:Localize Scheduler_DeleteRecurrenceChoiceDialog_Delete_Occurrence}"
                             IsChecked="True"
                             Style="{StaticResource SchedulerDialogRadioButtonStyle_Desktop}"
                             GroupName="RecurrenceModeChoice" />
                <RadioButton Grid.Row="1"
                             Content="{telerikMauiControls:Localize Scheduler_DeleteRecurrenceChoiceDialog_Delete_Series}"
                             Style="{StaticResource SchedulerDialogRadioButtonStyle_Desktop}"
                             GroupName="RecurrenceModeChoice" />
            </Grid>
            <Grid Grid.Row="3"
                  Padding="{OnPlatform Default=20, WinUI='24, 16'}"
                  BackgroundColor="{DynamicResource RadSchedulerDialogFooterBackgroundColor}">
                <Grid HorizontalOptions="{OnPlatform MacCatalyst=End}"
                      ColumnDefinitions="{OnPlatform Default='Auto, Auto', WinUI='*, *'}"
                      ColumnSpacing="8">
                    <telerikMauiControls:RadButton Grid.Column="{OnPlatform WinUI=1}"
                                                   Text="{telerikMauiControls:Localize Scheduler_DeleteRecurrenceChoiceDialog_Cancel}"
                                                   Style="{StaticResource SchedulerDialogCancelButtonStyle}"
                                                   Command="{Binding CancelCommand}" />
                    <telerikMauiControls:RadButton Grid.Column="{OnPlatform MacCatalyst=1}"
                                                   Text="{telerikMauiControls:Localize Scheduler_DeleteRecurrenceChoiceDialog_Delete}"
                                                   Style="{StaticResource SchedulerDialogConfirmButtonStyle}"
                                                   IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertedBooleanConverter}}"
                                                   Command="{Binding DeleteCommand}"
                                                   CommandParameter="{Binding Source={x:Reference PART_OccurrenceChoice}, Path=IsChecked}" />
                </Grid>
            </Grid>
            <telerikScheduler:SchedulerBoxView Grid.Row="3"
                                               HeightRequest="1"
                                               BackgroundColor="{OnPlatform Default=Transparent, WinUI={DynamicResource RadBorderColor}}"
                                               IsVisible="{OnPlatform Default=False, WinUI=True}"
                                               VerticalOptions="Start" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="SchedulerDialogBaseStyle_Mobile" TargetType="telerikScheduler:SchedulerDialog">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadSchedulerBorderColor}" />
    </Style>

    <Style x:Key="SchedulerDialogBaseStyle_Desktop" TargetType="telerikScheduler:SchedulerDialog">
        <Setter Property="WidthRequest" Value="400" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=150, WinUI=215}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerDialogBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadSchedulerBorderColor}" />
        <Setter Property="Shadow" Value="{StaticResource SchedulerDialogShadow}" />
    </Style>

    <Style x:Key="SchedulerPreviewAppointmentDialogStyle_Mobile" TargetType="telerikScheduler:SchedulerDialog" BasedOn="{StaticResource SchedulerDialogBaseStyle_Mobile}">
        <Setter Property="ControlTemplate" Value="{StaticResource SchedulerPreviewAppointmentDialogControlTemplate_Mobile}" />
    </Style>

    <Style x:Key="SchedulerEditAppointmentDialogStyle_Mobile" TargetType="telerikScheduler:SchedulerDialog" BasedOn="{StaticResource SchedulerDialogBaseStyle_Mobile}">
        <Setter Property="ControlTemplate" Value="{StaticResource SchedulerEditAppointmentDialogControlTemplate_Mobile}" />
    </Style>

    <Style x:Key="SchedulerEditAppointmentDialogStyle_Desktop" TargetType="telerikScheduler:SchedulerDialog" BasedOn="{StaticResource SchedulerDialogBaseStyle_Desktop}">
        <Setter Property="ControlTemplate" Value="{StaticResource SchedulerEditAppointmentDialogControlTemplate_Desktop}" />
        <Setter Property="WidthRequest" Value="600" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=410, WinUI=470}" />
    </Style>

    <Style x:Key="SchedulerEditRecurrenceDialogStyle_Mobile" TargetType="telerikScheduler:SchedulerDialog" BasedOn="{StaticResource SchedulerDialogBaseStyle_Mobile}">
        <Setter Property="ControlTemplate" Value="{StaticResource SchedulerEditRecurrenceDialogControlTemplate_Mobile}" />
    </Style>

    <Style x:Key="SchedulerEditRecurrenceDialogStyle_Desktop" TargetType="telerikScheduler:SchedulerDialog" BasedOn="{StaticResource SchedulerDialogBaseStyle_Desktop}">
        <Setter Property="ControlTemplate" Value="{StaticResource SchedulerEditRecurrenceDialogControlTemplate_Desktop}" />
        <Setter Property="WidthRequest" Value="{OnPlatform Default=560, WinUI=580}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=380, WinUI=445}" />
    </Style>

    <Style x:Key="SchedulerEditRecurrenceChoiceDialogStyle_Mobile" TargetType="telerikScheduler:SchedulerDialog">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerChoiceDialogBackgroundColor}" />
        <Setter Property="ControlTemplate" Value="{StaticResource SchedulerEditRecurrenceChoiceDialogControlTemplate_Mobile}" />
    </Style>

    <Style x:Key="SchedulerEditRecurrenceChoiceDialogStyle_Desktop" TargetType="telerikScheduler:SchedulerDialog" BasedOn="{StaticResource SchedulerDialogBaseStyle_Desktop}">
        <Setter Property="ControlTemplate" Value="{StaticResource SchedulerEditRecurrenceChoiceDialogControlTemplate_Desktop}" />
    </Style>

    <Style x:Key="SchedulerDeleteAppointmentChoiceDialogStyle_Mobile" TargetType="telerikScheduler:SchedulerDialog">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerChoiceDialogBackgroundColor}" />
        <Setter Property="ControlTemplate" Value="{StaticResource SchedulerDeleteAppointmentChoiceDialogControlTemplate_Mobile}" />
    </Style>

    <Style x:Key="SchedulerDeleteAppointmentChoiceDialogStyle_Desktop" TargetType="telerikScheduler:SchedulerDialog" BasedOn="{StaticResource SchedulerDialogBaseStyle_Desktop}">
        <Setter Property="ControlTemplate" Value="{StaticResource SchedulerDeleteAppointmentChoiceDialogControlTemplate_Desktop}" />
    </Style>

    <Style x:Key="SchedulerDeleteRecurrenceChoiceDialogStyle_Mobile" TargetType="telerikScheduler:SchedulerDialog">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSchedulerChoiceDialogBackgroundColor}" />
        <Setter Property="ControlTemplate" Value="{StaticResource SchedulerDeleteRecurrenceChoiceDialogControlTemplate_Mobile}" />
    </Style>

    <Style x:Key="SchedulerDeleteRecurrenceChoiceDialogStyle_Desktop" TargetType="telerikScheduler:SchedulerDialog" BasedOn="{StaticResource SchedulerDialogBaseStyle_Desktop}">
        <Setter Property="ControlTemplate" Value="{StaticResource SchedulerDeleteRecurrenceChoiceDialogControlTemplate_Desktop}" />
    </Style>

    <ControlTemplate x:Key="RadSchedulerControlTemplate_Desktop">
        <Grid RowDefinitions="Auto, *">
            <telerikMauiControls:RadBorder Style="{TemplateBinding ActualHeaderBorderStyle}">
                <Grid ColumnDefinitions="*, Auto"
                      ColumnSpacing="12">
                    <telerikMauiDataControls:NonVirtualizedItemsControl ItemsSource="{TemplateBinding ViewDefinitions}"
                                                                        DisplayMemberPath="Title"
                                                                        ItemStyle="{TemplateBinding ActualViewDefinitionHeaderItemStyle}"
                                                                        SelectedItemStyle="{TemplateBinding ActualActiveViewDefinitionHeaderItemStyle}"
                                                                        SelectionMode="Single"
                                                                        Orientation="Horizontal"
                                                                        CanUnselectSingleItem="False"
                                                                        SelectedItem="{Binding ActiveViewDefinition, Source={RelativeSource TemplatedParent}, Mode=TwoWay}" />
                    <HorizontalStackLayout Grid.Column="1"
                                           Spacing="{OnPlatform MacCatalyst=4}">
                        <HorizontalStackLayout IsVisible="{TemplateBinding IsTodayButtonVisible}"
                                               Spacing="8"
                                               Margin="{OnPlatform Default='0, 0, 10, 0', WinUI='0, 0, 12, 0'}">
                            <telerikScheduler:SchedulerNavigationButton Style="{TemplateBinding ActualAddAppointmentButtonStyle}"
                                                                        Command="{TemplateBinding AddAppointmentCommand}" />
                            <telerikScheduler:SchedulerNavigationButton Style="{TemplateBinding ActualTodayButtonStyle}"
                                                                        Command="{TemplateBinding TodayCommand}" />
                            <BoxView WidthRequest="1"
                                     BackgroundColor="{DynamicResource RadSchedulerHeaderTodayButtonSeparatorBorderColor}" />
                        </HorizontalStackLayout>
                        <Label Style="{TemplateBinding ActualHeaderLabelStyle}" />
                        <telerikScheduler:SchedulerNavigationButton Style="{TemplateBinding ActualDecreaseVisibleRangeButtonStyle}"
                                                                    Command="{TemplateBinding DecreaseVisibleRangeCommand}" />
                        <telerikScheduler:SchedulerDatePicker x:Name="PART_DatePicker" />
                        <telerikScheduler:SchedulerNavigationButton Style="{TemplateBinding ActualIncreaseVisibleRangeButtonStyle}"
                                                                    Command="{TemplateBinding IncreaseVisibleRangeCommand}" />
                    </HorizontalStackLayout>
                </Grid>
            </telerikMauiControls:RadBorder>
            <telerikMauiControls:RadSchedulerContent x:Name="PART_SchedulerContent"
                                                     Grid.Row="1"
                                                     IsClippedToBounds="True" />
            <AbsoluteLayout x:Name="PART_DialogLayout"
                            Grid.RowSpan="2"
                            InputTransparent="True" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="SchedulerCalendarStyle_Mobile" TargetType="telerikMauiControls:RadCalendar">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="HeaderBorderStyle">
            <Setter.Value>
                <Style TargetType="telerikMauiControls:RadBorder">
                    <Setter Property="IsVisible" Value="False" />
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="HeightRequest" Value="308" />
    </Style>

    <ControlTemplate x:Key="RadSchedulerControlTemplate_Mobile">
        <Grid RowDefinitions="Auto, *">
            <telerikScheduler:SchedulerExpander x:Name="PART_HeaderExpander">
                <telerikScheduler:SchedulerExpander.Header>
                    <telerikMauiControls:RadBorder Style="{TemplateBinding ActualHeaderBorderStyle}">
                        <Grid ColumnDefinitions="*, Auto, Auto, Auto">
                            <HorizontalStackLayout HorizontalOptions="Start">
                                <Label Style="{TemplateBinding ActualHeaderLabelStyle}" />
                                <Label Style="{TemplateBinding ActualHeaderIndicatorLabelStyle}" />
                            </HorizontalStackLayout>
                            <Button Grid.Column="1"
                                    Style="{TemplateBinding ActualTodayButtonStyle}"
                                    IsVisible="{TemplateBinding IsTodayButtonVisible}"
                                    Command="{TemplateBinding TodayCommand}" />
                            <telerikScheduler:SchedulerNavigationButton Grid.Column="2"
                                                                        Style="{TemplateBinding ActualAddAppointmentButtonStyle}"
                                                                        Command="{TemplateBinding AddAppointmentCommand}" />
                            <telerikScheduler:SchedulerDropDownButton x:Name="PART_ViewDefinitionPicker"
                                                                      Grid.Column="3" 
                                                                      ItemStyle="{TemplateBinding ActualViewDefinitionHeaderItemStyle}"
                                                                      SelectedItemStyle="{TemplateBinding ActualActiveViewDefinitionHeaderItemStyle}" />
                        </Grid>
                    </telerikMauiControls:RadBorder>
                </telerikScheduler:SchedulerExpander.Header>
                <telerikScheduler:SchedulerExpander.Content>
                    <telerikMauiControls:RadCalendar x:Name="PART_HeaderCalendar"
                                                     Style="{StaticResource SchedulerCalendarStyle_Mobile}"
                                                     Culture="{Binding Culture, Source={RelativeSource TemplatedParent}}"
                                                     MinDate="{Binding MinDate, Source={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                     MaxDate="{Binding MaxDate, Source={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                     DisplayDate="{Binding CurrentDate, Source={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                     SelectedDate="{Binding CurrentDate, Source={RelativeSource TemplatedParent}, Mode=TwoWay}" />
                </telerikScheduler:SchedulerExpander.Content>
            </telerikScheduler:SchedulerExpander>
            <telerikMauiControls:RadSchedulerContent x:Name="PART_SchedulerContent"
                                                     Grid.Row="1"
                                                     IsClippedToBounds="True" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="RadSchedulerStyle_Mobile" TargetType="telerikMauiControls:RadScheduler">
        <Setter Property="ControlTemplate" Value="{StaticResource RadSchedulerControlTemplate_Mobile}" />
        <Setter Property="HeaderLabelStyle" Value="{StaticResource SchedulerHeaderLabelStyle_Mobile}" />
        <Setter Property="HeaderIndicatorLabelStyle" Value="{StaticResource SchedulerHeaderIndicatorLabelStyle}" />
        <Setter Property="PreviewAppointmentDialogStyle" Value="{StaticResource SchedulerPreviewAppointmentDialogStyle_Mobile}" />
        <Setter Property="EditAppointmentDialogStyle" Value="{StaticResource SchedulerEditAppointmentDialogStyle_Mobile}" />
        <Setter Property="EditRecurrenceDialogStyle" Value="{StaticResource SchedulerEditRecurrenceDialogStyle_Mobile}" />
        <Setter Property="EditRecurrenceChoiceDialogStyle" Value="{StaticResource SchedulerEditRecurrenceChoiceDialogStyle_Mobile}" />
        <Setter Property="DeleteAppointmentChoiceDialogStyle" Value="{StaticResource SchedulerDeleteAppointmentChoiceDialogStyle_Mobile}" />
        <Setter Property="DeleteRecurrenceChoiceDialogStyle" Value="{StaticResource SchedulerDeleteRecurrenceChoiceDialogStyle_Mobile}" />
    </Style>

    <Style x:Key="RadSchedulerStyle_Desktop" TargetType="telerikMauiControls:RadScheduler">
        <Setter Property="ControlTemplate" Value="{StaticResource RadSchedulerControlTemplate_Desktop}" />
        <Setter Property="HeaderLabelStyle" Value="{StaticResource SchedulerHeaderLabelStyle}" />
        <Setter Property="IncreaseVisibleRangeButtonStyle" Value="{StaticResource SchedulerIncreaseVisibleRangeButtonStyle}" />
        <Setter Property="DecreaseVisibleRangeButtonStyle" Value="{StaticResource SchedulerDecreaseVisibleRangeButtonStyle}" />
        <Setter Property="EditAppointmentDialogStyle" Value="{StaticResource SchedulerEditAppointmentDialogStyle_Desktop}" />
        <Setter Property="EditRecurrenceDialogStyle" Value="{StaticResource SchedulerEditRecurrenceDialogStyle_Desktop}" />
        <Setter Property="EditRecurrenceChoiceDialogStyle" Value="{StaticResource SchedulerEditRecurrenceChoiceDialogStyle_Desktop}" />
        <Setter Property="DeleteAppointmentChoiceDialogStyle" Value="{StaticResource SchedulerDeleteAppointmentChoiceDialogStyle_Desktop}" />
        <Setter Property="DeleteRecurrenceChoiceDialogStyle" Value="{StaticResource SchedulerDeleteRecurrenceChoiceDialogStyle_Desktop}" />
    </Style>

    <Style x:Key="RadSchedulerStyle" TargetType="telerikMauiControls:RadScheduler" BasedOn="{OnPlatform Android={StaticResource RadSchedulerStyle_Mobile}, iOS={StaticResource RadSchedulerStyle_Mobile}, MacCatalyst={StaticResource RadSchedulerStyle_Desktop}, WinUI={StaticResource RadSchedulerStyle_Desktop}}">
        <Setter Property="Background" Value="{DynamicResource RadSchedulerBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadSchedulerBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HeaderBorderStyle" Value="{StaticResource SchedulerHeaderBorderStyle}" />
        <Setter Property="ViewDefinitionHeaderItemStyle" Value="{OnPlatform Android={StaticResource SchedulerViewDefinitionHeaderItemStyle_Android}, iOS={StaticResource SchedulerViewDefinitionHeaderItemStyle_iOS}, MacCatalyst={StaticResource SchedulerViewDefinitionHeaderItemStyle_MacCatalyst}, WinUI={StaticResource SchedulerViewDefinitionHeaderItemStyle_WinUI}}" />
        <Setter Property="ActiveViewDefinitionHeaderItemStyle" Value="{OnPlatform Android={StaticResource SchedulerActiveViewDefinitionHeaderItemStyle_Android}, iOS={StaticResource SchedulerActiveViewDefinitionHeaderItemStyle_iOS}, MacCatalyst={StaticResource SchedulerActiveViewDefinitionHeaderItemStyle_MacCatalyst}, WinUI={StaticResource SchedulerActiveViewDefinitionHeaderItemStyle_WinUI}}" />
        <Setter Property="TodayButtonStyle" Value="{OnPlatform Default={StaticResource SchedulerTodayButtonStyle}, MacCatalyst={StaticResource SchedulerTodayButtonStyle_MacCatalyst}, WinUI={StaticResource SchedulerTodayButtonStyle_WinUI}}" />
        <Setter Property="AddAppointmentButtonStyle" Value="{StaticResource SchedulerAddAppointmentButtonStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadScheduler" BasedOn="{StaticResource RadSchedulerStyle}" />
</ResourceDictionary>