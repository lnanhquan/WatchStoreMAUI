<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.Entry">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <telerikMauiControls:CornerRadiusToThicknessConverter x:Key="CornerRadiusToThicknessConverter" />

    <Style x:Key="DefaultTextInputStyle" TargetType="telerikMauiControls:RadTextInput">
        <!-- These following two properties are bound to the whole control's Minimum properties. This might lead to a larger size due to the presence of additional elements in the template.
             Consider exposing TextInputStyle or specifically TextInputMinimumHeightRequest and TextInputMinimumWidthRequest. See #85779. -->
        <Setter Property="MinimumHeightRequest" Value="{TemplateBinding MinimumHeightRequest}" />
        <Setter Property="MinimumWidthRequest" Value="{TemplateBinding MinimumWidthRequest}" />
        <Setter Property="Padding" Value="{TemplateBinding Padding}" />
        <Setter Property="SelectionOnFocus" Value="{TemplateBinding SelectionOnFocus}" />
        <Setter Property="HorizontalTextAlignment" Value="{TemplateBinding HorizontalTextAlignment}" />
        <Setter Property="VerticalTextAlignment" Value="{TemplateBinding VerticalTextAlignment}" />
        <Setter Property="IsPassword" Value="{TemplateBinding IsPassword}" />
        <Setter Property="ReturnType" Value="{TemplateBinding ReturnType}" />
        <Setter Property="ReturnCommand" Value="{TemplateBinding ReturnCommand}" />
        <Setter Property="ReturnCommandParameter" Value="{TemplateBinding ReturnCommandParameter}" />
        <Setter Property="Keyboard" Value="{TemplateBinding Keyboard}" />
        <Setter Property="IsSpellCheckEnabled" Value="{TemplateBinding IsSpellCheckEnabled}" />
        <Setter Property="IsTextPredictionEnabled" Value="{TemplateBinding IsTextPredictionEnabled}" />
        <Setter Property="IsReadOnly" Value="{TemplateBinding IsReadOnly}" />
        <Setter Property="TextColor" Value="{TemplateBinding TextColor}" />
        <Setter Property="CharacterSpacing" Value="{TemplateBinding CharacterSpacing}" />
        <Setter Property="TextTransform" Value="{TemplateBinding TextTransform}" />
        <Setter Property="FontAttributes" Value="{TemplateBinding FontAttributes}" />
        <Setter Property="FontFamily" Value="{TemplateBinding FontFamily}" />
        <Setter Property="FontSize" Value="{TemplateBinding FontSize}" />
        <Setter Property="FontAutoScalingEnabled" Value="{TemplateBinding FontAutoScalingEnabled}" />
        <Setter Property="MaxLength" Value="{TemplateBinding MaxLength}" />
        <Setter Property="Placeholder" Value="{TemplateBinding Placeholder}" />
        <Setter Property="PlaceholderColor" Value="{TemplateBinding PlaceholderColor}" />
    </Style>

    <VisualStateGroupList x:Key="CloseButton_VisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadSubtleColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadSubtleColor}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource ClearButtonPointerOverBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadEntryActionButtonPressedTextColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="CloseButton_VisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadSubtleColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadSubtleColor}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource ClearButtonPointerOverBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha45}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource ClearButtonPressedBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha36}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="DefaultClearButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="Grid.Column" Value="1" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=22, Android=20, iOS=24}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=22, Android=20, iOS=34}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=19, Android=20, MacCatalyst=18, WinUI=10}" />
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Content" Value="{OnPlatform Default={x:Static telerikMaui:TelerikFont.IconCancel2}, WinUI={x:Static telerikMaui:TelerikFont.IconCancel}}" />
        <Setter Property="Command" Value="{TemplateBinding ActualClearButtonCommand}" />
        <Setter Property="IsVisible" Value="{TemplateBinding ActualIsClearButtonVisible}" />
        <Setter Property="Padding" Value="{OnPlatform Default=0, iOS='0, 0, 10, 0', MacCatalyst='0, 0, 10, 0'}" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, Android='0, 0, 8, 0', WinUI='4, 0'}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Default={StaticResource CloseButton_VisualStates}, WinUI={StaticResource CloseButton_VisualStates_WinUI}}" />
    </Style>

    <telerikMauiControls:DictionaryConverter x:Key="ErrorImageOpacityConverter">
        <telerikMauiControls:DictionaryConverterItem Key="{x:Boolean True}" Value="{x:Double 0}" />
        <telerikMauiControls:DictionaryConverterItem Key="{x:Boolean False}" Value="{x:Double 1}" />
    </telerikMauiControls:DictionaryConverter>

    <FontImageSource x:Key="ErrorImageSource"
                     FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconError}" />

    <Style x:Key="DefaultValidationErrorImageStyle" TargetType="Image">
        <Setter Property="WidthRequest" Value="{OnPlatform Default=16, Android=18, iOS=20}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=16, Android=18, iOS=20}" />
        <Setter Property="Source" Value="{StaticResource ErrorImageSource}" />
        <Setter Property="telerikMauiControls:FontImageSourceUtils.FallbackColor" Value="{DynamicResource RadErrorColor}" />
        <Setter Property="ToolTipProperties.Text" Value="{TemplateBinding ActualValidationErrorMessage}" />
        <Setter Property="IsVisible" Value="{TemplateBinding ActualIsValidationErrorImageVisible}" />
        <Setter Property="Opacity" Value="{TemplateBinding IsValueValid, Converter={StaticResource ErrorImageOpacityConverter}}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="Margin" Value="{OnPlatform Android='4.5, 0, 0, 0.5', iOS='8, 0, 0, 0', MacCatalyst='6, 0, 0, 0', WinUI='8, 1, 0, 0'}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:FontImageSourceUtils.FallbackColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="DefaultValidationErrorLabelStyle" TargetType="Label">
        <Setter Property="Text" Value="{TemplateBinding ValidationErrorMessage}" />
        <Setter Property="TextColor" Value="{DynamicResource RadErrorColor}" />
        <Setter Property="IsVisible" Value="{TemplateBinding ActualIsValidationErrorLabelVisible}" />
        <Setter Property="Opacity" Value="{TemplateBinding IsValueValid, Converter={StaticResource ErrorImageOpacityConverter}}" />
        <Setter Property="Margin" Value="{OnPlatform Android='8.5, 4, 0, 0', iOS='8, 4, 0, 0', MacCatalyst='6, 4, 0, 0', WinUI='10, 4, 0, 0'}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Focused" />
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <ControlTemplate x:Key="RadEntry_ControlTemplate">
        <Grid RowDefinitions="*, Auto"
              ColumnDefinitions="*, Auto">
            <Grid ColumnDefinitions="*, Auto"
                  BackgroundColor="{TemplateBinding BackgroundColor}"
                  Background="{TemplateBinding Background}"
                  telerikMauiControls:ViewUtils.CornerRadius="{TemplateBinding CornerRadius}">
                <telerikMauiControls:RadTextInput Text="{TemplateBinding Text, Mode=TwoWay}"
                                                  Style="{TemplateBinding ActualTextInputStyle}"
                                                  AutomationId="RadEntryTextInput" />
                <telerikMauiControls:RadTemplatedButton Style="{TemplateBinding ActualClearButtonStyle}"
                                                        AutomationId="RadEntryClearButton" />
            </Grid>
            <telerikMauiControls:RadBorder BackgroundColor="Transparent"
                                           InputTransparent="True"
                                           BorderColor="{TemplateBinding BorderColor}"
                                           BorderBrush="{TemplateBinding BorderBrush}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}" />
            <OnPlatform x:TypeArguments="View">
                <On Platform="Android, iOS">
                    <Label Grid.Row="1"
                           Grid.ColumnSpan="2"
                           Style="{TemplateBinding ActualValidationErrorLabelStyle}"
                           AutomationId="RadEntryErrorIndicator" />
                </On>
                <On Platform="MacCatalyst, WinUI">
                    <Image Grid.Column="1"
                           Style="{TemplateBinding ActualValidationErrorImageStyle}"
                           AutomationId="RadEntryErrorIndicator" />
                </On>
            </OnPlatform>
        </Grid>
    </ControlTemplate>

    <VisualStateGroupList x:Key="RadEntry_VisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="0, 0, 0, 1" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="0, 0, 0, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Invalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="InvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="0, 0, 0, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnly" />
            <VisualState Name="ReadOnlyFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryReadOnlyFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="0, 0, 0, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidFocusedReadOnlyBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="0, 0, 0, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.PlaceholderColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadEntry_VisualStates_iOS">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="1" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryFocusedBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Invalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="InvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidFocusedBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnly" />
            <VisualState Name="ReadOnlyFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryReadOnlyFocusedBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidFocusedReadOnlyBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.PlaceholderColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryDisabledBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadEntry_VisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="1" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="1" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="3" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Invalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="InvalidMouseOver" />
            <VisualState Name="InvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="3" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnly" />
            <VisualState Name="ReadOnlyMouseOver" />
            <VisualState Name="ReadOnlyFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryReadOnlyFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="3" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalidMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidFocusedReadOnlyBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="3" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.PlaceholderColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryDisabledBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadEntry_VisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="1" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="1" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Focused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryFocusedBorderBrush}" /> 
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="1, 1, 1, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Invalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="InvalidMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="InvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidFocusedBorderBrush}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="1, 1, 1, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnly">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryReadOnlyBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource ReadOnlyBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource ReadOnlyBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryFocusedBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryReadOnlyFocusedBorderBrush}" /> 
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="1, 1, 1, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalid">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryReadOnlyBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalidMouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ReadOnlyInvalidFocused">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryReadOnlyBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryInvalidFocusedReadOnlyBorderBrush}" /> 
                    <Setter Property="telerikMauiControls:RadEntry.BorderThickness" Value="1, 1, 1, 2" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadEntry.PlaceholderColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.TextColor" Value="{DynamicResource RadEntryDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BackgroundColor" Value="{DynamicResource RadEntryDisabledBackgroundColor}" />
                    <Setter Property="telerikMauiControls:RadEntry.BorderBrush" Value="{DynamicResource RadEntryDisabledBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="RadEntryStyle" TargetType="telerikMauiControls:RadEntry">
        <Setter Property="ControlTemplate" Value="{StaticResource RadEntry_ControlTemplate}" />
        <Setter Property="ClearButtonStyle" Value="{StaticResource DefaultClearButtonStyle}" />
        <Setter Property="ValidationErrorImageStyle" Value="{StaticResource DefaultValidationErrorImageStyle}" />
        <Setter Property="ValidationErrorLabelStyle" Value="{StaticResource DefaultValidationErrorLabelStyle}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=32, Android=44, iOS=36}" />
        <Setter Property="PlaceholderColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="TextColor" Value="{DynamicResource RadEntryTextColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadEntryBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadEntryBorderBrush}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=1, Android='0, 0, 0, 1'}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Android='4, 4, 0, 0', iOS=10, MacCatalyst=6, WinUI=4}" />
        <Setter Property="ValidationErrorColor" Value="{DynamicResource RadErrorColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Android={StaticResource RadEntry_VisualStates_Android},
                                   iOS={StaticResource RadEntry_VisualStates_iOS},
                                   MacCatalyst={StaticResource RadEntry_VisualStates_MacCatalyst},
                                   WinUI={StaticResource RadEntry_VisualStates_WinUI}}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadEntry" BasedOn="{StaticResource RadEntryStyle}" />
</ResourceDictionary>
