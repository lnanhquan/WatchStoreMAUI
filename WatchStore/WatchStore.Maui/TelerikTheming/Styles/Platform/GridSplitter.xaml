<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    x:Class="Telerik.Theming.Platform.Styles.GridSplitter"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikGridSplitter="clr-namespace:Telerik.Maui.Controls.GridSplitter;assembly=Telerik.Maui.Controls">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <VisualStateGroupList x:Key="RadGridSplitterVisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadGridSplitter.GripperColor" Value="{DynamicResource RadGridSplitterDisabledGripperColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadGridSplitterVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="MouseOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadGridSplitter.BackgroundColor" Value="{DynamicResource RadSurfaceAltColorDarken8}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadGridSplitter.GripperColor" Value="{DynamicResource RadGridSplitterDisabledGripperColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="GridSplitterRootBorderStyle" TargetType="telerikMauiControls:RadBorder">
        <Setter Property="BackgroundColor" Value="{TemplateBinding BackgroundColor}" />
        <Setter Property="Background" Value="{TemplateBinding Background}" />
        <Setter Property="BorderColor" Value="{TemplateBinding BorderColor}" />
        <Setter Property="BorderBrush" Value="{TemplateBinding BorderBrush}" />
        <Setter Property="BorderThickness" Value="{TemplateBinding BorderThickness}" />
        <Setter Property="CornerRadius" Value="{TemplateBinding CornerRadius}" />
        <Setter Property="Padding" Value="{TemplateBinding ContentPadding}" />
    </Style>

    <ControlTemplate x:Key="RadGridSplitterControlTemplate">
        <telerikMauiControls:RadBorder Style="{StaticResource GridSplitterRootBorderStyle}">
            <telerikGridSplitter:GridSplitterGripperView x:Name="PART_GripperView" />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="RadGridSplitterStyle" TargetType="telerikMauiControls:RadGridSplitter">
        <Setter Property="ControlTemplate" Value="{StaticResource RadGridSplitterControlTemplate}" />
        <Setter Property="MinimumHeightRequest" Value="16" />
        <Setter Property="MinimumWidthRequest" Value="16" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadGridSplitterBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="GripperColor" Value="{DynamicResource RadGridSplitterGripperColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Default={StaticResource RadGridSplitterVisualStates}, WinUI={StaticResource RadGridSplitterVisualStates_WinUI}}" />
    </Style>

    <Style TargetType="telerikMauiControls:RadGridSplitter" BasedOn="{StaticResource RadGridSplitterStyle}" />
</ResourceDictionary>
