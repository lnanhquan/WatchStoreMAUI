<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikTheming="clr-namespace:Telerik.Maui.Controls.Theming;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.SpeechToTextButton">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <telerikMauiControls:CornerRadiusToThicknessConverter x:Key="CornerRadiusToThicknessConverter" />

    <ControlTemplate x:Key="RadSpeechToTextButton_ControlTemplate">
        <telerikMauiControls:RadEffectsView x:Name="PART_EffectsView"
                                            BackgroundColor="{TemplateBinding BackgroundColor}"
                                            Background="{TemplateBinding Background}"
                                            BorderColor="{TemplateBinding BorderColor}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="{TemplateBinding CornerRadius, Converter={StaticResource CornerRadiusToThicknessConverter}}"
                                            Padding="{TemplateBinding Padding}">
            <ContentPresenter telerikMauiControls:ContentPresenterUtils.Content="{TemplateBinding ActualContent}" />
        </telerikMauiControls:RadEffectsView>
    </ControlTemplate>

    <VisualStateGroupList x:Key="SpeechToTextButtonVisualStates_Android">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="{DynamicResource RadPrimaryColorLighten12}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Listening">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ListeningPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Error">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadErrorColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="Transparent" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha38}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="SpeechToTextButtonVisualStates_iOS">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadSpeechToTextButtonPressedTextColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="{DynamicResource RadSpeechToTextButtonPressedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Listening">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ListeningPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Error">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadErrorColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="Transparent" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="SpeechToTextButtonVisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnBaseColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="{DynamicResource RadBaseHoverColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha60}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.Background" Value="{DynamicResource RadPrimaryButtonGradientBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Listening">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ListeningPointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ListeningPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Error">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadErrorColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="Transparent" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="Transparent" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="SpeechToTextButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="{DynamicResource RadBaseColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="{DynamicResource RadBaseActiveColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Listening">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BorderThickness" Value="0" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ListeningPointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BorderThickness" Value="0" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ListeningPressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.Content" Value="{x:Static telerikMaui:TelerikFont.IconStopRecording}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BorderThickness" Value="0" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Error">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadOnErrorColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="{DynamicResource RadErrorColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BorderThickness" Value="0" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadSpeechToTextButton.BackgroundColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha38}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="RadSpeechToTextButtonStyle" TargetType="telerikMauiControls:RadSpeechToTextButton">
        <Setter Property="telerikTheming:RadEffects.RippleMode" Value="Pulse" />
        <Setter Property="telerikTheming:RadEffects.RippleColor" Value="{DynamicResource RadOnAppSurfaceColorAlpha12}" />
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=16, MacCatalyst=12}" />
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconMicrophone}" />
        <Setter Property="TextColor" Value="{OnPlatform Default={DynamicResource RadOnPrimaryColor}, WinUI={DynamicResource RadPrimaryColor}}" />
        <Setter Property="BackgroundColor" Value="{OnPlatform Default={DynamicResource RadPrimaryColor}, WinUI={DynamicResource RadBaseColor}}" />
        <Setter Property="BorderBrush" Value="{OnPlatform WinUI={DynamicResource RadButtonBorderBrush}}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, WinUI=1}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=40, iOS=36, MacCatalyst=24, WinUI=32}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=40, iOS=36, MacCatalyst=22, WinUI=32}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=20, MacCatalyst=5, WinUI=4}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="Shadow" Value="{x:Null}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Android={StaticResource SpeechToTextButtonVisualStates_Android},
                                   iOS={StaticResource SpeechToTextButtonVisualStates_iOS},
                                   MacCatalyst={StaticResource SpeechToTextButtonVisualStates_MacCatalyst},
                                   WinUI={StaticResource SpeechToTextButtonVisualStates_WinUI}}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadSpeechToTextButton" BasedOn="{StaticResource RadSpeechToTextButtonStyle}" />

</ResourceDictionary>
