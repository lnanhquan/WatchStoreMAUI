<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMauiCollectionView="clr-namespace:Telerik.Maui.Controls.CollectionView;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMauiDataControls="clr-namespace:Telerik.Maui.Controls.DataControls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikMauiAIPrompt="clr-namespace:Telerik.Maui.Controls.AIPrompt;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.AIPrompt">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <telerikMauiControls:InvertedNullToBooleanConverter x:Key="InvertedNullToBooleanConverter" />
    <telerikMauiControls:FirstNotNullBindingMultiValueConverter x:Key="FirstNotNullBindingMultiValueConverter" />
    <telerikMauiControls:DictionaryConverter x:Key="ZeroToFalseConverter">
        <telerikMauiControls:DictionaryConverterItem Key="{x:Int32 0}" Value="{x:Boolean False}" />
    </telerikMauiControls:DictionaryConverter>

    <Shadow x:Key="AIPromptPopupContentViewShadow"
            Offset="{OnPlatform Default='0, 0', MacCatalyst='0, 8', WinUI='0, 8'}"
            Radius="{OnPlatform Default=0, MacCatalyst=40, WinUI=16}"
            Brush="{OnPlatform Default=Transparent, MacCatalyst=#40000000, WinUI=#24000000}" />
    <Shadow x:Key="AIPromptViewItemShadow"
            Offset="{OnPlatform Default='0, 0', WinUI='0, 2'}"
            Radius="{OnPlatform Default=0, WinUI=4}"
            Brush="{OnPlatform Default=Transparent, WinUI=#1F000000}" />
    <Shadow x:Key="AIPromptPopupButtonShadow"
            Offset="{OnPlatform Default='0, 0', MacCatalyst='0, 1'}"
            Radius="{OnPlatform Default=0, MacCatalyst=3}"
            Brush="{OnPlatform Default=Transparent, MacCatalyst={DynamicResource RadPrimaryColor}}"
            Opacity="{OnPlatform Default=0, MacCatalyst=0.24}" />
    <Shadow x:Key="AIPromptInputButtonShadow"
            Offset="0, 1"
            Radius="1.5"
            Brush="{DynamicResource RadOnAppSurfaceColor}"
            Opacity="0.2" />
    <Shadow x:Key="RadPrimaryButtonShadow"
            Offset="0, 1"
            Radius="3"
            Brush="{DynamicResource RadPrimaryColor}"
            Opacity="0.24" />

    <VisualStateGroupList x:Key="AIPromptItemViewVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadBorderContentView.BackgroundColor" Value="{DynamicResource RadAIPromptViewItemViewPointerOverBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled" />
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="AIPromptInputSuggestionViewLabelStyle" TargetType="Label">
        <Setter Property="Text" Value="{TemplateBinding Text}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <ControlTemplate x:Key="AIPromptInputSuggestionViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       Background="{TemplateBinding Background}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <Label Style="{TemplateBinding ActualLabelStyle}" />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="AIPromptSuggestionStyle_WinUI" TargetType="telerikMauiAIPrompt:AIPromptInputSuggestionView">
        <Setter Property="Shadow" Value="{StaticResource AIPromptViewItemShadow}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{StaticResource AIPromptItemViewVisualStates_WinUI}" />
    </Style>

    <Style x:Key="AIPromptSuggestionStyle" TargetType="telerikMauiAIPrompt:AIPromptInputSuggestionView" BasedOn="{OnPlatform WinUI={StaticResource AIPromptSuggestionStyle_WinUI}}">
        <Setter Property="LabelStyle" Value="{StaticResource AIPromptInputSuggestionViewLabelStyle}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAIPromptInputSuggestionViewBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=1, MacCatalyst=0}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=10}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Android='16, 15', iOS='16, 13.5', MacCatalyst='8, 4', WinUI='8, 9.5'}" />
    </Style>

    <DataTemplate x:Key="AIPromptSuggestionTemplate">
        <telerikMauiAIPrompt:AIPromptInputSuggestionView Text="{Binding}"
                                                         Style="{Binding ActualSuggestionStyle, Source={RelativeSource AncestorType={x:Type telerikMauiAIPrompt:AIPromptInputView}}}" />
    </DataTemplate>

    <DataTemplate x:Key="AIPromptSuggestionsItemsControlItemsLayoutTemplate">
        <VerticalStackLayout Spacing="{OnPlatform Default=4, MacCatalyst=8, WinUI=8}"
                             Padding="{OnPlatform WinUI='16, 0'}" />
    </DataTemplate>

    <Style x:Key="AIPromptSuggestionsExpanderHeaderStyle" TargetType="telerikMauiControls:ExpanderHeader">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="IndicatorColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, WinUI='16, 0'}" />
    </Style>

    <Style x:Key="AIPromptSuggestionsExpanderStyle" TargetType="telerikMauiControls:RadExpander">
        <Setter Property="HeaderText" Value="{TemplateBinding SuggestionsHeaderText}" />
        <Setter Property="IsExpanded" Value="True" />
        <Setter Property="IsVisible" Value="{Binding Suggestions.Count, Source={RelativeSource Mode=TemplatedParent}, Converter={StaticResource ZeroToFalseConverter}, TargetNullValue=False, FallbackValue=False}" />
        <Setter Property="Margin" Value="{OnPlatform Default='16, 0', MacCatalyst=0, WinUI=0}" />
        <Setter Property="HeaderStyle" Value="{StaticResource AIPromptSuggestionsExpanderHeaderStyle}" />
        <Setter Property="Content">
            <Setter.Value>
                <DataTemplate>
                    <telerikMauiDataControls:NonVirtualizedItemsControl ItemTemplate="{StaticResource AIPromptSuggestionTemplate}"
                                                                        SelectionMode="None"
                                                                        ItemTappedCommand="{TemplateBinding ActualSuggestionTappedCommand}"
                                                                        ItemsSource="{TemplateBinding Suggestions}"
                                                                        ItemsLayoutTemplate="{StaticResource AIPromptSuggestionsItemsControlItemsLayoutTemplate}"
                                                                        Margin="{OnPlatform Default='0, 8, 0, 0', iOS='0, 4, 0, 0', WinUI='0, 6, 0, 0'}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AIPromptInputEditorStyle" TargetType="Editor">
        <Setter Property="Text" Value="{TemplateBinding InputText}" />
        <Setter Property="Placeholder" Value="{telerikMauiControls:Localize AIPrompt_InputPlaceholder}" />
        <Setter Property="PlaceholderColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="AutoSize" Value="TextChanges" />
        <Setter Property="MaximumHeightRequest" Value="200" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, Android='14, 8, 14, 0', WinUI='16, 16, 16, 0'}" />
    </Style>

    <DataTemplate x:Key="InputButtonContentTemplate">
        <Grid ColumnDefinitions="Auto, *"
              ColumnSpacing="4">
            <Label FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                   FontSize="{OnPlatform Default=16, MacCatalyst=12}"
                   TextColor="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=TextColor}"
                   Text="{x:Static telerikMaui:TelerikFont.IconSparkle}"
                   HorizontalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=HorizontalTextAlignment}"
                   VerticalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=VerticalTextAlignment}" />
            <Label Grid.Column="1"
                   FontFamily="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=FontFamily}"
                   FontSize="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=FontSize}"
                   FontAttributes="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=FontAttributes}"
                   TextColor="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=TextColor}"
                   Text="{Binding}"
                   HorizontalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=HorizontalTextAlignment}"
                   VerticalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=VerticalTextAlignment}" />
        </Grid>
    </DataTemplate>

    <VisualStateGroupList x:Key="AIPromptInputButtonVisualStates_Mobile">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{OnPlatform Default={DynamicResource RadPrimaryColorLighten12}, iOS={DynamicResource RadPrimaryButtonPressedBackground}}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
        <VisualStateGroup Name="FocusStates">
            <VisualState Name="Unfocused" />
            <VisualState Name="Focused" />
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="AIPromptInputButtonVisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha60}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadAIPromptButtonPressedGradientBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadAIPromptButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadSurfaceAltColor}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Shadow" Value="{DynamicResource AIPromptInputButtonShadow}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
        <VisualStateGroup Name="FocusStates">
            <VisualState Name="Unfocused" />
            <VisualState Name="Focused" />
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="AIPromptInputButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadPrimaryColorAlpha90}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadPrimaryColorAlpha80}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha50}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource LabelDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.BorderBrush" Value="{DynamicResource RadButtonBorderBrush}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
        <VisualStateGroup Name="FocusStates">
            <VisualState Name="Unfocused" />
            <VisualState Name="Focused" />
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="AIPromptInputButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="Content" Value="{TemplateBinding InputButtonText}" />
        <Setter Property="ContentTemplate" Value="{StaticResource InputButtonContentTemplate}" />
        <Setter Property="Command" Value="{TemplateBinding ActualPromptRequestCommand}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="Background" Value="{OnPlatform Default={DynamicResource RadPrimaryColor}, MacCatalyst={DynamicResource RadPrimaryButtonGradientBackground}}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadAIPromptInputButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=0, MacCatalyst=0.5, WinUI=1}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=18, MacCatalyst=5}" />
        <Setter Property="HorizontalOptions" Value="{OnPlatform Default=End, WinUI=Start}" />
        <Setter Property="Padding" Value="{OnPlatform Android='8, 7', iOS='14, 9.5', MacCatalyst='8, 1.5', WinUI='8, 5, 8, 6'}" />
        <Setter Property="Margin" Value="{OnPlatform Android=8, iOS='16, 10', MacCatalyst=0, WinUI='12, 8'}" />
        <Setter Property="Shadow" Value="{OnPlatform Default={x:Null}, MacCatalyst={DynamicResource RadPrimaryButtonShadow}}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Default={StaticResource AIPromptInputButtonVisualStates_Mobile},
                                   MacCatalyst={StaticResource AIPromptInputButtonVisualStates_MacCatalyst},
                                   WinUI={StaticResource AIPromptInputButtonVisualStates_WinUI}}" />
    </Style>
    
    <ControlTemplate x:Key="AIPromptInputViewControlTemplate">
        <Grid x:Name="rootGrid"
              RowDefinitions="Auto, *, Auto"
              RowSpacing="{OnPlatform Default=4, MacCatalyst=20, WinUI=8}"
              Margin="{OnPlatform Default=0, MacCatalyst=20}">
            <telerikMauiControls:RadBorder BackgroundColor="{OnPlatform Default={DynamicResource RadEntryBackgroundColor}, Android=Transparent}"
                                           BorderBrush="{OnPlatform iOS={DynamicResource RadEntryBorderBrush}, MacCatalyst={DynamicResource RadEntryBorderBrush}}"
                                           BorderThickness="{OnPlatform iOS=1, MacCatalyst=1}"
                                           Padding="{OnPlatform iOS='1, 0', MacCatalyst='1, 0'}"
                                           CornerRadius="{OnPlatform iOS=10, MacCatalyst=6}"
                                           Margin="{OnPlatform iOS='16, 8, 16, 0'}">
                <Editor x:Name="inputEditor"
                        Style="{TemplateBinding ActualInputEditorStyle}"
                        BackgroundColor="Transparent" />
            </telerikMauiControls:RadBorder>
            <OnPlatform x:TypeArguments="View">
                <On Platform="Android, WinUI">
                    <Grid InputTransparent="True">
                        <telerikMauiControls:RadBorder BorderBrush="{DynamicResource EditorBorderBrush}"
                                                       BorderThickness="{OnPlatform Android='0, 0, 0, 1', WinUI=1}"
                                                       CornerRadius="{OnPlatform Android='4, 4, 0, 0', WinUI=4}"
                                                       Margin="{OnPlatform Android='18, 0, 18, 8', WinUI='16, 16, 16, 0'}" />
                        <telerikMauiControls:RadBorder BorderBrush="{DynamicResource RadPrimaryColor}"
                                                       BorderThickness="0, 0, 0, 2"
                                                       CornerRadius="{OnPlatform Android='4, 4, 0, 0', WinUI=4}"
                                                       Margin="{OnPlatform Android='18, 0, 18, 7', WinUI='16, 16, 16, 0'}"
                                                       IsVisible="{Binding IsFocused, Source={x:Reference inputEditor}}" />
                    </Grid>
                </On>
            </OnPlatform>
            <telerikMauiControls:RadExpander Grid.Row="1"
                                             Style="{TemplateBinding ActualSuggestionsExpanderStyle}" />
            <OnPlatform x:TypeArguments="View">
                <On Platform="WinUI">
                    <telerikMauiControls:RadBorder Grid.Row="2"
                                                   BackgroundColor="{DynamicResource RadAIPromptInputViewFooterBackgroundColor}" />
                </On>
            </OnPlatform>
            <OnPlatform x:TypeArguments="View">
                <On Platform="Android, iOS, WinUI">
                    <Line Grid.Row="2"
                          Stroke="{DynamicResource RadBorderBrush}"
                          X2="{Binding Width, Source={x:Reference rootGrid}}"
                          VerticalOptions="Start" />
                </On>
            </OnPlatform>
            <telerikMauiControls:RadTemplatedButton Grid.Row="2"
                                                    Style="{TemplateBinding ActualInputButtonStyle}" />
        </Grid>
    </ControlTemplate>

    <Style TargetType="telerikMauiAIPrompt:AIPromptInputView">
        <Setter Property="ControlTemplate" Value="{StaticResource AIPromptInputViewControlTemplate}" />
    </Style>

    <ControlTemplate x:Key="AIPromptRatingViewControlTemplate">
        <HorizontalStackLayout Spacing="{OnPlatform iOS=8}">
            <HorizontalStackLayout.Resources>
                <Style TargetType="telerikMauiControls:RadToggleButton">
                    <Setter Property="MinimumWidthRequest" Value="{OnPlatform Default=36, WinUI=32}" />
                    <Setter Property="MinimumHeightRequest" Value="{OnPlatform Default=36, MacCatalyst=20, WinUI=32}" />
                    <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
                    <Setter Property="FontSize" Value="{OnPlatform Default=16, MacCatalyst=12}" />
                    <Setter Property="TextColor" Value="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, Android={DynamicResource RadSubtleColor}, iOS={DynamicResource RadSubtleColor}}" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Shadow" Value="{x:Null}" />
                    <Setter Property="VerticalOptions" Value="Center" />
                    <Setter Property="VisualStateManager.VisualStateGroups">
                        <VisualStateGroupList>
                            <VisualStateGroup Name="CommonStates">
                                <VisualState Name="Normal" />
                                <VisualState Name="MouseOver">
                                    <VisualState.Setters>
                                        <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadAIPromptButtonPointerOverBackgroundColor}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadAIPromptButtonPressedTextColor}" />
                                        <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadAIPromptButtonPressedBackgroundColor}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="Toggled">
                                    <VisualState.Setters>
                                        <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                                        <Setter Property="telerikMauiControls:RadToggleButton.Shadow" Value="{x:Null}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="ToggledMouseOver">
                                    <VisualState.Setters>
                                        <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                                        <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadAIPromptButtonPointerOverBackgroundColor}" />
                                        <Setter Property="telerikMauiControls:RadToggleButton.Shadow" Value="{x:Null}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="ToggledPressed">
                                    <VisualState.Setters>
                                        <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource RadAIPromptButtonToggledPressedTextColor}" />
                                        <Setter Property="telerikMauiControls:RadToggleButton.Background" Value="{DynamicResource RadAIPromptButtonPressedBackgroundColor}" />
                                        <Setter Property="telerikMauiControls:RadToggleButton.Shadow" Value="{x:Null}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="ToggledDisabled">
                                    <VisualState.Setters>
                                        <Setter Property="telerikMauiControls:RadToggleButton.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                                        <Setter Property="telerikMauiControls:RadToggleButton.Shadow" Value="{x:Null}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup Name="FocusStates">
                                <VisualState Name="Unfocused" />
                                <VisualState Name="Focused" />
                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </Setter>
                </Style>
            </HorizontalStackLayout.Resources>
            <telerikMauiControls:RadToggleButton x:Name="PART_LikeToggleButton"
                                                 Content="{x:Static telerikMaui:TelerikFont.IconThumbsUp}" />
            <telerikMauiControls:RadToggleButton x:Name="PART_DislikeToggleButton"
                                                 Content="{x:Static telerikMaui:TelerikFont.IconThumbsDown}" />
        </HorizontalStackLayout>
    </ControlTemplate>

    <Style x:Key="AIPromptRatingViewStyle" TargetType="telerikMauiAIPrompt:AIPromptRatingView">
        <Setter Property="ControlTemplate" Value="{StaticResource AIPromptRatingViewControlTemplate}" />
        <Setter Property="HorizontalOptions" Value="{OnPlatform Default=Start, WinUI=End}" />
        <Setter Property="Margin" Value="{OnPlatform Android='8, 4', iOS='8, 10', MacCatalyst='8, 6', WinUI=8}" />
    </Style>

    <Style x:Key="AIPromptOutputItemViewTitleLabelStyle" TargetType="Label">
        <Setter Property="Text" Value="{TemplateBinding Title}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="FontSize" Value="{OnPlatform Android=20, iOS=22, MacCatalyst=15, WinUI=14}" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="LineBreakMode" Value="TailTruncation" />
        <Setter Property="Margin" Value="{OnPlatform Default='16, 12, 16, 0', iOS='16, 10, 16, 0'}" />
    </Style>

    <Style x:Key="AIPromptOutputItemViewInputTextLabelStyle" TargetType="Label">
        <Setter Property="Text" Value="{TemplateBinding InputText}" />
        <Setter Property="TextColor" Value="{DynamicResource RadSubtleColor}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Margin" Value="{OnPlatform Default='16, 0', WinUI='16, 8, 16, 12'}" />
    </Style>

    <Style x:Key="AIPromptOutputItemViewResponseTextLabelStyle" TargetType="Label">
        <Setter Property="Text" Value="{TemplateBinding ResponseText}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Margin" Value="{OnPlatform Default='16, 12', iOS='16, 18, 16, 10'}" />
    </Style>

    <Style x:Key="AIPromptOutputItemViewButtonBaseStyle" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="TextColor" Value="{DynamicResource RadPrimaryColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver" />
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadAIPromptButtonToggledPressedTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadButtonDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="AIPromptOutputItemViewButtonBaseStyle_WinUI" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{StaticResource AIPromptOutputItemViewButtonBaseStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadAIPromptButtonPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadAIPromptButtonPressedTextColor}" />
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadAIPromptButtonPressedBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <DataTemplate x:Key="AIPromptCopyButtonContentTemplate">
        <Grid ColumnDefinitions="Auto, *"
              ColumnSpacing="4">
            <Label FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                   FontSize="{OnPlatform Default=16, MacCatalyst=12}"
                   TextColor="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=TextColor}"
                   Text="{x:Static telerikMaui:TelerikFont.IconCopy}"
                   HorizontalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=HorizontalTextAlignment}"
                   VerticalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=VerticalTextAlignment}" />
            <Label Grid.Column="1"
                   FontFamily="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=FontFamily}"
                   FontSize="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=FontSize}"
                   FontAttributes="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=FontAttributes}"
                   TextColor="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=TextColor}"
                   Text="{Binding}"
                   HorizontalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=HorizontalTextAlignment}"
                   VerticalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=VerticalTextAlignment}" />
        </Grid>
    </DataTemplate>

    <Style x:Key="AIPromptOutputItemViewCopyButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{OnPlatform Default={StaticResource AIPromptOutputItemViewButtonBaseStyle}, WinUI={StaticResource AIPromptOutputItemViewButtonBaseStyle_WinUI}}">
        <Setter Property="Content" Value="{telerikMauiControls:Localize AIPrompt_Copy}" />
        <Setter Property="ContentTemplate" Value="{StaticResource AIPromptCopyButtonContentTemplate}" />
        <Setter Property="CommandParameter" Value="{Binding}" />
        <Setter Property="Command" Value="{Binding ActualOutputItemCopyCommand, Source={RelativeSource AncestorType={x:Type telerikMauiAIPrompt:AIPromptOutputView}}}" />
        <Setter Property="Margin" Value="{OnPlatform Android='8, 4', iOS='8, 11', MacCatalyst='8, 6', WinUI='12, 8, 4, 8'}" />
        <Setter Property="Shadow" Value="{x:Null}" />
    </Style>

    <DataTemplate x:Key="AIPromptRetryButtonContentTemplate">
        <Grid ColumnDefinitions="Auto, *"
              ColumnSpacing="4">
            <Label FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                   FontSize="{OnPlatform Default=16, MacCatalyst=12}"
                   TextColor="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=TextColor}"
                   Text="{x:Static telerikMaui:TelerikFont.IconRefresh}"
                   HorizontalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=HorizontalTextAlignment}"
                   VerticalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=VerticalTextAlignment}" />
            <Label Grid.Column="1"
                   FontFamily="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=FontFamily}"
                   FontSize="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=FontSize}"
                   FontAttributes="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=FontAttributes}"
                   TextColor="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=TextColor}"
                   Text="{Binding}"
                   HorizontalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=HorizontalTextAlignment}"
                   VerticalTextAlignment="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadTemplatedButton}}, Path=VerticalTextAlignment}" />
        </Grid>
    </DataTemplate>

    <Style x:Key="AIPromptOutputItemViewRetryButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{OnPlatform Default={StaticResource AIPromptOutputItemViewButtonBaseStyle}, WinUI={StaticResource AIPromptOutputItemViewButtonBaseStyle_WinUI}}">
        <Setter Property="Content" Value="{telerikMauiControls:Localize AIPrompt_Retry}" />
        <Setter Property="ContentTemplate" Value="{StaticResource AIPromptRetryButtonContentTemplate}" />
        <Setter Property="CommandParameter" Value="{Binding}" />
        <Setter Property="Command" Value="{Binding ActualOutputItemRetryCommand, Source={RelativeSource AncestorType={x:Type telerikMauiAIPrompt:AIPromptOutputView}}}" />
        <Setter Property="IsVisible" Value="{Binding OutputItemRetryCommand, Source={RelativeSource AncestorType={x:Type telerikMauiAIPrompt:AIPromptOutputView}}, Converter={StaticResource InvertedNullToBooleanConverter}}" />
        <Setter Property="Margin" Value="{OnPlatform Android='8, 4, 0, 4', iOS='8, 11, 0, 11', MacCatalyst='8, 6, 0, 6', WinUI='0, 8'}" />
        <Setter Property="Shadow" Value="{x:Null}" />
    </Style>

    <ControlTemplate x:Key="AIPromptOutputItemView_ControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       Background="{TemplateBinding Background}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <VerticalStackLayout x:Name="root">
                <Label Style="{TemplateBinding ActualTitleLabelStyle}" />
                <Label Style="{TemplateBinding ActualInputTextLabelStyle}" />
                <Label Style="{TemplateBinding ActualResponseTextLabelStyle}" />
                <Line Stroke="{TemplateBinding BorderBrush}" X2="{Binding Width, Source={Reference root}}" />
                <Grid ColumnDefinitions="{OnPlatform Default='*, Auto, Auto', WinUI='Auto, Auto, *'}">
                    <telerikMauiAIPrompt:AIPromptRatingView Rating="{TemplateBinding Rating}"
                                                            Grid.Column="{OnPlatform Default=0, WinUI=2}"
                                                            Style="{TemplateBinding ActualRatingStyle}" />
                    <telerikMauiControls:RadTemplatedButton Grid.Column="{OnPlatform Default=1, WinUI=1}"
                                                            Style="{TemplateBinding ActualRetryButtonStyle}" />
                    <telerikMauiControls:RadTemplatedButton Grid.Column="{OnPlatform Default=2, WinUI=0}"
                                                            Style="{TemplateBinding ActualCopyButtonStyle}" />
                </Grid>
            </VerticalStackLayout>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="AIPromptOutputItemViewStyle" TargetType="telerikMauiAIPrompt:AIPromptOutputItemView">
        <Setter Property="ControlTemplate" Value="{StaticResource AIPromptOutputItemView_ControlTemplate}" />
        <Setter Property="TitleLabelStyle" Value="{StaticResource AIPromptOutputItemViewTitleLabelStyle}" />
        <Setter Property="InputTextLabelStyle" Value="{StaticResource AIPromptOutputItemViewInputTextLabelStyle}" />
        <Setter Property="ResponseTextLabelStyle" Value="{StaticResource AIPromptOutputItemViewResponseTextLabelStyle}" />
        <Setter Property="CopyButtonStyle" Value="{StaticResource AIPromptOutputItemViewCopyButtonStyle}" />
        <Setter Property="RetryButtonStyle" Value="{StaticResource AIPromptOutputItemViewRetryButtonStyle}" />
        <Setter Property="RatingStyle" Value="{StaticResource AIPromptRatingViewStyle}" />
        <Setter Property="Title" Value="{Binding Title}" />
        <Setter Property="InputText" Value="{Binding InputText}" />
        <Setter Property="ResponseText" Value="{Binding ResponseText}" />
        <Setter Property="Rating" Value="{Binding Rating}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAIPromptOutputItemViewBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=8, Android=4, iOS=12}" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, WinUI='16, 0'}" />
    </Style>

    <DataTemplate x:Key="AIPromptOutputItemTemplate">
        <telerikMauiAIPrompt:AIPromptOutputItemView Style="{Binding ActualOutputItemStyle, Source={RelativeSource AncestorType={x:Type telerikMauiAIPrompt:AIPromptOutputView}}}"
                                                    Shadow="{StaticResource AIPromptViewItemShadow}" />
    </DataTemplate>

    <Style x:Key="AIPromptOutputItemStyle" TargetType="telerikMauiControls:RadCollectionViewItemView">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="ControlTemplate">
            <ControlTemplate>
                <ContentPresenter />
            </ControlTemplate>
        </Setter>
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="MouseOver" />
                    <VisualState Name="Selected" />
                    <VisualState Name="Disabled" />
                </VisualStateGroup>
                <VisualStateGroup Name="OrientationStates">
                    <VisualState Name="Vertical" />
                    <VisualState Name="Horizontal" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <ControlTemplate x:Key="AIPromptOutputViewControlTemplate">
        <telerikMauiControls:RadCollectionView ItemTemplate="{StaticResource AIPromptOutputItemTemplate}"
                                               ItemViewStyle="{StaticResource AIPromptOutputItemStyle}"
                                               ItemsSource="{TemplateBinding OutputItems}"
                                               SelectionMode="None"
                                               Margin="{OnPlatform Default='16, 8', MacCatalyst=20, WinUI='0, 16'}">
            <telerikMauiControls:RadCollectionView.ItemsLayout>
                <telerikMauiCollectionView:CollectionViewLinearLayout Orientation="Vertical"
                                                                      ItemSpacing="{OnPlatform Default=4, MacCatalyst=12, WinUI=16}" />
            </telerikMauiControls:RadCollectionView.ItemsLayout>
        </telerikMauiControls:RadCollectionView>
    </ControlTemplate>

    <Style x:Key="AIPromptCommandLabelStyle" TargetType="Label">
        <Setter Property="Text" Value="{TemplateBinding Text}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <Setter.Value>
                <VisualStateGroupList>
                    <VisualStateGroup Name="CommonStates">
                        <VisualState Name="Normal" />
                        <VisualState Name="Disabled">
                            <VisualState.Setters>
                                <Setter Property="Label.TextColor" Value="{DynamicResource RadAIPromptButtonDisabledTextColor}" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="AIPromptCommandItemViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       Background="{TemplateBinding Background}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <HorizontalStackLayout Spacing="4">
                <Image Source="{TemplateBinding ImageSource}"
                       telerikMauiControls:FontImageSourceUtils.FallbackColor="{Binding TextColor, Source={Reference label}}">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup Name="CommonStates">
                            <VisualState Name="Normal" />
                            <VisualState Name="Disabled">
                                <VisualState.Setters>
                                    <Setter Property="Image.Opacity" Value="0.38" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                </Image>
                <Label x:Name="label"
                       Style="{TemplateBinding ActualLabelStyle}" />
            </HorizontalStackLayout>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="AIPromptCommandItemViewStyle_WinUI" TargetType="telerikMauiAIPrompt:AIPromptCommandItemView">
        <Setter Property="Shadow" Value="{StaticResource AIPromptViewItemShadow}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{StaticResource AIPromptItemViewVisualStates_WinUI}" />
        <Setter Property="Margin" Value="16, 0, 16, 8" />
    </Style>

    <Style x:Key="AIPromptCommandItemViewStyle" TargetType="telerikMauiAIPrompt:AIPromptCommandItemView" BasedOn="{OnPlatform WinUI={StaticResource AIPromptCommandItemViewStyle_WinUI}}">
        <Setter Property="ControlTemplate" Value="{StaticResource AIPromptCommandItemViewControlTemplate}" />
        <Setter Property="ImageSource" Value="{Binding ImageSource}" />
        <Setter Property="Text" Value="{Binding Text}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAIPromptCommandItemViewBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default=1, MacCatalyst=0}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=10, WinUI=8}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Android='16, 15', iOS='16, 13.5', MacCatalyst='8, 4', WinUI='8, 9.5'}" />
        <Setter Property="LabelStyle" Value="{DynamicResource AIPromptCommandLabelStyle}" />
        <Setter Property="Command">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource FirstNotNullBindingMultiValueConverter}">
                    <Binding Path="CommandTappedCommand" Source="{RelativeSource AncestorType={x:Type telerikMauiControls:RadAIPrompt}}" />
                    <Binding Path="Command" />
                </MultiBinding>
            </Setter.Value>
        </Setter>
        <Setter Property="CommandParameter" Value="{Binding}" />
    </Style>

    <Style x:Key="AIPromptCommandGroupExpanderHeaderStyle" TargetType="telerikMauiControls:ExpanderHeader">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="IndicatorColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, WinUI='16, 0, 16, 8'}" />
    </Style>

    <Style x:Key="AIPromptCommandGroupExpanderStyle" TargetType="telerikMauiControls:RadExpander">
        <Setter Property="HeaderText" Value="{TemplateBinding Text}" />
        <Setter Property="HeaderStyle" Value="{StaticResource AIPromptCommandGroupExpanderHeaderStyle}" />
        <Setter Property="Content">
            <Setter.Value>
                <DataTemplate>
                    <telerikMauiDataControls:NonVirtualizedItemsControl SelectionMode="None"
                                                                        ItemTemplate="{StaticResource AIPromptCommandTemplateSelector}"
                                                                        ItemTappedCommand="{Binding CommandTappedCommand, Source={RelativeSource AncestorType={x:Type telerikMauiAIPrompt:AIPromptCommandView}}}"
                                                                        ItemsSource="{TemplateBinding Commands}"
                                                                        ItemsLayoutTemplate="{StaticResource AIPromptCommandsGroupItemsControlItemsLayoutTemplate}"
                                                                        Margin="{OnPlatform Default='0, 4, 0, 0', MacCatalyst='0, 8, 0, 0', WinUI=0}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="AIPromptCommandGroupView_ControlTemplate">
        <telerikMauiControls:RadExpander Style="{TemplateBinding ActualExpanderStyle}" />
    </ControlTemplate>

    <Style x:Key="AIPromptCommandGroupViewStyle" TargetType="telerikMauiAIPrompt:AIPromptCommandGroupView">
        <Setter Property="ControlTemplate" Value="{StaticResource AIPromptCommandGroupView_ControlTemplate}" />
        <Setter Property="ExpanderStyle" Value="{StaticResource AIPromptCommandGroupExpanderStyle}" />
        <Setter Property="ImageSource" Value="{Binding ImageSource}" />
        <Setter Property="Text" Value="{Binding Text}" />
        <Setter Property="Commands" Value="{Binding Commands}" />
    </Style>

    <DataTemplate x:Key="AIPromptCommandTemplate">
        <telerikMauiAIPrompt:AIPromptCommandItemView Style="{Binding ActualCommandStyle, Source={RelativeSource AncestorType={x:Type telerikMauiAIPrompt:AIPromptCommandView}}}" />
    </DataTemplate>

    <DataTemplate x:Key="AIPromptCommandGroupTemplate">
        <telerikMauiAIPrompt:AIPromptCommandGroupView Style="{Binding ActualCommandGroupStyle, Source={RelativeSource AncestorType={x:Type telerikMauiAIPrompt:AIPromptCommandView}}}" />
    </DataTemplate>

    <telerikMauiAIPrompt:AIPromptCommandTemplateSelector x:Key="AIPromptCommandTemplateSelector"
                                                         CommandTemplate="{StaticResource AIPromptCommandTemplate}"
                                                         GroupTemplate="{StaticResource AIPromptCommandGroupTemplate}" />

    <DataTemplate x:Key="AIPromptCommandsGroupItemsControlItemsLayoutTemplate">
        <VerticalStackLayout Spacing="{OnPlatform Default=4, MacCatalyst=8, WinUI=0}" />
    </DataTemplate>

    <DataTemplate x:Key="AIPromptCommandsItemsControlItemsLayoutTemplate">
        <VerticalStackLayout Spacing="{OnPlatform Default=4, MacCatalyst=8, WinUI=0}" />
    </DataTemplate>

    <ControlTemplate x:Key="AIPromptCommandViewControlTemplate">
        <telerikMauiDataControls:NonVirtualizedItemsControl SelectionMode="None"
                                                            ItemTemplate="{StaticResource AIPromptCommandTemplateSelector}"
                                                            ItemTappedCommand="{TemplateBinding CommandTappedCommand}"
                                                            ItemsSource="{TemplateBinding Commands}"
                                                            ItemsLayoutTemplate="{StaticResource AIPromptCommandsItemsControlItemsLayoutTemplate}"
                                                            Margin="{OnPlatform Default='16, 8', MacCatalyst=20, WinUI='0, 16'}" />
    </ControlTemplate>

    <Style TargetType="telerikMauiAIPrompt:AIPromptCommandView">
        <Setter Property="ControlTemplate" Value="{StaticResource AIPromptCommandViewControlTemplate}" />
    </Style>

    <Style x:Key="AIPromptTabViewHeaderStyle" TargetType="telerikMauiControls:TabViewHeader">
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAIPromptTabViewHeaderBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="ContentPadding" Value="{OnPlatform Android=8, iOS='16, 14', MacCatalyst=20, WinUI='12, 8'}" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="VerticalOptions" Value="Fill" />
    </Style>

    <VisualStateGroupList x:Key="AIPromptTabViewHeaderItemVisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadAIPromptTabViewHeaderItemSelectedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadAIPromptTabViewHeaderItemDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="AIPromptTabViewHeaderItemVisualStates_Desktop">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadAIPromptTabViewHeaderItemPointerOverBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadPrimaryColor}" />
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.BackgroundColor" Value="{DynamicResource RadAIPromptTabViewHeaderItemSelectedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:TabViewHeaderItem.TextColor" Value="{DynamicResource RadAIPromptTabViewHeaderItemDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="AIPromptTabViewHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem">
        <Setter Property="TextColor" Value="{OnPlatform Default={DynamicResource RadPrimaryColor}, WinUI={DynamicResource RadOnAppSurfaceColor}}" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=8, MacCatalyst=5}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Android='8, 9', iOS='8, 9.5', MacCatalyst='8, 1.5', WinUI='8, 6.5'}" />
        <Setter Property="ImagePosition" Value="Left" />
        <Setter Property="ImageSpacing" Value="4" />
        <Setter Property="ImageWidth" Value="{OnPlatform Default=16, MacCatalyst=12}" />
        <Setter Property="ImageHeight" Value="{OnPlatform Default=16, MacCatalyst=12}" />
        <Setter Property="Margin" Value="{OnPlatform Default='0, 0, 16, 0', MacCatalyst='8, 0, 0, 0', WinUI='0, 0, 4, 0'}" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="VerticalTextAlignment" Value="Center" />
        <Setter Property="HorizontalOptions" Value="{OnPlatform Default=Start, MacCatalyst=End}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Default={StaticResource AIPromptTabViewHeaderItemVisualStates},
                                   MacCatalyst={StaticResource AIPromptTabViewHeaderItemVisualStates_Desktop},
                                   WinUI={StaticResource AIPromptTabViewHeaderItemVisualStates_Desktop}}" />
    </Style>

    <Style x:Key="AIPromptInputViewHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem" BasedOn="{StaticResource AIPromptTabViewHeaderItemStyle}" />

    <Style x:Key="AIPromptOutputViewHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem" BasedOn="{StaticResource AIPromptTabViewHeaderItemStyle}" />

    <Style x:Key="AIPromptCommandViewHeaderItemStyle" TargetType="telerikMauiControls:TabViewHeaderItem" BasedOn="{StaticResource AIPromptTabViewHeaderItemStyle}">
        <Setter Property="ImageSpacing" Value="0" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=18, MacCatalyst=10}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Android='8, 9', iOS=10, MacCatalyst=4, WinUI='8, 6.5'}" />
    </Style>

    <telerikMauiAIPrompt:AIPromptHeaderItemStyleSelector x:Key="AIPromptTabViewHeaderItemStyleSelector"
                                                         InputViewHeaderItemStyle="{StaticResource AIPromptInputViewHeaderItemStyle}"
                                                         OutputViewHeaderItemStyle="{StaticResource AIPromptOutputViewHeaderItemStyle}"
                                                         CommandViewHeaderItemStyle="{StaticResource AIPromptCommandViewHeaderItemStyle}"
                                                         DefaultHeaderItemStyle="{StaticResource AIPromptTabViewHeaderItemStyle}" />

    <Style x:Key="AIPromptTabViewContentStyle" TargetType="telerikMauiControls:TabViewContent">
        <Setter Property="BackgroundColor" Value="{OnPlatform Default=Transparent, WinUI={DynamicResource RadBaseColor}}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <DataTemplate x:Key="AIPromptTabViewItemTemplate">
        <telerikMauiControls:TabViewItem HeaderText="{Binding HeaderText}"
                                         ImageSource="{Binding HeaderImageSource}"
                                         Content="{Binding}"
                                         IsVisible="{Binding IsVisible}" />
    </DataTemplate>

    <Style x:Key="AIPromptTabViewStyle" TargetType="telerikMauiControls:RadTabView">
        <Setter Property="IsHeaderOverlaid" Value="False" />
        <Setter Property="HeaderStyle" Value="{StaticResource AIPromptTabViewHeaderStyle}" />
        <Setter Property="HeaderItemStyle" Value="{x:Null}" />
        <Setter Property="HeaderItemStyleSelector" Value="{StaticResource AIPromptTabViewHeaderItemStyleSelector}" />
        <Setter Property="ContentStyle" Value="{StaticResource AIPromptTabViewContentStyle}" />
        <Setter Property="ItemTemplate" Value="{StaticResource AIPromptTabViewItemTemplate}" />
        <Setter Property="ItemsSource" Value="{TemplateBinding Views}" />
        <Setter Property="AnimationDuration" Value="0" />
    </Style>

    <ControlTemplate x:Key="RadAIPromptControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       Background="{TemplateBinding Background}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <telerikMauiControls:RadTabView SelectedIndex="{Binding SelectedIndex, Mode=TwoWay, Source={RelativeSource Mode=TemplatedParent}}"
                                            Style="{TemplateBinding ActualTabViewStyle}" />
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="RadAIPromptStyle" TargetType="telerikMauiControls:RadAIPrompt">
        <Setter Property="ControlTemplate" Value="{StaticResource RadAIPromptControlTemplate}" />
        <Setter Property="TabViewStyle" Value="{StaticResource AIPromptTabViewStyle}" />
        <Setter Property="InputButtonStyle" Value="{StaticResource AIPromptInputButtonStyle}" />
        <Setter Property="InputEditorStyle" Value="{StaticResource AIPromptInputEditorStyle}" />
        <Setter Property="SuggestionsExpanderStyle" Value="{StaticResource AIPromptSuggestionsExpanderStyle}" />
        <Setter Property="SuggestionStyle" Value="{StaticResource AIPromptSuggestionStyle}" />
        <Setter Property="OutputItemStyle" Value="{StaticResource AIPromptOutputItemViewStyle}" />
        <Setter Property="CommandStyle" Value="{StaticResource AIPromptCommandItemViewStyle}" />
        <Setter Property="CommandGroupStyle" Value="{StaticResource AIPromptCommandGroupViewStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadAIPrompt" BasedOn="{StaticResource RadAIPromptStyle}" />

    <Style x:Key="AIPromptClosePopupButtonStyle_MacCatalyst" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadAIPromptClosePopupButtonPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadAIPromptButtonPressedTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="AIPromptClosePopupButtonStyle_WinUI" TargetType="telerikMauiControls:RadTemplatedButton">
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadAIPromptClosePopupButtonPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadAIPromptButtonPressedTextColor}" />
                            <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadAIPromptButtonPressedBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="AIPromptClosePopupButtonStyle" TargetType="telerikMauiControls:RadTemplatedButton" BasedOn="{OnPlatform MacCatalyst={StaticResource AIPromptClosePopupButtonStyle_MacCatalyst}, WinUI={StaticResource AIPromptClosePopupButtonStyle_WinUI}}">
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconCancel}" />
        <Setter Property="WidthRequest" Value="{OnPlatform Default=20, Android=24, WinUI=22}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=20, Android=24, WinUI=22}" />
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="{OnPlatform Android=16, iOS=12, MacCatalyst=12, WinUI=14}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="Background" Value="{DynamicResource RadAIPromptClosePopupButtonBackgroundColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, iOS=10, MacCatalyst=10}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Command" Value="{TemplateBinding ActualClosePopupCommand}" />
        <Setter Property="HorizontalOptions" Value="{OnPlatform Default=Center, MacCatalyst=Start}" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="Margin" Value="{OnPlatform Default=0, MacCatalyst='20, 0, 0, 0', WinUI='0, 0, 12, 0'}" />
    </Style>

    <ControlTemplate x:Key="AIPromptPopupContentViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       Background="{TemplateBinding Background}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       Shadow="{StaticResource AIPromptPopupContentViewShadow}">
            <Grid RowDefinitions="{OnPlatform Android='52, *', iOS='64, *', MacCatalyst='60, *', WinUI='48, *'}">
                <ContentPresenter Grid.RowSpan="2" />
                <Grid Padding="{OnPlatform Android='8, 6', iOS=8}"
                      Margin="{OnPlatform Default=0, Android=8, iOS='0, 0, 16, 0'}"
                      HorizontalOptions="{OnPlatform Default=End, MacCatalyst=Start}"
                      VerticalOptions="Center">
                    <View.GestureRecognizers>
                        <TapGestureRecognizer Command="{TemplateBinding ActualClosePopupCommand}" />
                    </View.GestureRecognizers>
                    <telerikMauiControls:RadTemplatedButton Style="{TemplateBinding ActualClosePopupButtonStyle}" />
                </Grid>
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="AIPromptPopupContentViewStyle" TargetType="telerikMauiAIPrompt:AIPromptPopupContentView">
        <Setter Property="ControlTemplate" Value="{StaticResource AIPromptPopupContentViewControlTemplate}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadAIPromptPopupContentViewBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadAIPromptPopupContentViewBorderColor}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=8, Android=4, iOS=12}" />
        <Setter Property="Padding" Value="{OnPlatform Default='0, 4', Android='16, 4', iOS='8, 4'}" />
        <Setter Property="ClosePopupButtonStyle" Value="{StaticResource AIPromptClosePopupButtonStyle}" />
    </Style>

    <VisualStateGroupList x:Key="RadAIPromptButtonVisualStates_Mobile">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadAIPromptPopupButtonPressedBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadAIPromptPopupButtonDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
        <VisualStateGroup Name="FocusStates">
            <VisualState Name="Unfocused" />
            <VisualState Name="Focused" />
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadAIPromptButtonVisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver" />
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadOnPrimaryColorAlpha60}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadAIPromptPopupButtonPressedBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadAIPromptPopupButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadBaseColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
        <VisualStateGroup Name="FocusStates">
            <VisualState Name="Unfocused" />
            <VisualState Name="Focused" />
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="RadAIPromptButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadPrimaryColorAlpha90}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Pressed">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadAIPromptPopupButtonPressedBackground}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="telerikMauiControls:RadTemplatedButton.TextColor" Value="{DynamicResource RadAIPromptPopupButtonDisabledTextColor}" />
                    <Setter Property="telerikMauiControls:RadTemplatedButton.Background" Value="{DynamicResource RadOnAppSurfaceColorAlpha38}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
        <VisualStateGroup Name="FocusStates">
            <VisualState Name="Unfocused" />
            <VisualState Name="Focused" />
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="RadAIPromptButtonStyle" TargetType="telerikMauiControls:RadAIPromptButton">
        <Setter Property="WidthRequest" Value="{OnPlatform Default=40, iOS=36, MacCatalyst=22}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Default=40, iOS=36, MacCatalyst=22}" />
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=16, MacCatalyst=12}" />
        <Setter Property="Content" Value="{x:Static telerikMaui:TelerikFont.IconSparkle}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
        <Setter Property="Background" Value="{OnPlatform Default={DynamicResource RadPrimaryColor}, MacCatalyst={DynamicResource RadPrimaryButtonGradientBackground}}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=20, iOS=18, MacCatalyst=11}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="PopupContentViewStyle" Value="{StaticResource AIPromptPopupContentViewStyle}" />
        <Setter Property="Shadow" Value="{OnPlatform Default={x:Null}, MacCatalyst={DynamicResource AIPromptPopupButtonShadow}}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Default={StaticResource RadAIPromptButtonVisualStates_Mobile},
                                   MacCatalyst={StaticResource RadAIPromptButtonVisualStates_MacCatalyst},
                                   WinUI={StaticResource RadAIPromptButtonVisualStates_WinUI}}" />
    </Style>

    <Style TargetType="telerikMauiControls:RadAIPromptButton" BasedOn="{StaticResource RadAIPromptButtonStyle}" />
</ResourceDictionary>