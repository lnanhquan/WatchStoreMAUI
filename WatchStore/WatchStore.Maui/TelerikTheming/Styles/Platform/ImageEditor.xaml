<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:telerikImageEditor="clr-namespace:Telerik.Maui.Controls.ImageEditor;assembly=Telerik.Maui.Controls"
                    xmlns:telerikToolbar="clr-namespace:Telerik.Maui.Controls.Toolbar;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.ImageEditor">
    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <!-- Dependencies: Buttons, Toolbar.xaml -->

    <Style x:Key="ImageEditorOptionsToolbarItemViewContentHeaderLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=20, MacCatalyst=22}" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="Margin" Value="0, 4, 0, 32" />
    </Style>

    <Style x:Key="ImageEditorOptionsToolbarItemViewContentCaptionLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=14, MacCatalyst=13}" />
        <Setter Property="Margin" Value="{OnPlatform Default='0, 0, 0, 2', MacCatalyst='0, 0, 0, 3'}" />
    </Style>

    <Style TargetType="telerikMauiControls:ImageEditorZoomToolbarItemView" BasedOn="{StaticResource SplitButtonToolbarItemViewStyle}">
        <Setter Property="BorderBrush" Value="{DynamicResource RadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="MinimumWidthRequest" Value="180" />
        <Setter Property="DisplayOptions" Value="Image, Text" />
    </Style>
    
    <Style TargetType="telerikMauiControls:ImageEditorCropOptionsToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style TargetType="telerikMauiControls:ImageEditorResizeOptionsToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    <Style TargetType="telerikMauiControls:ImageEditorFilterOptionsToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}" />
    
    <Style x:Key="ImageEditorToolbarApplyStyle" TargetType="telerikMauiControls:ButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}">
        <Setter Property="HorizontalOptions" Value="End" />
    </Style>

    <Style x:Key="ImageEditorToolbarCancelStyle" TargetType="telerikMauiControls:ButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewBaseStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>

    <Style x:Key="ImageEditorToolbarTitleStyle" TargetType="telerikMauiControls:LabelToolbarItemView" BasedOn="{StaticResource LabelToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="DisplayOptions" Value="Text" />
    </Style>

    <Style x:Key="ImageEditorToolbarSliderStyle" TargetType="telerikMauiControls:SliderToolbarItemView" BasedOn="{StaticResource SliderToolbarItemViewStyle}">
        <Setter Property="HorizontalOptions" Value="Fill" />
    </Style>

    <Style x:Key="ImageEditorToolbarPickerStyle" TargetType="telerikMauiControls:ListPickerButtonToolbarItemView" BasedOn="{StaticResource ButtonToolbarItemViewStyle}">
        <Setter Property="DisplayOptions" Value="Text" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="HorizontalTextAlignment" Value="Center" />
        <Setter Property="IsDropDownIndicatorVisible" Value="False" />
    </Style>

    <Style x:Key="ImageEditorOptionsToolbarItemViewContentApplyButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}">
        <Setter Property="TextColor" Value="{OnPlatform Default={DynamicResource RadOnAppSurfaceColor}, MacCatalyst={DynamicResource RadOnPrimaryColor}}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=14, MacCatalyst=13}" />
        <Setter Property="BackgroundColor" Value="{OnPlatform Default={DynamicResource RadBaseColor}, iOS=Transparent, MacCatalyst={DynamicResource RadPrimaryColor}}" />
        <Setter Property="BorderWidth" Value="{OnPlatform Default=0, WinUI=1, MacCatalyst=1}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, WinUI=4, MacCatalyst=5}" />
        <Setter Property="VisualStateManager.VisualStateGroups" Value="{OnPlatform Default={StaticResource RadButtonVisualStates}, WinUI={StaticResource RadButtonVisualStates_WinUI}}" />
    </Style>

    <Style x:Key="ImageEditorOptionsToolbarItemViewContentCancelButtonStyle" TargetType="telerikMauiControls:RadButton" BasedOn="{StaticResource RadButtonStyle}">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="FontSize" Value="{OnPlatform Default=14, MacCatalyst=13}" />
        <Setter Property="BorderWidth" Value="{OnPlatform Default=0, WinUI=1, MacCatalyst=1}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, WinUI=4, MacCatalyst=5}" />
    </Style>

    <Style x:Key="ImageEditorOptionsToolbarItemViewContentStyle" TargetType="telerikMauiControls:ImageEditorOptionsToolbarItemViewContent">
        <Setter Property="WidthRequest" Value="300" />
        <Setter Property="HeaderLabelStyle" Value="{StaticResource ImageEditorOptionsToolbarItemViewContentHeaderLabelStyle}" />
        <Setter Property="CaptionLabelStyle" Value="{StaticResource ImageEditorOptionsToolbarItemViewContentCaptionLabelStyle}" />
        <Setter Property="ApplyButtonStyle" Value="{StaticResource ImageEditorOptionsToolbarItemViewContentApplyButtonStyle}" />
        <Setter Property="CancelButtonStyle" Value="{StaticResource ImageEditorOptionsToolbarItemViewContentCancelButtonStyle}" />
    </Style>

    <ControlTemplate x:Key="ImageEditorCropOptionsToolbarItemViewContentControlTemplate">
        <VerticalStackLayout Padding="20">
            <Label Text="{TemplateBinding HeaderText}"
                   Style="{TemplateBinding ActualHeaderLabelStyle}" />
            <Label Text="{TemplateBinding CropOperationText}"
                   Style="{TemplateBinding ActualCaptionLabelStyle}" />
            <telerikMauiControls:RadComboBox ItemsSource="{TemplateBinding CropOperations}"
                                             SelectedItem="{TemplateBinding SelectedCropOperation}"
                                             Margin="{OnPlatform Default='0, 0, 0, 24', MacCatalyst='0, 0, 0, 20'}" />
            <Grid ColumnDefinitions="Auto, Auto"
                  ColumnSpacing="{OnPlatform Default=4, MacCatalyst=8}"
                  HorizontalOptions="End">
                <telerikMauiControls:RadButton Grid.Column="{OnPlatform Default=0, WinUI=1}"
                                               Text="{TemplateBinding CancelText}"
                                               Command="{TemplateBinding CancelCommand}"
                                               Style="{TemplateBinding ActualCancelButtonStyle}" />
                <telerikMauiControls:RadButton Grid.Column="{OnPlatform Default=1, WinUI=0}" 
                                               Text="{TemplateBinding ApplyText}"
                                               Command="{TemplateBinding ApplyCommand}"
                                               Style="{TemplateBinding ActualApplyButtonStyle}" />
            </Grid>
        </VerticalStackLayout>
    </ControlTemplate>

    <ControlTemplate x:Key="ImageEditorResizeOptionsToolbarItemViewContentControlTemplate">
        <VerticalStackLayout Padding="20">
            <Label Text="{TemplateBinding HeaderText}" Style="{TemplateBinding ActualHeaderLabelStyle}" />
            <Label Text="{TemplateBinding ResizeTypeText}" Style="{TemplateBinding ActualCaptionLabelStyle}" />
            <telerikMauiControls:RadComboBox SelectedItem="{TemplateBinding ResizeType}"
                                             Margin="{OnPlatform Default='0, 0, 0, 24', MacCatalyst='0, 0, 0, 20'}">
                <telerikMauiControls:RadComboBox.ItemsSource>
                    <x:Array Type="{x:Type telerikImageEditor:ResizeType}">
                        <telerikImageEditor:ResizeType>Percents</telerikImageEditor:ResizeType>
                        <telerikImageEditor:ResizeType>Pixels</telerikImageEditor:ResizeType>
                    </x:Array>
                </telerikMauiControls:RadComboBox.ItemsSource>
            </telerikMauiControls:RadComboBox>
            <VerticalStackLayout Margin="{OnPlatform Default='0, 0, 0, 24', MacCatalyst='0, 0, 0, 20'}">
                <VerticalStackLayout.Triggers>
                    <DataTrigger TargetType="VerticalStackLayout"
                                 Binding="{TemplateBinding ResizeType}"
                                 Value="Percents">
                        <Setter Property="IsVisible" Value="False" />
                    </DataTrigger>
                    <DataTrigger TargetType="VerticalStackLayout"
                                 Binding="{TemplateBinding ResizeType}"
                                 Value="Pixels">
                        <Setter Property="IsVisible" Value="True" />
                    </DataTrigger>
                </VerticalStackLayout.Triggers>
                <Grid ColumnDefinitions="*, *"
                      RowDefinitions="Auto, Auto"
                      ColumnSpacing="10"
                      Margin="{OnPlatform Default='0, 0, 0, 24', MacCatalyst='0, 0, 0, 20'}">
                    <Label Grid.Column="0"
                           Grid.Row="0"
                           Text="{TemplateBinding ResizeWidthText}"
                           Style="{TemplateBinding ActualCaptionLabelStyle}" />
                    <telerikMauiControls:RadNumericInput Grid.Column="0"
                                                         Grid.Row="1"
                                                         Minimum="0"
                                                         Value="{TemplateBinding ResizeWidth}" />
                    <Label Grid.Column="1"
                           Grid.Row="0"
                           Text="{TemplateBinding ResizeHeightText}"
                           Style="{TemplateBinding ActualCaptionLabelStyle}" />
                    <telerikMauiControls:RadNumericInput Grid.Column="1"
                                                         Grid.Row="1"
                                                         Minimum="0"
                                                         Value="{TemplateBinding ResizeHeight}" />
                </Grid>
                <HorizontalStackLayout Spacing="4">
                    <telerikMauiControls:RadCheckBox IsChecked="{TemplateBinding IsAspectRatioLocked}" />
                    <Label Text="{TemplateBinding AspectRatioLockedText}"
                           Style="{TemplateBinding ActualCaptionLabelStyle}" />
                </HorizontalStackLayout>
            </VerticalStackLayout>
            <VerticalStackLayout Margin="{OnPlatform Default='0, 0, 0, 24', MacCatalyst='0, 0, 0, 20'}">
                <VerticalStackLayout.Triggers>
                    <DataTrigger TargetType="VerticalStackLayout"
                                 Binding="{TemplateBinding ResizeType}"
                                 Value="Percents">
                        <Setter Property="IsVisible" Value="True" />
                    </DataTrigger>
                    <DataTrigger TargetType="VerticalStackLayout"
                                 Binding="{TemplateBinding ResizeType}"
                                 Value="Pixels">
                        <Setter Property="IsVisible" Value="False" />
                    </DataTrigger>
                </VerticalStackLayout.Triggers>
                <Label Text="{TemplateBinding ResizeScaleText}"
                       Style="{TemplateBinding ActualCaptionLabelStyle}" />
                <telerikMauiControls:RadSlider Maximum="{TemplateBinding ResizeScaleMaximum}"
                                               Minimum="{TemplateBinding ResizeScaleMinimum}"
                                               Value="{TemplateBinding ResizeScale, Mode=TwoWay}" />
            </VerticalStackLayout>
            <Grid ColumnDefinitions="Auto, Auto"
                  ColumnSpacing="{OnPlatform Default=4, MacCatalyst=8}"
                  HorizontalOptions="End">
                <telerikMauiControls:RadButton Grid.Column="{OnPlatform Default=0, WinUI=1}"
                                               Text="{TemplateBinding CancelText}"
                                               Command="{TemplateBinding CancelCommand}"
                                               Style="{TemplateBinding ActualCancelButtonStyle}" />
                <telerikMauiControls:RadButton Grid.Column="{OnPlatform Default=1, WinUI=0}"
                                               Text="{TemplateBinding ApplyText}"
                                               Command="{TemplateBinding ApplyCommand}"
                                               Style="{TemplateBinding ActualApplyButtonStyle}" />
            </Grid>
        </VerticalStackLayout>
    </ControlTemplate>

    <ControlTemplate x:Key="ImageEditorFilterOptionsToolbarItemViewContentControlTemplate">
        <VerticalStackLayout Padding="20">
            <Label Text="{TemplateBinding HeaderText}"
                   Style="{TemplateBinding ActualHeaderLabelStyle}" />
            <Label Text="{TemplateBinding HueText}"
                   Style="{TemplateBinding ActualCaptionLabelStyle}" />

            <telerikMauiControls:RadSlider Maximum="{TemplateBinding HueMaximum}"
                                           Minimum="{TemplateBinding HueMinimum}"
                                           Value="{TemplateBinding Hue, Mode=TwoWay}"
                                           Margin="{OnPlatform Default='0, 0, 0, 24', MacCatalyst='0, 0, 0, 20'}" />
            <Label Text="{TemplateBinding SaturationText}"
                   Style="{TemplateBinding ActualCaptionLabelStyle}" />
            <telerikMauiControls:RadSlider Maximum="{TemplateBinding SaturationMaximum}"
                                           Minimum="{TemplateBinding SaturationMinimum}"
                                           Value="{TemplateBinding Saturation, Mode=TwoWay}"
                                           Margin="{OnPlatform Default='0, 0, 0, 24', MacCatalyst='0, 0, 0, 20'}" />
            <Label Text="{TemplateBinding BrightnessText}"
                   Style="{TemplateBinding ActualCaptionLabelStyle}" />
            <telerikMauiControls:RadSlider Maximum="{TemplateBinding BrightnessMaximum}"
                                           Minimum="{TemplateBinding BrightnessMinimum}"
                                           Value="{TemplateBinding Brightness, Mode=TwoWay}"
                                           Margin="{OnPlatform Default='0, 0, 0, 24', MacCatalyst='0, 0, 0, 20'}" />
            <Label Text="{TemplateBinding ContrastText}"
                   Style="{TemplateBinding ActualCaptionLabelStyle}" />
            <telerikMauiControls:RadSlider Maximum="{TemplateBinding ContrastMaximum}"
                                           Minimum="{TemplateBinding ContrastMinimum}"
                                           Value="{TemplateBinding Contrast, Mode=TwoWay}"
                                           Margin="{OnPlatform Default='0, 0, 0, 24', MacCatalyst='0, 0, 0, 20'}" />
            <Label Text="{TemplateBinding BlurText}"
                   Style="{TemplateBinding ActualCaptionLabelStyle}" />
            <telerikMauiControls:RadSlider Maximum="{TemplateBinding BlurMaximum}"
                                           Minimum="{TemplateBinding BlurMinimum}"
                                           Value="{TemplateBinding Blur, Mode=TwoWay}"
                                           Margin="{OnPlatform Default='0, 0, 0, 24', MacCatalyst='0, 0, 0, 20'}" />
            <Label Text="{TemplateBinding SharpenText}"
                   Style="{TemplateBinding ActualCaptionLabelStyle}" />
            <telerikMauiControls:RadSlider Maximum="{TemplateBinding SharpenMaximum}"
                                           Minimum="{TemplateBinding SharpenMinimum}"
                                           Value="{TemplateBinding Sharpen, Mode=TwoWay}"
                                           Margin="{OnPlatform Default='0, 0, 0, 24', MacCatalyst='0, 0, 0, 20'}" />
            <Grid ColumnDefinitions="Auto, Auto"
                  ColumnSpacing="{OnPlatform Default=4, MacCatalyst=8}"
                  HorizontalOptions="End">
                <telerikMauiControls:RadButton Grid.Column="{OnPlatform Default=0, WinUI=1}" 
                                               Text="{TemplateBinding CancelText}"
                                               Command="{TemplateBinding CancelCommand}"
                                               Style="{TemplateBinding ActualCancelButtonStyle}" />
                <telerikMauiControls:RadButton Grid.Column="{OnPlatform Default=1, WinUI=0}"
                                               Text="{TemplateBinding ApplyText}"
                                               Command="{TemplateBinding ApplyCommand}"
                                               Style="{TemplateBinding ActualApplyButtonStyle}" />
            </Grid>
        </VerticalStackLayout>
    </ControlTemplate>

    <Style x:Key="ImageEditorCropOptionsToolbarItemViewContentStyle" TargetType="telerikMauiControls:ImageEditorCropOptionsToolbarItemViewContent">
        <Setter Property="WidthRequest" Value="300" />
    </Style>

    <Style x:Key="ImageEditorResizeOptionsToolbarItemViewContentStyle" TargetType="telerikMauiControls:ImageEditorResizeOptionsToolbarItemViewContent">
        <Setter Property="WidthRequest" Value="300" />
    </Style>

    <Style x:Key="ImageEditorFilterOptionsToolbarItemViewContentStyle" TargetType="telerikMauiControls:ImageEditorFilterOptionsToolbarItemViewContent">
        <Setter Property="WidthRequest" Value="300" />
    </Style>

    <Style TargetType="telerikMauiControls:ImageEditorCropOptionsToolbarItemViewContent" BasedOn="{StaticResource ImageEditorOptionsToolbarItemViewContentStyle}">
        <Setter Property="ControlTemplate" Value="{StaticResource ImageEditorCropOptionsToolbarItemViewContentControlTemplate}" />
    </Style>

    <Style TargetType="telerikMauiControls:ImageEditorResizeOptionsToolbarItemViewContent" BasedOn="{StaticResource ImageEditorOptionsToolbarItemViewContentStyle}">
        <Setter Property="ControlTemplate" Value="{StaticResource ImageEditorResizeOptionsToolbarItemViewContentControlTemplate}" />
    </Style>

    <Style TargetType="telerikMauiControls:ImageEditorFilterOptionsToolbarItemViewContent" BasedOn="{StaticResource ImageEditorOptionsToolbarItemViewContentStyle}">
        <Setter Property="ControlTemplate" Value="{StaticResource ImageEditorFilterOptionsToolbarItemViewContentControlTemplate}" />
    </Style>

    <Style TargetType="telerikMauiControls:RadImageEditorToolbar" BasedOn="{StaticResource RadToolbarStyle}" />
</ResourceDictionary>