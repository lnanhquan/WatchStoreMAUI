<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:expander="clr-namespace:Telerik.Maui.Controls.Expander;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.Accordion">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <expander:ExpandCollapseIndicatorLocationToGridColumnConverter x:Key="ExpandCollapseIndicatorLocationToGridColumnConverter" />
    <expander:InverseThicknessConverter x:Key="InverseThicknessConverter" />

    <ControlTemplate x:Key="AccordionItemHeader_ControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       Background="{TemplateBinding Background}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       Margin="{TemplateBinding Padding, Converter={StaticResource InverseThicknessConverter}}">
            <telerikMauiControls:RadBorder.GestureRecognizers>
                <TapGestureRecognizer Command="{TemplateBinding ToggleExpandCommand}" />
            </telerikMauiControls:RadBorder.GestureRecognizers>
            <Grid ColumnDefinitions="Auto, *, Auto">
                <expander:ExpandCollapseIndicator x:Name="PART_ExpandCollapseIndicator"
                                                  Text="{TemplateBinding IndicatorText}"
                                                  Margin="{TemplateBinding IndicatorMargin}"
                                                  IsExpanded="{TemplateBinding IsExpanded}"
                                                  IsAnimationEnabled="{TemplateBinding IsAnimationEnabled}"
                                                  FontFamily="{TemplateBinding IndicatorFontFamily}"
                                                  TextColor="{TemplateBinding IndicatorColor}"
                                                  IndicatorLocation="{TemplateBinding IndicatorLocation}"
                                                  IndicatorAnimationDuration="{TemplateBinding IndicatorAnimationDuration}"
                                                  IndicatorAnimationEasing="{TemplateBinding IndicatorAnimationEasing}"
                                                  Grid.Column="{TemplateBinding IndicatorLocation, Converter={StaticResource ExpandCollapseIndicatorLocationToGridColumnConverter}}"
                                                  FontSize="{TemplateBinding IndicatorFontSize}"
                                                  VerticalOptions="Center"
                                                  AutomationId="RadAccordionExpandCollapseIndicator" />
                <ContentPresenter Grid.Column="1" AutomationId="RadAccordionHeader" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="ExpandableViewHeaderTextStyle" TargetType="Label">
        <Setter Property="FontSize" Value="{OnPlatform Default=16, MacCatalyst=13}" />
        <Setter Property="FontAttributes"  Value="Bold" />
        <Setter Property="TextColor"  Value="{DynamicResource RadOnAppSurfaceColor}" />
    </Style>

    <Style x:Key="AccordionItemHeaderStyle" TargetType="telerikMauiControls:AccordionItemHeader">
        <Setter Property="ControlTemplate" Value="{StaticResource AccordionItemHeader_ControlTemplate}" />
        <Setter Property="IndicatorColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadSurfaceAltColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default='16, 0, 4, 0', MacCatalyst='8, 0, 4, 0'}" />
        <Setter Property="IndicatorMargin" Value="{OnPlatform Default='0, 0, 12, 0', MacCatalyst='0, 0, 4, 0'}" />
        <Setter Property="IndicatorFontSize" Value="{OnPlatform Default=14, MacCatalyst=10, WinUI=12}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=44, MacCatalyst=10, WinUI=40}" />
        <Setter Property="TextStyle" Value="{StaticResource ExpandableViewHeaderTextStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:AccordionItemHeader" BasedOn="{StaticResource AccordionItemHeaderStyle}" />

    <Style x:Key="RadAccordionStyle" TargetType="telerikMauiControls:RadAccordion">
        <Setter Property="BackgroundColor" Value="Transparent" />
    </Style>
    <Style TargetType="telerikMauiControls:RadAccordion" BasedOn="{StaticResource RadAccordionStyle}" />
</ResourceDictionary>