<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:treeView="clr-namespace:Telerik.Maui.Controls.TreeView;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.TreeView">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <ControlTemplate x:Key="TreeViewItemViewControlTemplate_WinUI">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{OnPlatform Default={TemplateBinding ContentPadding}, WinUI=0}">
            <Grid>
                <treeView:TreeViewItemLayout Indentation="{TemplateBinding Indentation}"
                                             Spacing="{TemplateBinding Spacing}"
                                             Padding="{TemplateBinding ContentPadding}">
                    <ContentPresenter />
                </treeView:TreeViewItemLayout>
                <telerikMauiControls:RadBorder IsVisible="{TemplateBinding IsSelected}"
                                               BackgroundColor="{DynamicResource RadPrimaryColor}"
                                               HorizontalOptions="Start"
                                               CornerRadius="2"
                                               WidthRequest="3"
                                               ScaleY="0.6" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="TreeViewItemViewControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{OnPlatform Default={TemplateBinding ContentPadding}, WinUI=0}">
            <treeView:TreeViewItemLayout Indentation="{TemplateBinding Indentation}" Spacing="{TemplateBinding Spacing}">
                <ContentPresenter />
            </treeView:TreeViewItemLayout>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="TreeViewItemViewExpandButtonControlTemplate_WinUI">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{OnPlatform Default={TemplateBinding ContentPadding}, WinUI=0}"
                                       AutomationId="RadTreeViewItem">
            <Grid>
                <treeView:TreeViewItemLayout Indentation="{TemplateBinding Indentation}"
                                             Spacing="{TemplateBinding Spacing}"
                                             Padding="{TemplateBinding ContentPadding}">
                    <treeView:TreeViewItemBusyIndicator Style="{TemplateBinding BusyIndicatorStyle}"
                                                        IsBusy="{TemplateBinding IsBusy}"
                                                        IsVisible="{TemplateBinding IsExpandButtonVisible}"
                                                        AutomationId="RadTreeViewItemBusyIndicator">
                        <treeView:TreeViewItemExpandButton Style="{TemplateBinding ExpandButtonStyle}"
                                                           Command="{TemplateBinding ToggleExpandedCommand}"
                                                           TextColor="{TemplateBinding TextColor}"
                                                           IsExpanded="{TemplateBinding IsExpanded}"
                                                           IsLeaf="{TemplateBinding IsLeaf}"
                                                           IsEnabled="{TemplateBinding IsEnabled}"
                                                           AutomationId="RadTreeViewExpandButton" />
                    </treeView:TreeViewItemBusyIndicator>
                    <ContentPresenter />
                </treeView:TreeViewItemLayout>
                <telerikMauiControls:RadBorder IsVisible="{TemplateBinding IsSelected}"
                                               BackgroundColor="{DynamicResource RadPrimaryColor}"
                                               HorizontalOptions="Start"
                                               CornerRadius="2"
                                               WidthRequest="4"
                                               ScaleY="0.61" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="TreeViewItemViewExpandButtonControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{OnPlatform Default={TemplateBinding ContentPadding}, WinUI=0}"
                                       AutomationId="RadTreeViewItem">
            <treeView:TreeViewItemLayout Indentation="{TemplateBinding Indentation}"
                                         Spacing="{TemplateBinding Spacing}">
                <treeView:TreeViewItemBusyIndicator Style="{TemplateBinding BusyIndicatorStyle}"
                                                    IsBusy="{TemplateBinding IsBusy}"
                                                    IsVisible="{TemplateBinding IsExpandButtonVisible}"
                                                    AutomationId="RadTreeViewItemBusyIndicator">
                    <treeView:TreeViewItemExpandButton Style="{TemplateBinding ExpandButtonStyle}"
                                                       Command="{TemplateBinding ToggleExpandedCommand}"
                                                       TextColor="{TemplateBinding TextColor}"
                                                       IsExpanded="{TemplateBinding IsExpanded}"
                                                       IsLeaf="{TemplateBinding IsLeaf}"
                                                       IsEnabled="{TemplateBinding IsEnabled}"
                                                       AutomationId="RadTreeViewExpandButton" />
                </treeView:TreeViewItemBusyIndicator>
                <ContentPresenter />
            </treeView:TreeViewItemLayout>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="TreeViewItemViewImageControlTemplate_WinUI">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       AutomationId="RadTreeViewItem">
            <Grid>
                <treeView:TreeViewItemLayout Indentation="{TemplateBinding Indentation}"
                                             Spacing="{TemplateBinding Spacing}"
                                             Padding="{TemplateBinding ContentPadding}">
                    <treeView:TreeViewItemBusyIndicator Style="{TemplateBinding BusyIndicatorStyle}"
                                                        IsBusy="{TemplateBinding IsBusy}"
                                                        IsVisible="{TemplateBinding IsExpandButtonVisible}"
                                                        AutomationId="RadTreeViewItemBusyIndicator">
                        <treeView:TreeViewItemExpandButton Style="{TemplateBinding ExpandButtonStyle}"
                                                           Command="{TemplateBinding ToggleExpandedCommand}"
                                                           TextColor="{TemplateBinding TextColor}"
                                                           IsExpanded="{TemplateBinding IsExpanded}"
                                                           IsLeaf="{TemplateBinding IsLeaf}"
                                                           IsEnabled="{TemplateBinding IsEnabled}"
                                                           AutomationId="RadTreeViewExpandButton" />
                    </treeView:TreeViewItemBusyIndicator>
                    <treeView:TreeViewItemImage Source="{TemplateBinding ImageSource}"
                                                Style="{TemplateBinding ImageStyle}"
                                                IsEnabled="{TemplateBinding IsEnabled}"
                                                IsVisible="{TemplateBinding IsImageVisible}"
                                                AutomationId="RadTreeViewImage" />
                    <ContentPresenter />
                </treeView:TreeViewItemLayout>
                <telerikMauiControls:RadBorder IsVisible="{TemplateBinding IsSelected}"
                                               BackgroundColor="{DynamicResource RadPrimaryColor}"
                                               HorizontalOptions="Start"
                                               CornerRadius="2"
                                               WidthRequest="4"
                                               ScaleY="0.61" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="TreeViewItemViewImageControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       AutomationId="RadTreeViewItem">
            <treeView:TreeViewItemLayout Indentation="{TemplateBinding Indentation}"
                                         Spacing="{TemplateBinding Spacing}"
                                         Padding="{TemplateBinding ContentPadding}">
                <treeView:TreeViewItemBusyIndicator Style="{TemplateBinding BusyIndicatorStyle}"
                                                    IsBusy="{TemplateBinding IsBusy}"
                                                    IsVisible="{TemplateBinding IsExpandButtonVisible}"
                                                    AutomationId="RadTreeViewItemBusyIndicator">
                    <treeView:TreeViewItemExpandButton Style="{TemplateBinding ExpandButtonStyle}"
                                                       Command="{TemplateBinding ToggleExpandedCommand}"
                                                       TextColor="{TemplateBinding TextColor}"
                                                       IsExpanded="{TemplateBinding IsExpanded}"
                                                       IsLeaf="{TemplateBinding IsLeaf}"
                                                       IsEnabled="{TemplateBinding IsEnabled}"
                                                       AutomationId="RadTreeViewExpandButton" />
                </treeView:TreeViewItemBusyIndicator>
                <treeView:TreeViewItemImage Source="{TemplateBinding ImageSource}"
                                            Style="{TemplateBinding ImageStyle}"
                                            IsEnabled="{TemplateBinding IsEnabled}"
                                            IsVisible="{TemplateBinding IsImageVisible}"
                                            AutomationId="RadTreeViewImage" />
                <ContentPresenter />
            </treeView:TreeViewItemLayout>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="TreeViewItemViewCheckBoxControlTemplate_WinUI">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{OnPlatform Default={TemplateBinding ContentPadding}, WinUI=0}"
                                       AutomationId="RadTreeViewItem">
            <Grid>
                <treeView:TreeViewItemLayout Indentation="{TemplateBinding Indentation}"
                                             Spacing="{TemplateBinding Spacing}"
                                             Padding="{TemplateBinding ContentPadding}">
                    <treeView:TreeViewItemBusyIndicator Style="{TemplateBinding BusyIndicatorStyle}"
                                                        IsBusy="{TemplateBinding IsBusy}"
                                                        IsVisible="{TemplateBinding IsExpandButtonVisible}"
                                                        AutomationId="RadTreeViewItemBusyIndicator">
                        <treeView:TreeViewItemExpandButton Style="{TemplateBinding ExpandButtonStyle}"
                                                           Command="{TemplateBinding ToggleExpandedCommand}"
                                                           TextColor="{TemplateBinding TextColor}"
                                                           IsExpanded="{TemplateBinding IsExpanded}"
                                                           IsLeaf="{TemplateBinding IsLeaf}"
                                                           IsEnabled="{TemplateBinding IsEnabled}"
                                                           AutomationId="RadTreeViewExpandButton" />
                    </treeView:TreeViewItemBusyIndicator>
                    <treeView:TreeViewItemCheckBox Style="{TemplateBinding CheckBoxStyle}"
                                                   IsChecked="{TemplateBinding IsChecked}"
                                                   IsEnabled="{TemplateBinding IsEnabled}"
                                                   IsVisible="{TemplateBinding IsCheckBoxVisible}"
                                                   AutomationId="RadTreeViewCheckBox" />
                    <treeView:TreeViewItemImage Source="{TemplateBinding ImageSource}"
                                                Style="{TemplateBinding ImageStyle}"
                                                IsEnabled="{TemplateBinding IsEnabled}"
                                                IsVisible="{TemplateBinding IsImageVisible}"
                                                AutomationId="RadTreeViewImage" />
                    <ContentPresenter />
                </treeView:TreeViewItemLayout>
                <telerikMauiControls:RadBorder IsVisible="{TemplateBinding IsSelected}"
                                               BackgroundColor="{DynamicResource RadPrimaryColor}"
                                               HorizontalOptions="Start"
                                               CornerRadius="2"
                                               WidthRequest="4"
                                               ScaleY="0.61" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="TreeViewItemViewCheckBoxControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{OnPlatform Default={TemplateBinding ContentPadding}, WinUI=0}"
                                       AutomationId="RadTreeViewItem">
            <treeView:TreeViewItemLayout Indentation="{TemplateBinding Indentation}"
                                         Spacing="{TemplateBinding Spacing}">
                <treeView:TreeViewItemBusyIndicator Style="{TemplateBinding BusyIndicatorStyle}"
                                                    IsBusy="{TemplateBinding IsBusy}"
                                                    IsVisible="{TemplateBinding IsExpandButtonVisible}"
                                                    AutomationId="RadTreeViewItemBusyIndicator">
                    <treeView:TreeViewItemExpandButton Style="{TemplateBinding ExpandButtonStyle}"
                                                       Command="{TemplateBinding ToggleExpandedCommand}"
                                                       TextColor="{TemplateBinding TextColor}"
                                                       IsExpanded="{TemplateBinding IsExpanded}"
                                                       IsLeaf="{TemplateBinding IsLeaf}"
                                                       IsEnabled="{TemplateBinding IsEnabled}"
                                                       AutomationId="RadTreeViewExpandButton" />
                </treeView:TreeViewItemBusyIndicator>
                <treeView:TreeViewItemCheckBox Style="{TemplateBinding CheckBoxStyle}"
                                                   IsChecked="{TemplateBinding IsChecked}"
                                                   IsEnabled="{TemplateBinding IsEnabled}"
                                                   IsVisible="{TemplateBinding IsCheckBoxVisible}"
                                                   AutomationId="RadTreeViewCheckBox" />
                <treeView:TreeViewItemImage Source="{TemplateBinding ImageSource}"
                                            Style="{TemplateBinding ImageStyle}"
                                            IsEnabled="{TemplateBinding IsEnabled}"
                                            IsVisible="{TemplateBinding IsImageVisible}"
                                            AutomationId="RadTreeViewImage" />
                <ContentPresenter />
            </treeView:TreeViewItemLayout>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="TreeViewItemViewFullControlTemplate_WinUI">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{OnPlatform Default={TemplateBinding ContentPadding}, WinUI=0}"
                                       AutomationId="RadTreeViewItem">
            <Grid>
                <treeView:TreeViewItemLayout Indentation="{TemplateBinding Indentation}"
                                             Spacing="{TemplateBinding Spacing}"
                                             Padding="{TemplateBinding ContentPadding}">
                    <treeView:TreeViewItemDragHandle Style="{TemplateBinding DragHandleStyle}"
                                                     TextColor="{TemplateBinding TextColor}"
                                                     IsEnabled="{TemplateBinding IsEnabled}"
                                                     IsVisible="{TemplateBinding IsDragHandleVisible}" />
                    <treeView:TreeViewItemBusyIndicator Style="{TemplateBinding BusyIndicatorStyle}"
                                                        IsBusy="{TemplateBinding IsBusy}"
                                                        IsVisible="{TemplateBinding IsExpandButtonVisible}"
                                                        AutomationId="RadTreeViewItemBusyIndicator">
                        <treeView:TreeViewItemExpandButton Style="{TemplateBinding ExpandButtonStyle}"
                                                           Command="{TemplateBinding ToggleExpandedCommand}"
                                                           TextColor="{TemplateBinding TextColor}"
                                                           IsExpanded="{TemplateBinding IsExpanded}"
                                                           IsLeaf="{TemplateBinding IsLeaf}"
                                                           IsEnabled="{TemplateBinding IsEnabled}"
                                                           AutomationId="RadTreeViewExpandButton" />
                    </treeView:TreeViewItemBusyIndicator>
                    <treeView:TreeViewItemCheckBox Style="{TemplateBinding CheckBoxStyle}"
                                                   IsChecked="{TemplateBinding IsChecked}"
                                                   IsEnabled="{TemplateBinding IsEnabled}"
                                                   IsVisible="{TemplateBinding IsCheckBoxVisible}"
                                                   AutomationId="RadTreeViewCheckBox" />
                    <treeView:TreeViewItemImage Source="{TemplateBinding ImageSource}"
                                                Style="{TemplateBinding ImageStyle}"
                                                IsEnabled="{TemplateBinding IsEnabled}"
                                                IsVisible="{TemplateBinding IsImageVisible}"
                                                AutomationId="RadTreeViewImage" />
                    <ContentPresenter />
                    <treeView:TreeViewItemContextMenuButton Style="{TemplateBinding ContextMenuButtonStyle}"
                                                            TextColor="{TemplateBinding TextColor}"
                                                            IsEnabled="{TemplateBinding IsEnabled}"
                                                            IsVisible="{TemplateBinding IsContextMenuButtonVisible}" />
                </treeView:TreeViewItemLayout>
                <telerikMauiControls:RadBorder IsVisible="{TemplateBinding IsSelected}"
                                               BackgroundColor="{DynamicResource RadPrimaryColor}"
                                               HorizontalOptions="Start"
                                               CornerRadius="2"
                                               WidthRequest="4"
                                               ScaleY="0.61" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="TreeViewItemViewFullControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{OnPlatform Default={TemplateBinding ContentPadding}, WinUI=0}"
                                       AutomationId="RadTreeViewItem">
            <treeView:TreeViewItemLayout Indentation="{TemplateBinding Indentation}"
                                         Spacing="{TemplateBinding Spacing}">
                <treeView:TreeViewItemDragHandle Style="{TemplateBinding DragHandleStyle}"
                                                 TextColor="{TemplateBinding TextColor}"
                                                 IsEnabled="{TemplateBinding IsEnabled}"
                                                 IsVisible="{TemplateBinding IsDragHandleVisible}" />
                <treeView:TreeViewItemBusyIndicator Style="{TemplateBinding BusyIndicatorStyle}"
                                                    IsBusy="{TemplateBinding IsBusy}"
                                                    IsVisible="{TemplateBinding IsExpandButtonVisible}"
                                                    AutomationId="RadTreeViewItemBusyIndicator">
                    <treeView:TreeViewItemExpandButton Style="{TemplateBinding ExpandButtonStyle}"
                                                       Command="{TemplateBinding ToggleExpandedCommand}"
                                                       TextColor="{TemplateBinding TextColor}"
                                                       IsExpanded="{TemplateBinding IsExpanded}"
                                                       IsLeaf="{TemplateBinding IsLeaf}"
                                                       IsEnabled="{TemplateBinding IsEnabled}"
                                                       AutomationId="RadTreeViewExpandButton" />
                </treeView:TreeViewItemBusyIndicator>
                <treeView:TreeViewItemCheckBox Style="{TemplateBinding CheckBoxStyle}"
                                               IsChecked="{TemplateBinding IsChecked}"
                                               IsEnabled="{TemplateBinding IsEnabled}"
                                               IsVisible="{TemplateBinding IsCheckBoxVisible}"
                                               AutomationId="RadTreeViewCheckBox" />
                <treeView:TreeViewItemImage Source="{TemplateBinding ImageSource}"
                                            Style="{TemplateBinding ImageStyle}"
                                            IsEnabled="{TemplateBinding IsEnabled}"
                                            IsVisible="{TemplateBinding IsImageVisible}"
                                            AutomationId="RadTreeViewImage" />
                <ContentPresenter />
                <treeView:TreeViewItemContextMenuButton Style="{TemplateBinding ContextMenuButtonStyle}"
                                                        TextColor="{TemplateBinding TextColor}"
                                                        IsEnabled="{TemplateBinding IsEnabled}"
                                                        IsVisible="{TemplateBinding IsContextMenuButtonVisible}" />
            </treeView:TreeViewItemLayout>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="TreeViewItemImageStyle" TargetType="treeView:TreeViewItemImage">
        <Setter Property="WidthRequest" Value="{OnPlatform Android=48, iOS=44, MacCatalyst=24, WinUI=26}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Android=48, iOS=44, MacCatalyst=24, WinUI=26}" />
        <Setter Property="Aspect" Value="Center" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="TreeViewItemButtonStyle" TargetType="treeView:TreeViewItemButton">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="BorderWidth" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="WidthRequest" Value="{OnPlatform Android=48, iOS=44, MacCatalyst=24, WinUI=26}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Android=48, iOS=44, MacCatalyst=24, WinUI=26}" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <Style x:Key="TreeViewItemCheckBoxStyle" TargetType="treeView:TreeViewItemCheckBox">
        <Setter Property="IsAnimated" Value="False" />
        <Setter Property="WidthRequest" Value="{OnPlatform Android=48, iOS=44, MacCatalyst=24, WinUI=26}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Android=48, iOS=44, MacCatalyst=24, WinUI=26}" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="UncheckedColor" Value="{OnPlatform Default={DynamicResource RadOnAppSurfaceColorLighten80}, Android={DynamicResource RadOnAppSurfaceColor}, WinUI={DynamicResource RadOnAppSurfaceColorLighten60}}" />
    </Style>

    <Style x:Key="TreeViewItemExpandButtonStyle" TargetType="treeView:TreeViewItemExpandButton" BasedOn="{StaticResource TreeViewItemButtonStyle}">
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconEmpty}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="ExpandStates">
                    <VisualState Name="Leaf" />
                    <VisualState Name="Expanded">
                        <VisualState.Setters>
                            <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconDownArrow}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Collapsed">
                        <VisualState.Setters>
                            <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconRightArrow}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="TreeViewItemContextMenuButtonStyle" TargetType="treeView:TreeViewItemContextMenuButton" BasedOn="{StaticResource TreeViewItemButtonStyle}">
        <Setter Property="HorizontalOptions" Value="End" />
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconDotsVert}" />
    </Style>

    <Style x:Key="TreeViewItemDragHandleStyle" TargetType="treeView:TreeViewItemDragHandle" BasedOn="{StaticResource TreeViewItemButtonStyle}">
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconMenu}" />
    </Style>

    <Style x:Key="TreeViewItemBusyIndicatorStyle" TargetType="treeView:TreeViewItemBusyIndicator">
        <Setter Property="AnimationContentWidthRequest" Value="16" />
        <Setter Property="AnimationContentHeightRequest" Value="16" />
        <Setter Property="AnimationContentColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="AnimationType" Value="Animation9" />
        <Setter Property="HorizontalOptions" Value="Start" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="TreeViewItemViewStyle" TargetType="treeView:TreeViewItemView">
        <Setter Property="ControlTemplate" Value="{OnPlatform Default={StaticResource TreeViewItemViewControlTemplate}, WinUI={StaticResource TreeViewItemViewControlTemplate_WinUI}}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=44, MacCatalyst=24, WinUI=32}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default=0, iOS='16, 0', MacCatalyst='6, 0', WinUI='8, 0'}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadTreeViewItemBorderColor}" />
        <Setter Property="BorderThickness" Value="{OnPlatform Default='0, 0, 0, 1', WinUI='6, 3'}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=0, WinUI=4}" />
        <Setter Property="ExpandButtonStyle" Value="{StaticResource TreeViewItemExpandButtonStyle}" />
        <Setter Property="BusyIndicatorStyle" Value="{StaticResource TreeViewItemBusyIndicatorStyle}" />
        <Setter Property="ImageStyle" Value="{StaticResource TreeViewItemImageStyle}" />
        <Setter Property="CheckBoxStyle" Value="{StaticResource TreeViewItemCheckBoxStyle}" />
        <Setter Property="VerticalContentOptions" Value="Center" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadTreeViewItemPointerOverBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{DynamicResource RadTreeViewItemSelectedBackgroundColor}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
        <Style.Triggers>
            <Trigger TargetType="treeView:TreeViewItemView" Property="IsExpandButtonVisible" Value="True">
                <Setter Property="ControlTemplate" Value="{OnPlatform Default={StaticResource TreeViewItemViewExpandButtonControlTemplate}, WinUI={StaticResource TreeViewItemViewExpandButtonControlTemplate_WinUI}}" />
            </Trigger>
            <Trigger TargetType="treeView:TreeViewItemView" Property="IsImageVisible" Value="True">
                <Setter Property="ControlTemplate" Value="{OnPlatform Default={StaticResource TreeViewItemViewImageControlTemplate}, WinUI={StaticResource TreeViewItemViewImageControlTemplate_WinUI}}" />
            </Trigger>
            <Trigger TargetType="treeView:TreeViewItemView" Property="IsCheckBoxVisible" Value="True">
                <Setter Property="ControlTemplate" Value="{OnPlatform Default={StaticResource TreeViewItemViewCheckBoxControlTemplate}, WinUI={StaticResource TreeViewItemViewCheckBoxControlTemplate_WinUI}}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="RadTreeViewStyle" TargetType="telerikMauiControls:RadTreeView">
        <Setter Property="LevelIndentation" Value="{OnPlatform Android=48, MacCatalyst=24, iOS=44, WinUI=26}" />
        <Setter Property="ItemStyle" Value="{StaticResource TreeViewItemViewStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadTreeView" BasedOn="{StaticResource RadTreeViewStyle}" />
</ResourceDictionary>