<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    xmlns:navigationView="clr-namespace:Telerik.Maui.Controls.NavigationView;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.NavigationView">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <ControlTemplate x:Key="NavigationViewControlTemplate" x:DataType="telerikMauiControls:RadNavigationView">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       Background="{TemplateBinding Background}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <navigationView:NavigationViewLayout DisplayMode="{TemplateBinding DisplayMode}"
                                                 CompactPaneWidth="{TemplateBinding CompactPaneWidth}">
                <navigationView:NavigationViewLayout.Header>
                    <navigationView:NavigationViewHeader Text="{TemplateBinding HeaderText}"
                                                         DisplayMode="{TemplateBinding DisplayMode}"
                                                         ExpandedWidth="{TemplateBinding ExpandedPaneWidth}"
                                                         CompactWidth="{TemplateBinding CompactPaneWidth}"
                                                         IsHeaderButtonToggled="{TemplateBinding IsPaneOpen}"
                                                         IsEnabled="{TemplateBinding IsEnabled}"
                                                         ContentTemplate="{TemplateBinding HeaderTemplate}"
                                                         Style="{TemplateBinding HeaderStyle}" />
                </navigationView:NavigationViewLayout.Header>
                <navigationView:NavigationViewLayout.Pane>
                    <navigationView:NavigationViewPane x:Name="PART_NavigationViewPane"
                                                       IsOpen="{TemplateBinding IsPaneOpen}"
                                                       IsEnabled="{TemplateBinding IsEnabled}"
                                                       DisplayMode="{TemplateBinding DisplayMode}"
                                                       ExpandedWidth="{TemplateBinding ExpandedPaneWidth}"
                                                       CompactWidth="{TemplateBinding CompactPaneWidth}"
                                                       Style="{TemplateBinding PaneStyle}" />
                </navigationView:NavigationViewLayout.Pane>
                <navigationView:NavigationViewLayout.Overlay>
                    <navigationView:NavigationViewDismissOverlay BackgroundColor="{TemplateBinding DismissOverlayColor}"
                                                                 Background="{TemplateBinding DismissOverlayBrush}"
                                                                 DismissCommand="{Binding Source={RelativeSource AncestorType={x:Type telerikMauiControls:RadNavigationView}}, Path=ClosePaneCommand}"
                                                                 IsVisible="{TemplateBinding IsDismissOverlayVisible}" />
                </navigationView:NavigationViewLayout.Overlay>
                <ContentPresenter />
            </navigationView:NavigationViewLayout>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="NavigationViewHeaderControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       Background="{TemplateBinding Background}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <Grid ColumnDefinitions="Auto, *"
                  ColumnSpacing="{OnPlatform WinUI=4}">
                <Grid WidthRequest="{TemplateBinding CompactWidth}"
                      IsVisible="{TemplateBinding IsHeaderButtonVisible}">
                    <navigationView:NavigationViewToggleButton IsToggled="{TemplateBinding IsHeaderButtonToggled}" 
                                                               IsEnabled="{TemplateBinding IsEnabled}"
                                                               Style="{TemplateBinding HeaderButtonStyle}"
                                                               HorizontalOptions="Center"
                                                               VerticalOptions="Center"
                                                               Margin="{OnPlatform Default='4, 0', MacCatalyst='9, 0', WinUI='8, 0'}" />
                </Grid>
                <ContentPresenter Grid.Column="1"
                                  Margin="{OnPlatform MacCatalyst='0, 0, 9, 0', WinUI='0, 0, 8, 0'}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="NavigationViewPaneControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       Background="{TemplateBinding Background}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <Grid RowDefinitions="Auto, *, Auto">
                <navigationView:NavigationViewPaneHeader Grid.Row="0"
                                                         Style="{TemplateBinding HeaderStyle}"
                                                         ControlTemplate="{TemplateBinding HeaderTemplate}"
                                                         Content="{Binding Source={RelativeSource AncestorType={x:Type navigationView:NavigationViewPane}}, Path=HeaderContent}"
                                                         IsVisible="{TemplateBinding HasHeaderItems}"
                                                         IsEnabled="{TemplateBinding IsEnabled}" />
                <ContentPresenter Grid.Row="1"
                                  Padding="{OnPlatform Default='0, 8', WinUI='0, 4'}" />
                <navigationView:NavigationViewPaneFooter Grid.Row="2"
                                                         Style="{TemplateBinding FooterStyle}"
                                                         ControlTemplate="{TemplateBinding FooterTemplate}"
                                                         Content="{Binding Source={RelativeSource AncestorType={x:Type navigationView:NavigationViewPane}}, Path=FooterContent}"
                                                         IsVisible="{TemplateBinding HasFooterItems}"
                                                         IsEnabled="{TemplateBinding IsEnabled}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="NavigationViewPaneHeaderControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       Background="{TemplateBinding Background}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       Content="{Binding Source={RelativeSource AncestorType={x:Type navigationView:NavigationViewPaneHeader}}, Path=Content}" />
    </ControlTemplate>

    <ControlTemplate x:Key="NavigationViewPaneFooterControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       Background="{TemplateBinding Background}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       Content="{Binding Source={RelativeSource AncestorType={x:Type navigationView:NavigationViewPaneFooter}}, Path=Content}" />
    </ControlTemplate>

    <ControlTemplate x:Key="NavigationViewItemViewControlTemplate">
        <Grid BackgroundColor="Transparent">
            <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                           Background="{TemplateBinding Background}"
                                           BorderColor="{TemplateBinding BorderColor}"
                                           BorderBrush="{TemplateBinding BorderBrush}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="{TemplateBinding CornerRadius}"
                                           Margin="{TemplateBinding ContentPadding}" />
            <Grid ColumnDefinitions="Auto, *"
                  ColumnSpacing="{TemplateBinding Spacing}">
                <Grid WidthRequest="{TemplateBinding LeadingWidth}">
                    <Image Source="{TemplateBinding ActualImageSource}"
                           Aspect="{TemplateBinding ImageAspect}"
                           WidthRequest="{TemplateBinding ImageWidth}"
                           HeightRequest="{TemplateBinding ImageHeight}" />
                </Grid>
                <ContentPresenter Grid.Column="1" />
            </Grid>
            <OnPlatform x:TypeArguments="View">
                <On Platform="WinUI">
                    <telerikMauiControls:RadBorder IsVisible="{TemplateBinding IsSelected}"
                                                   BackgroundColor="{DynamicResource RadPrimaryColor}"
                                                   HorizontalOptions="Start"
                                                   CornerRadius="2"
                                                   WidthRequest="3"
                                                   ScaleY="0.5"
                                                   Margin="{TemplateBinding ContentPadding}" />
                </On>
            </OnPlatform>
        </Grid>
    </ControlTemplate>

    <VisualStateGroupList x:Key="NavigationViewToggleButtonVisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Toggled" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="Button.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="NavigationViewToggleButtonVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="Button.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                    <Setter Property="Button.BackgroundColor" Value="{DynamicResource RadNavigationViewItemSelectedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="ToggledPointerOver">
                <VisualState.Setters>
                    <Setter Property="Button.TextColor" Value="{DynamicResource RadButtonPressedTextColor}" />
                    <Setter Property="Button.BackgroundColor" Value="{DynamicResource RadNavigationViewItemSelectedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Toggled" />
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="Button.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="NavigationViewToggleButtonStyle" TargetType="navigationView:NavigationViewToggleButton">
        <Setter Property="WidthRequest" Value="{OnPlatform Android=48, iOS=44, MacCatalyst=24, WinUI=32}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Android=48, iOS=44, MacCatalyst=24, WinUI=32}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, MacCatalyst=6}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontFamily" Value="{x:Static telerikMaui:TelerikFont.Name}" />
        <Setter Property="Text" Value="{x:Static telerikMaui:TelerikFont.IconMenu}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Default={StaticResource NavigationViewToggleButtonVisualStates},
                                   WinUI={StaticResource NavigationViewToggleButtonVisualStates_WinUI}}" />
    </Style>

    <Style x:Key="NavigationViewHeaderStyle" TargetType="navigationView:NavigationViewHeader">
        <Setter Property="ControlTemplate" Value="{StaticResource NavigationViewHeaderControlTemplate}" />
        <Setter Property="HeaderButtonStyle" Value="{StaticResource NavigationViewToggleButtonStyle}" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadNavigationViewPaneBackgroundColor}" />
        <Setter Property="HeightRequest" Value="{OnPlatform Android=56, iOS=44, MacCatalyst=32, WinUI=40}" />
    </Style>

    <Style x:Key="NavigationViewPaneHeaderStyle" TargetType="navigationView:NavigationViewPaneHeader">
        <Setter Property="ContentPadding" Value="{OnPlatform Default='0, 0, 0, 8', MacCatalyst='0, 8', WinUI='0, 4'}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadNavigationViewPaneSeparatorColor}" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
    </Style>

    <Style x:Key="NavigationViewPaneFooterStyle" TargetType="navigationView:NavigationViewPaneFooter">
        <Setter Property="ContentPadding" Value="{OnPlatform Default='0, 8, 0, 0', MacCatalyst='0, 8', WinUI='0, 4'}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadNavigationViewPaneSeparatorColor}" />
        <Setter Property="BorderThickness" Value="0, 1, 0, 0" />
    </Style>

    <Style x:Key="NavigationViewPaneStyle" TargetType="navigationView:NavigationViewPane">
        <Setter Property="ControlTemplate" Value="{StaticResource NavigationViewPaneControlTemplate}" />
        <Setter Property="HeaderTemplate" Value="{StaticResource NavigationViewPaneHeaderControlTemplate}" />
        <Setter Property="HeaderStyle" Value="{StaticResource NavigationViewPaneHeaderStyle}" />
        <Setter Property="FooterTemplate" Value="{StaticResource NavigationViewPaneFooterControlTemplate}" />
        <Setter Property="FooterStyle" Value="{StaticResource NavigationViewPaneFooterStyle}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadNavigationViewPaneBackgroundColor}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default=0, Android=4, iOS=4}" />
    </Style>

    <VisualStateGroupList x:Key="NavigationViewItemViewVisualStates">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="navigationView:NavigationViewItemView.BackgroundColor" Value="{DynamicResource RadNavigationViewItemSelectedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="navigationView:NavigationViewItemView.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="NavigationViewItemViewVisualStates_MacCatalyst">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="navigationView:NavigationViewItemView.BackgroundColor" Value="{DynamicResource RadNavigationViewItemPointerOverBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="navigationView:NavigationViewItemView.BackgroundColor" Value="{DynamicResource RadPrimaryColor}" />
                    <Setter Property="navigationView:NavigationViewItemView.TextColor" Value="{DynamicResource RadOnPrimaryColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="navigationView:NavigationViewItemView.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <VisualStateGroupList x:Key="NavigationViewItemViewVisualStates_WinUI">
        <VisualStateGroup Name="CommonStates">
            <VisualState Name="Normal" />
            <VisualState Name="PointerOver">
                <VisualState.Setters>
                    <Setter Property="navigationView:NavigationViewItemView.BackgroundColor" Value="{DynamicResource RadNavigationViewItemSelectedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Selected">
                <VisualState.Setters>
                    <Setter Property="navigationView:NavigationViewItemView.BackgroundColor" Value="{DynamicResource RadNavigationViewItemSelectedBackgroundColor}" />
                </VisualState.Setters>
            </VisualState>
            <VisualState Name="Disabled">
                <VisualState.Setters>
                    <Setter Property="navigationView:NavigationViewItemView.TextColor" Value="{DynamicResource LabelDisabledTextColor}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateGroupList>

    <Style x:Key="NavigationViewItemViewStyle" TargetType="navigationView:NavigationViewItemView">
        <Setter Property="ControlTemplate" Value="{StaticResource NavigationViewItemViewControlTemplate}" />
        <Setter Property="MinimumWidthRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=24}" />
        <Setter Property="MinimumHeightRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=24}" />
        <Setter Property="VerticalOptions" Value="Start" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="CornerRadius" Value="{OnPlatform Default=4, MacCatalyst=6}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Default=0, MacCatalyst='7, 0', WinUI='4, 2'}" />
        <Setter Property="Spacing" Value="{OnPlatform Default=4, MacCatalyst=0}" />
        <Setter Property="VisualStateManager.VisualStateGroups"
                Value="{OnPlatform Default={StaticResource NavigationViewItemViewVisualStates},
                                   MacCatalyst={StaticResource NavigationViewItemViewVisualStates_MacCatalyst},
                                   WinUI={StaticResource NavigationViewItemViewVisualStates_WinUI}}" />
    </Style>
    <Style TargetType="navigationView:NavigationViewItemView" BasedOn="{StaticResource NavigationViewItemViewStyle}" />

    <Style x:Key="RadNavigationViewStyle" TargetType="telerikMauiControls:RadNavigationView">
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="PaneStyle" Value="{StaticResource NavigationViewPaneStyle}" />
        <Setter Property="HeaderStyle" Value="{StaticResource NavigationViewHeaderStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadNavigationView" BasedOn="{StaticResource RadNavigationViewStyle}" />
</ResourceDictionary>
