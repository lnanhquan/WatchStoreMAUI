<?xml version="1.0" encoding="UTF-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
                    xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
                    xmlns:telerikMauiControls="clr-namespace:Telerik.Maui.Controls;assembly=Telerik.Maui.Controls"
                    x:Class="Telerik.Theming.Platform.Styles.DataForm">

    <!-- This is autogenerated file. Use as reference. Do not modify. -->

    <ControlTemplate x:Key="DataFormGroupControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}">
            <VerticalStackLayout Spacing="{TemplateBinding Spacing}">
                <telerikMauiControls:DataFormGroupHeaderView Style="{TemplateBinding ActualHeaderStyle}"
                                                             ControlTemplate="{TemplateBinding HeaderTemplate}"
                                                             HeaderText="{TemplateBinding HeaderText}"
                                                             HeaderDisplayOptions="{TemplateBinding HeaderDisplayOptions}"
                                                             HeaderLabelStyle="{TemplateBinding HeaderLabelStyle}"
                                                             HeaderImageSource="{TemplateBinding HeaderImageSource}"
                                                             HeaderImageStyle="{TemplateBinding HeaderImageStyle}" />
                <telerikMauiControls:DataFormGroupContentView Style="{TemplateBinding ActualContentStyle}"
                                                              ControlTemplate="{TemplateBinding ContentTemplate}"
                                                              Padding="{TemplateBinding ContentPadding}"
                                                              Content="{TemplateBinding Content}" />
            </VerticalStackLayout>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <ControlTemplate x:Key="DataFormGroupHeaderControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <Grid ColumnDefinitions="Auto, *">
                <Image Grid.Column="0"
                       Style="{TemplateBinding ActualHeaderImageStyle}"
                       Source="{TemplateBinding HeaderImageSource}"
                       IsVisible="{TemplateBinding IsHeaderImageVisible}" />
                <Label Grid.Column="1"
                       Style="{TemplateBinding ActualHeaderLabelStyle}"
                       Text="{TemplateBinding HeaderText}"
                       IsVisible="{TemplateBinding IsHeaderLabelVisible}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="DataFormGroupHeaderStyle" TargetType="telerikMauiControls:DataFormGroupHeaderView">
        <Setter Property="ControlTemplate" Value="{StaticResource DataFormGroupHeaderControlTemplate}" />
        <Setter Property="BorderColor" Value="{DynamicResource RadBorderColor}" />
        <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
        <Setter Property="HeightRequest" Value="{OnPlatform Android=48, iOS=44, WinUI=40, MacCatalyst=22}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Android='16, 0', iOS='16, 0', WinUI='12, 0', MacCatalyst='8, 0'}" />
    </Style>

    <Style x:Key="DataFormGroupHeaderLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadOnAppSurfaceColor}" />
        <Setter Property="FontSize" Value="{OnPlatform Android=16, iOS=17, WinUI=18, MacCatalyst=13}" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style x:Key="DataFormGroupHeaderImageStyle" TargetType="Image">
        <Setter Property="Aspect" Value="Center" />
    </Style>

    <ControlTemplate x:Key="DataFormGroupContentControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       Content="{TemplateBinding Content}" />
    </ControlTemplate>

    <Style x:Key="DataFormGroupContentStyle" TargetType="telerikMauiControls:DataFormGroupContentView">
        <Setter Property="ControlTemplate" Value="{StaticResource DataFormGroupContentControlTemplate}" />
        <Setter Property="ContentPadding" Value="{OnPlatform Android='16, 24', iOS='16, 24', WinUI='12, 20', MacCatalyst='8, 20'}" />
    </Style>

    <Style x:Key="DataFormGroupStyle" TargetType="telerikMauiControls:DataFormGroup">
        <Setter Property="ControlTemplate" Value="{StaticResource DataFormGroupControlTemplate}" />
        <Setter Property="ContentStyle" Value="{StaticResource DataFormGroupContentStyle}" />
        <Setter Property="HeaderStyle" Value="{StaticResource DataFormGroupHeaderStyle}" />
        <Setter Property="HeaderImageStyle" Value="{StaticResource DataFormGroupHeaderImageStyle}" />
        <Setter Property="HeaderLabelStyle" Value="{StaticResource DataFormGroupHeaderLabelStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:DataFormGroup" BasedOn="{StaticResource DataFormGroupStyle}" />

    <ControlTemplate x:Key="DataFormEditorControlTemplate">
        <telerikMauiControls:DataFormEditorPresenter BackgroundColor="{TemplateBinding BackgroundColor}"
                                                     BorderColor="{TemplateBinding BorderColor}"
                                                     BorderThickness="{TemplateBinding BorderThickness}"
                                                     CornerRadius="{TemplateBinding CornerRadius}"
                                                     ColumnSpacing="{TemplateBinding ColumnSpacing}"
                                                     RowSpacing="{TemplateBinding RowSpacing}"
                                                     EditorContent="{TemplateBinding Content}"
                                                     HeaderLength="{TemplateBinding ActualHeaderLength}"
                                                     HeaderPosition="{TemplateBinding ActualHeaderPosition}"
                                                     ErrorLength="{TemplateBinding ActualErrorLength}"
                                                     ErrorPosition="{TemplateBinding ActualErrorPosition}"
                                                     Padding="{TemplateBinding ContentPadding}">
            <telerikMauiControls:DataFormEditorPresenter.HeaderContent>
                <Grid ColumnDefinitions="Auto, *">
                    <Image Style="{TemplateBinding ActualHeaderImageStyle}"
                           Source="{TemplateBinding ActualHeaderImageSource}"
                           IsVisible="{TemplateBinding IsHeaderImageVisible}" />
                    <Label Grid.Column="1"
                           Style="{TemplateBinding ActualHeaderLabelStyle}"
                           Text="{TemplateBinding HeaderText}"
                           IsVisible="{TemplateBinding IsHeaderLabelVisible}" />
                </Grid>
            </telerikMauiControls:DataFormEditorPresenter.HeaderContent>
            <telerikMauiControls:DataFormEditorPresenter.ErrorContent>
                <Grid ColumnDefinitions="Auto, *">
                    <Image Style="{TemplateBinding ActualErrorImageStyle}"
                           Source="{TemplateBinding ActualErrorImageSource}"
                           IsVisible="{TemplateBinding IsErrorImageVisible}" />
                    <telerikMauiControls:DataFormEditorValidationView Grid.Column="1"
                                                                      IsVisible="{TemplateBinding IsErrorLabelVisible}"
                                                                      ItemsSource="{TemplateBinding ValidationErrors}"
                                                                      ItemStyle="{TemplateBinding ActualErrorLabelStyle}" />
                </Grid>
            </telerikMauiControls:DataFormEditorPresenter.ErrorContent>
        </telerikMauiControls:DataFormEditorPresenter>
    </ControlTemplate>

    <Style x:Key="DataFormEditorHeaderLabelStyle" TargetType="Label">
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>
    
    <Style x:Key="DataFormEditorHeaderImageStyle" TargetType="Image">
        <Setter Property="Aspect" Value="Center" />
    </Style>

    <Style x:Key="DataFormEditorErrorLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadErrorColor}" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <FontImageSource x:Key="DataFormEditorErrorImageSource"
                     FontFamily="TelerikFont"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconError}"
                     Size="{OnPlatform Android=24, iOS=24, WinUI=16, MacCatalyst=16}"
                     Color="{DynamicResource RadErrorColor}" />

    <Style x:Key="DataFormEditorErrorImageStyle" TargetType="Image">
        <Setter Property="Aspect" Value="Center" />
    </Style>

    <Style TargetType="telerikMauiControls:DataFormEditor">
        <Setter Property="ControlTemplate" Value="{StaticResource DataFormEditorControlTemplate}" />
        <Setter Property="ErrorImageSource" Value="{DynamicResource DataFormEditorErrorImageSource}" />
        <Setter Property="ErrorLabelStyle" Value="{StaticResource DataFormEditorErrorLabelStyle}" />
        <Setter Property="ErrorImageStyle" Value="{DynamicResource DataFormEditorErrorImageStyle}" />
        <Setter Property="HeaderImageStyle" Value="{StaticResource DataFormEditorHeaderImageStyle}" />
        <Setter Property="HeaderLabelStyle" Value="{StaticResource DataFormEditorHeaderLabelStyle}" />
    </Style>

    <ControlTemplate x:Key="DataFormValidationControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       Content="{TemplateBinding Content}"
                                       IsVisible="{TemplateBinding HasItems}" />
    </ControlTemplate>

    <Style x:Key="DataFormValidationStyle" TargetType="telerikMauiControls:DataFormValidationView">
        <Setter Property="ControlTemplate" Value="{StaticResource DataFormValidationControlTemplate}" />
    </Style>
    <Style TargetType="telerikMauiControls:DataFormValidationView" BasedOn="{StaticResource DataFormValidationStyle}" />

    <Style x:Key="DataFormErrorLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadErrorColor}" />
        <Setter Property="FontSize" Value="12" />
    </Style>
    
    <ControlTemplate x:Key="DataFormValidationSummaryControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}"
                                       IsVisible="{TemplateBinding HasItems}">
            <Grid ColumnDefinitions="Auto, *"
                  ColumnSpacing="4">
                <Image Source="{TemplateBinding ImageSource}"
                       Style="{TemplateBinding ImageStyle}" />
                <ContentPresenter Grid.Column="1"
                                  VerticalOptions="Center" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="DataFormValidationSummaryStyle" TargetType="telerikMauiControls:DataFormValidationSummaryView">
        <Setter Property="ControlTemplate" Value="{StaticResource DataFormValidationSummaryControlTemplate}" />
        <Setter Property="BackgroundColor" Value="{DynamicResource RadDataFormValidationSummaryBackgroundColor}" />
        <Setter Property="CornerRadius" Value="3" />
        <Setter Property="ContentPadding" Value="8" />
    </Style>

    <Style x:Key="DataFormValidationSummaryLabelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource RadDataFormValidationSummaryLabelTextColor}" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <FontImageSource x:Key="DataFormValidationSummaryImageSource"
                     FontFamily="TelerikFont"
                     Glyph="{x:Static telerikMaui:TelerikFont.IconError}"
                     Size="{OnPlatform Android=24, iOS=24, WinUI=16, MacCatalyst=16}"
                     Color="{DynamicResource RadErrorColor}" />

    <Style x:Key="DataFormValidationSummaryImageStyle" TargetType="Image">
        <Setter Property="Aspect" Value="Center" />
    </Style>

    <ControlTemplate x:Key="DataFormMaskedEntryControlTemplate">
        <telerikMauiControls:RadEntry AutomationId="MaskedEntryView"
                                      IsReadOnly="{Binding IsReadOnly, Source={x:RelativeSource AncestorType={Type telerikMauiControls:RadMaskedEntryBase}}}"
                                      Placeholder="{Binding Placeholder, Source={x:RelativeSource AncestorType={Type telerikMauiControls:RadMaskedEntryBase}}}"
                                      FontFamily="{Binding FontFamily, Source={x:RelativeSource AncestorType={Type telerikMauiControls:RadMaskedEntryBase}}}"
                                      FontSize="{Binding FontSize, Source={x:RelativeSource AncestorType={Type telerikMauiControls:RadMaskedEntryBase}}}"
                                      ClearButtonVisibility="{Binding ClearButtonVisibility, Source={x:RelativeSource AncestorType={Type telerikMauiControls:RadMaskedEntryBase}}}"
                                      BackgroundColor="{Binding EntryBackgroundColor, Source={x:RelativeSource AncestorType={Type telerikMauiControls:RadMaskedEntryBase}}}"
                                      CornerRadius="{Binding EntryCornerRadius, Source={x:RelativeSource AncestorType={Type telerikMauiControls:RadMaskedEntryBase}}}"
                                      TextColor="{Binding TextColor, Source={x:RelativeSource AncestorType={Type telerikMauiControls:RadMaskedEntryBase}}}"
                                      ReserveSpaceForErrorView="False" />
    </ControlTemplate>

    <Style TargetType="telerikMauiControls:DataFormRadEntryEditor">
        <Setter Property="ControlTemplate" Value="{StaticResource DataFormMaskedEntryControlTemplate}" />
    </Style>

    <Style x:Key="DataFormCheckBoxEditorEditorStyle" TargetType="CheckBox">
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>

    <Style x:Key="DataFormRadCheckBoxEditorStyle" TargetType="telerikMauiControls:DataFormRadCheckBoxEditor">
        <Setter Property="EditorStyle" Value="{StaticResource DataFormCheckBoxEditorEditorStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:DataFormRadCheckBoxEditor" BasedOn="{StaticResource DataFormRadCheckBoxEditorStyle}" />

    <Style x:Key="DataFormMultiLineEditorEditorStyle" TargetType="Editor">
        <Setter Property="MinimumHeightRequest" Value="80" />
    </Style>

    <Style x:Key="DataFormMultiLineEditorStyle" TargetType="telerikMauiControls:DataFormMultiLineEditor">
        <Setter Property="EditorStyle" Value="{StaticResource DataFormMultiLineEditorEditorStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:DataFormMultiLineEditor" BasedOn="{StaticResource DataFormMultiLineEditorStyle}" />

    <ControlTemplate x:Key="RadDataFormControlTemplate">
        <telerikMauiControls:RadBorder BackgroundColor="{TemplateBinding BackgroundColor}"
                                       BorderColor="{TemplateBinding BorderColor}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       Padding="{TemplateBinding ContentPadding}">
            <Grid RowDefinitions="*, Auto">
                <ScrollView>
                    <ContentPresenter />
                </ScrollView>
                <telerikMauiControls:DataFormValidationSummaryView Grid.Row="1"
                                                                   IsVisible="{TemplateBinding IsValidationSummaryVisible}"
                                                                   Style="{TemplateBinding ActualValidationSummaryStyle}"
                                                                   ItemStyle="{TemplateBinding ActualValidationSummaryLabelStyle}"
                                                                   ItemsSource="{TemplateBinding ValidationErrors}"
                                                                   ImageSource="{TemplateBinding ValidationSummaryImageSource}"
                                                                   ImageStyle="{TemplateBinding ActualValidationSummaryImageStyle}" />
            </Grid>
        </telerikMauiControls:RadBorder>
    </ControlTemplate>

    <Style x:Key="RadDataFormStyle" TargetType="telerikMauiControls:RadDataForm">
        <Setter Property="ControlTemplate" Value="{StaticResource RadDataFormControlTemplate}" />
        <Setter Property="ErrorLabelStyle" Value="{StaticResource DataFormErrorLabelStyle}" />
        <Setter Property="ValidationSummaryImageStyle" Value="{StaticResource DataFormValidationSummaryImageStyle}" />
        <Setter Property="ValidationSummaryImageSource" Value="{StaticResource DataFormValidationSummaryImageSource}" />
        <Setter Property="ValidationSummaryLabelStyle" Value="{StaticResource DataFormValidationSummaryLabelStyle}" />
        <Setter Property="ValidationSummaryStyle" Value="{StaticResource DataFormValidationSummaryStyle}" />
    </Style>
    <Style TargetType="telerikMauiControls:RadDataForm" BasedOn="{StaticResource RadDataFormStyle}" />
</ResourceDictionary>